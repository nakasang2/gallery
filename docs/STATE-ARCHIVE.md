# STATE-ARCHIVE.md — STATE.md から移した過去の記録

> `docs/STATE.md` は**セッション開始時に必ず読むファイル**なのに、「最終更新」の1行が
> 26,171文字・「以下が前回の記録」の連鎖が51回、`## 進行中` が250行まで育ち、**Read が
> 途中で打ち切られて読めない**状態になっていた（2026-08-13 の /kaizen で実測）。
> 情報を捨てずに STATE.md を読めるサイズへ戻すため、過去分をここへ移した。
> **ここは読み物で、運用の入口ではない**。必要なときに grep して掘る。

## 移した「最終更新」

- 2026-08-18 の「最終更新」（照明の焼き込み①＝部屋で1枚のアトラス。同日の `compileAsync` 修正で押し出された分。**開いている用件は STATE.md の「最終更新」に引き継いである**）:
- **最終更新**: 2026-08-18（**①`/demo` が空の部屋になる不具合を直した（緊急）②照明の焼き込み ① の土台（部屋で1枚の画像）を作った**。どちらも PR #38・**マージ待ち**。方針は DECISIONS 2026-08-18 の2件。）
  - **【不具合・ユーザー報告 iPhone】`/demo` が保存された設定1つで永久に空の部屋になり、自力で戻れなかった。** 「デモ作品も表示する」を外すと、作品と一緒に**作品ごとの照明・銘板・キャプション・順路ボタン・ステッパーが全部消え、BGMボタンだけ残る**。設定パネルの入口はサインインが要るのでゲストには無い＝**チェックを戻せない**。**サインイン済みで部屋はあるが作品0点の人**にも同じ穴があった。直し: visitor 以外で自分の作品が0点ならデモを出す ── **既に詰んでいる端末はデプロイだけで治る**（消去操作を頼まない）。**公開ページは対象外**（作品0点の部屋に架空の作品を掛けてしまう）。
  - **ユーザー決定（この日）**: ①焼くのは**公開ボタンを押したとき・作家の端末**（A案。作家が何もしなくても確実に保存され、スマホしか無い作家の部屋にも本物の影が付く）②**進捗インジケータは必須**③**公開中に構成が変わったら「焼き直しが必要」をダッシュボードで示唆する**（必須）④保存する画像は**部屋で1枚**にまとめる。
  - **できたもの（コミット `2b4d010`＋レビュー修正）**: `WallShadowBaker` が作品ごとの 256px テクスチャ（最大16枚）ではなく**1枚のアトラス**にタイルで焼く。**見た目は不変**で、保存の経路はまだ無い。
  - **検証**: 変更前（`origin/main`）の作品ごとテクスチャと**10作品すべてでアルファが完全一致**（合計・非ゼロ画素数・最大値／先に読めた3枚は16分割の平均も一致）。uv の割り当ても実測（tile→作品が焼いた順と一致・重複なし）。**後処理が無い経路（`?perf=nofx`）でも10枚全部埋まる**ことを確認。tsc／番人9種／本番ビルド／別視点レビュー1巡（2件・どちらも修正）。
  - **【次】② 保存と読み出し**（migration・R2・来場者側）→ **③ 公開時に焼く導線とUI**。タスクは #2 / #3。
  - **【リリース前に必ず消す】診断スイッチ `?perf=`** ── 「進行中」の先頭に固定。
  - **【ユーザー判断待ち・繰越】3Dなしのヒーローは、スマホ幅で文字と絵が重なる**（既存のCSS。GooglebotとPSIが見る画面）。
  - **【運用】会場費の数字を変えるときは `docs/EVIDENCE-COMPARE.md` も必ず直す。**
  - **【この日の失敗（測り方）】×11 を踏んだ**: **ペインはツール呼び出しの外ではフレームを1枚も進めない**ので、1フレームに1作品ずつ焼く状態機械が `4/10` で止まって見え、実装のバグだと10分疑った。フレームで進むものは `requestAnimationFrame` の回数で数える。詳細は LESSONS 2026-08-18（検証・ツール ×11 と 3D/R3F 2件）。

- 2026-08-18 の「最終更新」（同日、照明の焼き込み①に着手して押し出された分）
- **最終更新**: 2026-08-18（**LPは完了。展示室は「重さの正体」を実測で確定し、当面の緩和策まで出荷**。全文は DECISIONS 2026-08-18 の3件。）
  - **LP（完了・ユーザー確認済み）**: ①GPUの無い環境では3Dを出さない（PSIがタブごと落ちていた件）②3D本体の軽量化（初回に焼くテクスチャ 23→9枚／常駐照明 30→最大15個）③**廊下の板6枚の照明が、6枚とも壁の裏に立っていて1枚も照らしていなかった**のを正しい位置へ（PR #19〜#21 の「まだ暗い」×3 の真因）。**光が当たっていること・スマホの体感・PSI・Lighthouse すべてユーザー確認済み。**
  - **展示室（重さの正体が確定）**: ユーザーのFPS実測 ── 何もしない **13.8** / 解像度を等倍 **33.6** / 後処理を外す 17.4 / 床の反射を外す **12.6（誤差）** / 全部外す 36.0 / **作品の照明を外す 30以上**。**効くのは画素数と照明の数だけ。**
  - **出荷済みの緩和策**: 自動引き下げの中身を「落としたまま固定」→「**歩行中だけ落とし、立ち止まったら最高画質**」に差し替え（PR #37）。**今より悪くなる人はいない**（速い機械は一度も落ちない／遅い機械は観る瞬間がくっきり戻る）。
  - **【次にやる本命】照明の焼き込み**。順序は ①**焼いた結果を保存する仕組み**（今は来場者全員が毎回焼いている。**ユーザー提案: アーティストの出展時に1回だけ焼いて保存する** ── 来場者は小さな画像を受け取るだけ／入場も速くなる／低スペック端末も同じ絵を受け取れる）→ ②その上に光を焼く → ③作品と額の扱い（立体なので別の工夫）。**空中の光の筋（`LightCone`）は照明と無関係の独立メッシュなので消えない。**
  - **【リリース前に必ず消す】診断スイッチ `?perf=`** ── 「進行中」の先頭に固定。
  - **【ユーザー判断待ち・繰越】3Dなしのヒーローは、スマホ幅で文字と絵が重なる**（既存のCSS。GooglebotとPSIが見る画面）。
  - **【運用】会場費の数字を変えるときは `docs/EVIDENCE-COMPARE.md` も必ず直す。**
  - **【この日の失敗（測り方）】3回まちがえた**: ①効果を確かめられない環境の数字を効果と報告 ②±250msのばらつきを効果と読んだ ③**`Rendering`/`Painting` に WebGL の重さは出ないのに「出ていない＝無罪」と断定**し、いちばん疑うべき演出を長く外した。**すべてを覆したのはユーザーの「ウィンドウを小さくすると40以上に上がる」の一言。** 詳細は LESSONS 2026-08-18（3件）。

- 2026-08-17 の「最終更新」（同日のLPパフォーマンス改善で押し出された分。**開いている用件は STATE.md の「次にやること」へ移してある**）:
- **最終更新**: 2026-08-17（**この日は7本出荷した**: ①明朝を Bodoni Moda に ②テーマプレビューを実際の展示室と同じ部品で建て直し ③テーマ選択を焼いた写真のカードに ④**部屋タブを「選ぶだけの場所」にして Design Tools を撤去** ⑤**LP廊下で画像を入れた枠は写真だけにする**（板・額・スポットを外す）。すべてユーザー選択。**push・本番反映まで完了**。詳細は DECISIONS 2026-08-17 の4件。）
  - **④で分かった重要な事実**: **Design Tools は `DESIGN_TOOLS_VISIBLE = false` でずっと画面に出ていなかった**（色の設定は作家に一度も見えていない）。作家に見えていた変更は「大プレビュー」と「照明チップ」の2つだけで、残りはデッドコードの掃除。旗は `LOGO_UPLOAD_VISIBLE` に改名した。
  - **③→④で前提が変わった**: ③の時点では「Design Tools が連続値を持つので大プレビューは画像化できない」と判断したが、④でその Design Tools 自体を外したので制約が消え、大プレビューを外せた。**訂正ではなく前提の変化。**
  - **`check:i18n` の孤児検出が初めて実戦で効いた**（2026-08-14 に入れた番人）。撤去で呼ばれなくなった辞書キー7件を機械が拾い、11言語から消せた。
  - **検証**: tsc・番人9種（gates 77）・SQLテスト全項目・本番ビルド／**実バンドルで部屋タブを描画して実測**（大プレビューとcanvasが0・1カラム・375pxではみ出し0）／別視点レビュー2巡で計5件修正。
  - **⑤LP廊下**: ユーザーが本番のスクショで指摘（画像をアップすると板と横並びになり、同じことを二度言っている／写真に額とスポットが付いている）。画像があるスロットは板を描かず写真1枚だけにし、額・マット・スポットを外した。**材質を `meshBasicMaterial` にして光を要らなくした** ── スポットだけ消すと暗い廊下で真っ黒になる（PR #19〜#21 で3回踏んだ形）。**位置を板と同じ `p.z` へ移したのが要点**で、従来の `p.z - 1.9` のままだと**カメラが空の壁を見る**ところだった。文言は `.corridor-fallback`（sr-only）に残るのでSEOと読み上げからは消えない。
  - **⑥LP「くらべる」節＋廊下の光**（ユーザー選択）。仮想敵は**会場を借りて開く個展**で、**実名の競合は出さない**（景表法：比較広告は客観的に実証された事実が要り、実名だと相手の条件変更でこちらの表示が虚偽になる）。**金額の行も置かない**（料金セクションが唯一の出どころ）。軸は 会場/会期/届く範囲/終わったあと の4つ、11言語。廊下の写真は**壁にだけ光を当てる**形にした（写真の材質は光の影響を受けないままなので、色は撮ったまま・暗くても真っ黒にならない）。
  - **別視点レビューが2件とも実害だった**: ①横送りを `table` 自身の `display:block` でやっていたため**PC幅でも表の意味が落ちて**いた（div で包んで解決）②`role` が table/rowgroup だけで行・セルに無く「**行を1つも持たない表**」になっていた。どちらも見た目には出ない。
  - **⑦くらべる表に会場費を入れた**（ユーザー選択）。**体感ではなく調査して書いた** ── 実際に公表されている貸しギャラリーの料金を調べ、`docs/EVIDENCE-COMPARE.md` に調査日・出典URL・為替・換算を残した（景表法7条2項は15日以内の根拠提出を求める）。表示は「1週間でおよそ $200〜$2,000（国・地域・立地で大きく変わる）」。単位は週（日割りは比較の方法として不公正になりうる）、範囲は控えめ（米都市部は上限超え＝優位を過大に見せない側へ倒す）。あわせてユーザーの指摘で「**集客の労力は同じ**」と正直に書き、違いは**来場者の負担**の行で示す形にした。

- 2026-08-15 の「最終更新」（同日のLP訴求の作り直しで押し出された分）:
- **最終更新**: 2026-08-15（**LPの構成レビュー（訴求・競合・法務）と、そこから出た修正4件を実施**。ユーザー依頼「LPの構成をレビューしてほしい / 訴求内容は問題ないか・他社と比べて魅力的か・法的に問題ないか」）
  - **入れたもの（4点）**: ①**6言語の `stat4` が「AIを一切使っていない」と読めていた**のを学習に限定した言い方へ（`en:'No AI'→'No AI training'`／es・fr・it・id・pt-br も同様。zh-hans/hant も `不用于 AI`→`不用于 AI 训练` に締めた）。**読み上げ `/api/tts` で OpenAI を使っている**ので、約束しているのは「作品を学習に使わない」（規約 §3・プライバシー §2/§3）であって「AIを使わない」ではない。ja・de・ko は元から正しかった。②**`ja.freePrice` の `¥0` を `$0` に**（有料は全部ドルなので円建てと誤認しうる）＋ 新キー `lp.pricingCurrency`（11言語）を料金カード直下の `.pricing-note` に追加し、**表示は米ドル・税は決済画面で加算**を明示。③**利用規約に §6「If we ever close Xibit360」を新設**（旧6〜9は7〜10へ繰り下げ）。買い切りを「ずっと」と売っているのに、**サービス終了時の扱いが規約のどこにも無かった**（景表法の有利誤認・消費者契約法のリスク）。**90日前の告知・告知期間中は公開と取り出しを維持・告知前12か月以内の買い切りは申し出により返金**。特商法の返品特約（`legal.valReturns` ja/en）と最終更新日も追随させた。④**ナビの並びをページの並びに合わせた**（コンセプト→できること だったが DOM は features が先。押すと未読の features を飛び越していた）。
  - **検証**: tsc 0件／番人8種（gates 71・i18n は ja 100% 1040/1040 を維持）／本番ビルド／**Playwright で `stat4` の実測**（11言語 × 1280/900/375px、実CSSを layout と同じ順で連結。`document.scrollWidth == innerWidth`・`.concept-stats` の scrollWidth==box で**はみ出し0件**。fr・it・id は1280pxで2行に折り返すが、サブ文言はもともと列ごとに行数が違う設計なので許容と判断）。
  - **【重要・未修正】検証中に既存のCSS衝突を発見**: `app/me.css:390-410` の `.stat` / `.stat b` / `.stat span`（ダッシュボードの統計タイル）が、**`app/layout.tsx` の import 順で `landing.css` より後に来るため、LPのコンセプト節の統計行を同一詳細度で上書きしている**。LPは `2.4rem` の枠なしを意図しているのに、実際は **`1.3rem` ＋ 枠線＋角丸＋ホバー**で描かれている。**DECISIONS 2026-08-12・D-1（`:root` を landing/gallery で二重定義して書体が死んでいた）と完全に同じ形**で、【絶対ルール】2026-08-12 の「同じ意味のものを2か所で定義しない」に当たる。どちらの見た目を正とするかはユーザー判断なので**手を付けていない**。
  - **【ユーザー判断待ち・レビューの残り】**: 上の4件は「今すぐ効く安いもの」だけ。残りは下の「次にやること」に。**規約 §6 の2つの数字（90日・12か月）は事業判断**なので、変えるなら1行で直せる。**特商法の電話番号（省略の可否）と、Stripe Managed Payments が merchant of record であることと「販売業者=個人」表記の整合は、専門家確認が要る**（Claudeでは決められない）。

- 2026-08-13 の「最終更新」（2026-08-15 のLPレビューで押し出された分）:
- **最終更新**: 2026-08-13（**【ship済・本番適用済み。ただし重大な不具合が判明 → 2026-08-14 の記録を下の「進行中」で読むこと】合同展示を「参加作家ごとに部屋」の形に組み替え（migration 0062）**。0061の「1展示1部屋」を「1展示・1作家1部屋」に訂正し、招待を承諾した瞬間に作家自身の部屋が自動でできる形にした。ユーザー指摘: 「招待された側のUIが特殊すぎる（受信箱の作品トレイ）ので、従来のタブ型UIの合同展示verにしたい」）
  - **①DB（migration 0062）**: `enforce_room_allowance`/`switch_room_expo`の所有チェックを緩め（承諾済みの招待がある作家も自分の部屋を作れる）、部屋数の上限を「展示全体に1部屋」から「自分がその展示に1部屋」に変更。新規トリガ`create_room_on_expo_accept`が`expo_invites`のstatusが`accepted`になった瞬間にその作家自身の部屋（work_cap=15）を自動で作る（冪等）。`drop_placements_on_expo_revoke`に「辞退・取り消しで作家自身の部屋も消す」を追加（アップロード済みの作品は`artworks.gallery_id`を`on delete cascade`→`on delete set null`に変更したので失われず共有プールへ解放される）。新規`galleries.expo_ready_at`と RPC `set_expo_room_ready`で「準備できた」トグル（主催者へ`room_ready`通知、自己通知はしない）。新規RLS `galleries_select_expo_owner`で主催者が展示内の全部屋を読める（書き込みは不可）。SQLテスト21項目・既存0047/0061への回帰なしをローカルPostgresで確認、schema.sqlとの完全一致・冪等性(3回適用)も確認。
  - **②フロント**: `lib/expos.ts`に`Expo.ownerId`と`setExpoRoomReady()`を追加。`GalleryCard`の「参加者」タブは主催者だけに絞り（`isOrganizer = expo.ownerId === user.id`）、「公開」タブは**主催者なら従来の会期選択＋決済、参加作家なら「準備できた」トグル**に分岐。`InviteInbox`（受信箱）は、承諾済みで自動生成の部屋がある招待には**旧・作品トレイの代わりに「部屋を開く」ボタン**を出す（0062以前に承諾済みで部屋が無い招待は、後方互換として旧トレイのまま残す＝**表・関数は消さず、新規の呼び出しだけ切り替えた**）。`ParticipantsPanel`は各作家の部屋の準備状態（準備完了/準備中）を表示し、`onOpenRoom`でその部屋を直接開けるように。`ExpoManager`の「部屋を追加」ボタンの判定を「展示に部屋が0件」から「**自分自身の**部屋がまだ無い」に訂正し、新規「参加している合同展示」節（招待されて参加中の展示の一覧、部屋を開く入口）を追加。通知に`room_ready`種別を追加（`lib/notifications.ts`・`NotificationBell.tsx`）。
  - **意図的に残したもの**: `expo_submissions`（旧・提出モデル）のDB表・関数、`lib/invites.ts`の`setMySubmissions`/`listSubmittedArtworksForRoom`/`listSubmittedArtworks`、`app/me/page.tsx`の`guests`/`reloadGuests`（配置トレイでの合成表示）は**あえて削除していない** — 0062のトリガは新規/更新される`expo_invites`行にしか発火せず、既に`accepted`で運用中の既存の合同展示（本番に実在しうる）は自動では部屋が作られないため、旧モデルの経路を残さないと**進行中の展示の壁掛け作業が止まる**。新しい招待は自動で新モデルに乗り、古い招待は自然に旧モデルのまま動き続ける（併存）。
  - 検証: tsc・i18n（ja 100% 1041/1041）・css・schema・sns・expo・price-disclosure・gates 71件・本番ビルド／SQLテスト新規21項目＋既存0047・0061の全回帰なし／ローカルPostgresでschema.sqlとmigrations 0001〜0062の完全一致・冪等性を確認。**Playwright等の実ブラウザ操作は未実施**（このセッションでは行っていない） — タブ分岐・トグル・ExpoManagerの一覧は目視未確認。
  - **【解消済 2026-08-14】push も本番適用も完了している**（別セッションが `1d5e87b` と追随の `9f2b50d` で push 済み。0061・0062 とも本番に適用済みで、トリガ `expo_invites_create_room`・列 `galleries.expo_ready_at`・`artworks.gallery_id` の存在を本番DBで確認した）。**この行はもともと「未push・未適用」と書かれていて、2026-08-14 のセッションが実態と食い違うのを見つけて直した** ── 台帳が古いまま残ると次のセッションが誤った前提で判断する。

## 移した「完了ログ」（〜2026-08-13）

- 2026-08-16: **【ship済・PR #19としてmainへマージ済み】LP廊下パネル、実機で暗すぎた照明を修正し、フォールバック文言をSEOで読める形に**（マージコミット `ed1d6de`。PR #18の続き）。①実機（スマホ）で「暗すぎて見えない」との指摘を受け、PR #18の素通り時18／フォーカス時170を100／190に修正（指スクロールは狙った1pxで止まらず、暗い方を見ている時間が長かった）。②SEO相談「壁の文字をcanvasに焼いたが大丈夫か」を受け、3D非対応フォールバック（`.corridor-fallback`、本物の`<h3>`/`<p>`）が `.has-hero3d` 時に `display:none` で丸ごと隠れ、WebGL対応のGooglebotにもほぼ見えていなかった可能性を特定。`display:none` をやめ既存の `.sr-only`（U3のa11y対応）にセレクタを足し、視覚的には隠しつつクローラー・スクリーンリーダーには読める形に（同じ変更で3D有効時のa11yの隙間も解消）。検証: tsc・番人8種・本番ビルド・Playwrightでモバイル可読性と`.corridor-fallback`のDOM残存を確認。**未解決**: 「アートと違い光の輪郭が無い」はユーザー判断待ち。

- 2026-08-12: **【ship済・本番反映確認済み】リリース前4観点レビュー（PM／UX-UI／QA／Legal）を実施し、先行修正5件＋要意思決定4件（すべてユーザーがA案を選択）を12コミットで出荷**（`702c61c`〜`28cbd6f`）。**Legal**: 特商法の価格表示に合同展示の場所代が載っていなかった（**同じ漏れの3回目**・本番で売れていた）／引渡時期が予約と猶予削除を語っていなかった／通報が DMCA §512(c)(3) の4要件を1つも受け取っていなかった（migration 0056。**著作権のときだけ**要求する check 制約をDB側にも置いた）／通報の入口がフッタから辿れなかった。**PM**: LPの料金カードに部屋$25・ビデオパス$20・合同展示$15–$40 が載っていなかった（単価の高い商品ほど不可視）。**UX-UI**: **`app/landing.css` と `app/gallery.css` が同じトークンを別の値で `:root` に定義し、import 順の後勝ちで gallery 側が全ページを支配**していた。しかもその指す日本語書体は一度も読み込まれておらず、**サイト全体の本文と見出しがOS標準フォントで描かれていた**（決め手は3Dの壁に焼く文字が 'Instrument Serif'/'Geist' を直書きしていたこと＝HUDと壁が食い違っていた）。色は効いていた側の値を採って見た目を保ち、書体だけ直した。装飾番号のコントラスト 1.8–1.9:1 → 3.14/3.17:1。**×3の機械化**: `npm run check:price-disclosure`（AGENTS.md 5.9）を新設し check:gates を56→66件に。**番人自身を4通りに壊して自己テストが赤くなることを確認**。**別視点レビューで自分の変更のバグ3件を検出・修正**（9言語で `/legal` の言語が混ざる／法務ページの最終更新日が据え置き／番人の除外リストに一般名）。**検証中に既存の不具合3件も発見・修正**: `0055` が再実行で落ち**後続の 0056 が適用されない**（ユーザーがまさに踏むところだった）／料金カードの値段が float で次行に落ち別項目の隣に見える（**スクリーンショットで発見・矩形測定は緑だった**）／`.demo` の見出しが日本語375pxで23pxはみ出す（`git stash` で変更前と比較して既存バグと切り分け）。検証: tsc／番人8種（gates 66）／本番ビルド／SQLテスト11ファイル162項目／0052〜0056を3回当てる冪等性と schema.sql 完全一致／en・ja・de × 320・375・1280 の実測。

- 2026-08-10: **【ship済・本番マージ済み】合同展示（Expo）機能をPR #8としてmainにマージ**（migration 0044〜0049、`719c3d4`）。複数作家が共同で開ける展示 `/expo/{slug}`。会期（7日$15/14日$25/30日$40）を主催者が公開時に1回払う場所代モデル、招待は@ハンドル直打ちと配れる招待リンク（0048・`/join/{token}`で参加希望→主催者承認）の2経路。**マージ前の別視点レビューで確定バグ2件を発見し、migration 0049で修正**: ①`guard_expo_run` が `before update` にしか付いておらず、authenticated が INSERT で `starts_at` を直接指定するだけで**決済なしに即公開**できた（scratch Postgresで実際に再現＝30日ぶん・$0）。②`enforce_room_allowance` が `expo_id is not null` を見た瞬間に無条件returnしていたため、**他人の展示IDを付けるだけで所有者チェックもwork_capの上限(15枠)チェックも素通り**していた（work_cap=999999の部屋を無料で作れることを再現。しかもその部屋は他人の展示に紐づいた）。migration 0044〜0048のSQLテスト195項目は全部緑だったが、どちらの攻撃もテストに入っていなかった。0049で所有者チェックとwork_cap上限を追加し、両方の攻撃が拒否されること・既存の正常経路に回帰が無いことを新規7項目で検証。検証: tsc / i18n(950キー×10言語100%) / css / schema / gates(56) / 本番ビルド / SQLテストハーネス7ファイル・計228項目すべて通過。**LESSONS「上限の材料は誰が決めたか」系の教訓が×3に達した**（2026-07-27・2026-08-09に続き3回目）→ 次の /kaizen で絶対ルール化・スキル化の検討を提案する。**残（ユーザー作業）**: ①migration 0044〜0049を `supabase/schema.sql` でSupabaseに一括適用 ②pg_cron拡張の有効化（Dashboard→Database→Extensions。無効のままだと会期切れ展示は表示上は非公開になるが行の掃除ジョブが動かない）③Stripeの実カードE2E（下書き作成→部屋を用意→会期を選んで決済→`/expo/{名前}`が公開されるか）④`/explore`・作家ページからの「この展示に招く」入口は未着手（@ハンドルと招待リンクの2経路は動作）。

- 2026-07-29: **【/kaizen 昇格2件・ship済】プレビュー検証をスキル化し、push直前の並行セッション番人を機械化した**（`8866595`、DECISIONS 2026-07-29「/kaizen 昇格2件」）。未昇格のまま ×6 に達したパターン2つをユーザー判断で昇格。①**`.claude/skills/preview-verify`**（＋AGENTS.md 5.6、`~/.claude/skills/` にも同一内容で複製）— 「検証ツールに嘘をつかれて実装のバグだと誤診する」が6回、1回は誤診に基づいて別のバグを作った。原則は「**測った値が理屈と食い違ったら、まず測り方を疑う**」。信用度は スクショ ＞ `getBoundingClientRect` ＞ `getComputedStyle`。**真っ黒スクショの真因も判明** — 撮影側ではなく、ペインが前面に無いと `document.hidden` が真になり3Dが意図的に描画を止めている（`canvas.width` が 300x150 のままなのが証跡）。②**`npm run check:ship-ready`**（＋AGENTS.md 5.7、`scripts/check-ship-ready.mjs`）— base が HEAD の祖先でないときと作業ツリーが汚れているときだけ止め、他の作業ツリーの未pushは警告に留める（他人のものだが、知らせないと本番に出ないまま消える）。`check:gates` に4ケース追加（**相手の22件と合わせて26件**）・変異テスト済み。**初日から効いた** — 導入時に4セッション分の取り残し（他セッションの実バグ修正を含む）を検出し、この ship の直前に2回止めた。**この作業中に `origin/main` が2回動き rebase 2回**。**衝突解消で相手の作業項目2件を一度消した**（正規表現が複数の衝突ブロックを巻き込んだ）— `git show :2:` / `:3:` の集合比較で気づいて復元、LESSONS に ×4 として記録。

- 2026-07-29: **`schema.sql` の取り残しを検出する番人 `npm run check:schema` を追加**（AGENTS.md 5.8 / LESSONS「検証・ツール」2026-07-29）。直前のセッションが `schema.sql` の欠落10本を修復し「番人化は未実施」と残した宿題を機械化した。`scripts/check-schema-sync.mjs` が migration 1本ごとに ①`-- # NNNN_*.sql` の節があるか ②**コメントを除いたコード行が自分の節より後ろに順序どおり全部あるか**（部分列一致）③README §1 の**箇条書き**に載っているか を静的照合する（SQLは実行しないのでPostgres不要）。おまけに schema.sql 前文の統合範囲と、migration が消えた取り残しヘッダも見る。`check:gates` に13ケース追加（**計39件**）。**変異テスト13種すべてで落ちることを確認**し、その過程で穴を1つ実際に見つけて塞いだ — 「本文は自分のヘッダより後ろ」という下限を外しても緑のままだった（migration どうしは `);` や grant 行を共有するので、下限が無いと別の節の行で新しい節が満たされる）。また**番人を入れた自分の作業で番人を空振りさせた** — README §1 の地の文に「下の 0001〜0034 が」と書くとそれが全34本をカバーし照合が無効化された（→ 箇条書き限定に修正）。**この番人を、別セッションが修復済みの `schema.sql`（Postgres 16.14 で実行検証済み）に当てて 0件＝緑**なので、番人と実測の答えが一致している。検証: check:schema / check:gates 39件 / check:i18n / check:css / check:ship-ready / tsc / 本番ビルド。**ship 中に base が8コミット進んでいた**（`check:ship-ready` が検出）ため、重複していた自分の schema.sql 同期コミットは捨てて番人だけを乗せ直した。

## 移した「最終更新」の連鎖（〜2026-08-13）

- **最終更新**: 2026-08-13（**【ship済・本番反映確認済み 13:25】3Dのロード周り2件を修理し `bab9b1c` を push**（Vercel Ready）。**次のスコープ＝自動保存をやめて「1ボタンで保存」に変える**（下の【次のスコープ】。仕様はユーザー確定済み））
  - **①「ロードが二回される」の正体は進捗の後退**（ユーザー報告）。three の `LoadingManager` は `itemsTotal`/`itemsLoaded` を**通しで数え一度も0に戻さない**（node_modules を読んで確認。「波ごとに数え直す」という思い込みで書いた積み上げ実装は、条件が永久に成立しない死んだコードだった）。実際は 20/20（100%）の後に12件登録されて 20/32（62%）へ落ちる ＝ **伸び切って戻ってまた伸びる**。→ **単調増加にし、100% は「扉を開ける条件が揃ったとき」だけ**（`assetsIdle` だけで判定すると、キャッシュが温かいとき 1/1 = 100% で固まる → 92% で足止め）。
  - **②「ロード直後重い」の正体はシェーダのコンパイル**。WebGL はマテリアルが**初めて画面に映る瞬間**に作るので、入室後に振り向くたび数十msの停止が入る。→ `Warmup` を追加して `gl.compileAsync` を**ロード画面の裏で**走らせ、終わるまで扉を開けない。
  - **`/code-review` で1件**: `Warmup` の cleanup で完了通知を打ち消していたため、`armed`（`assetsIdle` を含む）がコンパイル中に一度でも false へ落ちると**完了が捨てられ扉が12秒開かなかった**（`gallery_loading_timeout` も誤って飛ぶ）。打ち消すのはアンマウントのときだけに。
  - 検証: tsc・番人7種・本番ビルド／進捗の式を3つの並びで照合／完了通知は effect の再実行を模して修正前後を比較。**3Dの体感はユーザー確認待ち**。
  - **【ユーザー作業】`supabase/migrations/0060_profile_cv.sql`（来歴の列）の適用が未了。**

- **最終更新**: 2026-08-13（**【ship済・本番反映確認済み 12:14】マークダウン周りのユーザー要望3件に対応し `cc3bcc4` まで push・本番配信を実測確認**）
  - **①文字数制限を撤去**（展示情報200字×2・来歴2000字）。3Dの板には焼かれないので長くても壁からはみ出さない。**タイトルと作品キャプションは板・名札に焼かれるので上限を残した**。
  - **②展示情報・自己紹介・来歴をマークダウン対応**。既存の `lib/markdown`（依存ゼロ・React要素なのでXSS面が無い）を再利用し、**誰でも書ける文なので2点変えた**: `images: false`（任意のURLの画像は**来場者のIPを第三者へ渡す**＝追跡ピクセル）と `breaks: true`（1行1件で書かれる来歴が1段落に潰れないように）。**指定は `ARTIST_TEXT_MD` の1か所**に集約（面ごとに書いて作家ページだけ食い違っていたのをレビューで検出）。**描く面**＝情報パネル・作家ページ・素のHTML一覧・カタログ・招待ページ／**落とす面**＝`<meta>`・`og:`・`twitter:`・構造化データ（`stripMarkdown` ＋ `metaText` で200字に詰める）。
  - **③見出しの段・入れ子の箇条書き・入力欄のプレビュー**。見出しは全段同じ大きさだったので3段に分けた（`#` の後の空白は必須のまま ── `#展示` のようなハッシュタグを見出しにしないため）。字下げした `- ` を**入れ子として読む**ようにした（以前は段落に落ちて空白がCSSで詰められ字下げが消えていた。空白2つでも4つでも同じ扱い・印は disc/circle/square）。`<textarea>` は文字しか描けないので**入力欄の下にプレビュー**を出す（記法を使ったときだけ・`hasMarkdown`）。
  - **`/code-review` で止めた4件**: ①`<meta>` が無制限になり6000字が4回入って1ページ24KBになる ②作家ページだけ改行指定が抜けて同じ自己紹介の見た目が食い違う ③`2024. 個展` の**年が番号の記号として食われて消える**（`\d{1,2}\.` に制限） ④プレビューを `<label>` の中に置いたため**押すとフォーカスが入力欄に吸われ**リンクが押せない。
  - 検証: tsc・番人7種・本番ビルド／パーサを実物のまま Node で照合（strip 9・入れ子と見出し11・年7・hasMarkdown 15ケース）／体裁は**本番と同じCSSで実測**（見出し3段・入れ子18.3px・破線の枠・375pxではみ出し無し）。**測る前にリロードしないと古いCSSで測れて全部「効いていない」に見える**（今回1回踏んだ）。
  - **【ユーザー作業が1つ残っている】`supabase/migrations/0060_profile_cv.sql`（来歴の列）の適用。** 未適用でも他は壊れないが、来歴は保存・表示されない。

- **最終更新**: 2026-08-13（**【ship済・本番反映確認済み 11:28】ユーザー要望2件に対応し `4251265` を push・本番配信を実測確認**（Vercel `gallery-9plnzn5ly` = Ready））
  - **①文字数制限を撤去**（ユーザー指示「この入力欄に文字数制限は不要です」）: 展示情報（200字×2か所）と来歴（2000字）。**3Dの板には焼かれない**（`makeTitleTexture` は `statement`/`bio` を読んでいない）ので長くても壁からはみ出さない。**タイトルと作品キャプションは板・名札に焼かれるので上限を残した**。
  - **②展示情報・自己紹介・来歴をマークダウン対応**（ユーザー要望）: 既存の `lib/markdown`（依存ゼロ・React要素を返すのでXSS面が無い）を再利用し、**記事と違い誰でも書ける文**なので2点変えた ── `images: false`（任意のURLの画像は**来場者のIPが第三者へ渡る**＝追跡ピクセルになるので代替テキストにする。規則を「外す」だけではURLが素の文字として残ることを実測して確認し、代替テキスト専用の規則に差し替え）と `breaks: true`（来歴は1行1件で書かれるので、既定の1段落連結では潰れる）。**描く面**＝情報パネル・作家ページ・素のHTML一覧・カタログ・招待ページ／**落とす面**＝`<meta>`・`og:`・`twitter:`・構造化データ（`stripMarkdown`）。4つの入力欄にマークダウンの案内（11言語）。
  - **`/code-review` 2件をどちらも修正**: ①制限を外したことで `<meta name="description">` が無制限になり、6000字の展示情報が meta・og・twitter・JSON-LD に4回入って1ページ24KBの重複になる状態 → `lib/seo.metaText` で記号を落として200字に詰める（全文を見せる面は詰めない）②作家ページだけ `breaks` を渡しておらず、同じ自己紹介が公開ページでは1段落に潰れ3Dでは改行される食い違い → 指定を揃えた。
  - 検証: tsc・番人7種・本番ビルド／`stripMarkdown` 9ケース＋画像禁止＋改行＋説明文の詰めをNodeで単体照合／`.panel-md` を**本番と同じCSSで実測**（箇条書き1.3em・リンクが金・強調がink・引用の金の縦線・先頭末尾の余白0・375pxではみ出し無し・改行が3行）。**測る前にリロードを忘れると古いCSSで測れて全部「効いていない」に見える**（今回1回踏んだ）。
  - **【ユーザー作業が1つ残っている】`supabase/migrations/0060_profile_cv.sql`（来歴の列）の適用。** 未適用でも他は壊れないが、来歴は保存・表示されない。
  - **並行セッションが同時に動いている**（直前の `cf98ab8` は別セッションの /kaizen）。

- **最終更新**: 2026-08-13（**/kaizen で昇格2件を実施。①SQLテストの「空振り」を機械で検出するようにした ②このSTATE.mdを読めるサイズへ戻した**。
  - **①空振りの検出（LESSONS ×4・前回の /kaizen からの持ち越し）**: `scripts/sql-verdict.mjs` に「番号付きの項目に `t`/`f` が1つも出ていなければ落とす」を追加した。
    この形は判定器が `ERROR` を見ない設計（拒否の確認では期待しているため）と噛み合って生まれ、**拒否されれば ERROR・通ってしまっても `INSERT 0 1`** でどちらも `f` にならない＝守りが静かに壊れた日も「すべて期待どおり」と出る。
    **実測: 307項目のうち61項目（20%）がこの形だった**（12ファイル）。既知分は `scripts/sql-vacuous-baseline.json` で抑制し、`check:css` と同じ作法で**減らす方向にしか変えない**。
    `check:gates` にケース5件追加（計71件）。判定器の該当分岐を無効化すると2件が赤くなること、`0059` の項目16を「ERROR目視」形に退行させると落ちることを実測。**61項目の書き直しは別タスク**（基準線から1つずつ外していく）。
  - **②STATE.md の再構成**: 「最終更新」を直近1件だけにし、51回の連鎖と `## 進行中` の250行を `docs/STATE-ARCHIVE.md` へ**全文そのまま**移した（情報は捨てていない）。
  - この直前に、ユーザー選択B案の**二段構え**を完了: **0058**（配置の書き換えを1トランザクションに）と **0059**（作品数・配置数の上限をDB側で強制）。**どちらもユーザーが本番に適用済み**。
    0059 の要点は「**すでに超過している部屋を閉じ込めない**」こと（枠は部屋の削除や合同展示からの離脱で普通に減る）。規則は「操作が状況を悪くしていないこと」で、操作前の点数は `note_placement_baseline()` が申告する。
  - さらにその前に、ユーザー報告「ダッシュボードで15枠設定しても3D Viewに反映されない」を修理（**A案「配置したものを正とする」**、`lib/arrangement.usableSlots` に判定を集約）。3回ルールで【絶対ルール】「同じ意味の値を2か所で計算しない」を DECISIONS の冒頭に昇格。
  - **並行セッションが同時に動いている**: `components/gallery/*`・`lib/i18n/*`・`app/me/*`・migration 0060（プロフィールCV）が未コミットで動いている。触っていない。）

- **最終更新**: 2026-08-13（**【ship済・本番反映確認済み 2026-08-13 09:38】ユーザー要望3件に対応し `c2a0adb` を push・本番配信を実測確認**（Vercel `gallery-lqyzov9ty` = Ready）。**【ユーザー作業が1つ残っている】`supabase/migrations/0060_profile_cv.sql` の適用**（未適用でも壊れないように二段構えを入れたが、貼るまで来歴は保存も表示もされない）。①**プロフィールに来歴（展示歴・受賞歴）を追加**（migration 0060 `profiles.cv`。自己紹介とは別列 ── 箇条書きで積み上がる記録と読み物を混ぜると両方読みにくい）②**情報パネルを3タブに**（展示情報／自己紹介／来歴。中身の無いタブは出さない・1つならタブ列も出さない）③**西壁は壁のどこを押しても展示情報が開く**（以前はタイトルの板だけ。振る舞いは `TitleWall.openExhibitionInfo` の1か所）④**廊下の突き当たりの扉を作り直した**（開口いっぱいの框戸＋板目。**`emissive` は法線を見ないので上げるほど陰影の無い塗り面になる** → 廊下に `pointLight` を1つ置き、`emissive` は真っ黒を避ける下限まで落とした）。**`/code-review` が公開ページ全滅の手前で止めた1件**: `cv` を読む select が5経路あり、0060 未適用のあいだ 42703 で落ちて**公開展示ページが404**・ダッシュボードがプロフィールを空で読み込んで**次の自動保存で名前と自己紹介を空で上書き**する状態だった（既存のフォールバックにも `cv` を足していたので二段目も同じ理由で落ちた）。`missingColumn` で退避し列を外して引き直す形に。検証: tsc・番人7種・本番ビルド・タブを実CSSで実測（3言語で44px確保／375pxで折り返してはみ出し0／来歴の改行が3行として描画）。**3Dの実描画は私が検証できない**ため扉の見え方はユーザーのスクショ待ち。以下が直前の記録: **【ship済・本番反映確認済み 2026-08-13 08:58】ユーザー指摘「（廊下の）奥が行き止まりなので見えちゃいますね。ここはドアか何かを奥に配置しましょうか」に対応し `69402b3` を push・本番配信を実測確認**（Vercel `gallery-oeh5sf2lm` = Ready）。廊下の突き当たりに**閉じた扉**（面＋枠＋レバーハンドル）を立てた。**明るさは光に任せず弱い `emissive` で下限を作る**（扉の面 0.045・枠 0.02・金物 0.10 ＝ sRGB で約 41/28/72。**リニアで計算した値をコメントに残した** ── 前日 0.12 を「控えめ」と見積もって枠を中間グレーにした失敗の再発防止）。検証: tsc・番人7種・本番ビルド（**別セッションの 0058/0059 が入った base の上で**再実行）・同一平面ペア0組（変異2種で3組/1組を検出＝空振りなし）・`/code-review` 2件（縦枠が床下へ7cm潜る・レバーが座から8mm浮く）を両方修正。**3Dの実描画は私が検証できない**ため判定はユーザーのスクショ待ち。以下が直前の記録: **【第1段=ship済／第2段=実装済・検証済・push待ち】枠の上限をDB側で強制する（B案・二段構え。DECISIONS 2026-08-12 に全文）**。**第2段 = migration 0059 `pool_limits`**: `artworks` と `placements` に statement 単位＋遷移表のトリガを載せ、**口座全体**（`lib/limits.poolCapacityOf` と同じ意味）で数える。判定の材料は `work_slot_pool()` と `placement_pool_used()` の**2つだけ**で、トリガと `replace_placements` の両方がそこを通る（絶対ルール遵守）。**設計上いちばん重要なのは「すでに超過している部屋を閉じ込めない」こと** ── 枠は部屋の削除や合同展示からの離脱（0050 が `work_cap` を15→5に戻す）で普通に減り、厳密に `使用<=上限` を課すと「1点外す」保存すら拒否されて二度と直せなくなる。規則を「**操作が状況を悪くしていないこと**」にし、操作前の点数は `note_placement_baseline()`（definer・引数なし＝自分の分しか見ない）がトランザクション限りの設定に申告する（設定が無い＝RPCを通らない直接の書き込みなので厳密に判定）。**旧経路 `rebuildPlacementsLegacy` は削除した**（残すと件数トリガが正常な配置移動を拒否する）。**0057・0058・0059 はユーザーが本番に適用済み（2026-08-13）**＝枠の上限はDB側で効いている。検証: SQLテスト 0058=20項目・0059=29項目、**壊して赤くなることも実測**（固定値で数える／超過中の猶予を外す／`security definer` 注入／所有者チェック削除）、3回流してエラーゼロ、`check:schema` 0件、tsc・番人7種・本番ビルド通過、`/code-review high` を2巡（計4件すべて修正）。**未対応として記録**: 作品のinsertが拒否されるとR2のファイルが行なしで残る（競合時のみ到達。DECISIONS に詳細）。以下が直前の記録: **【ship済・本番反映確認済み 2026-08-13 00:56】ユーザーの実機フィードバック4件に対応し `16c127d` を push・本番配信を実測確認**（Vercel `gallery-2jetjewjb` = Ready）。①**扉のフレームが灰色**→ 前日入れた枠の `emissive` を撤去（`emissive` はリニア値。0.12 は sRGB で中間グレー約102/255になる ── 合図は既に HUD が言葉で出しているので3D側は不要）②**床が途切れて奥行きが無い → ユーザー選択A案「壁に本物の開口を開ける」**（DECISIONS 2026-08-13 に全文）。壁を開口の左/右/上の3枚に割り、廊下 1.2m を**壁の裏＝部屋の外**へ。廊下に床を敷き板目の向きを部屋と合わせ、幅木も開口で2本に割った。**位置決めは `components/gallery/doorway.ts` の `useDoorway` 1つに集約**（壁を切る `Room` と枠を建てる `RoomPortals` が食い違うと「壁に穴だけ空く」）。あわせて**掛かる作品の半幅の見込み 0.85m** を隙間に追加 ── 従来の取り方では `corridor` 間取りで作品が開口に**0.5m 重なっていた**（切り抜くと露出する。機械照合で発見）③**スマホの影の焼き込みが不自然** → 作り物の影（作品より0.6m大きい柔らかい板2枚）を撤去。焼き込みが走らない端末では**これが恒久的な見た目**だった④**作品の空間表示で裏に回ると透ける** → `OrbitControls` に方位角±72°・仰角±60°の制限（額縁は片面の板で出来ている）。検証: tsc・番人7種・本番ビルド（**出荷される木そのものを別 worktree に切って**実施 ── 作業ツリーには並行セッションの未コミット変更があり、混ざった状態は誰も検証していない）・全22間取りの機械照合（穴と扉の完全一致／開口＋枠が壁に収まる／作品と重ならない。`uSign` 破壊で9件検出＝空振りなし）・同一平面ペア0組・`/code-review` 2件（廊下の板目の向き＝修正、焼き込み影の開口へのはみ出し＝黒地に黒で影響小のため記録のみ）。**3Dの実描画は私が検証できない**ため、判定はユーザーのスクショ待ち。**並行セッションが同時進行**（`7ab9a67` migration 0058 を push 済み、0059＋`lib/galleries.ts` は未コミット。触っていない）。以下が直前の記録: **【第1段=完了・検証済／第2段=未着手】枠の上限をDB側で強制する（B案・二段構え。ユーザー指示 2026-08-13）**。**第1段（前提工事）= migration 0058 `replace_placements`**: `rebuildPlacements` の「upsert → 余りを delete」の**2回のリクエストを1つのRPC（1トランザクション）に**した。**振る舞いは1つも変えていない** — 第2段の件数トリガが「枠を移す瞬間に上限＋1行」という中間状態を拒否してしまうのを防ぐための土台。**要点は `security definer` にしないこと**で、`placements` のRLS（0037: using=部屋の所有者／with check=所有者かつ `may_place_artwork`）がそのまま効くので同意判定を写さない。所有者チェックだけは関数の先頭で明示的に落とす（RLS任せだと他人の部屋に空配列を渡したとき黙って成功する）。検証: SQLテスト20項目＋**壊して赤くなることを実測**（`security definer` 注入で4項目・所有者チェック削除で2項目）／0058 を3回流してエラーゼロ・権限は `authenticated` のみ／`check:schema` 0件（schema.sql・README §1 も追随）／tsc・番人7種・本番ビルド通過／`/code-review high` 2件とも修正済み。**0058 は適用済み（2026-08-13）。****第2段の設計上の縛り（別視点レビュー指摘）**: ①上限の判定は必ず `placements` の**トリガ**に置く（RPCの中に書くと旧経路が素通りする）②そのとき `rebuildPlacementsLegacy` を**削除**し `PGRST202` は「0058 未適用」としてはっきり投げる。以下が直前の記録: **【ship済・本番反映確認済み 2026-08-13 00:08】ユーザー報告「（3Dの扉の）下部がちらついてる」を修理し `99bc273` を push・本番配信を実測確認**（Vercel `gallery-o9p0tftpl` = Ready）。原因は **Zファイティング（同一平面）**: 前日入れた扉のトンネル（C案）の床を `y = 0` に置いたが、部屋の床（`Room.tsx` は `position` 指定なし＝ y=0）と**完全な同一平面**だった → **床は置かない**（実際の戸口と同じく部屋の床が凹みの床を兼ねる）。**別視点レビューで同型がさらに3組**見つかり同時に修理（左右の枠の内側の面 × トンネル側壁、まぐさの下面 × トンネル天井 ── 枠の箱は `z=0` 中心なので奥行きの半分 0.096m がトンネル側へ食い込む）。内壁を枠が終わる位置から張る `TUNNEL_INNER_D` / `TUNNEL_INNER_Z` を導入。あわせて、ユーザーのスクショで**明るい灰色の板**に見えていた敷居の帯（`theme.stripColor` が whitecube では純白）を撤去し、「この扉は使える」の合図は**枠そのものの `emissive`** で表す（面を足さない）。検証: 同一平面ペアの全数照合で**旧の値=4組 / 修正後=0組**（空振りでないことを確認済み）・tsc・番人7種・本番ビルド通過・`/code-review`（2件どちらも上記）。**3Dの実描画は私が検証できない**ため、判定は構成上の確認＋ユーザーのスクショ待ち。以下が直前の記録: **【次のスコープ・未着手】枠の上限をDB側で強制する（B案・ユーザー選択。DECISIONS 2026-08-12 に全文）** ── 調査で「サーバ側に点数の強制が1つも無い」ことが判明した（アップロードAPIは容量300MBだけ・`artworks` の件数トリガ無し・`placements` のトリガは同意取り消し専用）。**前提工事**: `rebuildPlacements` が「upsert → delete」を別リクエストで行うため一時的に上限を超えた状態がコミットされる。先に**1つのRPC（1トランザクション）にまとめないと、件数トリガが正常な配置操作を拒否する**（過去2回の「D&Dできない」と同じ形になる）。以下が直前の記録: **【ship済・本番反映確認済み】ユーザー報告「ダッシュボードで作品を15枠設定しても3D Viewに反映されない」を修理し `756410e` まで push・本番配信を実測確認**（本番の配置ロジックのチャンク `2556-cba55e96aa724cb7.js` が、私が検証したローカルビルドと**md5 完全一致**）。**修正コミット `8a870bf` は並行セッションが自分の push に同梱して先に出した**（相手はその経緯を `f43d00c` に記録）。原因は**同日の「枠は口座全体の共通プール」への変更が3経路のうち配置エディタにだけ入っていた**こと ── 保存（`lib/galleries.rebuildPlacements`）と3D描画（`lib/roomPlan.slotCount`）は昔の「その部屋自身の `work_cap`」のままで、無料部屋（5枠）に15点並べると**保存時に10行が delete され**、描画でもう一度5点に絞られていた（実 `lib/*` を呼んで 15→5→5 を実測）。**ユーザー確定=A案「配置したものを正とする」**: 枠が縛るのは「まだ並べていない部屋に自動で掛ける枚数」だけにし、手で並べた部屋は物理スロット数（15）まで出す。判定を `lib/arrangement.usableSlots` の**1関数に集約**したので3経路が今後は食い違えない。あわせて、枠を使い切った部屋で配置図ごと消えて「壁には出ているのに外せない」閉じ込みになるのを防いだ。**副作用（ユーザー伝達済み）**: `work_cap` は「壁に出せる点数」の最後の砦ではなくなり、枠の総量を守るのは配置エディタの `drop()` だけになる（DB側に placements の件数チェックは元から無いので、失うのは描画側の二重の網だけ）。**ユーザー作業（重要）**: `galleries.arrangement` には15点分が残っているが `placements` は5行に削られており、3Dと公開ページは `placements` を読むので、**各部屋で一度ダッシュボードの配置を動かす（＝再保存させる）まで元の点数には戻らない**。検証: 6シナリオで3経路の一致を実測／tsc・番人7種（i18n・css・schema・sns・expo・price-disclosure・gates66）・本番ビルド通過／`/code-review high` で2件（どちらも上に記載）。**この時点で `origin/main` より2コミット先**（もう1本は並行セッションの `5c13266` 扉のトンネル・C案）。**この作業中に並行セッションと衝突**: 相手が私の未コミット変更を「別セッションの作業（usableSlots）」として `git stash` に退避 → 自分のコミット後に pop で戻してくれた（失われたものは無い）。以下がこの前の記録: **【ship済・本番反映確認済み】ユーザーの実機フィードバック4件に対応し `a87aa33` まで push・本番配信を実測確認**。①**ヘルプ（ダッシュボードのモーダル）を機能単位の章立てに組み替え**（B案）── 着手前は4章10問で入口の質問しか無く、**出荷済み16機能のうち6個しか載っていなかった**。章をダッシュボードのタブに合わせ、8問（16キー×11言語）を追加して **16/16** に。本番で章立てと10機能の記載を実測確認。②**いいね数を非表示・架空タグ「Exhibited」を撤去** ── `lib/cloud.ts`（読み出し時）と `lib/upload.ts`（ゲストの新規作品）の2箇所で作っていた。**タグの入力UIはどこにも無い**ので実ユーザーには必ず出ており、`textures.ts` の題箋経由で**3Dの壁にも焼かれていた**。マイグレーション不要（DBには壊れたデータが無い）。本番JSから消えたことを確認。③**「空間で見る」を作品の真下に浮かせた**（A案）── `walkRef.focusedScreenRect()` を新設し四隅を投影。**ユーザーの指摘で懸念が消えた**（`focusExhibit` はトゥイーン後に `setFocused` するのでカメラは正面に静止している）。**幾何を three.js で計算検証**: PCは余地160〜320pxで問題なし・作品は中央より左なので右ドロワーと衝突しない／**スマホは下シートが50vhを占めるため余地がほぼ無く、大きい作品ではボタンが作品の下端に約30px重なる**（P1として現状これで出荷。代案P2/P3は報告済み・ユーザー判断待ち）。④**扉の透けを解消**（B案）── 開口部が半透明の板1枚だったのを不透明のグラデーションに。**Zオフセットではなく `renderOrder`** で前後させた（扉は壁沿いに4方向どこにでも立ち面は両面なので、向きに依存させない）。実関数を実バンドルで呼びcanvasの画素を実測（全画素 alpha 255・中心6.1が最暗・床側20.1が明るい）。**③④は3Dの実描画を私が確認できていない**（ペインが背面判定で描画が止まる）── ユーザーのスクリーンショット待ち。**0057 は適用済み（2026-08-13）。**以下がこの前の記録: **【/kaizen 実施・ship済・本番反映確認済み】4観点レビュー → UXレビュー3面 → 指摘対応まで完了。計17コミットを `ac0efe6` まで push・本番配信を実測確認**。**この日の流れ**: ①PM/UXUI/QA/Legal の4観点レビュー（先行修正5件＋要意思決定4件、すべてユーザーがA案）②UXレビューを3面（LP／`/me`／3D）に分けて実施 ③残った指摘3件をユーザー判断で処理（✕は「タイルを大きくする」・キーボード対応は「対応する」・D-5返金運用は「非対応」）。**migration 0056・0057 はユーザー適用待ちのうち 0056 は適用済み・0057 が未適用**（未適用でも通報は届く／会期中の取り下げ通知だけが出ない）。**/kaizen の結果**: LESSONS の重複見出しを統合（CSS系が ×6/×5 に分裂して3回ルールの引き金が鈍っていた → ×9 に統合）、成功パターン3件と「プレビューの計測に嘘をつかれる ×8〜×10」を追記。**残る実機QA**: 3Dの実描画（ペインが背面判定で描画が止まるため未検証）／額縁の太さスライダー（高さ16px・ブラウザ任せ）／スマホの配置トレイが4列になったこと／PCでのキーボード配置。**未対応として記録済み**: ミニマップとステッパーの重なり（320pxで32×4px・機能影響なし・3Dが描けず直した結果を確認できないため報告のみ）。以下がこの日の詳細: **【ship済・本番反映確認済み 2026-08-12】4観点レビューの成果12コミットを `28cbd6f` まで push・本番配信を実測確認**。確認したもの: 配信CSSの `:root` トークン宣言が**1つだけ**（Shippori/ZenKaku の残存0件・`--serif` が Instrument Serif・`--sans` が Geist）／`.flow-no` が `rgba(236,236,234,.38)`／`.price-card li` が flex／`/legal` に「7日 $15、14日 $25、30日 $40」と「会期終了から7日後に展示とURLは削除されます」と「最終更新：2026年8月12日」と「著作権に関する通知」欄／LPの料金カードに6行（枠$3・部屋$25・ビデオパス$20・テーマ$8・間取り$5・合同展示$15–$40）／フッタの `/report` リンク。**`www.xibit360.art` はこの環境から到達できるようになっていた**（STATE の旧記述「ネットワークポリシーで遮断」は解消）。**migration 0056 はユーザーが適用済み**（0052〜0055 もまとめて適用済み）。**残る実機QA**: 3Dギャラリーの実描画と `/me` の画面（どちらもプレビューの制約で未確認 — ペインが背面に落ちると3Dが描画を止める／ローカルに認証キーが無い）。**D-5（返金運用）だけ判断保留**。以下がその作業の内容: **リリース前の4観点レビュー（PM／UX-UI／QA／Legal）をユーザー指示で実施。意思決定の要らない5件を先行修正・検証済み（未コミット）、意思決定が要る5件は報告して判断待ち。**先行修正**: ①**特商法の価格表示に合同展示の場所代が載っていなかった**（7日$15/14日$25/30日$40。2026-08-10 から売れる状態）→ `legal.valPriceExpo` を新設し `expoRunOptions()` から一覧を導出（会期を1つ足しても漏れない）。行の書式は購入ボタンと同じ `expo.payOption` を使い回し訳を二重に持たない。②**`legal.valDelivery`（「お支払いの確認後ただちに」）が合同展示について嘘だった**（0051 で開始日時の予約が入った／会期に終わりがあり猶予7日後に消える）→ 予約・会期・猶予（`EXPO_GRACE_DAYS` から導出）を明記。③**`/me` の会期購入ボタン直上の `expo.payNote` が「支払った瞬間に公開」と言い切っていた**——真上に開始日時の入力欄があるのに矛盾していた → `scheduleInput` から分岐（`payNoteScheduled`）＋購入前に会期終了と削除を開示する `payNoteRun` を追加（11言語）。④**LPの装飾番号の文字色がWCAG 1.4.3を割っていた**（`.cfeat-no` 1.92:1・`.flow-no` 1.82:1、いずれも大きい文字の下限3:1未満）→ α0.24/0.22→0.38 で 3.14:1／3.17:1（実測）。⑤**通報の入口がサイト共通フッタから辿れなかった**（作品/作家ページ・規約・プライバシーからは辿れる）→ LPフッタに追加（EU DSA 16条の「容易に見つけられる」通報手段。ラベルは既存の `artist.reportProblem` を再利用＝新しい訳を増やさない）。**検証**: tsc / check:i18n（11言語100%・直書き0・プレースホルダ0）/ css / schema / sns / expo / gates(56) / 本番ビルド すべて通過。`/legal` は ja・en の両方をブラウザで実測（会期一覧が「7日 $15、14日 $25、30日 $40」と ja の読点で出る・引渡時期に猶予7日が出る）。`/me` は鍵が無く開けないため一時ルート `app/qaverify`（検証後削除）で `getServerT()` 経由の両分岐を ja/en で実測。**LESSONS の「特商法の開示漏れ」が予告どおり ×3 に到達**したので、機械化（`PRICE_USD_CENTS` の各キーが `/legal` の開示に現れているかの照合）を3回ルールに従い提案済み・未実施。**最大の指摘は先行修正していない**＝`app/landing.css` と `app/gallery.css` が同じ `:root` トークン（`--bg` `--ink` `--muted` `--hairline` `--serif` `--sans`）を別の値で二重定義し、layout.tsx の import 順で**後勝ちの gallery.css が全ページを支配**している。結果 `--sans: 'Zen Kaku Gothic New'` / `--serif: 'Shippori Mincho'` はどこにも読み込まれておらず、**サイト全体の本文と見出しがOS標準フォントで描かれている**（Geist と Instrument Serif は読み込んでいるのにトークン経由では使われていない）。ビルド成果物のCSS順・`getComputedStyle`・文字幅の実測（`declared_body` 808.8px ＝ `generic_sans` 808.8px ≠ `geist_300` 713.34px、`.hero h1` は `generic_serif` と一致）で確定。見た目が全ページ変わる決定なのでユーザー判断待ち。**）以下は前回の記録: 2026-08-12（**ユーザー提案2件（画像トリミング位置の3択化・購入リンクの複数化）をAskUserQuestionで確認しどちらもA案を選択→実装・検証・別々にcommit済み（未push）。①`artworks.crop_align`（start/center/end、既定center。migration 0054）を追加し、`Exhibit.tsx`のcover-fit UVオフセット計算を軸に依存しないstart/end値に対応させた。3D側のtexture flipYで縦方向のstart/endが視覚的には上下逆（start=下・end=上）になる点を合成WebGLハーネス（実際のthree.jsで再現）で実測確認し、編集UI（`app/me/page.tsx`）側でLeft/RightまたはTop/Bottomラベルに正しく変換するようにした。全11言語に新規キー6個追加。②`artworks.purchase_url`（単一URL）を`purchase_links`（`{label,url}`のjsonb配列。migration 0055）に置き換え、既存の単一リンクは空ラベルの1件としてバックフィルした上で列を削除（二重の真実の元を残さない）。編集UIはSNSの「その他リンク」機能で確立済みの`sns-custom-row`行パターン（add/remove）をそのまま再利用。`ArtworkPanel.tsx`/`CatalogDoc.tsx`の購入ボタン表示も複数リンク対応に更新し、`.panel-buy + .panel-buy`の間隔CSSを追加。検証: tsc/check:css/check:i18n(11言語100%、`legal`節の既知ギャップのみ)/check:schema/check:gates(56)/本番ビルドすべて通過。合成DOMハーネス（devサーバーの`public/_verify.html`、検証後削除）で`panel-buy`の複数ボタン表示（スクリーンショット実測）と`sns-custom-row`流用レイアウト（`getBoundingClientRect`実測、ペインが背面化して以降はスクショが信用できなかったため矩形測定に切替）を確認。DECISIONS 2026-08-12に詳細記録。**）以下は前回の記録: 2026-08-12（**直前のpush直後、ユーザーから「合計20枠あるのにデフォルト部屋では5枠しか使えない」との指摘→AskUserQuestionで確認し、口座全体で完全に自由配分する方式（A案）を選択。部屋ごとの固定割りを撤廃し、この部屋が使える枠を「口座合計 − 他の部屋の配置数」の動的な値に変更。cap到達メッセージは常に口座合計（`poolTotal`）を見せる／この部屋の枠が0になる場合も文言で説明（空白のまま出さない）。DECISIONS 2026-08-12に詳細。検証: tsc/css/i18n/本番ビルド通過、合成ハーネスで`workCap`と`poolTotal`を分けて実際にドラッグし両方の挙動を実測確認。別視点レビューで「重複排除された集合(.size)を容量計算に使い回すと、同じ作品を複数の他室に掛けている場合に消費枠を過小評価する」穴を検出→`elsewherePlacedCount()`を新設し数値計算だけ差し替え済み。`e297f55`を push・本番反映確認済み（11:48）。以下は前回の記録: 2026-08-12（**直前のpush後、ユーザーが本番で実機フィードバック4件を報告→対応・検証済み・`c3488e2`を push・本番反映確認済み（10:57）。別視点レビューで「満室でも掛かっている作品を差し替えられない」退行を1件検出・修正済み。①扉の配置ロジックを再設計**——タイトルウォールに収まらない部屋でフロート配置にフォールバックしていたのが「どこでもドア」に見える再発だったため、フロートを完全に撤去し常に4つの壁のいずれかに壁沿いで配置する形に変更（実測13パターン全て壁沿い）。**②SNS表示を情報パネルの最下部→タイトル・作家名の直下に移動③展示タイトル入力欄のフォントを他の入力欄と統一（2026-07-31の差別化決定をユーザー自身が撤回）④配置タブのD&D不具合の続報**——無料枠(work_cap=5)の部屋で、共通プールの6点目以降をドラッグすると`placeWorks`のcap到達breakで静かに無視されていた（実データで再現・修正確認）。`drop()`にcapチェックとトーストを追加。DECISIONS 2026-08-12に詳細。検証: tsc/css/i18n/本番ビルド通過、扉配置は数値検証、cap拒否は実機ドラッグで再現・確認。以下は前回の記録: 2026-08-12（**ユーザー指示10件（3D View 5件＋ダッシュボード5件）に対応・`48ec819`を push・本番反映確認済み（10:07、www.xibit360.artのエイリアスが新デプロイを指すことを確認）。/code-reviewで別視点レビュー実施・確定バグなし。事前にAskUserQuestionで3点を確認：①合同展示の作成UIは新規作成（ExpoManager）に一本化②公開タイミングは現状維持＋参加者提出状況の可視化③着手順は3D Viewから。DECISIONS 2026-08-12に記録。**

## 移した「最終更新」に続いていた小見出し（2026-08-12 の作業の内訳）

  - **3D View①SNSアイコン**: `components/SnsLinks.tsx`は既に用意されていたのに3D側で未配線だった。`Hud.tsx`の`HudTop`（来場者ヘッダ左上）と`InfoPanel.tsx`（展示情報パネル）に`roomExhibitor(visitor).sns`を配線。合同展示は`roomExhibitor`が単独作家/団体を判定するので板の作家名と一致する。
  - **②作品の技法・サイズ**: 壁のパネル(`ArtworkPanel.tsx`)には既にあったが、全画面表示`ArtworkPreview3D.tsx`（こちらが手前に被さる）には無かった。タイトル・`widthCm/heightCm/medium`を左上に追加。
  - **③複数部屋の扉の位置**: `RoomPortals.tsx`の`doorPlacement()`は入場スポーン地点からの距離だけで決めており、タイトルウォール（奥の壁）の位置を見ていなかった。`lib/roomPlan.titleWallWidth()`を新設（TitleWall.tsx側の幅計算式を共有化）し、タイトルボードの端（奥の壁の同じ壁・端）に扉を寄せる`titleWallDoorPlacement()`を追加。**部屋が浅くて収まらない場合は旧来のentry基準の配置にフォールバック**（hall/portrait/広いcustomは新配置、corridor/island/狭いcustomはフォールバック——スクリプトで全パターンを数値検証、幾何的な非干渉を確認）。
  - **④右メニューの遷移先モーダル**: これまでミニマップ上部の常設トグル+インライン一覧(`RoomSwitch.tsx`)だったのを、常設トグルは撤去し**右下HUDメニューにドアアイコン**（新設`DoorIcon`、`components/icons.tsx`）を追加。押すと新設`RoomPickerModal.tsx`（`.purchase-backdrop`を再利用した通常のモーダル、Escで閉じる）が開く。ドアの前に立った時の「進む」プロンプト（`roomswitch-prompt`）はそのまま維持（1件なら直行・複数なら同じモーダルを開く）。
  - **⑤他人ギャラリー閲覧時のCTA**: `HudTop`の`visitor`分岐が`user`の有無を見ずに常に「無料ではじめる」を出していた。`user`がいれば（自分のでも他人のでも）「ダッシュボードへ」（`/me`）に差し替え。
  - **⑥展示室追加ボタンの余白**: `.me-rooms-row`のgapが0.6remで部屋タブ同士(0.3rem)と不一致なだけでなく、**`.me-rooms`が`flex:1 1 auto`で余白を全部吸って追加ボタンが行の右端まで離れていた**（gapを揃えるだけでは広い画面幅で直らない、実機検証で発見）。gapを0.3remに揃え、`.me-rooms`を`flex:0 1 auto`に変更——広い画面でも部屋タブの直後に追加ボタンが並ぶ。
  - **⑦追加ボタンの表示範囲**: `roomAdd`の表示条件が`stage==='room'`に限定されていた。`stage==='room'||'placement'||'publish'`の3ステージ全てに拡張（部屋タブ自体は既に3ステージ対応済みだった）。
  - **⑧配置タブのD&D不具合（実機再現・最重要）**: `app/qatest911/`にPlacementEditorの実データ検証ページを作り実際にpointer dragを打って再現。**トレイの作品タイル・✕削除ボタンとも`press()`に常に`fromSlot=null`を渡していた**ため、既に壁に掛かっている作品をトレイからドラッグして別の占有スロットに落とすと、`drop()`のfromSlot==nullの経路では元スロットの補充（swap）が行われず、**元あった作品が silently消える**（トレイにも戻らない）バグだった。マップ上で直接ドラッグする分には正しくswapされていたので「できる作品とできない作品がある」に見えていた。`fromSlot = pos ? current.indexOf(art.id) : null`を計算して両方のpressに渡すよう修正——同じ実機ページでswap・タップ移動・✕削除の3経路を再検証し正常動作を確認。
  - **⑨合同展示の作成UI一本化**: `RoomExpoBadge.tsx`から「既存の空部屋を選んだ展示に参加させる」変換リスト（`expos.map`で一覧表示→クリックで`switchRoomExpo(id)`）を撤去。合同展示の**作成**は常にExpoManagerからの新規作成（展示＋最初の部屋を自動生成）のみに。**「通常展示に戻す」（離脱）は残した**——これは作成ではなく離脱で、入口が2つになる話ではないため。`expo.roomSwitchAddHeading`のコピーを「合同展示は『合同展示』タブから新しく作成できます」に11言語で差し替え（旧文言「この部屋を合同展示に入れる」がユーザーの混乱の直接の原因だった）。
  - **⑩参加者の提出状況の可視化**: 調査の結果**既に実装済みだった**（`ExpoInvites.tsx`の`ParticipantsPanel`が`inv.status==='accepted'`のとき`t('invite.statusSubmitted',{count})`で提出点数をgold表示、招待中/参加希望/辞退は別表示）。新規実装は不要と判断——新たに作り込むと既存の同機能と重複するため。
  - **検証**: tsc / check:css・i18n(10言語100%)・schema・sns・expo・gates(56) / 本番ビルド、すべて通過。3D View側（SNSアイコン・技法サイズ・扉位置・右メニューモーダル・CTA分岐）とダッシュボード側（追加ボタン余白・表示範囲・合同展示UI）は合成HTML/DOMハーネス（`public/_qa-*.html`、検証後削除）で実描画確認。D&Dバグのみ実データを積んだ一時ルート(`app/qatest911/`、検証後削除)で実際のpointer drag列を打って再現・修正確認（認証必須の`/me`はローカルに鍵が無いため）。**Next.jsのアンダースコア始まりフォルダはルーティング対象外**という発見をLESSONSに記録。
  - **やらなかったこと**: 合同展示の「公開タイミング」自体の仕様変更（現状維持と決定済み）。
  - 以下は前回の記録: 2026-08-11（**ユーザー報告3件に対応。①配置タブでドラッグ&ドロップが効かないバグを修正**＝08843fa新設の「壁から外す✕」ボタンの`onPointerDown`が`stopPropagation`で親タイルの`press()`を握りつぶし、✕の小さな右上コーナーから掴もうとするとドラッグが完全に無反応になっていた。ボタン側でも`press()`を自前で呼ぶよう直し、あわせて新たに露見した「ドラッグ完了直後に✕のclickが遅延発火して置いたばかりの作品が消える」副作用も`justDraggedRef`で防止（`setPointerCapture`がクリックの実ターゲットを押し始めた要素へretargetする挙動が原因。詳細はLESSONS）。**認証必須の`/me`はローカルに鍵が無く開けないため、`esbuild`で`PlacementEditor.tsx`を実バンドルし、実CSS(`app/me.css`)付きの素のHTMLへ載せ、Playwright（同梱Chromium）で実際のpointerdown→move→up列を打って再現・検証**（jsdomではSVGの`getScreenCTM()`が無く不可）。**②プロフィールタブ直下の見出し「プロフィール」を削除**＝ステージタブ自体が既に「プロフィール」を表示・アクティブにしているため冗長だった（保存中インジケータは残す）。**③「この部屋はいっぱいです」が読み込み時に一瞬出る原因を調査・修正**＝`cloudArtworks`（口座全体の作品数、早く届く）と`poolCapacity`の材料`allOwnedRooms`（別fetch、後から届く）の競合。未着のあいだ`poolCapacity ?? row.work_cap`が**この部屋"だけ"の小さいwork_cap**にフォールバックし、共通プール化以降はそれが口座合計よりずっと小さいため、口座合計はまだ余裕があるのに一瞬だけ満室バナーが出ていた。`poolCapacity !== null`をガードに追加し、真の合計が届くまでバナーを出さないよう修正。ユーザー自身の疑問（「作品は部屋間で共通のはず」）が原因を正しく言い当てていた。検証: tsc/css/i18n(10言語100%)/schema/sns/expo/gates(56)/本番ビルドすべて通過。LESSONS 2件追記（イベント伝播バグ＋読み込みレース）。以下は前回の記録: 2026-08-11（**さらに続き、ユーザー指示4件に対応。①作品タブと部屋タブの順序を元に戻した**（作品→部屋→配置→公開。0809の「部屋→作品」から）。**②公開タブにも部屋切替タブを表示**（従来は部屋/配置タブのみ）。**③配置タブのトレイに3点追加**＝(a)口座内の他の部屋に既に配置済みの作品を示す金色ドット（右下。`lib/galleries.elsewherePlacedWorkIds`が各部屋の`arrangement`を集計）(b)配置済み番号バッジを右上→左上に移動(c)右上に✕（壁から外す）ボタンを新設。検証: tsc/css/i18n(10言語100%)/schema/sns/expo/gates(56)/本番ビルドすべて通過。合成DOMでトレイの3バッジ配置（左上番号・右上✕・右下ドット）を実測確認。**check:i18nの誤検知を1件発見・LESSONS記録**＝TS型定義で`ReadonlySet<string>`直後にコメント無しで別の型宣言が続くと「UI文言の直書き」に誤検出される（`i18n-ok`コメントで回避、再発したら3回ルールで検出ロジックを見直す）。以下は前回の記録: 2026-08-11（**前回pushの続きで、ユーザーが実機で見つけた確定バグ2件と要望2件に対応。①部屋切替で編集ステージが「作品」に戻るバグ**＝`GalleryCard`は`key={current.id}`で部屋ごとに再マウントされ、`stage`がそのuseStateにあったため切替のたびに初期値`'works'`に戻っていた。`stage`を`MePage`側に持たせ`stage`/`onStageChange`propで渡す形に直し、`Stage`型もモジュール直下へ移動。**②作品アップロード上限のバグ**＝共通プール化（前回）で表示・追加ボタン・満室表示は`poolCapacity`に揃えたが、実アップロード処理`onFiles`だけ`row.work_cap`（この部屋自身、最大15）を見ていたため、口座合計を50枠に増やしても15枚超で「上限に達しました」誤爆が出ていた。`onFiles`も`poolCapacity ?? row.work_cap`に統一。**③部屋追加ボタンをピル形に統一**＝説明文つき点線ボックス(`.me-room-add`)を廃止し、部屋タブと同じ角丸ピル(`.me-room-add-tab`)に。説明は`title`属性へ退避、無料枠が使える時だけ金色。**④「選んだ部屋だけアクティブにしたい」への回答**＝調査の結果、公開(Publish)ステージの`is_public`トグルが既にこれを実現している（オフの部屋は来場者向けの部屋一覧`RoomSwitch`/`lib/publish.ts`のsibling取得から`is_public=true`で絞られるため出てこない）。新機能は追加せず、ユーザーに使い方を確認中。検証: tsc/css/i18n(10言語100%)/schema/sns/expo/gates(56)/本番ビルドすべて通過。合成DOMで部屋タブ+追加ピルの横並び表示を実測確認。以下は前回の記録: 2026-08-11（**前回pushしたダッシュボードUI改善3点に対するユーザーFBに対応。①作品スロットの「移動」を撤回し口座全体の共通プールに作り直した**＝ユーザーが実際に触って「移動とかややこしいので、共通扱いにしてほしい」と指摘。DBスキーマ（`galleries.work_cap`・等級ロジック・購入経路）は無変更、集計をアプリ側だけで行う`lib/limits.poolCapacityOf()`（口座内の全部屋のwork_capを合算）に置き換え、作品タブの`{count}/{cap}`・追加ボタン・満室表示のcapをこれに差し替えた。**配置(PlacementEditor)側のworkCapは今回は変えていない**（この部屋自身のwork_capのまま＝ユーザーが反応したのは作品タブの話だったため）。migration 0053で0052の`transfer_room_capacity`関数を`drop function if exists`で撤去（CapacityTransfer.tsxも削除）。**②通知ベルの角丸(6px→10px)とwidth（PC幅で可変だったのを常時44px固定）をハンバーガーに揃えた**（ユーザー指摘: 角丸とwidthが不一致）。**③部屋タブ・部屋追加ボタン・間取り選択のUI再配置**＝(a)部屋切替タブ(`roomSwitcher`)を「部屋」「配置」ステージの箱(`.me-card.me-subcard`)の外・ステージタブ直下に移動、部屋追加ボタン(`roomAdd`)をその横に並べる新しい行`.me-rooms-row`を追加（「部屋」ステージのみ）(b)間取り選択・カスタムサイズ調整を「部屋」ステージから「配置」ステージの先頭へ移動（テーマ・照明・環境音は「部屋」ステージに残す）。いずれもJSXの描画位置だけの変更でロジック・propsは変えていない。検証: tsc/css/i18n(10言語100%)/schema/sns/expo/gates(56)/本番ビルドすべて通過。合成DOMでベル/ハンバーガーの44×44px・角丸10px一致、部屋タブ+追加ボタンの横並び配置を実測確認。**migration 0053はユーザーがSupabase側に未適用（要適用。0052を適用済みでも未適用でも安全）**。以下は前回の記録: 2026-08-10（**ダッシュボードのUI改善3点をユーザー指示で実装。①作品スロットの部屋間移動（migration 0052）**＝無料室5枠＋合同展示部屋15枠のように部屋ごとに固定されていた枠を、口座内の合計を変えずに部屋間で移動できるRPC `transfer_room_capacity(from,to,amount)` を新設（移動元は最低1枠残す・移動先は物理上限15枠まで・`switch_room_expo`と同じ security definer の作法で`guard_work_cap_raise`を経路だけ素通り）。UIは`components/me/CapacityTransfer.tsx`（作品ステージの「枠を追加」の隣に「枠を移動」ボタン→ドロップダウンで移動先の部屋と枚数を選ぶ）。`lib/galleries.ts`に`listAllOwnedRooms`（合同展示の部屋も含む一覧）と`transferRoomCapacity`/`transferCapacityErrorKey`を追加。**②合同展示の作成フローを1モーダルに統合**＝以前は「バッジ→タブ遷移→作成ボタン→別モーダル」の多段階だったのを、バッジ押下で開く1つのモーダル（`ExpoManager`自体を`.purchase-backdrop`+`.help-modal`の自前モーダルに作り替え）に一本化。一覧⇄作成フォームは同じパネル内で切り替え（入れ子モーダルにしない）。`app/me/page.tsx`の`MeTab`から`'expo'`を削除し`expoManagerOpen`状態に置き換えた。**③ダッシュボードヘッダーをPCでもハンバーガー化＋通知は仕様確認の末アイコンのまま**＝`app/me.css`の`.me-top-burger`/`.me-top-actions`の640px以下限定の分岐を撤去し常時ハンバーガーに（通知ベルは前日2026-08-09の「畳むと気づけない通知になる」という決定を優先し、ユーザー確認の結果アイコンのまま常時表示・ハンバーガーの左に配置を維持）。3件ともDECISIONS 2026-08-10に記録。検証: tsc/css/i18n(en+ja 100%)/schema/sns/expo/gates(56)/本番ビルドすべて通過。合成DOM・合成データでヘッダー折り畳み・合同展示モーダル・枠移動パネルの見た目を実描画確認。**migration 0052はユーザーがSupabase側に未適用（要適用）**。以下は前回の記録: 2026-08-10（**合同展示に3点追加（ユーザー指示）。①展示のタイトル+URLを入力して「作成」を押すと、展示だけでなく最初の部屋も自動でできる（従来は別途「部屋を追加」を押す2手間だった）②その作成フォームをポップアップ化（`.help-modal`を再利用）③**会期の開始日時を予約できるようにした**（migration 0051）。`record_expo_purchase`に7つ目の引数`p_starts_at`を追加（省略時は従来どおり即時、上限なしでユーザー選択）。**`expo_is_live()`の判定式は元から`now() >= starts_at`だったので、DB側のロジックは1行も変えていない**。表示用に`ExpoPhase`へ`scheduled`（支払い済みだが開始日時前）を追加。checkout route→Stripe metadata→webhookで日時を運ぶ。**この過程で確定バグを1件発見・同時に修理**＝2つのチェックアウトを並行して開き両方で支払いを終える競合状態で、2件目が別セッションidのため再送判定を素通りし`purchases`の一意制約に落ちてwebhookがクラッシュ→Stripeが延々と再送する状態を実際に再現した（判定を「セッションid一致、またはこの展示への支払いが既にある」に拡張して解決）。検証: tsc/i18n(11言語100%)/css/schema/gates(56)/check:expo/本番ビルド/SQLテストハーネス全9ファイル・252項目すべて通過。合成データの一時ページでモーダル・予約入力欄・予約済み表示を実描画。**追記: 「部屋一覧のカプセルタブ」は縦並びの全部屋同時表示ではなく、もっと軽い形で解決した**（ユーザーが自分で「待って、縦並びが正解かな」と再検討）。カプセルタブのUIとロジックは一切変えず、**置き場所だけ**「全ステージの上」から「部屋タブ・配置タブの中だけ」に移した。`GalleryCard`に`rooms`/`frontDoorId`/`onSwitchRoom`の3propsを追加し、コンポーネント内で`roomSwitcher`という1つのJSX変数を作って`stage==='room'`と`stage==='placement'`の先頭にだけ差し込む。作品・公開・参加者タブでは出ない（作品は口座全体の作品庫・公開は「いま開いている部屋」の話で、どちらも切替の意味が薄いため）。GalleryCardの内部状態は1つも触っていないので、前段で懸念した「最も複雑な状態管理の構造分離」は不要だった。検証: tsc/css/i18n/本番ビルド、合成データの一時ページで4ステージ×切替タブの有無・アクティブ状態の遷移を実測（`.click()`直後に`className`を読んで`active`が正しく移ることを確認）。以下は前回の記録: 2026-08-10（**合同展示の部屋編集画面を通常展示と統合（DECISIONS 2026-08-10。ユーザー「通常展示も合同展示も類似するUIになるといいかな」への対応）。合同展示の部屋にだけ「参加者」タブを追加（`ExpoManager`の`ParticipantsPanel`を移設）、「公開」タブは通常展示ならis_publicトグル・合同展示なら会期を選んで公開＝決済に分岐。`ExpoManager`は「展示一覧＋部屋を追加」だけに縮小。実装中に別の既存バグ（`ExpoManager`の「部屋を開く」が実は玄関の部屋を開いていた）も発見・修理（`getGalleryById`新設）。検証: tsc/i18n(11言語100%)/css/schema/gates(56)/本番ビルド、合成データの一時ページでタブバー・公開タブ3状態を実描画。**以下は前回の記録**: 2026-08-10（**合同展示の導線をHero右上に移設し、部屋の「通常展示↔合同展示」切替（migration 0050）を追加（ユーザー指示）。範囲は作成直後・空の部屋だけ（作品を置いた後は本格的な巻き戻し工事が要るため）。**唯一の部屋は合同展示に持っていけないガードを追加**（`/@handle` の玄関が無くなるのを防ぐ）。**あわせて既存バグを1件修理**＝ExpoManagerの「部屋を開く」で立てた `roomId` が `listMyGalleries()`（合同展示の部屋を弾く設計）に無いため、`current` が黙って玄関の部屋にフォールバックしていた（`getGalleryById` を新設して解決）。検証: tsc/i18n(959キー×10言語100%)/css/schema/gates(56)/check:expo/本番ビルド/SQLテストハーネス8ファイル・239項目すべて通過。**この過程でテストのUUID帯が別ファイル(0043)と衝突しフルスイートが謎の途中終了に見える事象に遭遇→LESSONSに記録**。以下は前回の記録: 2026-08-10（**合同展示（Expo）機能をPR #8としてmainにマージ（`719c3d4`）。マージ前レビューで確定バグ2件（決済回避によるINSERT公開・他人の展示IDを使ったwork_cap無制限）を発見しmigration 0049で修正。詳細は完了ログ参照**）。以下は前回の記録: 2026-08-10（**③合同展示の公開ページを実装（ユーザー選択A＝読み経路の切り出し）**。`buildExhibition()` を切り出して入口を2つに（`is_public` で絞る既存の入口／**会期で決まる** `fetchExpoExhibition`）。URLの組み立ては `roomPath()` 1か所に集約したので、canonical と3Dの扉が食い違えない。`/expo/{名前}` は**合同展示 → アカウント別名(0040)** の順で解決。猶予中は「終了しました」の帯＋`noindex`、sitemap には**会期中のものだけ**（ロビー＋各部屋）。既存の穴も1つ塞いだ＝`fetchOwnExhibition` が合同展示の部屋を下書きとして掴む経路。**あわせて migration 0046**＝0045 で `is_public` を捨てた副作用で、**会期中の合同展示だけ芳名帳・いいね・人影が死んでいた**のを直した（払った展示でだけ体験が欠ける形）。**さらに migration 0047**＝⑤の本体。招待と提出を**部屋単位から展示単位へ載せ替え、部屋への招待を撤去**（2室目を作るたびに招き直しが必要だった）。参加者の管理は合同展示タブの各展示の中に移した。**そして migration 0048**＝**招待リンク**（`/join/{token}`。配れる1本のURLで参加希望を集め、主催者が承認する）。承認前は何の権限も無いので流出しても「承認待ちが増える」だけ。**希望を出した本人が自分を承認できてしまう穴**をガードで塞いだ。**番人が2度目の嘘をついた**ので SQL テストの判定を `scripts/sql-verdict.mjs` に出し、行頭単独の `f` も見るようにして check:gates に4ケース追加。tsc/build/番人6種（56）/SQL 7本（211項目）＋冪等3周とschema.sql完全一致 通過。**`.env` が無い容器なので `/@ハンドル` の実描画による回帰確認は未実施**＝次にプレビューを持つ環境で1度歩くこと）。以下は前回の記録: 2026-08-09（**指摘の2巡目に対応。migration 0043 を追加＝ユーザー適用待ち**。①通知ベルを行の高さに揃えさせる形に作り直し（前回は**フォントを読み込んでいないハーネスの実測値16pxを焼き込んでいた**ため本番でだけズレていた）＋`.me-top-nav` に `gap` が無くベルが隣とくっついていたのを修正。sans/serif/等幅×3サイズで高さ一致・正方形・隙間8pxを実測②**Threads の正規ドメインが古かった**（2025年に threads.net → threads.com へ移行）ため、貼ったURLが書き換わっていたのを修正。あわせて `check:sns` が「表示→再保存の往復で保存形が変わる」バグ（Bluesky）も検出したので修正 ③**migration 0043**: `/admin` の「展示室を追加」で**部屋が実際に増える**ようにした（`admin_add_room` が台帳と galleries を1トランザクションで。14項目検証）④合同展示のフローはユーザーに確認中（部屋ごとに別URLを持つので、合同展示は「専用の部屋を1室作ってそこに招く」形になる。参加者タブは非表示のまま、恒久的な導線は未決）**）。以下は前回の記録: **合同展示の招待を実装して push（migration 0041。作家が出す形・同意は作品単位・辞退でその場で壁から下りる。前提工事として `listMyArtworks` の所有者絞り漏れも修理。別視点レビュー7件全部修正）。この前に、展示のURLをサブドメインから `xibit360.art/expo/{slug}` に切り替えて push（migration 0040 で `subdomain` → `expo_slug` に改名。Hobby の50ドメイン上限と展示ごとの手作業を理由にユーザーが撤回。ホスト解析は env で切って残置＝`NEXT_PUBLIC_EXPO_DOMAIN` は未設定のまま）。この前に、展示室を複数にする工事（$25/室・15枠込み・玄関1つ・3Dの扉＋2Dの部屋一覧）を実装し検証を通して push。migration `0036`〜`0040` の本番適用がユーザー作業として残る**）。以下はさらに前の記録: 2026-08-04（**SNS入力欄を「URLを貼るだけ」に作り直して ship（白地の是正・フルURL表示・取り違えの注意・Bluesky/Discord/LINE追加）。`70a81fa` まで push・本番反映確認済み**）。以下は前回の記録: 2026-08-03（**ヘルプ/FAQページ `/help` とダッシュボード(/me)のポップアップを新設して ship（`b60d50e`・本番反映確認済み、`/ja` `/en` `/ko` で表示＋ビデオパス$20の差し込みを実測）。公開ページとポップアップが共有コンポーネント `HelpContent` を描画（DECISIONS 2026-08-03, C案）。中身は使い方＋FAQ（10問）で、ビデオパス$20・追加枠$3・1部屋15点・動画40MB は pricing/limits の設定値から導出し直書きしない。`/help` を `LOCALIZED_PATHS` と sitemap に登録し全11言語＋hreflang。ユーザー指示で「動画は展示できますか？＝ビデオパス」の1問を追加（`PRICE_VIDEO_PASS` を pricing に新設）。別視点レビューで実バグ0件。**このpushには別セッションのログイン/サインアップ強化＋メールテンプレの5コミット（`190bcdd`〜`195f557`）も同梱**（ユーザー「他セッションで切り分けました」）。ただし各言語ファイルの `auth.*` 追加キーは push 時点で working tree に未コミットのまま残置＝別セッションが要コミット。i18n ファイルは help ハンクだけを `git apply --cached` で選択ステージし、相手の未コミット auth キーは触れず保全した。以下は前回の記録: 記事に構造化データ（JSON-LD）を追加（ユーザー「構造化お願い」）。`BlogPosting`＋`Blog`＋`Organization`＋`WebSite`＋`BreadcrumbList`、一覧は `Blog.blogPost` にインライン。`inLanguage:en` はギャラリー側と逆に**書く**（記事は ARTICLE_LOCALE で構造的に英語＝知っていること）。`author`/`publisher` は Organization、`publisher.logo` は指せる画像が無いので入れない、`headline` は110字超でも切り詰めない（ページと食い違わせない）。合成4通りで JSON妥当・未解決@id 0件・`</script>` 非漏洩を実測。この前に: ブログの多言語URLを1本に集約（ユーザー「英語がベース・日本語だけの記事は書かない・SEOに最も効く形にしたい」）。記事は英語1本なのに11言語ぶんのURLが sitemap に載り hreflang が相互に翻訳を名乗っていた（1記事の評価が11分割・宣言が嘘）→ `singleLanguageAlternates` を新設し canonical/hreflang/sitemap を `/en/articles/…` に集約。他10URLは人間向けに残す（noindexは付けない）。og の published/modified_time も追加。実測で `/ja/articles` `/zh-hant/articles` の canonical が `/en/articles` を指し、sitemap の articles 行が1本になったことを確認。DECISIONS に記録。以下は前回の記録: ブログ（/articles 一覧・本文）にサイドバーを追加（ユーザー指示「流入した人が何のサービスのブログかわからない」）。`ArticleSidebar` を新設し、①「XIBIT360とは」案内＋無料ではじめる／デモを歩く ②本文ページは「ほかのガイド」最大5本 ③公開中の展覧会を見る。900px以上で2カラム＋sticky、以下は縦積み。本文ページのヘッダの「← Guides」をロゴ XIBIT360 に替え、戻る導線は本文上の `.article-back` へ。新キー4×11言語。プレビュー実測（1440/1280/1024/900/820/375）。以下は前回の記録: PCで配置ステージを2カラムに（左=配置図/右=作品トレイ、他ステージと同様）。721px以上で `.place-editor` を grid-template-areas 'map tray' に、↑は非表示、トレイ5列維持。720px以下は縦積み継続。`0f76d06` を push・本番反映確認済み。プレビューで1024px=2カラム/375px=縦積みを実測。以下は前回の記録: 配置図は俯瞰マップに戻し、『5列15』は下部の作品トレイに適用（前回の取り違え修正。ユーザー「5列15は作品群のこと・配置図はこれじゃわからない」）。俯瞰マップ版に git checkout で戻し、①トレイを5列グリッド②配置済みトレイ作品に配置番号バッジ③配置済みスロット枠をグレーに(選択時のみ金)④マップとトレイの間に↑ を再適用。`8d5db99` を push・本番反映確認済み。プレビューでマップ復活・トレイ5列・グレー枠・↑・番号バッジ一致を実測。取り違えを LESSONS に記録。以下は前回の記録: 配置UIを俯瞰SVGマップ→5列×最大3行=15のタイルグリッドに（ユーザー指示）。当たり判定を elementFromPoint→data-slot のDOMベースにして幾何計算を撤去し大幅簡素化。①配置済みタイル枠を空きと同じグレーに（Primary金は tap選択/ドラッグ先だけ）②トレイの配置済み作品に配置番号バッジ（移動で更新）③グリッドとトレイの間に↑。`5839fb2` まで push・本番反映確認済み。別視点レビューで確定バグなし。プレビューで5列15セル・グレー枠・picked金・番号バッジ・ドラッグでW1が1→9移動＋バッジ更新を実測。以下は前回の記録: Publishタブの見た目調整（ユーザー指示）＝①展示タイトル/説明を他の入力欄と同じ『箱』の見た目に（bg＋1px枠＋角丸）②公開URL行を2段に（1段目URL＋金色コピーアイコン／2段目トグル＋OPEN）でトグルとラベルの改行分断を解消。`bbc6173` を push・本番反映確認済み。プレビュー実測。**配置トレイの下部作品群は現状の折返し（wrap）を維持**＝横1列＋横スクロールにするとドラッグ(配置)とスワイプ(スクロール)が競合するため（touch-action:noneでスワイプ不可）。以下は前回の記録: 配置マップに『配置済み作品を壁から外す（トレイに戻す・自動補充しない）』を追加（ユーザー指示）。配置済みをタップ→「壁から外す」バー、または壁の作品をトレイにドラッグで外す。`placeWorks` を「arrangement が非空なら自動補充せず並びを正とする」挙動に（外した作品・未配置の新規は壁に出ない／空 arrangement は従来どおり全補充）。`eaaaa0e` まで push・本番反映確認済み。別視点レビューで確定1件（レイアウト縮小で範囲外ピン作品が消える退行）を修正＝『名前は残るが座れなかった作品だけ救済』。node で権威挙動＋縮小救済を実証、プレビューでタップ→外すを実測。以下は前回の記録: 配置マップの操作性を5点改善（ユーザー指示）＝ドロップ先を拡大＋金枠で強調／スロットを壁線の中心へ（NUDGE0）／スロット拡大（S1.9・地図860px）／ロック撤去で自由配置（作品数上限は works 側で担保）／タップ配置を追加。`3b04c19` まで push・本番反映確認済み。別視点レビューで確定2件を修正＝①中央壁スロットの重なり（noWireだけ0.8m逃がす）②onFilesの上限超過（残容量クランプ）。プレビューでタップpick→place・ドラッグswap・island重なり0を実測。以下は前回の記録: 配置ステージを『俯瞰マップへのドラッグ＆ドロップ＋作品トレイ』に作り替え（ユーザー指示。並べ替えリストから再転換）。トレイの作品をスロットにドラッグして掛け、掛けた作品どうしはドラッグで入れ替え（swap）、鍵付きへのドロップは容量購入。`c6d6717` まで push・本番反映確認済み。別視点レビューで確定バグなし＋LOW1件（保存往復中の再ドロップ巻き戻し）を世代管理で修正。プレビュー実測で pointerdown が装飾rectに取られる取りこぼしを発見し、SVG座標ヒットテストへ設計変更＝LESSONS に記録**）。以下は前回の記録: 2026-07-31（**ステージタブの調整＝完了チェックアイコン削除＋横スクロールのスクロールバー非表示（`ad2b814`、本番反映確認済み）。あわせて `0035_light_override.sql` はユーザー適用済みを記録。この前に、プロフィールを一番左のタブに・配置ステージを『並べ替えリスト』に（マップ撤去。ユーザー指示「配置換えが複雑すぎて操作方法わからない」→「並べ替えるだけにする」）＝別視点レビューが2件の確定バグを検出→修正（①arrangementクリアの競合状態 ②並べ替え後にrow.arrangementが古いまま残り後続の部屋/公開編集で書き戻される回帰）。`b757e69` を push・本番反映確認済み**）。以下は前回の記録: 2026-07-30（**プロフィールのSNSを主要9プラットフォーム＋その他(カスタム)に拡張・公式ブランドアイコン(Simple Icons CC0)を表示。データは {links,custom} 構造で DB無変更(profiles.sns jsonb)＋旧形式を自動マイグレーション。別視点レビューで確定バグなし。`58f0574` を push・本番反映確認待ち**）。以下は前回の記録: 2026-07-30（**芳名帳を公開ステージへ統合（5ステージに）・配置ステージを『配置だけ』に（作品編集シートを撤去＝作品ステージとの重複解消・1カラム全幅でスロット25px）・ステージバーの縦スクロールを解消（overflow-y:hidden）。別視点レビューで確定バグなし。`56e1133` を push・本番反映確認待ち**）。以下は前回の記録: 2026-07-30（**芳名帳・プロフィールをステージバーに追加（6ステージ）・上部タブとヘッダカード（「スクショの領域」）を削除。別視点レビューで確定バグなしを確認（芳名帳ステージのfetch中フラッシュのみ既知・本番は0008適用済みで軽微）。`57557a6` を push・本番反映確認待ち**）。以下は前回の記録: 2026-07-30（**3D作品画像をcover表示（アスペクト比保持＋トリミング）・トリミング時に編集シートで警告・配置図の枠を部屋輪郭の内側へ（S1.25→1.0/NUDGE0.5→0.55）。別視点レビューで確定バグなしを確認（共有テクスチャのミューテーションは src 一意＋物理サイズが純関数で冪等）。`58112c1` を push・本番反映確認待ち**）。以下は前回の記録: 2026-07-30（**配置ステージをPC2カラム化（左=配置図sticky/右=設定）・作品削除を各サムネ右上の×へ・配置図の黒い浮いた下地を解消。別視点レビューがタブレット幅のはみ出しを検出→修正（`6a5d83a` まで push・本番反映確認済み）。**LESSONS「CSSが書いてあるのに効いていない」に 『シェア再利用時は全メディアクエリを洗う』を追記**）。以下は前回の記録: 2026-07-30（**ダッシュボードのヘッダ整理と配置ステージの地図中心化を ship（`60ee9f4` を push・本番反映確認済み）— ヘッダのグラデ帯リボン削除／「部屋を歩く」をヘッダから公開ステージのボタン列（埋め込み・カタログの隣）へ移動／配置ステージの3Dプレビュー削除（配置図がプレビュー役）／配置図をスクロール時に上部固定(sticky)。**この作業は原点(`a4460e6`)からブランチが動いておらず単独で着地**）。以下は前回の記録: 2026-07-30（**ダッシュボードの操作感5点をユーザー指示で改修して ship（`87f29b3` まで push・本番反映確認済み）— アカウント設定をタブから右上メニューへ／展示タイトルの編集を公開タブへ（ヘッダは表示のみ）／作品ステージを横一列＋常時1点選択＋詳細を常時表示（ハーフモーダル廃止）／部屋のテンプレート行を削除／配置図のスロットを15px→31pxに（地図を1カラム＋全幅化）。**並行セッションのSEO対応3コミットも同じ push に含めた**。この作業中に `git commit --amend` で相手のコミットを壊し復旧＝LESSONS に記録（相手も直後に同型を踏み ×2）**）。以下は前回の記録: 2026-07-29（**本番の決済が Managed Payments の制約で400になっていたのを修理（`custom_text` を削除）。あわせてユーザー指示で購入時の同意チェックボックスを廃止し、CTA下の一文＋規約リンクにした。`b94df50` を push**）。以下は前回の記録: 2026-07-29（**文言チェックの穴（タグや式と同じ行に始まり行末まで続く地の文）を塞ぎ、残っていた英語5文を11言語へ。プレースホルダの取り違えの番人も追加。別セッションの配置図の作業と一緒に ship**）。以下は前回の記録: 2026-07-29（**配置図(placement)を囲みなし＋15枠すべて表示＋容量超過は鍵付きで購入（ユーザー指示）。あわせて `/me` の横スクロール不具合（ユーザー報告）の原因2件を実測で特定して修理。コミット済・push待ち**）。以下は前回の記録: 2026-07-29（**選択肢の並びを「使えるものが左」に統一（ユーザー指示）＋その作業中に見つけた課金の抜け穴＝3Dパネルのテンプレートが有料テーマ/間取りを素通りさせていたのを塞いだ。`15db047` を ship・本番反映確認済み**）。以下は前回の記録: 2026-07-29（**購入モーダルの `{unit}` 差し込みを廃止し、単位の語を各言語の文に直接書いた（5言語で壊れていた。ko は助詞、de/it は複数形の語、es/pt-br は語尾s）。日本語の助数詞「10つ」も直した**）。以下は前回の記録: 2026-07-29（**未pushで取り残されていたコミットを全部着地させた（ユーザー指示）— `d3f972f`（決済不具合の台帳記録）は内容を現在の台帳へ移し、`dd4c59b`（{式}内リテラルの検出＋英語44文言の翻訳）は17ファイルの衝突を解消して `476d54e` として ship・本番反映確認済み。未pushコミットは全ブランチでゼロになった**）。以下は前回の記録: 2026-07-29（**`schema.sql` が migrations から取り残されるのを検出する番人 `npm run check:schema` を追加（LESSONS の「番人化は未実施」を機械化）**）。以下は前回の記録: 2026-07-29（**/kaizen で昇格2件を ship（プレビュー検証のスキル化 ＋ push直前の並行セッション番人 `check:ship-ready`）**）。以下は前回の記録: 2026-07-29（**i18nの複数形フォールバックのバグを修正して ship（count=1 で「1 works」と読める文が出ていた）**）。以下は前回の記録: 2026-07-29（**統合スキーマ `supabase/schema.sql` が 0024 で止まっていた（0025〜0034 の10本が欠落）のを修復して ship。ローカルにPostgres 16を入れて実行検証できる体制になった**）。以下は前回の記録: 2026-07-29（**トップページの3Dヒーローに残っていた英語14文を辞書化し、CJKで壁から溢れる折り返しを直して ship**）。以下は前回の記録: 2026-07-29（**モバイルUIの細部6件と販売構造2件を ship（`563ed60` まで push・本番反映確認済み）。額を売る工事（C）も ship 済（`b500dd8`）＝残るは `0034_frame_purchases.sql` の適用**）。以下は前回の記録: 2026-07-28（**/kaizen で昇格2件（番人の自己テスト `check:gates` ／ `.next` 共有ガードの穴2つ）＋モバイル修正の取り残し `6bc728e` を cherry-pick で出荷（入場ガイド1行化＋レール縦リスト化）＋404ページの新設（`6a5f3c2`、別セッション）＋死んだCSS90件の掃除＝基準線を空配列に（`a15237c`）＋`check:css`をIDセレクタにも広げ空振りの`#stage`を削除（`949f8a3`、本番反映確認済み）**。以下は既存の記録: モバイル6件＋Phase 0（課金・法務）4件＋オンボーディング3件＋通報運用/芳名帳＋CJK組版＋i18n基盤とEN/JA辞書＋B15/B16/B17（タブレット幅・入力欄ズーム・タップ対象）＋**B18（srcset）**＋**UI文言の直書き検出（`npm run check:i18n`）と残っていた英語29箇所の辞書化**をship。辞書は614キーがen/jaで一致。残: support@ の受信設定、実カードE2E、ユーザーが自分の個展を1つ公開する実機QA、**多言語の網羅範囲（どの言語を入れるか）はユーザー判断待ち**、B14は他の改善完了まで待機）

## 移した「進行中」（〜2026-08-13。ほとんどが出荷済みだが `進行中` に残っていたもの）

- **【実装済・検証済／push直後】展示室を複数にする（ユーザー決定 2026-08-09。DECISIONS 2026-08-09 に全文）**
  - **$25/室・買い切り・繰り返し可・その部屋の15枠込み**（`PRICE_USD_CENTS.room = 2500`）。**レイアウト/テーマは別売りのまま**。枠は「数」で持ち（`roomAllowance`）、**部屋は購入後に本人が作る**ので webhook は台帳1行だけ書く＝決済で半端な部屋が残る経路が無い／部屋を消しても購入は焼けない。
  - **URLは作家1本**。玄関（メイン部屋・`galleries.is_main`、migration 0036）を1つ決め、`/@name` はその部屋を描く。サブ部屋は `/@name/[slug]`。canonical・sitemap・breadcrumb も `isMain` 基準に切り替えた（旧: `publicGalleryCount === 1`）。
  - **移動は新規ロード**。3Dは常に1室だけ描く。扉は壁に穴を開けず床に立つ枠（`RoomPortals`）で、位置は `layout.entry` の側方・spawnから2.2m・最大4枚。2DのHUDに全室の一覧（`RoomSwitch`）。埋め込みでは一覧を出さず扉は新規タブ。
  - **番人はDBに置いた**（0036 のトリガ2本）。insert はブラウザから RLS 経由で来るため、クライアントが渡す購入数を信じるだけでは**購入ゼロで部屋が無限に作れた**。`enforce_room_allowance` が購入台帳を数え初期capも縛り、`guard_work_cap_raise` が `work_cap` の引き上げを購入経路だけに許す。
  - **満杯時に2択を並べて開示**: 1部屋目を5→15は $30、新室は15枠込み $25 なので**容量だけなら新室が安い**。値段は変えず両方見せた。
  - **作品ごとの額装を部屋ごとに直した**（ストア経由だと玄関の部屋に保存されていた＝別視点レビューの最重要指摘）。出どころは `fetchPlacementOverrides(row.id)`、保存は `rebuildPlacements(row.id)`。
  - **新しい部屋は空で開く**（`arrangement` を明示 null 配列に）。未設定だと隣の部屋と同じ内容で開いてしまう。
  - **別視点レビュー（/code-review high）で8件指摘され、実バグ7件を修正**（額装のスコープ／扉の向きが90°ずれて全部横向き／扉がspawn上に立つ／購入数がクライアント任せ＝課金の穴／`walk in 3D` が常に玄関の部屋へ／新室が既存作品で埋まる／docs未記載）。**幾何は自作の全数照合スクリプトで3件を検出**（レイアウト5種＋customの両端 × 部屋1/2/3/4/5/8）。
  - **ロンダリングは塞いだ**（migration 0038・ユーザー指示 2026-08-09）。等級 `slots_included` を行に持ち、部屋数を作成順ではなく等級で数える。育てた無料部屋は無料のままなので買った人が損をせず、購入した部屋を消しても購入は焼けない。等級は作成時に固定。
  - **無断掲載の穴を塞いだ**（migration 0037・ユーザー指示 2026-08-09）。placement は「自分の作品」か「受諾済み招待がある作品」だけ。招いた側は受諾できず、辞退で壁から下りる。**UIはまだ無い**（招待画面は合同展示と一緒）ので既存の全ケースで no-op。**これが合同展示の土台**。
  - **特商法の価格表示とFAQを追随**（ユーザー指示 2026-08-09）。`/legal` の列挙に**部屋($25)とビデオパス($20)**を足した（ビデオパスは 2026-08-03 から漏れていた＝LESSONS ×2）。FAQ の「お金がかかるのはスペース追加のときだけ」も3軸に直した。
  - **ユーザー作業**: **`supabase/migrations/0036_main_room.sql`・`0037_placement_consent.sql`・`0038_room_grade.sql` を本番DBに適用する（番号順に）**（0036 は未適用でも動く＝`mainRoomOf` が最古の部屋にフォールバックし複数室UIは出ないが、**トリガが無い＝課金の番人も効かない**ので部屋を売る前に必ず適用する。**0037 は未適用のあいだ無断掲載の穴が開いたまま**なので、こちらは売る売らないに関係なく適用する）。
  - **【実装済 2026-08-09】作者の配管**（DECISIONS 2026-08-09「作者は作品ごと」）。作者名を作品の所有者から取るようにし、板の作家欄を部屋の中身から導出（1人ならその作家、混在なら団体）、構造化データも作品ごとの `creator` に。**合同展示の必須前提はこれで済んだ**。
  - **【実装済・検証済 2026-08-09】展示のURLは `xibit360.art/expo/{slug}`**（DECISIONS 2026-08-09「サブドメインを撤回してパスにする」）。ユーザー判断「URLですが xibit360.art/name-of-expo/ じゃだめですかね / サブドメインが hobby だと難しい気がする」。**サブドメインは実機で動くところまで確認した**（`tokyo-expo.xibit360.art` が 200 OK）が、**展示1件ごとに Vercel のドメイン追加と Cloudflare の CNAME が要る＝運用コストが展示数に比例**し、Hobby の1プロジェクト50ドメインという天井もあるので撤回。パスなら追加作業ゼロ・上限なし。ルート直下にしなかったのは、ルートにアプリのルート14個と言語コード11個が居て将来のルートとも衝突するため。
    - **列は改名した**（migration 0040）: `profiles.subdomain` → `expo_slug`、`set_expo_subdomain` → `set_expo_slug`、`guard_subdomain` → `guard_expo_slug`、制約と索引も同様。必要なものは「展示ごとにユニークな名前」でこの列がまさにそれなので作り直さず、**パスを駆動するものが `subdomain` という名前のままだと後で必ず誤解される**ので名前だけ揃えた。**0040 は 0039 を適用済みでも飛ばしていても同じ結果になる**。
    - **ホスト解析のコードは消さず env で切って残した**（ユーザー承認）。`NEXT_PUBLIC_EXPO_DOMAIN` 未設定で `expoSlugFromHost()` が常に null を返し、middleware も canonical もパス側に倒れる。**今は未設定のままにする**（＝以前お願いした env 設定は不要になった）。将来サブドメインをやるなら環境変数とDNSだけで戻せる。
    - ルートは `app/expo/[slug]/`（`[room]` 付き）。付与は `/admin` から管理者だけ（行だけ先に入ると canonical が実在しないURLを指す）。公開物ゼロの slug は 404（一覧へのフォールバックは持たない）。
    - **検証**: 0040 を Postgres 16 に**27項目 × 3経路**（0039済→0040／0039を飛ばして0040だけ／0040を3回）で実測。tsc / build / 番人4種通過、11言語100%。**1回目の測定は全部 vacuous な `t` だった**（ロールの重複でハーネスが `profiles` を作る前に止まっていた）→ ハーネスに存在アサーションを入れて再測。LESSONS 2026-08-09「検証・ツール」に記録。
    - **ユーザー作業**: `supabase/migrations/0039_expo_subdomain.sql` と `0040_expo_slug.sql` を番号順に適用（0039 を未適用なら **0040 だけでよい**）。**DNS作業とVercelのドメイン追加はもう要らない**（tokyo-expo のホストは残しておいても害はない — env 未設定なので誰も canonical に書かない）。
  - **【実装済・検証済 2026-08-09】合同展示の招待（migration 0041）**（DECISIONS 2026-08-09「招待は『作家が出す』」に全文）。形は **主催者が `@ハンドル` で招く → 作家が受諾 → **作家が出す作品を選ぶ** → 主催者が配置図で掛ける**。課金は主催者だけ（参加者無料・新SKUなし）。
    - **主催者は相手のライブラリを見ない**。pull（主催者が漁る）にすると、誘われただけで習作・未発表作まで見えてしまう。push なら**見える範囲＝出すと決めた範囲**。0037 は招待の表を作っただけで**主催者が相手の作品を見る経路が無く**、UIが原理的に描けなかった＝0041 がその経路（`artworks_select_submitted_to_my_room`）。
    - **同意を作品単位に締めた**（0037 の②を上書き）: 「受諾済み招待」→「受諾済み招待**かつ**提出済み」。辞退・取り下げ・主催者の取り下げのどれでも**掛かっている作品がその場で下りる**。
    - **述語は全部 `security definer` の関数**。ポリシー内に直接書いたら `room_submissions` ⇄ `artworks` で**RLSが無限再帰**して全部落ちた（実測）。`guard_*` が invoker でなければ意味が無いのと**逆の理由**で definer。
    - UI: 受信箱は `/me` の一番上（**招かれたことに気づく場所が他に無い** — 通知は無い）。参加者ステージは**招待があるときだけ**出し、0件のときの入口は配置ステージの下に1行。トレイの他作家の作品には**作家名をタイルに焼く**（誰の作品か分からないまま公開させない）。**提出は自動では並ばない**（申し出であって決定ではない）。
    - **前提工事で既存バグを1件修理**: `listMyArtworks()` が所有者で絞っておらず、**他人の公開作品が自分の作品一覧に自分名義で入っていた**（実DBで確認）。他の作家が公開するまで見えない潜在バグ。
    - **別視点レビューで7件指摘され全部修正**。最重要は **`rebuildPlacements` の guests 渡し忘れ2箇所**（`setGalleryPublic` / store の `runGallerySync`）＝「**公開したら他作家の作品だけ消える**」静かな損失で、既定値 `[]` のため tsc も黙る。→ **省略時は自分で取りに行く**ようにし、取得失敗は投げる。他に辞退された相手を招き直せない（無反応）・`listMyInvites` の絞り漏れ・エラーを握り潰して「0点提出」に見える等。
    - **検証**: 0041 を Postgres 16 に `schema.sql` ごと適用して**35項目**＋レビュー修正の**再測8項目**、`placeWorks` の guests を**11ケース**。tsc / build / 番人4種（52）通過、11言語100%（35キー×11）。
    - **既知の限界**: ①通知が無い（気づくのはダッシュボードを開いたときだけ）②作家側から「自分の作品が実際に掛かったか」は見えない③招待の一括送信・上限は無い。
    - **ユーザー作業**: `0041_room_submissions.sql` を適用（0037 → 0041 の順。0041 で `may_place_artwork` が作品単位に締まる）。
  - **以下は経緯**: 当初サブドメイン方式に決定 → 同日パスへ撤回（上記）。**セッションは localStorage なので認証クッキーを広げる必要は無い**（当初の懸念は外れ）。**個人でも使えるが、今のところ仕組みだけ用意して `/admin` から付与する**（作家が自分で取るUIは後回し）。残りは**招待のUI**（合同展示 #3）。
  - **動画のストレージ上限は 300MB のまま据え置き**（ユーザー判断 2026-08-09「一旦300MBで」）。多室でも `PLAN.storageBytes` は据え置き＝動画は1本40MBなので実質7本が上限。多室と動画を両方買った人が最初に詰まる場所なので、詰まった声が出たら別枠を検討する（今は増やさない）。
- **【実装済・push済 2026-08-06】`/admin` のユーザー表に「課金額」列を追加（ユーザー指示「課金額合計を追加して。合計がいいのか小計がいいのかは不明だけど」）**。
  - **合計か小計かはデータ側で決まっていた**: 通貨をまたいで足せない（Managed Payments が現地通貨で請求するため `$5.00` と `¥500` がどちらも `500` で入る＝migration 0031）。なので**通貨ごとに出す**の一択で、単一通貨なら `$29.00` と1つの合計に見え、混在した買い手だけ `$8.00 · ¥500` と並ぶ。**画面上部の全体売上と同じ見せ方**（`revenueLines.join(' · ')`）に揃えた。
  - **`sumByCurrency()` を `lib/admin.ts` に切り出して、全体売上と1ユーザーの支払額の両方をこれで出す**。「円とドルを足さない」規則が2か所に分かれるのを防ぐため。null 金額は0として扱う（Theme Collection は本体行に金額・付与された各テーマ行は null＝二重計上防止。webhook 側の設計に合わせる）。
  - 新キー `admin.colPaid` を11言語に。列は WORKS と PACKAGES の間。無料ユーザーは `—`。
  - **検証**: tsc / 番人5種（i18n 11言語100%・795キー）/ クリーン本番ビルド / `sumByCurrency` を node で5ケース（単一通貨・通貨混在・null混じり・空・currency空文字/大文字）/ **合成データの一時ページで実描画**（検証後に削除）— ヘッダ6列と各行6セルが一致、`$29.00` `$8.00 · ¥500` `—` を実測、**ユーザー別の合計（$29+$8）が画面上部の全体売上 $37.00 と一致**することも確認。
  - **やらなかったこと**: 課金額での並べ替え（現状は作品数の降順のまま）。必要なら足す。
- **【ship済・本番配信確認済み 2026-08-06】アナリティクス（GA4）を実装（ユーザー指示「アナリティクスを仕込むので計測ポイントを洗い出して。ビジターのギャラリー内の行動は細かく／出展者も」→ 手段の選択で「gaがいいな」）**。棚卸しは `docs/ANALYTICS.md`、実装は40イベント。**`main` にマージ済み・本番配信を確認済み**（**2026-08-06: 測定IDを `G-28ESGMFJN2` へ差し替え、本番で `gtag/js?id=G-28ESGMFJN2` の配信を実測確認済み**。旧 `G-2FPCW1P607` は初回検証時のもの）。
  - **測定ID `G-28ESGMFJN2`**（旧 `G-2FPCW1P607` から差し替え。**IDを変えたら再デプロイが要る** — `NEXT_PUBLIC_*` はビルド時に焼き込まれるため。**別プロパティに移った場合、保持期間14か月・Googleシグナルoff・広告データ共有の全不許可・カスタムディメンション27/指標5 はプロパティ単位の設定なので新プロパティで作り直しが要る**。同一プロパティ内の新ストリームなら引き継がれ、作り直しが要るのはストリーム単位の設定＝拡張計測機能と内部トラフィック除外だけ）。`NEXT_PUBLIC_GA_ID` は Vercel の Production のみに設定（Preview/Development は未設定＝プレビューがプロパティを汚さない）。未設定なら `window.dataLayer` すら作らない完全無音。
  - **一番の収穫**: 入場に失敗した訪問者が**完全にサイレント**だった（WebGL無し→2Dフォールバック `GalleryApp.tsx:182`/`:367`、iOSのコンテキストロスト `:277`、12秒ロードタイムアウト `:243`）。「訪問数は出ているのに反応が無い」の正体が真っ黒画面である可能性を、今のデータでは否定できなかった。全部イベント化した。
  - **設計**: ビジターのUI状態はほぼ全部 zustand にあるので **`useGallery.subscribe` 1本**（`components/gallery/AnalyticsProbe.tsx`）で focus / dwell / ツアー / パネル開閉 / セッション要約を取る＝3Dコンポーネント側の改造がほぼ不要。プローブは**`VisitorGallery` 側（`dynamic()` の外）**に置いた — `GalleryApp` の中だとロード中の離脱が見えない。移動は React の外（`camPose`）なので1秒サンプラ、`useFrame` からは呼ばない。
  - **`via` の撃ち分け**（`lib/analytics.ts` の `focusIntent` ＝ `walkRef`/`camPose` と同じモジュール単一値の作法）: click / stepper / key / swipe / tour。**ツアー由来のビューを人気と混ぜると、作品別ランキングが「関心」ではなく「スロット順」の計測になる**ため必須。
  - **滞在時間は可視時間のみ**（AGENTS 5.6 の裏返し）。実測で 1秒視聴→3秒バックグラウンド→1秒視聴 が **1006ms + 1003ms**（素朴な実装なら約5000ms）。
  - **プライバシーポリシーを改訂**（`app/privacy/page.tsx`）。**「第三者アナリティクスを使わない」の一文は削除**＝実装と食い違う文言を残さない。EEA/UK/CH は Consent Mode v2 で `analytics_storage: denied` 既定・広告系は全地域常時denied。**GA4側の「広告向けデータ共有」は307地域すべて許可が既定だったため、依頼先が全不許可に変更済み**（ポリシーの記述と食い違う箇所だった）。
  - **GA4の限界（記録）**: ①**作家向けの数字には使えない** — `/me` の訪問数・♡は今後も Supabase の `visits`/`likes` が出どころ②広告ブロッカーで落ちるので**下限であって全数ではない**③`artwork_id` のような高カーディナリティは標準レポートで `(other)` に丸められる→BigQueryエクスポート。
  - **⚠️ この作業で2回、他者を空振りさせた（LESSONS 2026-08-06 に2件記録）**: ①**未マージのブランチ上の実装を「コード側は完了」と伝え**、GA4管理画面の作業を先にやってもらった結果まったく動かなかった（Vercel本番は `main` からビルドする）②**渡した確認コマンドから `-L` が抜けており**、`/` の307リダイレクトのせいで配信済みでも `0` が返った。→ **他者に渡す報告では「実装した」と「本番ブランチに入っている」を必ず区別する。渡す検証コマンドは自分が走らせたものをそのままコピーする。**
  - **残（GA4管理画面・依頼先が実施可能）**: ①キーイベント4件の指定（`me_publish_on` / `gallery_work_buy_click` / `checkout_return` / `gallery_signup_cta`）②内部トラフィック除外③**`page_view` の二重発火チェック**（拡張計測機能の「ブラウザ履歴イベントに基づくページ変更」との併発。ローカル検証は gtag.js をブロックして行ったため**この組み合わせだけ未検証**）④（任意）BigQueryエクスポート。完了済み: プロパティ/ストリーム作成・env・再デプロイ・データ保持14か月・Googleシグナルoff・広告データ共有全不許可・カスタムディメンション27/指標5の登録。
  - **【実装済・push済／main未マージ 2026-08-06】同意バナー（ユーザー判断「同意バナーは出すよ」）**: `components/ConsentBanner.tsx`。**EEA/UK/CH の訪問者にだけ**下部バーで尋ねる（root layout が Vercel の `x-vercel-ip-country` を読んで判定。**ヘッダが無ければ出さない**＝最悪でも「測れない」側に倒す。EEAのCookieを止める仕事は Consent Mode の地域別デフォルトがバーと無関係に続ける）。答えは `localStorage`（`xibit360.consent.v1`）で、**インライン bootstrap が同期的に読む**ので再訪の同意済みユーザーは1ヒット目から granted（500msレースを踏まない）。**拒否は同意と同じ容易さ**＝同じ見た目・44px・1タップ（金の主ボタンを置かない）。撤回は `/privacy` §8 の `ConsentReset`（GDPR 7(3)）。`consent.*` 7キー×11言語。**GA4側の追加作業は不要**。
    - 検証: tsc / 番人5種（i18n 11言語100%・802キー）/ クリーン本番ビルド / **実ブラウザ実測** — DE=表示・JP=非表示・ヘッダ無し=非表示／許可で `consent update: granted`＋再訪の既定が初手から granted／拒否は denied のまま聞き直さない／撤回でクリア→バー再表示／1280pxと375pxで横あふれ0・ボタン44px／3Dギャラリー（`body.gallery-mode`）でも z-index 120 で最前面。
- **【実装済・push済 2026-08-06】`/admin` のユーザー表に「課金額」列を追加（ユーザー指示「課金額合計を追加して。合計がいいのか小計がいいのかは不明だけど」）**。
  - **合計か小計かはデータ側で決まっていた**: 通貨をまたいで足せない（Managed Payments が現地通貨で請求するため `$5.00` と `¥500` がどちらも `500` で入る＝migration 0031）。なので**通貨ごとに出す**の一択で、単一通貨なら `$29.00` と1つの合計に見え、混在した買い手だけ `$8.00 · ¥500` と並ぶ。**画面上部の全体売上と同じ見せ方**（`revenueLines.join(' · ')`）に揃えた。
  - **`sumByCurrency()` を `lib/admin.ts` に切り出して、全体売上と1ユーザーの支払額の両方をこれで出す**。「円とドルを足さない」規則が2か所に分かれるのを防ぐため。null 金額は0として扱う（Theme Collection は本体行に金額・付与された各テーマ行は null＝二重計上防止。webhook 側の設計に合わせる）。
  - 新キー `admin.colPaid` を11言語に。列は WORKS と PACKAGES の間。無料ユーザーは `—`。
  - **検証**: tsc / 番人5種（i18n 11言語100%・795キー）/ クリーン本番ビルド / `sumByCurrency` を node で5ケース（単一通貨・通貨混在・null混じり・空・currency空文字/大文字）/ **合成データの一時ページで実描画**（検証後に削除）— ヘッダ6列と各行6セルが一致、`$29.00` `$8.00 · ¥500` `—` を実測、**ユーザー別の合計（$29+$8）が画面上部の全体売上 $37.00 と一致**することも確認。
  - **やらなかったこと**: 課金額での並べ替え（現状は作品数の降順のまま）。必要なら足す。
- **【実装済・push済／本番はGA_ID待ち 2026-08-06】アナリティクス（GA4）を実装（ユーザー指示「アナリティクスを仕込むので計測ポイントを洗い出して。ビジターのギャラリー内の行動は細かく／出展者も」→ 手段の選択で「gaがいいな」）**。棚卸しは `docs/ANALYTICS.md`、実装は39イベント。
  - **手段はGA4（gtag.js）に確定**。`NEXT_PUBLIC_GA_ID` 未設定なら**`window.dataLayer` すら作らない完全無音**（Stripe/OpenAI が鍵なしで501を返すのと同じ規律）。**本番で計測が始まるのは、ユーザーがGA4プロパティを作って Vercel に env を入れてから**。
  - **一番の収穫**: 入場に失敗した訪問者が**完全にサイレント**だった（WebGL無し→2Dフォールバック `GalleryApp.tsx:182`/`:367`、iOSのコンテキストロスト `:277`、12秒ロードタイムアウト `:243`）。「訪問数は出ているのに反応が無い」の正体が真っ黒画面である可能性を、今のデータでは否定できなかった。全部イベント化した。
  - **設計**: ビジターのUI状態はほぼ全部 zustand にあるので **`useGallery.subscribe` 1本**（`components/gallery/AnalyticsProbe.tsx`）で focus / dwell / ツアー / パネル開閉 / セッション要約を取る＝3Dコンポーネント側の改造がほぼ不要。プローブは**`VisitorGallery` 側（`dynamic()` の外）**に置いた — `GalleryApp` の中だとロード中の離脱が見えない。移動は React の外（`camPose`）なので1秒サンプラ、`useFrame` からは呼ばない。
  - **`via` の撃ち分け**（`lib/analytics.ts` の `focusIntent` ＝ `walkRef`/`camPose` と同じモジュール単一値の作法）: click / stepper / key / swipe / tour。**ツアー由来のビューを人気と混ぜると、作品別ランキングが「関心」ではなく「スロット順」の計測になる**ため必須。
  - **滞在時間は可視時間のみ**（AGENTS 5.6 の裏返し）。タブが後ろだと3Dは描画を止めるのに `setTimeout` は動くので、素朴に測ると放置＝長時間鑑賞になる。実測で 1秒視聴→3秒バックグラウンド→1秒視聴 が **1006ms + 1003ms**（素朴な実装なら約5000ms）。
  - **プライバシーポリシーを改訂**（`app/privacy/page.tsx`）。**「第三者アナリティクスを使わない」の一文は削除**＝実装と食い違う文言を残さない。§1に計測内容、§3の処理者にGoogle、§8にCookie。**規約/プライバシーは英語版が正（check-i18n の ALLOW）なので11言語作業なし**。EEA/UK/CH は Consent Mode v2 で `analytics_storage: denied` 既定・広告系は全地域常時denied。
  - **GA4の限界（記録）**: ①**作家向けの数字には使えない** — `/me` の訪問数・♡は今後も Supabase の `visits`/`likes` が出どころ。「あなたの作品でこれが一番見られました」を作家に見せたくなったら別途DB工事が要る（`events` テーブル案は ANALYTICS.md §6.2 に残置）②広告ブロッカーで落ちるので**下限であって全数ではない**（自前 `visits` と突き合わせれば欠測率が測れる）③`artwork_id` のような高カーディナリティは標準レポートで `(other)` に丸められる→本気でやるならBigQueryエクスポート。
  - **`visits` の dedupe 仕様は触っていない**。`visitCount` が3Dのゴースト数を決めている（`lib/ghosts.ts:12`）ので母数を変えると見た目が変わる。
  - **検証**: tsc / 番人5種 / クリーン本番ビルド / `track()` のGA4ガードをnodeで総当たり（不正名を弾く・null落ち・100文字切り・25個打ち切り・未設定で無音）/ 全39イベント名を GA4 の命名規則と予約語に機械照合（invalid 0）/ **実ブラウザ実測** — Consent Mode の積み順（EEA denied→granted→js→config、gtag.js 1回）、`page_view` が1ページ1件、`/demo` で `gallery_loading_timeout`(8/21)と `gallery_loading_done` が実発火、合成データの一時ページ（**検証後に削除済み**）で `via` の撃ち分け・dwell 1500ms→1505ms・セッション要約1件・hidden除外を実測。
  - **残（ユーザー作業）**: ①**GA4プロパティ作成＋Vercelに `NEXT_PUBLIC_GA_ID`**（これを入れるまで何も起きない）②GA4管理で**Googleシグナル/広告向けデータ共有をオフのまま**（privacyに「広告に使わない」と書いたため）③カスタム定義に `artwork_id` `via` `stage` `surface` `kind` `reason` `result` をイベントスコープで登録（登録しないと標準レポートに出ない）④作品別ランキングを本気でやるならBigQueryエクスポート⑤**同意バナーを出すか判断**（出さない限りEEA/UK/CHは推計のまま。出すなら11言語のUI文言が要る＝AGENTS 5.1。呼び口 `grantAnalyticsConsent()` は用意済み）。
- **【ship済・本番反映確認】SNS入力欄の作り直し**（コミット `70a81fa`。別セッションの `551dd4b`（Apple Mailの背景が白くなる不具合の修理）も同時にpushした）（DECISIONS 2026-08-03、ユーザー指摘3件）。①**白地で浮いていた**のは、入力欄の見た目を配っている**セレクタの列挙にSNSの欄が入っていなかった**から（CSSが無い入力欄は既定の白で描かれる。`check:css` の逆向きなので番人では出ない）②**欄はフルURLを出し入れする形**にした（プレースホルダも完成形のURL。`instagram.com/yusuke/` `@yusuke` `www付き` どれでも受けて、離れたときにURLへ整える。保存の形はハンドルのままなので公開ページ側の変更なし）③**取り違えはホストで判定して注意文だけ**出す（欄から離れたときだけ。取り違え中はその欄を保存しない）④**Bluesky・Discord・LINE を追加**（Discord/LINEは招待リンクなのでURL型）。
  - 検証: `lib/sns.ts` 単体16通り＋実UI9通り（一時ページに本物の `ProfileCard` を描いて実操作 → 削除済み）、375pxで横あふれ0、tsc / check:i18n（826キー・10言語100%）/ check:css / check:schema / check:gates(52) / 本番ビルド。
  - **本番で確認済み**: 配信CSSの `.sns-field input` に暗い見た目（`background: rgba(236,231,222,.05)` ＋ hairline）が載り、`.sns-field-warn` も配信されている。配信JSに Discord/LINE/Bluesky の見本URLとホスト照合（`twitter.com` を X 扱い）が入り、**旧 `hint:"instagram.com/"` は0件**。`/me` `/ja` `/ja/explore` `/signup` すべて200。※`/me` のHTMLはサインイン前だと入力欄そのものが出ないので、欄の中身はJS側で確認した。
  - **残**: 実機QA（サインインして、欄が暗い見た目になっているか・URLを貼って整うか・わざとDiscordの欄にInstagramのURLを貼ると注意文が出るか・公開ページのアイコン列が正しく開くか）。
- **【ship済・本番反映確認済み 2026-08-03】認証まわり総仕上げ（Googleボタン・メルマガ同意・回帰修正）**。`d600581..cb9f08b` を push・Vercel Ready・本番 `/signup` 実測OK（強度ラベル「パスワードの強度: まあまあ」・生キー漏れ0）。
  - **今回pushの3コミット**: `ff41cb9`（認証i18nキー11言語の確定＝下記回帰の修正）／`f544a35`（メルマガ別途オプトイン）／`cb9f08b`（トグル連打の保存競合修正＝レビュー指摘）。
  - **直した本番回帰**: 認証コード `190bcdd` を別セッションが先に push した結果、origin/main に**新認証コードはあるがi18nキーが無い**状態になり、本番で強度メーター/エラーが「auth.strengthLabel」等の生キー表示に。`ff41cb9` で解消。
  - **Google**: ボタンを公式4色Gロゴ付きに（`GoogleButton.tsx`、`195f557`）。**残りは Supabase の Google プロバイダ有効化のみ**（Google Cloud→OAuthクライアント→リダイレクトURI `https://ncffdcvsksiutsjerpeb.supabase.co/auth/v1/callback`→Supabase Providers に Client ID/Secret）。
  - **メルマガ同意**: 規約（必須）とは別の**任意・初期オフ・送信を阻害しない**チェックを signup に。user_metadata（`marketing_opt_in`＋`marketing_opt_in_at`）保存＝migration不要。`/me` に入切トグル。privacy に根拠(同意)・撤回・取引メールとの区別を英語で明記。GDPR抱合せ禁止/特電法オプトイン準拠。**Supabase内蔵は言語別出し分け不可＝配信は別途 Resend Broadcasts 等が必要（未実装）**。
  - **【完了・ユーザー実施 2026-08-03】メール設定＋Googleログイン有効化**: ①Resend（Xibit360専用の別アカウント。Ktlystと1アカウント1ドメイン無料枠が競合するため新規作成）でドメイン認証(SPF/DKIM、Cloudflare自動連携でVerified)→Supabase Emails に独自SMTP（`smtp.resend.com:465`/`resend`/APIキー）設定 ②Google Cloud に**Xibit360専用の新規プロジェクト**（既存Ktlystプロジェクトの流用は同意画面のアプリ名/ロゴがKtlyst名で出るため回避）→OAuth同意画面（外部・本番公開）→OAuthクライアント作成→Supabase Providers→Google に設定。本番で `/signin` のGoogleボタン→`accounts.google.com`まで実際に遷移し「Xibit360」名で認可要求されることを確認 ③`/reset` から実メール送信テストで到達確認（初回は迷惑メールフォルダ、Resend送信ログの追跡は不要になるくらい安定して到達）。
  - **【解決・実機で原因究明 2026-08-03】メールがApple Mailでだけ背景白くなる不具合**: 中のカード/ボタンの色は保持されるのに外側の全幅背景だけが白くなる、という一貫した症状。`bgcolor`属性併記／`color-scheme`メタ／完全なHTML文書化を試しても直らず、**Gmailに転送して開くと正しくダーク表示**されたことで「配信HTMLは正しい・Apple Mail側のレンダリングが原因」と確定 → **`<meta name="x-apple-disable-message-reformatting">` を`<head>`に追加して解消**（Apple Mailの自動メッセージ整形が外側背景だけを剥がすクセ。詳細は `docs/LESSONS.md`「メール」に記録）。4テンプレート（confirm-signup/magic-link/reset-password/change-email）すべてこの骨格＋メタタグ込みの完全文書に統一。**Reset Passwordは実送信で動作確認済み。他3種は骨格が同一なので動作するはずだが個別の実送信テストは未実施**。
  - **残（ユーザー作業）**: ①`support@xibit360.art` 受信設定(Cloudflare Email Routing、法務ページの連絡先として必須) ②（推奨・任意）DMARCレコード追加で迷惑メール行きを改善 ③（任意）Confirm signup等の残り3テンプレートを実際に1回送って表示確認 ④実機QA(/me メルマガトグルの getUser/updateUser)。
- **【ship済・本番反映確認済み 2026-08-03】ログイン/サインアップ強化＋英語→ダークのメールテンプレート（ユーザー指示 2026-08-03、`190bcdd`/`06cbdfb`）**。ユーザー「ログインサインナップ周りを強化して、メール設定も済ませたい」。
  - **コード側4点**（ユーザーが全選択）: ①**エラーの多言語化**＝これまで `error.message` で Supabase の英語生メッセージを直出ししていたのを `lib/authErrors.ts` で既知エラーを i18n キーに対応づけ11言語表示（未知は英語を漏らさず `errGeneric` へ）。signin/signup/reset 全経路。②**パスワード表示トグル＋強度メーター**＝`components/auth/PasswordField.tsx` を新設し3ページの生 input を置換。強度は `lib/password.ts`（0..4、長さ主・文字種従）。③**堅牢化**＝メール送信ボタン（マジックリンク/確認再送/リセット申請/サインアップ再送）に30秒クールダウン（`components/auth/useCooldown.ts`）。④見た目＝`auth.css` に金アクセントのトグル/メーター。
  - **メール設定＝英語のみ運用**（ユーザー選択）: `supabase/email-templates/` に確認/マジックリンク/リセット/メール変更の英語HTML4種＋README。**Supabase内蔵テンプレートは受信者の言語に関わらず1種類しか送れない**ため「11言語ぶん」は不要で、1テンプレート＝英語に確定。言語別に出すにはAuth Hook+Resendへの載せ替え（別工事）が必要。
  - **BIMI（Gmailのブランドロゴ）の回答**: Resendでできるのは SPF/DKIM/DMARC まで（BIMIの前提）。Gmailでロゴを出すにはBIMIレコード＋**VMC（登録商標を証明する有料証明書・年1000ドル超）**が必須で、Resendの外。メルマガ立ち上げ段階では後回し推奨。
  - **検証**: tsc緑／番人（自分のキーで i18n 100%・css 0）／`passwordScore`・`authErrorKey` を node で総当たり（8+11ケース）／プレビュー実測（強度メーター `abcdefgh12`→金2本「まあまあ」=score2、トグルの `show` 反転を React fiber で確認。非表示ペインの getComputedStyle 誤診は LESSONS 5.6 どおり回避）。
  - **⚠️ 並行セッションのコミット事故**: 番人チェック時に `git add` した**認証i18nキー（11言語）が別セッションの Video Pass コミット（`19b8342`、旧 `e3bbe07`）に巻き込まれてコミット済み**。機能上の実害なし（キーは正しく check:i18n も通過）だが帰属がねじれた。→ 以後、共有作業ツリーでは `git add -A` を避け**自分のファイルだけ明示ステージ**する。今回のコミットも `docs/DECISIONS.md`（別セッションの /help 決定）を含めず8ファイル＋テンプレートだけをステージした。
  - **残（ユーザー作業）**: ①**メール設定のダッシュボード/DNS作業**＝Resendでドメイン認証（SPF/DKIM）→Supabase Authentication→Emails にSMTP設定→URL Configuration を本番ドメインに→各テンプレート欄にHTMLを貼る（README参照）。②push前に `check:ship-ready` で base 更新（並行セッションが activ）を確認し**頭から再検証**。③`support@xibit360.art` の受信設定（Cloudflare Email Routing）は引き続き残。④実機QA（強度メーター・トグル・多言語エラー・クールダウン）。

- **【実装済・push待ち→push】記事に構造化データ（JSON-LD）を追加（ユーザー指示 2026-07-31「構造化お願い」）**。公開ギャラリー側には 2026-07-30 に入れたが、**記事には1件も無かった**。
  - **入れたもの**（`lib/seo.ts` に追記＝`website()`/`breadcrumbNode()`/`abs()`/`absoluteMedia()` を単一の出どころのまま使うため、新ファイルに分けなかった）: `articleJsonLd(a)` ＝ **`BlogPosting`**（headline / description / datePublished / dateModified / image / author / publisher / isPartOf / mainEntityOfPage）＋**`Blog`**＋**`Organization`**＋**`WebSite`**＋**`BreadcrumbList`**（Xibit360 → Guides → 記事）。`articlesIndexJsonLd(list)` ＝ **`Blog.blogPost` に各記事をインライン**（`@id` 参照だけにすると、そのページに存在しないノードへの未解決参照になる）。
  - **判断3件（いずれも『知り得ないことを断言しない』の適用）**: ①**`inLanguage:'en'` はここでは書く** — ギャラリー側では同じプロパティを**外した**（LESSONS 2026-07-30）が、記事は `ARTICLE_LOCALE` により構造的に英語なので根拠がある。同じプロパティでも逆の判断になる②**`publisher.logo` は入れない** — 指せるロゴ画像ファイルがサイトに無く（`public/` にアイコン無し・ワードマークは文字組み）、404するURLは property を欠くより悪い。Googleは記事リッチリザルトの logo 必須要件を撤廃済み③**`author` は `Organization`（Xibit360）** — 記事は管理コンソールから自分たちが書いており、記事ごとの著者欄は存在しない。人名をでっち上げない。
  - **`headline` は切り詰めない**: Googleは110字超で対象外にすることがあるが、切り詰めた見出しはページ上の題字と別物になる（構造化データがページと食い違うのは、それが防ぐはずの失敗そのもの）。コメントに残した。
  - **URLはすべて canonical と同じ `/en/articles/…`**（`guidePath()`）。
  - **検証**: tsc / 番人5種 / クリーン本番ビルド / **合成データ4通りを本番ビルドで実描画して実測**（①通常 ②カバー・日付・抜粋なし ③一覧2件 ④一覧0件）— **JSONが4本とも `json.loads` を通る／未解決の `@id` 参照 0件／`</script><b>x</b>` を含むタイトルが生で1件も漏れない**（`JsonLd` の `<` エスケープが効いている）／任意プロパティが値なしのとき正しく落ちる／パンくずが canonical と同じURLを指す。実タグは1本（もう1つの出現はRSCのフライトデータ内の文字列）。
  - **マージ前の通し点検（2026-07-31）で1件直した**: 記事本文ページが1リクエストで `fetchArticle`×2（generateMetadata と本体）＋`fetchPublishedArticles`×1 の**3クエリ**を打っていた。DECISIONS 2026-07-30 がギャラリー側で同型を `cache()` で潰しているのに、記事だけ取り残されていた（サイドバーの「ほかのガイド」で1本増やしたのは今回）→ `getArticle = cache(fetchArticle)` を `lib/seo.ts` に追加して**2クエリ**に。**それ以外は確定バグなし**（hreflang の自己参照は canonical 側で正しい／`@id` 未解決0／JSXの入れ子・型・番人すべて緑／主要15ルートが期待どおりの200・307）。
  - **残（ユーザー作業）**: 記事を1本公開したうえで、①**リッチリザルトテスト**にその記事URLを入れる（`BreadcrumbList` と、記事が条件を満たせば `Article` が出るはず。`Blog`/`Organization`/`WebSite` は同ツールの対象外なので出なくても異常ではない＝2026-07-30 の読み方と同じ）②validator.schema.org で全ノードが解析できるか。**本番の実データでの確認は記事が公開されるまでできない**（ローカルに Supabase の鍵が無く、検証は全部合成データ）。
- **【実装済・push待ち→push】ブログの翻訳の扱いを決めてURLを1本に集約（ユーザー質問→指示 2026-07-31）**。ユーザー「そういえばブログで、翻訳どうなる？」→ 調査で **まわりのUIだけが翻訳され、記事の中身は翻訳されない**ことが判明（`articles` テーブル(0020)に言語の列が無く、管理画面にも言語欄が無い）→ ユーザー「英語がベースになるので、基本は一言語の日本語記事は書かない想定。SEOに最も効く形にしたい」。
  - **見つかった実害（今回のサイドバーとは無関係の既存の作り）**: `/articles` が `LOCALIZED_PATHS` に入っているため、**記事1本につき11言語ぶんのURLが実在し、sitemap にも11本ずつ載り、hreflang が11言語を相互に指していた**。中身は全部同じ英語1本。hreflang は「同じ内容の別言語版」の宣言で Google が照合するので、`ja` を名乗るURLが英語本文を返すのは**宣言が嘘**であり、11本が自己参照 canonical で並ぶため**1記事の評価が11分割**されていた。記事10本なら sitemap に110URL。
  - **入れたもの**: ①`singleLanguageAlternates(pathname, locale)` を新設（`lib/i18n/metadata.ts`）＝**どのロケールURLで開かれても canonical は英語URL**、hreflang は `en` と `x-default` だけ②`ARTICLE_LOCALE = 'en'`（`lib/blog.ts`。別言語の記事を書くときは列に置き換える旨をコメントに明記）③sitemap を1記事1URLに（`localized()` を使わず `articleUrl()`。**canonical と sitemap が同じURLを名乗ることが要点**）④`og:article:published_time` / `modified_time` を追加⑤記事のタイトルタグ・説明文が英語なのは**訳し忘れではなく仕様**（英語文書を英語で説明）と `i18n-ok` で明示。
  - **他の10URLは残す**: `/ja/articles/x` は人間には有効なまま（まわりのUIは日本語・共有リンクが404にならない）で、クローラーにだけ canonical で寄せる。`noindex` は付けない（canonical と併用すると相反する指示になる）。
  - **却下したもの**: `/articles` を `LOCALIZED_PATHS` から外す案（URLの形が他ページと揃わず、将来の翻訳でURL移行が要る）／`articles.locale` 列を今足す案（英語だけなら定数で足り、**URLの形は今回で確定するので列は必要になった日に足しても移行コスト0**）／記事を11言語ぶん書く案（書く量11倍・更新の止まった訳文は逆効果）。
  - **検証**: tsc / 番人5種 / クリーン本番ビルド / **実測** — `/en/articles` `/ja/articles` `/zh-hant/articles` の3つとも canonical が `https://www.xibit360.art/en/articles`、hreflang は `en` と `x-default` の2本だけ。sitemap の articles を含む `<url>` が **1件**（`/en/articles`。以前は11件＋記事ごとに11件）。記事本文ページの head は合成データの一時ページで実測し canonical `/en/articles/open-a-web-solo-show`＋`article:published_time`/`modified_time` を確認（計測後に削除）。
  - **残（ユーザー作業・SEO）**: ①Search Console で **sitemap を再送信**し、以前登録された `/ja/articles/…` 等の重複URLが「代替ページ（canonical タグあり）」に落ちるのを確認②「Googleが選択した正規URL」が `/en/articles/{slug}` になっているか URL 検査で確認。
  - **次に効く未着手**: **記事に構造化データが1件も無い**（公開ギャラリー側には 2026-07-30 に入れた）。`BlogPosting`＋一覧の `ItemList` はブログでは検索結果の見え方に直接効く。**やるかはユーザー判断**。
- **【実装済・push待ち→push】ブログ（/articles）にサイドバーを追加（ユーザー指示 2026-07-31）**。ユーザー要望「blogのページ（一覧と本文）だけど、サイドバーをつけられる？ 流入した人が何のサービスのブログかわからないので」。検索から本文に直接降りてくる導線（DECISIONS 2026-07-30「公開ギャラリーのSEO」で索引を整えた先）に、ブランドの説明が本文の隣に常時あるようにした。
  - **入れたもの**: ①`components/ArticleSidebar.tsx`（server component）を新設し、一覧・本文の両方に置いた。中身は **a. 「XIBIT360とは」カード**（説明1段落＋「無料ではじめる」`/signup`＋「デモを歩く」`/demo`）／**b. 「ほかのガイド」**（本文ページのみ。自分を除いた公開記事を最大5本＝`SIDE_MORE_MAX`）／**c. 「公開中の展覧会を見る →」**（`/explore`）②レイアウトは `.article-layout`（grid）。**900px以上で 本文 + 280px の2カラム**、それ未満は本文の下に積む。サイドバーは `position:sticky; top:1.4rem` で追従③**本文ページのヘッダの「← Guides」をロゴ `XIBIT360` に置換**（ここが最初のブランドの手がかりになる。ついでに残っていた英語直書きも消えた）。戻る導線は本文の上の `.article-back`（`← {articles.allGuides}`）へ④一覧は `.articles-index .me-inner` を 880→**1120px** に広げ、カード2列の閾値を 720→**1024px** に（サイドバーが立つぶん列が細くなるため）⑤読み幅は器（`.article-inner`）が持つ＝**基本 765px（本文720＋padding）／900px以上でだけ 1080px に広げる**。`.article-layout` 側で絞るとヘッダだけ広いまま残って題字と行頭がずれるため（実測で発見）。2カラム時の本文は714pxで、1カラム時の720pxとほぼ同じ読み幅。
  - **カード2列の閾値は2区間に分けた**: `720–899px`（1カラム＝本文が全幅）と `1024px以上`（2カラム＝本文が700px超）。当初 `1024px以上` だけにしたところ、**720–899px のタブレットで従来の2列が1列に退行**していた（自己レビューで発見・実測で確認）。900–1023px は本文が534–658pxしかないので1列のまま。
  - **判断（狭い幅）**: 本文ページを1カラムに畳むと、サイドバーは本文末尾の `.article-cta` の**直後**に来て同じ売り文句が二度続く → **`@media (max-width:899px)` で `.article-page .article-side-about` だけ畳む**（「ほかのガイド」と explore リンクは残す）。狭い幅でのブランドの手がかりはヘッダのロゴと `.article-cta` が担う。一覧ページには `.article-cta` が無いので畳まない。
  - **i18n**: 新キー4つ（`articles.sideWhat` / `sideBody` / `sideMore` / `sideExplore`）を**11言語すべて**に。ボタンは既存キーを再利用（`common.startFree` / `lp.heroCta`）。
  - **検証**: tsc / check:i18n（11言語100%・742キー）/ check:css（死にCSS0）/ check:schema / check:gates（52）/ クリーン本番ビルド / **プレビュー実測**（ローカルに Supabase の鍵が無く `/articles/[slug]` は404になるため、同じシェルを合成データで描く一時ページを作って本番ビルドに対して計測し、計測後に削除）— 一覧のカード列数 1280→2 / 1024→2 / 960→1 / 899→2 / 820→2 / 720→2 / 700→1 / 375→1（意図どおり）、本文ページは 1280/900 で2カラム（本文714/534px・サイドバー280px）、899/820/375 で縦積み、**ロゴのx座標と題字のx座標が全幅で一致**（899→89 / 820→50 / 375→22）、**全幅で横はみ出し0**、スクロール1400px 後もサイドバーが `top:22px` に留まる（sticky有効）、375px で `.article-side-about` が `display:none`・「ほかのガイド」と explore は残る、日本語/英語の両方で文言が解決。
  - **途中で直した自分のバグ2件**: ①`align-items:start` を書いていたため `.article-side` が中身の高さに縮み、**sticky が効いていなかった**（スクロール後 `top:-1306`）→ 既定の stretch に戻して `top:22` を実測 ②デモのボタンに `t('landing.heroCta')` と書き、**画面にキー名がそのまま出た**（実際の親グループは `lp`。番人は全部緑のまま＝LESSONS に記録）。
  - **残（ユーザー作業）**: 実機QA（①PCで一覧・本文にサイドバーが出て、スクロールで追従するか ②スマホで本文の下に「ほかのガイド」が出るか ③本文ページのヘッダがロゴになり、その下に「← ガイド一覧」があるか ④サイドバーの「無料ではじめる」「デモを歩く」「公開中の展覧会を見る」が正しく飛ぶか）。**記事が1本しか公開されていない場合、本文ページの「ほかのガイド」は出ない**（仕様）。
- **【ship済・本番反映確認済み】配置を俯瞰マップへのドラッグ＆ドロップに（ユーザー指示 2026-07-31）**（コミット `67c9c75` ＋ レビュー修正 `c6d6717`）。ユーザー要望「worksで設定した作品サムネが下に並んでいて、そこからスロットにドラッグして配置したり、既に置いた作品を隣にドラッグで動かしたい」→ AskUserQuestion で**「部屋の俯瞰マップ」**を選択（並べ替えリストから再転換）。①`PlacementEditor` をタップ＋ピッカーから**ポインタベースのドラッグ＆ドロップ**に全面改修。マウス／タッチ両対応（Pointer Events＋`.place-map{touch-action:none}`）②下に**作品トレイ**（全作品。掛け済みは淡色＋金ドット）。トレイ→スロットで配置、スロット→スロットで入れ替え（swap）、押し出された作品はトレイへ、鍵付き（容量超過）へのドロップ／タップで容量購入モーダル③**ヒットテストは SVG コンテナ1枚で座標から**（`getScreenCTM().inverse()`→`slotAt`）。理由=スロットは bg/knockout/ring の重なった rect で、per-slot の pointerdown は装飾 rect に取られて発火しない（プレビュー実測で発見。LESSONS）。ドラッグ中状態は `dragRef`、ゴースト位置と `overSlot` のみ state。
  - **レビュー**: drop の swap/place/no-op/重複なしは正しい、ポインタ解放・need計算・touch-action は問題なし。**LOW1件を修正**（`c6d6717`）: 保存(700msデバウンス)の往復中に再ドロップすると、先の保存完了の refreshMyGallery が古い arrangement を返して楽観更新を巻き戻し得た → `editGen`/`savedGen` の**世代管理**で、未保存/保存中の local 編集がある間は re-seed が row から上書きしないようにした（真偽値 dirty では先行保存の完了が後発編集のフラグを消すため防げない）。
  - **検証**: tsc / check:i18n(11言語100%・736キー) / check:css(死にCSS0・place-order/picker掃除) / check:schema / check:gates(52) / クリーンビルド / **プレビュー実測**（合成6作品・cap4で ①スロット→スロット swap ②トレイ→占有スロットで配置＋押し出しがトレイへ ③鍵付きへドロップで「BUY 9 slots」＝arrangement不変 を arr ログで確認）。**本番CSS反映確認**（`.place-tray-item`/`.place-ghost` あり・`.place-order-row` 消滅）。
  - **残（ユーザー作業）**: 実機QA（**スマホのタッチでドラッグできるか**が最重要。マウスはプレビュー実測済みだがタッチは未実機／トレイ→スロット配置／スロット入れ替え／鍵付きから購入／並べ替えが公開ページに反映されるか）。
- **【ship済・本番反映確認済み】ステージタブの調整（ユーザー指示 2026-07-31）**（コミット `ad2b814`）。①各ステージの**完了チェックアイコンを削除**（`.me-stage-check`＋`CheckIcon`＋不要になった `done` 判定）②ステージバー横スクロールの**スクロールバーを非表示**（`scrollbar-width:none`＋`::-webkit-scrollbar{display:none}`。横スクロール自体は維持）。**検証**: tsc / 番人5種 / クリーンビルド / プレビュー実測（148px幅で scrollWidth310>clientWidth148＝スクロール可・余分高1px=罫線のみでバー無し・チェックアイコン0個・アクティブ下線維持）。**本番CSSハッシュ更新を確認**（`.me-stage-check` 消滅・`scrollbar-width:none`＋webkit非表示あり）。
  - **📌 `0035_light_override.sql` はユーザーが適用済み（2026-07-31）** — 以降の項目に残る「未適用」は解消済み。作品ごと照明は保存される。
- **【ship済・本番反映確認済み】プロフィールを一番左のタブに・配置ステージを『並べ替えリスト』に（ユーザー指示）**（コミット `1fcd638` ＋ レビュー修正 `1280750`・`b757e69`。**本番CSSに `place-order-row`/`placement-stage` 反映確認**）。ユーザー指示「profileを一番左のタブに」＋「placementの配置換え、複雑すぎて操作方法わからない」→ AskUserQuestion で**「並べ替えるだけにする」**を選択。①ステージバーの並びを **profile / works / room / placement / publish** に（profile 先頭）②**配置ステージのマップUI（`PlacementEditor`／作品ピッカー）を撤去し、作品を↑↓で並べ替えるだけのリスト（`.place-order`）に**。リストの順序＝歩く順序。`moveWork(from,to)` が `reorderOwnArtworks` で順序を永続化＋公開部屋を新順で rebuild。旧マップの固定スロット（`arrangement`）は移動時に空へ落として自動整列に戻す。
  - **レビューで2件の確定バグを検出→修正**: ①**競合状態**（`1280750`）— 当初 `editPlacement([])`（ローカルstate・700msデバウンス）と `reorderOwnArtworks`（store・1200msデバウンス）が別々に発火し、rebuild が古い store.arrangement を読む恐れ → `updateSettings({arrangement:[]})` で store を落として単一同期に束ねた。②**回帰**（`b757e69`）— store だけ落として MePage の `row` prop を再取得しないため `row.arrangement` が古いまま残り、その後 setSpace/editCustom/togglePublic が `rowToSettings(row)` 経由で古い arrangement を DB に書き戻し固定スロットを復活させていた（旧 editPlacement は `onChanged` で row を更新していた）→ `moveWork` を `run()`（refreshMyGallery＋onChanged 込み）経由にし、DBへ arrangement:[] 書き込み＋新順序で rebuild まで一括。`from` の範囲チェックも追加。
  - **検証**: tsc / check:i18n（11言語100%・734キー）/ check:css（死にCSS0）/ check:schema（35本）/ check:gates（52）/ check:ship-ready / クリーン本番ビルド / 2巡の別視点レビュー（1巡目=①、2巡目=②を検出、いずれも修正後に自己検証）。
  - **残（ユーザー作業）**: 実機QA（profile が先頭タブか・配置ステージが↑↓の並べ替えリストで直感的か・並べ替えが公開ページに反映されるか）。**旧マップで手動配置していた公開ギャラリーがあれば、並べ替え→部屋テーマ変更などをして固定スロットが復活しないか**も確認。**`0035_light_override.sql` の適用は引き続き未了**。
- **【ship済・本番反映は要確認】プロフィールのSNS拡張＋公式ブランドアイコン（ユーザー指示）**（コミット `58f0574`）。X/Instagram/Websiteの3固定から**主要9プラットフォーム＋その他(カスタム)**に。①**データ構造** `SnsLinks = {links: Record<id,string>, custom: {label,url}[]}`（`lib/sns.ts`）。**DB無変更**（`profiles.sns` jsonb のまま）＋`readSns` が旧 `{x,instagram,website}` を新形式へ自動マイグレーション（既存ユーザーのリンクは往復でロスなし＝単体検証済み）②**9プラットフォーム**: Instagram/X/TikTok/YouTube/Behance/Pinterest/Facebook/Threads/Website（**LinkedInは公式アイコンがSimple Iconsから削除済のため除外**）③**公式ブランドアイコン**を Simple Icons（CC0/パブリックドメイン）のパスから埋め込み（`components/BrandIcons.tsx`。旧Instagram簡易アイコンを公式グリフに置換。依存は追加せずパスのみ）。Website/カスタムは地球儀④**その他**: ラベル+URLを自由に追加/削除⑤保存時サニタイズ（ハンドルはURL/@を剥がしusername化、URLはhttps://補完＝`javascript:`等も無害化）⑥`SnsLinks`表示と `seo.ts` の sameAs を全リンク対応。
  - **検証**: tsc / 番人5種 / クリーンビルド / `lib/sns` 単体検証（後方互換・@剥がし・URL補完・sameAs・null/空）/ プレビューで9行の公式アイコン(Instagram 1988字グリフ)＋カスタム追加/削除を実測。
  - **レビュー**: 確定バグなし。後方互換・URL無害化ともに堅牢。**仕様上の限界**（許容）: YouTubeの channel/UC… や Facebook の profile.php?id=… のような数値ID系URLをハンドル欄に貼ると壊れリンクになる（@handle前提。回避はその他/Websiteにフルurl）。
  - **残（ユーザー作業）**: 実機QA（プロフィールで各SNSに公式アイコンが出るか・入力して保存されるか・その他リンクの追加/削除・公開ページ(/@username)にアイコンが並ぶか）。**`0035_light_override.sql` の適用は引き続き未了**。
- **【ship済・本番反映は要確認】芳名帳を公開ステージへ・配置ステージを配置専用に・タブの縦スクロール解消（ユーザー指示）**（コミット `e60c68e` ＋ deadキー掃除 `56e1133`）。①**芳名帳ステージを廃止し公開ステージ内の一区画に統合**（5ステージ:作品/部屋/配置/公開/プロフィール）。`GuestbookCard` は `me-card` の囲みを外し `.wd-group.gb-group`（見出し+上罫線）にして公開列に馴染ませた（カード入れ子0）②**配置ステージを『配置だけ』に**: 作品編集シート（タイトル/価格/額装等）を撤去＝作品ステージとの重複を解消。地図タップ→作品ピッカー（どの作品を飾るか）だけ。`onEditWork` を外し「この作品を編集」ボタンも消えた。**1カラム全幅**にして地図とスロットを大きく（スロット 16→25px）、見出し「配置」＋説明文を追加③**ステージバーの縦スクロール解消**: `overflow-x:auto` は `overflow-y` を `auto` に計算するため、アクティブタブの `-1px` 下マージンで縦スクロールバーが出ていた → `overflow-y:hidden` を明示。
  - **検証**: tsc / check:css(死にCSS0) / 番人5種 / クリーンビルド / 実測 — 5ステージ・overflow-y:hidden・配置は全幅で編集シートなし・ピッカーに「編集」ボタンなし・地図680px/スロット25px・芳名帳は公開列に配置(カード入れ子0)・スマホで縦スクロールバー消滅。**芳名帳の中身はモックDB無で非表示のため、実描画は本番(0008適用済み)で確認**。
  - **レビュー**: 確定バグなし。JSXの括弧対応・GuestbookCardの埋め込み・onEditWork撤去すべて問題なし。deadキー `me.placementEditHint` を11言語から削除。
  - **残（ユーザー作業）**: 実機QA（5ステージ・公開タブ内の芳名帳の表示と開閉・配置タブが配置専用で分かりやすいか・タブの縦スクロールが消えたか）。**`0035_light_override.sql` の適用は引き続き未了**。
- **【ship済・本番反映は要確認】芳名帳・プロフィールのステージ化＋上部タブ・ヘッダカード削除（ユーザー指示）**（コミット `57557a6`）。①ステージバーが **6ステージ**（作品/部屋/配置/公開/芳名帳/プロフィール）に。芳名帳・プロフィールは自前のカードを持つので `me-subcard` の外に描画（カードの入れ子なし）②上部タブ（`me-tabs`＝gallery/guestbook/profile）を丸ごと削除。トップレベルは gallery と account（右上メニュー）の2つだけ③ヘッダカード（展示名＋公開バッジの箱）と「あなたの展示」見出しを削除。ステージバーが唯一のナビ、公開状態はステージの✓で表示④6ステージは狭い幅で横スクロール（wrap しない）。死んだCSS（`.me-tabs`/`.me-tab`/`.hako-head*`/`.hako-title`/`button.hako-state`）を掃除。
  - **検証**: tsc / check:css(死にCSS0) / 番人5種 / クリーンビルド / 実測 — PC/スマホで6ステージ・ヘッダ/タブ/見出し消滅・カード入れ子0・スマホで横スクロール(scrollW440>clientW330)・ページ横はみ出し0。芳名帳/プロフィールの中身が描画（プレビューで3D cover も描画確認）。
  - **レビュー**: 確定バグなし。JSXの閉じ括弧・状態の取り残し・導線残存すべて問題なし。**唯一の既知点**: 芳名帳ステージは `GuestbookCard` が `entries===null`（fetch中/0008未適用）で null を返すため、旧構造の見出しが消えたぶん**fetch解決までの一瞬（本番は0008適用済みで一瞬）本文が空**になる。ProfileCard は失敗時もフォームを描くので非対称。実害軽微のため未対応。
  - **残（ユーザー作業）**: 実機QA（6ステージの表示・切替、芳名帳/プロフィールの編集、上部がスッキリしたか）。**`0035_light_override.sql` の適用は引き続き未了**。
- **【ship済・本番反映は要確認】3D画像のcover表示＋トリミング警告＋配置図の枠を輪郭内に（ユーザー指示）**（コミット `8b0a998` ＋ コメント修正 `58112c1`）。①**cover**: 作品画像を指定物理サイズの枠にアスペクト比を保って表示。板のジオメトリは実寸のまま、テクスチャのUV(`repeat`/`offset`)で切り出す（`Exhibit.tsx`。stretch を廃止）。Exhibit を共有する `Preview3D`（ダッシュボードのプレビュー）も自動で同じ挙動。動画テクスチャも同経路②**警告**: 指定サイズの縦横比が画像と異なりトリミングされるとき、作品編集シートのサイズ行直下に「約N%トリミング・⇄で入れ替え可」を金の左罫（`.me-note--warn`）で。11言語に `me.cropWarn`③**配置図**: 全15枠が部屋の輪郭線を跨いで外へはみ出し「中央でない」ように見えていたのを、枠を輪郭の内側へ（`S` 1.25→1.0＝半辺0.5、`NUDGE` 0.5→0.55＝0.05m内側に着地）。**4プリセット＋カスタム72通りで枠の重なり0・壁はみ出し0を総当たり確認**（NUDGE は安全域[0.37,0.6]内）。
  - **検証**: tsc / 番人5種 / クリーンビルド / 実測 — 警告が3:2画像を30×40枠で「約50%」、60×40(比一致)で消滅。配置図の可視枠(`.pe-slot-bg`)はみ出し0（15/15が輪郭内）。cover計算は警告の50%で数式一致。**3Dのcoverの見た目はプレビューが前面でないと描画が止まるため未撮影**（数式＋警告で担保、実機QAで最終確認）。
  - **レビュー**: 確定バグなし。**共有テクスチャの直接ミューテーションは、src が作品ごとに一意＋repeat/offsetが art の純関数なので冪等で安全**（将来「全面UV前提の新しい共有テクスチャ消費者」を足すと壊れる点だけ注意＝コメントに記載）。
  - **スロットサイズの注記**: PC2カラムの左列(~420px)＋S=1.0で **16px**（clean・輪郭内）。旧straddle時より小さいが重なりは解消。実機で小さく感じたら配置図の左列を広げる案がある（未着手）。
  - **残（ユーザー作業）**: 実機QA（①指定サイズを画像と違う比にして3Dで画像が歪まずcoverされ端がトリミングされるか ②その時に警告が出るか ③配置図の枠が部屋の枠内に綺麗に収まっているか）。**`0035_light_override.sql` の適用は引き続き未了**。
- **【ship済・本番反映は要確認】配置ステージのPC2カラム化＋作品削除の×ボタン化＋配置図の下地修正（ユーザー指示）**（コミット `cb7d743` ＋ レビュー修正 `6a5d83a`）。①配置ステージを他ステージと同じ2カラム（左=配置図sticky=プレビュー役／右=設定）に。`works-detail` シェルを再利用。PCは左右が並んで重ならないので、スクロール時に浮いて見えた黒い下地が不要に②配置図の背景: PC透明、スマホ/タブレット(≤1024・1カラム)は編集シートが裏をスクロールするので不透明な下地が要り、ページ色でなくカードに溶ける `#0e0d0b` に③作品編集シートの「作品を削除」を廃止し、横並びサムネの各右上に×ボタン（`.me-work-tile-del`。タイルとは別要素で選択を奪わない・押すと確認）。新キー `me.placementEditHint` を11言語。
  - **レビュー検出＆修正**: `works-detail` シェル再利用で、タブレット幅(721-1024)の汎用 `.we-left` 背景＋`pointer:coarse` の負マージンが組み合わさり、**iPadで地図が下地の外へはみ出す**（編集シートが透ける）バグ→ はみ出し打ち消しをメディアから外し詳細度で常勝させ、下地色も (0,3,0) で ≤1024 に統一。**coarse を素styleで再注入して実測**（preview は coarse を再現しないため）。2巡レビューで退行なしを確認。
  - **スロットサイズ**: PC2カラムの左列(~420px)で **19px**（全幅1カラムの31pxより小さいが、旧2カラム時の15pxより大きい）。タブレット1カラムは31px、スマホ1カラムは13px（はみ出し打ち消しで前回15pxから微減）。
  - **残（ユーザー作業）**: 実機QA（PC=配置図が左・設定が右／スマホ・タブレット=配置図が上sticky・下地が浮かないか／作品サムネ右上の×で削除できるか）。**`0035_light_override.sql` の適用は引き続き未了**（下の項目参照）。
- **【ship済・本番反映確認済み（2026-07-30 18:12頃）】ヘッダ整理＋配置ステージの地図中心化（ユーザー指示）**（コミット `60ee9f4`）。①ヘッダのテーマ色リボン（`.hako-ribbon`）削除＝ヘッダは題字＋公開状態バッジのみ②**「部屋を歩く」をヘッダから公開ステージの `.hako-url-actions` へ移動**（埋め込み・カタログの隣。部屋は公開前でも歩けるので常時表示、埋め込み/カタログは公開後のみ）③配置ステージの3Dプレビュー（`GalleryPreview`）を削除＝配置図がプレビュー役。1カラム全幅を保ちスロットは31pxのまま④**配置図を `.placement-sticky`（`position:sticky; top:.5rem`）で上部固定**。透明SVGの下地が透けないよう不透明背景を敷き、下の編集シートがその裏をスクロールする。
  - **本番反映の確認**: 配信CSSに `.placement-sticky{position:sticky;top:.5rem;z-index:2;background:var(--bg)}`、`hako-ribbon` と `works-detail--map` が**0件**。`/me` チャンクに `placement-sticky`/`placement-stage` あり・`hako-ribbon`/`hako-walk` が0件。`/ja` `/me` `/ja/demo` `/ja/explore` `/signin` `/legal` `/terms` すべて200。
  - **実測（プレビュー）**: リボン無し・ヘッダの歩くリンク無し（バッジのみ）・公開ステージに「部屋を歩く/埋め込み/カタログ」の3ボタン・配置に3Dプレビュー無し・sticky（PC scroll0→自然位置284px / scroll800→8pxに固定、スマホ scroll500→8px）・375pxではみ出し0・スロット PC 31px / スマホ 15px。
  - **残（ユーザー作業）**: 実機QA（ヘッダの見た目、公開タブの「部屋を歩く」導線、配置図がスクロールで固定されるか）。**`0035_light_override.sql` の適用は引き続き未了**（下の項目参照）。
- **【ship済・本番反映確認済み（2026-07-30 16:40頃）】ダッシュボードの操作感5点（ユーザー指示）**（コミット `ae05550` ＋ `e2deee0`）。①アカウント設定を4つ目のタブから右上メニューへ（`ME_TABS` は3つ、'account' はタブ値として残し メニューから遷移＋戻る導線）②展示タイトルの編集を公開ステージへ移し、ヘッダは `h2.hako-title` の表示だけに③**作品ステージ: サムネを横一列（+追加は先頭）・常に1点選択・詳細フォームを常時表示・選択中は金のバー。電話幅の `position:fixed` ハーフモーダル（`.me-sheet`）は廃止**④部屋のテンプレート行を削除（2026-07-30 に追加したばかりだが、テーマ/間取りの二重の入口だった＝**DECISIONS の「作成後も選び直せるように」を撤回**）⑤配置図のスロットを大きく。
  - **⑤の測り方**: 枠の一辺(S=1.25m)は**動かしていない** — `lib/presets` をコンパイルして4プリセット＋カスタム72通りを総当たりし、**最も近い2枠の距離は1.350m（portrait の角）** と確定したため（隙間10cm）。代わりに配置ステージを1カラムにして地図に全幅を渡し、外周の余白を1.6m→0.5m、`max-width` を360px→680pxに。**実測 15px→31px（PC）**。
  - **スマホは伸びが小さい（13px→15px）**: 26mの部屋を375px幅に描く物理的な制約で、地図の幅がそのまま枠の大きさを決める。タップ判定は41px確保。**さらに大きくするには「スマホでは地図を縦向きに回して描く」等の別案が必要（未着手・ユーザー判断待ち）**。
  - **この作業中に見つけて直した既存バグ**: `@media (max-width: 720px)` の `.place-map` を基本定義より**前**に置いていたため一切効いておらず、狭い幅で地図が広がっていなかった（詳細度が同じなので順序が勝敗を決める。LESSONS「CSSが書いてあるのに効いていない」の同型）。食い返す量もカードの左右padding と揃えた（1.2rem→1.6rem）。
  - **本番反映の確認**: 配信CSSに `.me-works-row` / `.me-work-tile-mark` / `.works-detail--map` / `.place-map{max-width:680px}` ＋ 720px内の `margin-inline:-1.6rem`、`me-sheet{position:fixed}` と旧 `me-works-grid` が**0件**。`/me` のページチャンクで `ME_TABS = ["gallery","guestbook","profile"]`・`tpl-grid` が1箇所（CreateCard のみ）・`hako-title-input` が1箇所（公開タブ）・表示用 `hako-title` あり。`/ja` `/me` `/ja/demo` `/ja/explore` `/signin` `/legal` `/terms` すべて200。
  - **残（ユーザー作業）**: 実機QA（3タブ＋メニューのアカウント、作品の横一列と常時開いた詳細、配置図の枠の大きさ、公開タブのタイトル欄）。**`0035_light_override.sql` の適用は引き続き未了**（下の項目参照）。
- **【ship済・本番反映確認】公開ギャラリーのSEO（B案）**（`99b70b3` `66a88ce` ＋ `b6d7faf`。DECISIONS 2026-07-30「公開ギャラリーのSEO」）。ユーザー要望「公開ギャラリーもSEO対策された上でindexされるように」。**索引の配線（sitemap・robots・OGカード）は既にあり、空だったのは中身**だった。直した8件のうち7件を実装（⑧多言語URL化＝C案は見送り、⑥キャッシュ化は root layout が `cookies()` を読むので構造上不可＝代わりに二重取得を潰した）。
  - 入れたもの: ①`FlatWorkList` を新設して `FlatGallery` のマークアップを切り出し、**公開ギャラリーのサーバー出力に作品一覧＋ステートメントを載せた**（3Dが立ち上がると消える段階的強化。`!armed` で切るので no-WebGL 版と二重にならない）②**JSON-LD**（`CollectionPage`＋`ItemList`／`VisualArtwork`（画像・素材・cm寸法・制作年）／`Person`（bio＋SNSの`sameAs`）／`BreadcrumbList`／`WebSite`）③**canonical**（単一公開ギャラリーは `/@name` に寄せ、2つ目を公開すると各 `/@name/slug` が自己参照へ）④`metadataBase`⑤sitemap の重複解消（単一なら `/@name` だけ・作家ページの `lastmod` を実データに）⑥`getExhibition`/`getProfile` を `cache()` 化（generateMetadata と本体の二重fetchを解消）。
  - **`lib/roomPlan.ts` を新設**（純粋な配置ロジックを `lib/exhibition.ts` から分離）。理由: `lib/exhibition.ts`→`lib/store.ts`→`lib/toast.ts` が `useState` を持つのでサーバーから import すると**ビルドが落ちる**。`lib/exhibition.ts` は全部を re-export するのでクライアント側の import 先は不変。
  - **検証済み（別セッションのコミット `ae05550` の上で全部やり直した）**: tsc / check:css / check:i18n（10言語100%・730キー）/ check:schema / check:gates（52）/ クリーン本番ビルド。**実測は一時ページ＋合成データで本番ビルドに対して行った**（ローカルに Supabase の鍵が無く `/@name` は404になるため）— 配信HTMLに作品3点のタイトル・説明・ステートメント・`<img>`（card/thumb の srcset）が載る／`role="main"` と `<h1>` は各1（重複landmarkなし）／`<script type="application/ld+json">` は実タグ1本で JSON も妥当／作品説明に入れた `<script>alert(1)</script>` が**生では1件も出ず** `<` に逃げている／`<link rel="canonical">` と `og:url` が `metadataBase` 経由で `https://www.xibit360.art/@yuka` に解決（公開2件のときは `/@yuka/tidewater` に切り替わり、breadcrumb も3段になる）／ハイドレーション後の実DOMで `.flat-gallery` は0個＝隠し文字にならず、**JSON-LDは残る**／no-WebGL経路を強制して一覧が実際に描かれること（ステートメントが `pre-line` で改行を保つ）と 375px で横あふれ0。sitemap は**実コードに合成行を流して**確認（`@solo` は `/@solo` のみで `/@solo/tidewater` が出ない・`@multi` は3本・`username` が空の行は落ちる・`/@multi` の `lastmod` が2件の新しい方）。
  - **効きの限界（正直に記録）**: Googleは**JSを実行した後のDOM**を索引するので、①のHTML一覧が効くのは「JSを実行しないクローラー」と「WebGLを持たないクローラー」だけ。WebGLが動くクローラーに作品情報を届けるのは②のJSON-LD。
  - **本番の実データで確認済み（2026-07-30 17:00）**: `/@yusukenakamae`（NEO JAPANESQUE・作品5点）で ①配信HTMLに `.flat-work` が**5件**＋ステートメント＋作品ごとの `<img>`（thumb 310w / card 621w / display 1241w の srcset）が載る — **この変更の前はタイトルと作家名の2行しか無かった** ②`role="main"` と `<h1>` が各1 ③`ld+json` 実タグ1本、JSONが妥当で `@id` の相互参照に未解決0、`ItemList.numberOfItems=5`、`Person` に実アバターとSNS3件の `sameAs` ④`canonical` と `og:url` が `https://www.xibit360.art/@yusukenakamae` に解決 ⑤**`og:image` が絶対URLになり実際に200で1200x630のPNGを返す**（`metadataBase` 前はデプロイURL基準だった。パスは Next が `@` を `%2540` に二重エンコードするが引ける） ⑥**sitemap から重複が消えた**（作家1人・公開1件なので `/@yusukenakamae` のみ。以前は `/@yusukenakamae/{slug}` も並んでいた。全48URL） ⑦生の `<script>` 混入0。`/@yusukenakamae` `/sitemap.xml` `/robots.txt` が200（`/` `/explore` は既定どおり307で言語別URLへ）。
  - **ship の途中で自分の差分から1件直した（`b6d7faf`）**: JSON-LD に `inLanguage: 'en'` を固定で書いていたが、**本番の実データはステートメントが日本語**だった。`name`/`description` は作家が書いた文章で、ページの枠は来場者のロケールで描かれるので1つの言語に決められない → 落とした（LESSONS「品質・レビュー」）。
  - **合成データでしか見ていないケース（本番に実物が無い）**: ステートメント空・作品0点・カバー未設定・動画のみ・公開ギャラリー2件以上（＝canonical が `/@name/slug` に切り替わる経路）。純関数を直呼びして全ケース確認済みだが、本番の実物は1つも無い。
  - **【追補・ship済・本番反映確認】作品画像に帰属表示を付けた**（`cd434bd`、DECISIONS 同日の追補）。`ImageObject` の `creator`／`creditText`／`copyrightNotice`。本番の実データ5点すべてに `© 2026 Yusuke Nakamae` と作家名が載り、`license`/`acquireLicensePage`/`inLanguage` は0件、`@id` の未解決参照も0件を確認。
  - **【ユーザー確認済 2026-07-30】リッチリザルトテスト＝合格**（有効なアイテム1件＝パンくずリスト、エラー0・警告0、クロール成功）。**「1件」が満点** — 同ツールはGoogleがリッチリザルトを出せる種類しか数えないので、私が入れた5種のうち対象は `BreadcrumbList` だけ。残りはエラーではなく「対象外の種類」。
  - **残（ユーザー作業）**: ①validator.schema.org で全ノードが解析できるかの念のため確認 ②Search Console の URL 検査 → インデックス登録をリクエスト、sitemap を再送信 ③しばらく後に「Googleが選択した正規URL」を URL 検査で確認（`/@name` になっているのが期待）④数週間後、Google画像検索で作品にクレジットが出るか。
  - **次にやるか未定の候補**: 作家がライセンス条件を入力できるようにする（ダッシュボードに1項目＋migration）→ `license`/`acquireLicensePage` を出せるようになり Google の「ライセンス可能」バッジの対象になる。**入力欄なしで出すのは不可**（DECISIONS 追補の理由）。
- **【ship済・本番反映確認済み（2026-07-30 13:57頃）】ダッシュボードのステージタブ型再編＋作品ごと照明**（DECISIONS 2026-07-30 の3エントリ＋追補をすべて実装。コミット `9d86eec`（4ステージ再編・レール廃止・作成直後の着地変更・スマホsticky プレビュー）→ `b98a70b` 相当（作品ごと照明 `light_override`＝migration 0035）→ `8c9bccb`（別視点レビューの修正3件: `.purchase-backdrop` z-index 50→100／applyTemplate の debounce 書き戻し競合／publish.ts の照明フォールバック過剰発動））。
  - **検証済み**: tsc / check:i18n（730キー・10言語100%）/ check:css / check:schema（35本）/ check:gates（52）/ クリーン本番ビルド／ローカルPostgres16で schema.sql 全文実行エラー0＋0035冪等／モック描画（`GalleryCard` 一時export方式）で 4ステージ×375px/1280px 実操作 — シート開閉・配置図の「この作品を編集」→シート切替・照明3択→localStorage→3Dプレビューの光の切り替わり（ピクチャーライト⇔天井トラック）・横オーバーフロー0 を実測。
  - **本番反映の確認（`b41281c` を push、Vercel Ready 52s）**: 配信CSSに `.me-stages` / `.me-sheet{position:fixed…z-index:60…}` があり `me-subnav` が0件（レール撤去）、`.purchase-backdrop` が `z-index:100`。配信チャンクに `lightOverrides` / `light_override`。`/ja/me` のSSR出力（flightデータ）に新キー `stageWorks` / `hintAddWork` / `lightFollowRoom`。`/ja` `/ja/demo` `/me` `/signin` `/ja/explore` `/legal` `/terms` すべて200。**出荷前の2巡レビュー実施済み**（1巡目: 確定1件＋懸念2件→修正 `8c9bccb`、2巡目: 退行なしを確認）。
  - **残（ユーザー作業）**: ①`supabase/migrations/0035_light_override.sql` を Supabase SQL Editor で適用（**未適用でも壊れない**＝light抜きへ段階フォールバックするが、作品ごと照明が保存されない）②実機QA — サインインして4ステージの操作感、スマホのボトムシート、作品ごと照明。
  - **既知の設計穴（今回のものではないが5軸目にも波及・レビュー指摘③）**: 非公開中にクリアした per-work 上書きは placements に残った旧値が再公開時に復活し得る（mergedOverrides が「削除」を表現できない。4軸に元からある穴）。直すならトゥームストーンか非公開中も上書き列だけ書く設計変更＝別工事。
  - 3Dパネルとの重複整理は引き続き別スコープ。
- **【ship済・本番反映は要確認】本番の決済が400で止まっていたのを修理＋購入時の同意の作り直し**（`8240636` → `b94df50`。DECISIONS 2026-07-29 の3件）。ユーザー報告「まだStripeでエラーが出る」＋Stripeダッシュボードのログ。
  - **原因**: Managed Payments（Stripeが販売者＝MoRとして各国の税を代行。このアカウントは既定オン）が `custom_text` を拒否していた。2026-07-27 の `tax_code` と同じ出どころで**×2**。`managed_payments[enabled]=false` は税の代行から降りることになるので選ばず、**文言を外す**（ユーザー選択）。
  - **そのあと3手でユーザー指示に追随した**: ①承諾文を法が要る2点まで短縮 → ②「規約に同意します」に（**EU/UKの撤回権の放棄は成立しなくなることを説明した上での再指示。意識して引き受けたリスク**）→ ③**チェックボックス自体を廃止**（新規登録時に同意済み＋購入はサインイン必須なので二度取りだった）。CTA下の一文 `続行すると{利用規約}に同意したことになります。` に置き換え、`TextWithSlot` で1キーに収めた。規約第5条も「購入時に別途確認します」を消して実装と揃えた。
  - **出荷前レビューで1件直した**: `finally { setBusy(false) }` は `return` で抜けても走るので、**リダイレクト中に購入ボタンが復活**していた（連打で Checkout セッションが2本。二重課金にはならない）。同意チェックという一手間を外したぶん踏みやすくなっていた（LESSONS「品質・レビュー（課金）」）。
  - **検証**: 一時ページに本物の `PurchaseModal` を描いて実操作（毎回削除）。tsc / check:i18n（10言語100%・726キー）/ check:css / check:schema / check:gates（52件）/ クリーン本番ビルド。**実カードでの購入完了は未検証**（ローカルにStripeキーが無く501で止まる）。
  - **残**: 本番で1点購入を実際に試す（**これがこの修理の唯一の最終確認**）。リンクが新しいタブで開くかもプレビューでは測れないので実機で。
- **【ship済】文言の番人の穴を塞ぎ、残っていた英語5文を11言語へ**（DECISIONS 2026-07-29「値を挟む文は1キーに収め…」）。**別セッションが同じ作業ツリーで `/me` と辞書11本を編集中だったので、隔離した worktree（`claude/i18n-holes`）で作業し、相手のコミットの上に rebase して出荷した**（相手の未コミット作業には触っていない。衝突したのは台帳2ファイルだけで、辞書11本は自動で合った）。
  - **塞いだ穴**: ルール2は `>text<`、ルール3は「タグの無い素の行」しか見ておらず、**タグや式と同じ行に始まって行末まで続く地の文**が素通りしていた（ルール2bを追加。誤検知0件）。
  - **直した文言5件**: `/me` の作成カード（新規作家が最初に見る画面）の案内文2つとテンプレートの「is premium」、`/reset` と `/signup` の案内文。後者2つは**日本語のキー＋英語の地の文**で1文を組んでいたので、日本語で見ると半分英語だった。
  - **「まだ必要か」の答え**: `/reset` のパスワード長エラーは**同じ文の辞書キーが既にあった**（`auth.passwordTooShort`）ので新キーは不要、直書きを差し替えるだけ。`artwork.confirmSentTo` と `auth.resetSentIfExists` は文の断片で、1キーに統合したので**11言語から削除**した。
  - **番人を1つ追加**: プレースホルダの取り違え（訳文が `{email}` を落とすと値が文から消える）。導入時0件。`check:gates` は 52 件に増え、**新ルール3種＋新番人2種すべてで変異テストが落ちることを確認**。
  - **本番で確認済み**: `/me` の配信HTMLに新しい案内文（`はじめる部屋を選びます` / `展示に名前をつけます` / `は有料です`）が載り、英語の残骸（`pick the room` / `is premium`）が0件。`/signup` を日本語で引くと `に確認用のリンクを送りました` と `再設定用のリンクを送りました` があり、削除した断片（`確認用のリンクを送りました：` / `アカウントが存在する場合、こちらに送りました`）は消えている。`/ja` `/ja/demo` `/signup` `/reset` `/me` `/ja/explore` すべて200。
  - **【実機QA通過 2026-07-30】ユーザーが実機で確認し、指摘なし**（最初の展示を作る画面の案内文が日本語で読めること）。**`/signup` `/reset` の「メールを送りました」画面だけは未確認のまま** — 実際にメールを送らないと出ない画面で、Claude 側はアカウント作成を踏まないため、配信辞書に正しい文が載っていることまでで止めている。
- **【ship済・本番反映確認】配置図(placement)の作り直し＋`/me` の横スクロール修理**（コミット `4269880`。**別セッションの作業を、ユーザーの指示で私の文言修正と一緒に push した**。本番の配信CSSで `.place-map` の枠線・背景・角丸・paddingが消えていることを確認）（DECISIONS 2026-07-29「配置図は枠で囲わない／…」、ユーザー指示3件）。
  - ①**囲みなし**（`.place-map` の枠線・背景・角丸・padding を削除）②**15枠すべて表示し、容量超過分は鍵付き**。押すとその枠に届く枚数を初期値にした購入モーダルが開く（`PurchaseModal` に `quantity.initial` を新設）③**横スクロールの不具合を修理**（ユーザー報告「プルダウンで設定を行き来していると勝手にwidthが広がる」）。原因は2つ — `.wd-row-block` が親から `flex-wrap: wrap` を継いで「折り返せる縦フレックス」になり列幅が中身で決まっていた／`.field-hint-pop` の吹き出しが ⓘ アイコン基準で画面右へ15pxはみ出していた。**375pxでページ幅425px→375px**。
  - 副産物: 地図の描画位置を作品の向きへ0.5mずらした（**island 間取りは15枠のうち13マスしか描けておらず、重なった2組は押せなかった**）。タップ判定の距離をチェビシェフ距離に変更（角を挟んだ斜めの隣と重なっていた）。
  - **検証で分かった重要な事実（LESSONS「検証・ツール」）**: `/me` は**`GalleryCard` を一時 export してモックのストアで描画すれば実DOMを測れる**（この手で長年あった横スクロールを捕まえた）。ただし検証ページに CSS を import すると `app/layout.tsx` と逆順になり cascade が壊れる（存在しないバグを2件「発見」した）。また **STATE に3回「未解明」と書いてきた「プレビューのタブが `/en` へ飛ぶ」現象は、プレビューのペインがタブを自分のURLへ戻しているだけ**で、アプリのバグではない（この項目はこれで閉じる）。
  - **本番反映を元セッションでも独立に確認した（2026-07-29、ユーザー指示「main取り込んでからpushして」への対応）**: push は上記のとおり既に済んでいたので、**合成された `main`（相手の `1182c0f` ＋ 私の `4269880`）を頭から再検証**した — tsc / check:i18n（**726キー・10言語100%**、直書き0・状態文言0・複数形の相方0・プレースホルダ0）/ check:css / check:schema / check:gates（**52件**）/ クリーン本番ビルド。私の新キー `design.spotLocked` が11辞書すべてに残っていることも確認（相手の変更が同じ11ファイルを触っているため）。本番の実物では ①配信CSSの `.place-map` が `display:block;width:100%;max-width:360px;margin:0 auto;color:var(--ink)` だけ＝囲みが無い ②`.pe-slot-lock` / `.pe-slot.locked` / `.wd-row-block{…flex-wrap:nowrap}` / `.me-field-label .field-hint{position:static}` / `.works-detail` の `minmax(0,1fr)` / canvas の `width:100%!important` が全部載っている ③`/me` のページチャンクに**南京錠のパス（`M -0.15 -0.02`）と `design.spotLocked` の参照**がある ④`/ja` の配信辞書に `枠 {n} — 鍵付き。枠を追加すると、ここに飾れます。` と `容量を超えた枠には鍵が付きます。押すと枠を追加できます。` がある。
  - **【追補・ship済・本番反映確認】枠が壁の線に溶けていたのを直した**（コミット `70a666e`、ユーザー指摘「壁のborderとスロットが混ざってる／空スロットの背景がないからだと思う」）。①マスの下地を**不透明**に（空き `#1e1b16` ＞ 鍵付き `#16130f` の明暗で状態も読める）②壁の線を細く暗く（0.34→0.22 / opacity 0.7→0.34）③**枠の外周で壁の線を消す縁取り**を敷いた（地の色 0.16m。**全枠まとめて先に描く** — 枠ごとだと portrait の角の 1.35m 隣の輪郭を食う）④空き枠の破線を実線に。本番の配信CSSで `.pe-slot-knockout{fill:none;stroke:#100e0c;stroke-width:.16}` / `.pe-slot-bg{fill:#1e1b16}` / `.pe-slot.locked .pe-slot-bg{fill:#16130f}` / `.lp-room{…stroke-width:.22;opacity:.34}` と、`/me` のページチャンクに縁取りの描画が載っていることを確認。
  - **【実機QA通過 2026-07-30】ユーザーが実機で確認し、指摘なし**（配置図の15枠・鍵の枠からの購入モーダル・横スクロールが生えないこと・枠と壁の線の見分け）。
- **【ship済・本番反映確認】選択肢の並び替えと、テンプレートの課金抜け穴**（コミット `15db047`）（DECISIONS 2026-07-29 の2件）。①**使えるもの（無料開放＋購入済み）を左へ**。`unlockedFirst()` を `lib/entitlements.ts` に置き、ロックが混ざる4つの一覧（テーマ・間取り・額装・テンプレート）を通した ②**3Dパネルのテンプレートが有料のテーマ/間取りを素通りさせていた**（真下のチップはロック済みなのにテンプレート経由なら適用できた）ので、ロック表示＋購入モーダルに変えた。
  - **額の販売の前提を確認した（ユーザーの理解どおり）**: `FOREVER_FREE_FRAME_IDS = ['black','gold','white','wood','none']` ＝**いま存在する額は全部無料のまま**で、今後 `FRAMES` に足す額だけが有料になる。加えて**パラメトリックの額（素材×色×太さ）は誰でも無料**なので、有料の額は「ツマミでは作れないもの（新しい仕上げ・断面）」でないと無料で再現されてしまう。
  - **本番で確認済み**: `/ja/demo` の3Dパネルで矩形を実測（`White Cube` と `回廊` が x=1333 の最左、有料はその右）、額装5つは無料のまま順序不変、テンプレートは White Cube が先頭で残り3つに「有料」バッジ。ロック中のテンプレートを実際に押して**テーマが切り替わらず**購入モーダル（「Chic のみ $8」）が出ることも本番で確認。
  - **残**: `/me` の実機QA（テーマ・間取り・テンプレートの並びが左から無料になっているか。ローカルではサインインできないため未確認）。
- **【ship済・本番反映確認】複数形フォールバックのバグ修正（i18n）**（コミット `d8f53d7`）。本番で確認したのは、**配信JSに新しい鎖が載っていること**（`for(let o of new Set("one"===e?[…"_one"],t,[…"_other"]]:[…]))` を実物のチャンクから抽出。`_one` という文字列リテラルはこの変更で初めて入るので、そのまま反映の証跡になる）と、**死んだキー `addsSlots` が全チャンクから0件**になったこと、`/en` `/ja` `/ja/explore` `/ja/demo` `/en/articles` `/legal` `/terms` が200のまま。（DECISIONS 2026-07-29「複数形の単数は…」、ユーザー報告）。英語など単複を区別する言語で **count=1 のとき複数形の文が出ていた**（"1 works"）。`translate()` の鎖が `_one → _other → 素のキー` で、単数を素のキーに書いた8キーが**構造上単数に到達できなかった**。**A′**（`one` のときだけ素のキーを読み、`few`/`many` は飛ばして `_other`）に変更。つでに ①`panel.overCapacity` の呼び出しに `count` が無く複数形分岐に入っていなかったのを修正 ②死んだキー `purchase.addsSlots`（11言語×2）を削除 ③**再発防止の番人**を `check:i18n` に追加（`_other` の相方が無いキーを検出、`check:gates` に5ケース＋変異4種で確認）④`check:gates` 自身の `notContains` が一度も照合されていなかったのを修正。
  - 検証: en/de/fr の count=1・2 の実出力を `translate()` 直呼びで確認（`npx tsc lib/i18n/index.ts --outDir <tmp> --module commonjs` でコンパイルすればnodeから呼べる）、CJK+id は常に `other` で1と2が同一＝影響なしを確認。tsc / check:i18n / check:css / check:gates（22件）/ 本番ビルド通過。**ブラウザでの実画面確認はしていない**（該当文言は `/me` のサインイン下と「作品1点だけ」等の特定データでしか出ないため、辞書を直接評価する方が確実）。
  - **出荷前レビューで自分の差分から3件直した**: ①form が `other` のとき鎖の先頭2つが同じキーで、CJK+id（常に other）が毎回同じ木を2度歩いていた → `Set` で重複を落とす ②番人が全キーで行番号を計算していた → 報告するときだけ数える ③その過程で発覚、**複数行コメントを1文字に潰していたため、コメントの後ろにあるキーの行番号がずれて報告が嘘になる** → 長さと行数を保つ伏せ字にし、ケースと変異テストで固定。
  - **【解消済 2026-07-29】並行ブランチ `dd4c59b` との衝突は着地させて解消した**（別セッションが実施。ユーザー指示「未処理のコミットをすべて処理して」）。17ファイルが衝突したが、**`check-i18n.mjs` の「複数形キーの対の点検」と `check-gates.mjs` のケースは両方残っている**（`check:gates` は48件＝両側のケースが揃っている）。解消の判断はコミットメッセージに全部書いてある。要点は3つ — ①`purchase` グループは main 側（`buyAria: 'Buy {name}'` / `fewerAria` / `moreAria`）を残し、あちらの `buyAria: 'Buy {item}'` / `fewer` / `more` は重複なので落とした ②`AdminDashboard` は「main の売り物の単一の出どころ（`paidThemeIds()` 等）＋あちらの `t()` ラベル」に合成し、不足していた `adminUi.frameItem` を11言語に足した（あちら側だけを採ると額の対応が丸ごと抜ける）③参照ゼロになった `common.upload` / `common.replace` / `adminUi.slotN` を11辞書から削除。
    - **本番反映の確認（`476d54e`）**: `/ja/demo` の配信HTMLに新しい訳が載っていること（`幅 {n}m` / `奥行き {n}m` / `画像・動画をアップロード` / `3Dプレビュー` / `無題`）、**私が書いた `額縁 · {name}` が `テーマ · {name}` / `間取り · {name}` と並んでいること**、**重複していた `saved` が消えていること**（配信辞書が `"refreshing":"更新中…","saveArticle":"記事を保存"` と詰まっている＝削除した行のぶん）、削除した `slotN` が0件、`/ja` `/ja/demo` `/ja/explore` `/en` `/legal` `/terms` が200 を実測。**`/me` と `/admin` の実画面はローカルでサインインできず未確認**（元セッションと同じ制約）。
  - **見つけたが直していない小さな穴**: 他言語混入の番人（`SCRIPT_GUARD`）は `//` 行コメントだけを除外していて**ブロックコメントの中身を見ている**ため、en.ts などに日本語の `/* */` コメントを書くと誤検知で push が止まる（en.ts は日本語の `//` コメントを多用しているので踏みうる）。今回は衝突面を増やさないため触っていない。
  - **残（別工事）**: 購入モーダルの `{unit}` 差し込みが**5言語**で壊れている — 独 `slot: 'Plätze'`・伊 `posti` が複数形の語なので単数の文が「1 Plätze」、西/葡は `{unit}s` で `lugars`（正しくは `lugares`）、韓は助詞が `걸이을`（正しくは `걸이를`）。今 `unitLabel` を渡す呼び出しは1か所だけなので、`{unit}` をやめて各言語の文に語を書くのが筋。**詳細は下の【調査済・着手待ち】の項目**（着手は `dd4c59b` の着地待ち）。

- **【修理済・push待ち】購入モーダルの単位の語（`{unit}` 差し込みの廃止）**（DECISIONS 2026-07-29「単位の名詞を `{unit}` で差し込むのをやめる」）。5言語で壊れていた（de/it は複数形の語が単数の文に入り「1 Plätze」「Aggiunge 1 posti」／es/pt-br は語尾sで「2 lugars」＝正しくは `lugares`／**ko は単複ではなく助詞**で `걸이을` ＝正しくは `걸이를`）。**ko の助詞は単数・複数を書き分けても直らない**種類の不一致で、`{unit}` という設計そのものが原因だった。
  - 直したもの: `purchase.optionDesc` / `optionDesc_other` / `howMany` / `fewerAria` / `moreAria` を11言語ぶん、単位の語を文に直接書く形に。単位の語だけを持っていた `purchase.slot` は削除。`PurchaseModal` の `quantity` から `unitLabel` を外し、`app/me/page.tsx` の受け渡しも削除（**`unitLabel` を渡す呼び出しは元から枠の1か所だけ**＝単位は1種類しかなかった）。
  - **ついでに直した既存の不具合2件**: ①**日本語の助数詞**が `{count}つ` で、和語の「つ」は1〜9までなのに `MAX_WORKS_PER_ROOM = 15` なので**「10つ」が実際に出ていた** → `{count}枠`（助数詞に「枠」を使う）②id の読み上げラベルが `{unit}` 始まりで**小文字で始まっていた** → `Kurangi tempat` / `Tambah tempat`。
  - **将来2種類目の単位が来たら、単位ごとにキーを分ける**（プレースホルダに戻さない）。`presets.frameOf` の決定と同じ理由 — 語形変化・助詞・語順は言語側に委ねるしかない。
  - 検証: tsc / check:i18n（直書き0件・状態文言0件・複数形の相方0件・**10言語100%・722キー**）/ check:css / check:schema / check:gates（48件）/ クリーン本番ビルド。`translate()` 直呼びで11言語 × count=1/2/10。**実UIでも確認**（数量ステッパーは `/me` のサインイン下にしか出ないので、検証用に `SettingsPanel` の購入モーダルへ一時的に `quantity` を渡して本番ビルドで実操作 → 日本語 1枠/3枠/10枠・合計 `$3 × 10 = $30`、ドイツ語 n=1「1 Platz」/ n=2「2 Plätze」を実測 → 一時変更を戻した）。
  - **残**: ①**push**（この作業ツリーにコミット済み・未push）②**`/me` の実機QA** — サインインして枠の追加モーダルを開き、数を1→10まで動かして日本語が「N枠を追加します」になるか。
  - **未解明（1件、既存項目に追記済み）**: 購入モーダル操作の直後にプレビューのタブが `/en` へ飛ぶ現象が2回。遷移イベント5種すべて未発火・`<form>` は不在・ステッパーは `type="button"` なので**フォーム送信は静的に否定できた**。ただし**本番ビルドでも起きた**ので、2026-07-29 の「本番ビルドでは一度も起きていない」という記録は誤り。読み取りを別の呼び出しに分ければ最後まで通る。
- **【ship済】統合スキーマ `supabase/schema.sql` の修復（0025〜0034 の欠落）**（コミット `32739ce`）。**本番は無関係・影響なし**（migrationは個別に適用済みで、壊れていたのは「1ファイル貼り付けで新しい環境を作る」経路だけ。この経路が最後に使われたのは 0022 の頃で、以後1回も試されていなかった）。0025〜0034 を番号順に追記し、README §1 の一覧も 0034 まで揃えた。**後の番号が前を上書きする4箇所**を schema.sql 冒頭に一覧化: `purchases_kind_check` 0019→0034 / `record_capacity_purchase` 0019→0028→0031 / `grant_entitlement` 0022→0034 / **`guestbook_insert_public` 0008→0033**（4つ目は指示に無かったもの。ポリシー名が違っていたら、Postgres は permissive な INSERT ポリシーを OR で評価するので「芳名帳をOFFにしても書き込める」状態になっていた）。
  - **検証（ローカルのPostgres 16.14で実行）**: 空DBへ全文実行→エラーゼロ、2回目・3回目も冪等。**`schema.sql` で作ったDBと `migrations/` を0001から個別適用したDBのスキーマが完全一致**（`pg_dump --schema-only` の差分は pg_dump が毎回変えるトークンのみ）＝「番号順に並べれば最終状態が正しい」を実測で確認。行が入ったDBへの再実行でデータ消失なし。未知の `kind` / `status` が実際に拒否されることも確認。**範囲外**: 実際のJWTでのRLS挙動とStorageの実挙動（Supabase固有の前提は最小限スタブした素のPostgresで検証したため）。
  - **この経路のズレを検出する番人を実装した → `npm run check:schema`**（`scripts/check-schema-sync.mjs`、AGENTS.md 5.8。別セッションで実施）。migration を足したら同じコミットで `schema.sql` と README §1 にも足すこと自体は変わらないが、忘れても push 前に機械が止める。
- **【解消 2026-08-09】3Dヒーローの多言語化**（本体は ship 済・完了ログ参照、`d63a798`）。`/admin` のLPヒーロー枠のラベル（中央/左/右 ＋ アップロード/差し替え/保存）の実画面を**ユーザーが確認済み**（「1-4まで確認しました」）。キーの11言語存在と本番配信HTMLへの載りは以前から確認済みだった。
  - **【要判断】「番人の視界の外にある表示文字列」が ×2**（モジュール直下の定数配列＝このセッション／`lib/pricing.ts` の購入モーダル＝別セッション、同日）。`check:i18n` の検出範囲を広げるかは**訳す対象でない定数の誤検知が読めない**ため未着手。次に同型が出たら3回ルールで昇格を検討する。
- **【ship済・本番反映確認】決済に進めない不具合（ユーザー報告 2026-07-29）**（DECISIONS / LESSONS 2026-07-29。コードは別セッションのコミット `9d02ab5` に同梱される形で本番に出ている＝件名は `docs:` なので `git log` では追えない）。報告は「本番のモバイルで作品スロットを複数まとめて買おうとしたら決済に進めなかった」。
  - **真因は文言の畳み込み**。`startCheckout` が「販売がまだ始まっていない」と「ログインが切れた」を同じ `unavailable` に畳んでいたため、**セッションが切れた人に「この機能はまだ存在しません」と表示**していた。決済リクエストはサーバーに届いてもいない（本番 `/api/checkout` は無認証POSTに401を返す＝環境変数は揃っている）。
  - **「複数まとめて」は原因ではなかった**（数量1→5・金額計算・上限表示・サーバー側クランプすべて実機相当で正常）。ユーザーの推測と違ったことは報告済み。
  - 直したもの: ①`signed-out` を別の戻り値にし、`purchase.signedOut` ＋ `/signin` 導線を出す（`getSession()` の `error` をログに残し、`refreshSession()` を一度試す。サーバー401も同じ扱い）②**同意チェック未チェックで購入ボタンが完全に無反応だった**のを、押せるまま `purchase.needsConsent` で理由を出す形に（`disabled` は処理中だけ）。新規2キー×11言語。
  - 検証: tsc / check:i18n（10言語100%・660キー）/ check:css / check:gates / クリーンbuild。**本番ビルドに対して**ダミー資格情報でセッション無しを再現し、2つの状態を実機相当（375×812）で2回ずつ実測。
  - **本番反映の確認（2026-07-29）**: 配信JSに `ログインの有効期限が切れました。もう一度サインインすると、この購入を続けられます。` が載っていること、`purchase.signedOut` / `purchase.needsConsent` が11辞書すべてにあること、`lib/checkout.ts` が `signed-out` を独立した戻り値として返すことを実測。
  - **残**: ①**実カードでのE2E**（サインインとStripeキーが要るのでローカル不可）②実機QAに1行追加: **購入モーダルを操作中に勝手にトップページへ戻らないか**（検証中にプレビューのタブが3回サイト直下へ飛んだ。クリックイベントは常に正しい要素で `location.*` も `beforeunload` も発火せず、本番ビルドでは再現しなかったため**プレビュー側の挙動が濃厚**だが、アプリ側の原因を特定できていないので残す）。
- **【ship済・本番反映確認】販売構造の作り直し 2件**（DECISIONS 2026-07-29、コミット `d1f4f4e` `f073347`。**別セッションの作業を、ユーザーの指示でモバイルUI修正と一緒に push した**）。本番で価格表示が1セントも変わっていないことを確認済み（トップ ja/en とも「1つ $3 / $8 / $5」、特商法ページも同額）。発端はユーザーの「テーマやフレームの種類を追加できる構造か、価格も別に設定できる構造か確認してほしい」。
  - **①1点ごとの価格（A）**: `lib/pricing.ts` の `ITEM_PRICE_CENTS` に id を書いた分だけその価格。**表は空で出荷したので現行価格は1セントも変わらない**。対外の価格表示（トップの料金カード・特商法）は表から導出し、1点でも違えば「1つ $8〜$12」に自動で変わる。「何が売り物か」は `paidThemeIds()` / `paidLayoutIds()` に集約（checkout・/admin の付与・webhook・価格範囲が同じ答えを見る）。checkout は売っていない id を400で拒否。
  - **②カスタム間取りを未公開に（B）**: `CUSTOM_LAYOUT_RELEASED = false`。有料扱いなのにロックが抜けていて無料で配られていた。既に `layout='custom'` の部屋はチップを残す（他へ切り替えると片道で消えるが `layout_params` は残る）。
  - **③額（フレーム）を売れる構造に（C）＝ship済・本番反映確認（`b500dd8`）**（DECISIONS 2026-07-29）。本番で確認したのは、価格表示が1セントも変わっていないこと（トップ ja「1つ $3 / $8 / $5」・額の行は出ない、特商法の価格欄も同じ3項目で額の1文なし）と、購入モーダルが日本語で出ること（「新しいテーマ / Chic のみ $8 / このテーマだけを使えるようにします。…」）。カスタム間取りのチップが本番の間取り一覧から消えていることも確認。ユーザー判断は「今あるものは全部無料のまま、今後追加する額を有料に」＋基本価格 $3。既存5種は `FOREVER_FREE_FRAME_IDS` に固定（遡ってロックしない）、`FRAMES` に足した額は自動で有料・鍵付き・購入可能。**パラメトリック額（素材×色×太さ）は無料のままだが、`makeFrameKey` が有料idに畳まないようにした**（無料パネルで有料の額を手に入れられる穴）。対外の価格表示（トップ・特商法）は売り物があるときだけ額の行を出す。
    - **⚠ ユーザー作業: `supabase/migrations/0034_frame_purchases.sql` を Supabase SQL Editor で適用**（`purchases.kind` に 'frame' を追加＋`grant_entitlement` の許可リスト更新）。**未適用のままでは額は売れない**（決済は通っても台帳に書けない）。他の機能には影響しない。
    - **有料の額は「素材×色×太さでは出せないもの」でないと意味がない**（見た目だけなら無料パネルで再現できる）。新しい仕上げ・断面はコード側（`Room.tsx` / `Exhibit`）の作業になる。
  - **副産物として修理: 購入モーダルの文言が11言語すべて英語だった**（`lib/pricing.ts` が英文を組み立てており、`check:i18n` は `lib/` を見ないので0件のまま通っていた）。11キー×10言語を辞書に入れ、モーダルは `t()` からだけ組む形に。既に辞書にあった `purchase.oneTimeSlots` を使わず同じ英文をpropsに直書きしていた箇所も直した（LESSONS 2026-07-29）。
- **【ship済・実機QA待ち】モバイルUIの細部6件**（ユーザー指摘 2026-07-29、コミット `656dc0b`）。①ボタンの文字が上に寄る（`.btn-line` を着せた `<a>`/`<label>` はUAの縦中央寄せが効かない）②作品パネルの「›」が閉じるの矩形にほぼ丸ごと内包されタップを奪われていた ③`/me`・`/admin` 上部の行き先を640px以下でハンバーガーに畳んだ（DECISIONS 2026-07-29）④照明行だけ見出しと選択肢が同じ行だったのをダッシュボード内で統一 ⑤音源アップロードの見出し〜ボタンの隙間 29px→10px ⑥（指摘外の発見）フッターが375pxで縦1文字組み・高さ148pxになっていた。**ユーザーの実機で見てほしいのは `/me` と `/admin`**（ローカルはサインインできず、幾何は合成DOMで実測・ハンバーガーの開閉挙動だけ公開ページに一時設置して実操作で確認した）。本番のCSSに4つの新ルールが載っていること・`/me` のHTMLに `me-top-burger` があることは確認済み。
  - **ship時に踏んだこと（LESSONS「検証・ツール」に記録）**: push直前の `tsc` が3件のエラーで落ちたが、原因は**別セッションがこの作業ツリーで額販売を実装中（`lib/entitlements.ts` が未コミット）**だったこと。コミット済みのHEADを一時worktreeに取り出し（`node_modules` は1階層ぶんのシンボリックリンクで共有）、そこで tsc / 番人3種 / 本番ビルドを通してから push した。

- **【ship済】多言語SEOと10言語の翻訳**（DECISIONS 2026-07-28 / 07-29）。11言語のサブディレクトリURL（`/ja/explore`）、middlewareでの書き換えと307/308、hreflang 12本＋canonical、言語別sitemap、`LocaleLink`、URL遷移する言語切替。
  - **カバレッジ: 10言語すべて 100%（631/631）**。分母から外しているのは、訳さないと決めた `legal.*`（特商法は日本語版が正）・`footer.legal`（導線は ja だけ）・`notFound.code`（"404"）で、`scripts/check-i18n.mjs` の `NOT_TRANSLATED` に理由付きで載せてある。
  - **英語のまま残しているのは意図的なもののみ**: `metadata` のページタイトル（URLに言語を入れる形にした今は言語別にできるが、`<title>` を訳すと検索結果の見え方が変わるので別途判断が必要）、テーマ名（`Chic` `White Cube` `Noir`）とテンプレート名、`/terms` `/privacy`（英語版が正）。
  - **`metadata` も言語別にした**（DECISIONS 2026-07-29）。言語別URLを持つ4ページの `title` / `description` / `openGraph` を `seo` グループから出す。単一URLのページは英語のまま。
  - **アラビア語（RTL）はやらない**（ユーザー判断 2026-07-29）。物理CSSプロパティの転換と `textures.ts` の文字方向対応が要る別工事で、必要になったら `LOCALE_META.dir` がフックとして残っている。

- **【待機・他の改善完了後】B14（多言語の残り）**（DECISIONS 2026-07-28）。他の改善が終わるまで着手しない。着手前に「どの言語を網羅するか」を決める（**この判断がユーザー待ち**）。
- **【ship済・残は実機QA】モバイル6件の本番QA**（下の完了ログ参照）。Claudeが本番で実測できたところまでは確認済み。**ユーザーの実機でしか確認できないものが3つ残っている** — ①ノッチ付きiPhoneでHUD/ボトムシートの下端がホームバーに食い込まないか（プレビューは`env(safe-area-inset-*)`が常に0で再現不可）②遅い回線でローディングが最後まで閉じずに待つか（ローカル・本番ともキャッシュ済みで再現不可）③サインインして作品をアップロードし、画質が上がっているか＋壊れていないか。
- **【残1件】Phase 0 のユーザー作業**: `support@xibit360.art` の受信設定（Cloudflare Email Routing）。法務3ページすべてに連絡先として記載済みで、届かないとGDPRの権利行使も特商法の開示請求も宙に浮く。**SQL 3本（0019/0028/0031）は2026-07-28に適用完了・検証済み** — `purchases.currency` 存在、`record_capacity_purchase` は6引数版のみ（security definer / owner postgres）、`service_role`=実行可・`anon`=実行不可を `has_function_privilege` で確認。**課金は技術的に完成**（残るは実カードでのE2E）。
- **【ship済・実機QA待ち】オンボーディングの詰まり3件**（コミット `156c275`）。①ダッシュボードから3Dエディタへの導線が**1本も無かった**（`/me` 内の `/demo` は `router.push` のみ）→ サブナビに「Walk the room」追加 ②その存在しない場所を指していた案内2箇所を修正 ③username が隠しゲート（未設定だと公開トグルの行ごと消え、理由がどこにも無かった）→ トグルの位置に理由と入力欄を移動。**`/me` はサインインが要るためローカルで通し検証できていない**（CSSのみ実測）。
- **【ship済・ユーザー作業待ち】Phase 0（課金と法務）4件**（4コミット `74a065e` `88a5a3f` `e934ba3` `a8cc87e`、DECISIONS 2026-07-28参照）。本番反映確認済み。（SQL適用と残作業は上の項目に集約）
- **【ship済・本番検証済み】Phase 1〜2の先行4件**（`4d62bbe` `d5f05da` `430e6e8` ＋DOM取得方式への修正）。①来場者HUDを静止0.55／歩行中0.16／接触1.0に（実機で画面の33%を占めていた）②長辺800pxの `card.jpg` を追加し、カード=card・小タイル=thumb・3D=display に整理（実測 display 105KB → card 25KB）③WebGLコンテキスト消失からの復帰（本番で消失→オーバーレイ→再構築→描画まで通しで確認）④`robots.txt` / `sitemap.xml` を新設（本番のsitemapに作家ページと展示が入ることを確認）。**migration 0031・0032 は適用済み**。
- **【要判断】レビューで挙がった未着手の指摘**。優先度順に整理済み（このセッションの報告参照）: **Phase 0 は完了（上記参照）**。(Phase 2) `/admin`に通報一覧も非公開化ボタンも無い（reportsはSELECTポリシー自体が無く件数表示のみ）／芳名帳が匿名・無審査・ON/OFF不可／公開＝Explore自動掲載で限定公開が選べない／本番の`/explore`が0件。(Phase 3) i18n基盤ゼロ・sitemap/robots無し／CJKタイトルで3Dプレート・タイトル壁が溢れる。
- **【解決済・記録のみ】3D展示だけ作品が空の額縁になる不具合**（DECISIONS 2026-07-27「CDN配信のCORS」参照）。**CDNキャッシュのCORS汚染**が真因で、コード変更なし・**Cloudflare設定のみで解消**（①R2バケットCORSの読み取りを`*`に ②Transform Ruleで`Access-Control-Allow-Origin: *`を無条件付与 ③Purge Everything）。ヘッダを実測して両方`*`を確認、実ブラウザで3作品の3D描画とコンソールエラーゼロも確認済み。**同種の症状が出たら、まずブラウザではなくCDNを疑う**（`Origin`あり/なしで`curl`を叩き分ける。手順は R2_SETUP §5.1 末尾）。
- **容量制限の実測化の実機確認**（DECISIONS 2026-07-27「容量制限の実測化」参照）。コードは本番反映済み、migration **0030 も適用済み**（`reserve_storage`の存在をユーザーが`pg_proc`で確認）。**残るは実機での挙動確認** — ①作品をアップロードして`/me`の「Storage: N MB of 300 MB used」が増えるか ②作品を削除して減るか ③BGMを上げてもメーターが増えるか（旧実装では増えなかった）。※**0030のDDLは 2026-07-29 にローカルのPostgres 16でも実行確認済み**（`storage_reservations` と `reserve_storage` が作られること・冪等であること）。ただし**判定ロジックが実際に効くところ（同時実行での予約・実測値との合算）はまだ誰も見ていない**ので、上の実機確認が初回になる。
- **削除時のCDNキャッシュパージの実効確認**（DECISIONS 2026-07-27参照）。コードは本番反映済み、Cloudflareトークン(`CLOUDFLARE_ZONE_ID`/`CLOUDFLARE_PURGE_TOKEN`)もVercel設定済み。**残るは実際に効いているかのE2E確認だけ** — R2_SETUP §7.5の手順で、捨ててよいテスト作品を1枚上げる→URLをエッジに載せる(`cf-cache-status: HIT`)→削除→同じURLが404になるか。効いていなければVercelログに `cache purge rejected` が出るので、§7.5の切り分け（トークン権限 / プレフィックス指定が拒否されるならURL指定へ切替）に従う。
- **Stripe決済の本番有効化**（DECISIONS 2026-07-24参照、本番モード＋Xibit360専用の新規アカウントで確定）。Claude済: `NEXT_PUBLIC_SITE_URL=https://www.xibit360.art`設定。ユーザー待ち: ⓪同じログイン下にXibit360専用アカウントを新規作成し本番有効化 ①`0019_checkout.sql`をSupabase SQL Editor適用 ②新アカウント(live)の`STRIPE_SECRET_KEY`(sk_live_)をVercelに ③新アカウントでWebhook作成(`https://www.xibit360.art/api/stripe/webhook`・`checkout.session.completed`)→`STRIPE_WEBHOOK_SECRET`(whsec_)をVercelに ④価格USD化に伴い`0028_capacity_clamp.sql`もSQL Editorで適用。完了後Claudeが再デプロイ＋疎通検証(checkout=401/webhook=400)、実カードで**スロット追加($3〜)購入→返金**でE2E確認（USD化で¥580→$3/枚に変更済み）。

- **【ship済・本番反映確認済み】UXレビューを3面（LP／`/me`／3D）で実施し、指摘3件をユーザー判断で処理**（`2fdb54b`〜`ac0efe6`）。**方法**: 認証必須の `/me` は実物のコンポーネントを `npx esbuild` で実バンドルし、`app/*.css` を layout と同じ順で連結した素のHTMLに載せ、zustand に合成データを入れて**本物のポインタ／キーボード操作**を打って測った（LESSONS の成功パターンに手順を記録）。**結果は思っていたより健全**で、`/me` 全5タブ・3D HUD とも 320/375px で**はみ出し0件・コントラスト違反0件**、配置エディタのドラッグ／入れ替え／外す／上限拒否＋トーストも全て正常だった。**直したもの3件**: ①「作品を追加」のラベルが8px（UI全体の最小の外れ値）→ 既存の下限 0.62rem に揃えた ②✕の当たり判定が15pxで WCAG 2.2 の24pxを割っていた → **400px以下を4列にしてタイルを60px以上にしてから**判定を24pxに広げた（順序が逆だと47pxのタイルで中心が削除ボタンになる。一度やって撤回した）③**配置エディタをキーボードで操作できるようにした**（WCAG 2.1 レベルA。枠15個に role/tabindex/aria-label/aria-pressed、Enter・Space・Escape、`:focus-visible`。フォーカス可能な要素が8→31個に。判断ロジックは `activate()` 1つに寄せてポインタと共用）。**D-5（予約した合同展示の返金運用）はユーザー判断で非対応**（残余リスクは伝達済み）。**過剰報告を今回5件自分で止めた**（横スクロール領域／SVGのfont-size／当たり判定の実装側補正／WCAG 2.2の間隔例外／要素の箱と文字インクの違い）── 閾値を割った要素を見つけても、例外と実装側の補正を当てないと直す必要のないものを直す。検証: tsc／番人8種（gates 66）／本番ビルド／配信CSSに5項目すべて入っていることを本番で実測。
- 2026-08-13: **【ship済・本番反映確認済み 14:26】自動保存をやめ「上帯の1ボタンで保存」に組み替えて `8dba5e0` を push**（Vercel Ready）。**置き換えた自動保存は8経路**: 部屋の詳細／テーマ・間取り・既定の額装／Design Tools／カスタム間取り／作品ごとの上書き／配置／作品の情報／プロフィール。`lib/pendingSave.ts` に**保存する処理そのもの（クロージャ）を溜める**形にしたので、経路ごとの保存の作りを書き写していない。作品は id 別枠・失敗はそこで止めて未保存のまま・**流している最中に同じ枠が書き換わったら消さない**。**プレビューは即時・公開は保存後**（ユーザー選択A）: テーマ等は `spaceDraft` に入れ、画面と3Dプレビューは `view = {...row, ...spaceDraft}` を見る。行を書く保存は `pendingSettings()`（走る瞬間の下書きを読む）を通すので、テーマと配置が同時に待っていても最後に走った方が古い値を書き戻さない。**即時のまま**: 画像のアップロード/削除・部屋の作成/公開切替/削除。ただし公開切替は**未保存の下書きを含めて**公開する。未保存のときは上帯のボタンが金色＋点。閉じる/リロードは `beforeunload`、**アプリ内の遷移（ロゴ・探す・管理）も自分で確認する**（`beforeunload` は鳴らないので素通しになる）。**`/code-review` 4件をすべて修正**: ①`useState` の初期値が下で宣言した `view` を読んでいて **TDZ でダッシュボードが真っ白**（tsc もビルドも通る）②テーマの保存と色の保存が同じ枠で**テーマの変更が黙って消えていた**③保存完了時に下書きを空にして**保存中の変更を捨てていた**④別の作品の未保存で「未保存」と出ていた。検証: tsc・番人7種・本番ビルド／台帳を15項目 Node で実物の store に通して照合／保存ボタンは本番と同じCSSで実測（44px・金色・畳まれても見える・375pxではみ出し無し）／宣言順の静的照合（TDZ再発防止）。
- 2026-08-13: **【ship済・本番反映確認済み】公開日時UI・合同展示URL表示・招待リンクの案内を修正して `main` へ push**（`757bd32`）。**①合同展示の公開日時（`datetime-local`）のカレンダーアイコンがダーク背景で黒く見えなかった** → `color-scheme: dark` で解消。**②世界中の利用者が使うのにタイムゾーンの表記が無かった** → ブラウザの現地タイムゾーンをラベル化する `localTimeZoneLabel()` を追加し入力欄の下にヒントで表示。**③合同展示の部屋を見ているとき、Heroのサブ行が個人の `/@username` のままだった** → `Hero` に `expo` propを足し、`current.expo_id` があるときだけ展示のURL（`/expo/{slug}`）に差し替え。**④招待リンクは元から人数無制限で使い回せる作りだった**が、UI文言が「1人につき1本」に見えると指摘され、案内文と「もう1本つくる」のラベルを明確化。検証: tsc・i18n（ja 100%）・css・schema・sns・expo・price-disclosure・gates 71件・本番ビルド／Playwrightで実測・スクリーンショット確認。**push が1回 `Internal Server Error` で失敗**（GitHub側の一時的な障害）→ 再試行で成功。
- 2026-08-13: **【ship済・本番反映確認済み】2DUIのユーザー指摘3件を修正して `main` へ push**（`946837e`、`f2565d2` の上にfast-forward）。**①多室ギャラリーで扉に近づいたときのプロンプト（`RoomSwitch`）を画面下部中央へ**（従来は左下・ミニマップの真上）。`bottom` の値は変えていない — 元々ミニマップ回避のため高めに積んであった値が、結果としてページャー・ヒント・トーストのどの帯とも重ならない高さだったため、`left:50%; transform:translateX(-50%)` の追加だけで済んだ。**②`/me` の「すべて保存」ボタンの高さがタブより高く見えていた原因を特定して修正**: `.me-save-all` が `font: inherit` で**本文の行間 1.8 を継いでいた**（`<button>` は本来UA既定で `line-height: normal` だが、`.btn-line` 側のコメントで指摘されている「`<a>`/`<label>` が行間1.8を継いで背が高くなる」現象を、この要素は `<button>` なのに `font: inherit` で自ら踏んでいた）。加えて `.me-stages-row` の `align-items: stretch` が入れ子の `.me-stages` にも既定で伝わり、`.me-stage` タブ側を行の高さいっぱいに引き伸ばしていたため、`line-height` を直しただけでは今度はタブの方が高くなる逆転が起きた。`.me-stages` に `align-items: flex-end` を足して解消。**Playwrightで実測**し、PCでは両ボタンとも33.97pxに一致することを確認。電話の固定バーは別途 `min-height:44px` でタッチ領域を維持。**③部屋の「通常展示」バッジ（`RoomExpoBadge`）の左端に切り替えアイコンを追加**: 新規 `SwitchIcon`（`components/icons.tsx`）。**ユーザーから「本番に反映されていない」と指摘を受けた**: 最初のpushはfeatureブランチ止まりで `main` に入っていなかった。ユーザーに反映方法（main直接push／PR経由）を確認し、main直接pushを選択。ブランチ間で `origin/main` が1コミット先行していたため、merge後に**検証をやり直した**（AGENTS.md 5.7）。検証: tsc・i18n・css・schema・sns・expo・price-disclosure・gates 71件・本番ビルド（すべてmerge後にやり直し）／Playwrightで3箇所とも実測・スクリーンショット確認。
- 2026-08-13: **【ship済・本番反映は未確認】部屋切替のロード演出、合同展示を1展示1部屋・作品専用プールに変更して `main` へ push**（`9c3eaa8`）。**①部屋切替のロード演出**: 多室ギャラリーで扉から次の部屋へ進むボタンを押しても、フルページ遷移（`window.location.assign`）が新しいドキュメントを取得する間、旧ページが固まったままで「押しても反応が無いように見える」区間があった（ユーザー指摘）。`enterRoom()` が押した瞬間に（今のページのまま）`LoadingScreen` を再表示してから、2連続 `requestAnimationFrame`（ペイントが済んでから遷移する定石）を挟んで遷移するようにした。**②合同展示の部屋を編集中、無関係な通常展示の部屋タブが出ていた**（ユーザー指摘のスクショで確認）。`!row.expo_id` を条件に足して丸ごと隠した。**③合同展示を1展示1部屋に制限**（ユーザー決定。**この決定は2026-08-13の本エントリ後、同日中に0062で「1展示・1作家1部屋」へ訂正されている**）。DB側2経路（新規部屋のINSERT＝`enforce_room_allowance`、既存部屋の切替＝`switch_room_expo`）の両方に「その展示に既に部屋が無いか」を追加。**④合同展示の部屋を口座の共有作品プールから独立させた**（ユーザー決定）。`artworks.gallery_id`（migration 0061）を新設し、合同展示の部屋だけ0から積む専用プールにした。`work_slot_pool`/`placement_pool_used`（0059）は合同展示の部屋を除いて計算するよう変更。検証: tsc・i18n・css・schema・sns・expo・price-disclosure・gates 71件・本番ビルド／SQLテスト新規21項目＋既存0047の書き直し／ローカルPostgresでschema.sqlとmigrations 0001〜0061の完全一致・冪等性を確認。**【ユーザー作業が必須・まだ未実施】`supabase/migrations/0061_expo_room_scope.sql` を本番に適用する**必要がある（0062はこの0061を前提にしている）。
- 2026-08-14: **【ship済・本番反映確認済み】合同展示のフローを一通り確認し、見つかった10件を修正して `main` へ push**（`7a9f404`、migration 0063・0064）。**発端はユーザーの実地報告3件**（招待された作家Bのダッシュボードにオンボが出る／作品を足すボタンが無反応／切替ボタンに新規作成しかない）。**3つとも同じ1つの事故が原因**で、`RoomExpoBadge` の「通常展示に戻す」が名前に反して**変換**であり、参加作家が押すと自分の合同展示の部屋が通常展示の部屋に変わって**展示から消える**（招待は `accepted` のまま残るので 0062 のトリガが再発火せず復帰不能）。**本番データで確定**（残った部屋の slug が自動生成名 `expo-21f070ac-1` のまま `expo_id` だけ null、`created_at` が承認時刻とマイクロ秒まで一致）。**ユーザーの設計判断で「変換ではなく行き来」に組み替え**、あわせてフロー全体（主催者の参加者タブ／会期の決済／準備できたトグルと通知）を検証して7件を追加で発見・修正。**主催者は会期前に参加作家の中身を一切見られず、$15〜$40を払ってから初めて中身を見る構図だった**のを「準備できたがオンの部屋だけ見える」（ユーザー選択C）に。検証: tsc・番人8種（gates 71）・**SQLテスト全339項目**（0063=13・0064=19を新規追加）・本番ビルド／**両方の番人を外すと赤くなることを実測**（0063は本番の事故そのものを再現）／`schema.sql` と全migrationのDBを `pg_dump` で diff／**ブラウザで実操作確認**（preview-verify の実バンドル手順）。**出荷前レビューが5巡**かかり、毎回前の修正が次の穴を開けた（LESSONS に昇格候補として記録）。**途中で設計を1回作り直している** — 部屋の自動作成を最初DBトリガで書いたらSQLテスト10ファイルが落ち、原因が【絶対ルール】違反（レシピの二重管理）だったので画面側へ移した。**未ログインの公開ページを全滅させる書き方（0041と同型）も1度踏み、SQLテストの `anon` 項目が push 前に止めた**。**【ユーザー作業が必須】`0063` → `0064` の順に本番DBへ適用する**。
- 2026-08-14: **【ship済・本番反映確認済み】合同展示の入口をモード切替の形に整え直して `main` へ push**（`c90b2b5` の続き）。ユーザーの実地の違和感3件から。**①部屋タブに合同展示の部屋を並べたのを撤回**（「合同展示は『もう1つの部屋』ではなく別のモードなので同列に並ぶと違和感」）。部屋タブは通常展示の部屋どうしの切替に戻し、表示も元の3ステージへ。合同展示の部屋を開いているあいだは丸ごと隠す（1展示1部屋なので切替の意味が無い）。**②Hero右上のボタンをモード切替にした** ── 「通常展示」「合同展示 →」の2つだけを置き、いま居る方を金色で示す。合同展示の行き先の一覧は `ExpoManager` のウィンドウが持ち、**主催分と参加分が両方並ぶ**。**③受信箱から参加中の展示の行を外した**（部屋への入口が二重になり、しかも「開く」は遷移せず下の編集画面を差し替えるだけで押しても何も起きないように見えた）。降りる導線は部屋の「公開」タブへ移設（`leaveExpo` 新設）。検証: tsc・番人8種・SQLテスト全339項目・本番ビルド／**ブラウザでモード切替を実操作確認**（通常展示に居るとき「通常展示」が金色で押せない／合同展示に居るとき「通常展示」が押せて `onOpenNormal` が呼ばれパネルが閉じる）。**別視点レビュー3巡で7件を修正** ── うち3件は私の直しが作った回帰（受信箱が見出しだけの空カードになる／戻る先が無いのに「通常展示」が押せて何も起きない／`missing_table` の経路だけウィンドウが見出しと作成ボタンだけになる）。
- 2026-08-15: **【ship済・PR #14としてmainへマージ済み】LPレビューの残りを片付けた ── 購入判断の3問を `/help` に足して料金セクションから導線を張り、`.stat` のCSS衝突を直した**（`061cf40`）。①FAQはLPに作らず `/help` に追加（DECISIONS 2026-08-15 決定4。既存19問のどこにも購入・登録を止める返金・作品の権利・AI学習・保存容量の答えが無かった穴を埋めた）②LPの料金セクション直下に `/help`・`/terms`・`/legal` への導線を新設③`app/me.css` の `.stat` 系4規則を `.stat-row .stat` に閉じてLPとの衝突を解消（DECISIONS 2026-08-12・D-1と同型で4回目）。ユーザー判断3件（実作例セクション・フッタタグライン・`stat1`）はすべて「作らない／変えない」。検証: tsc・番人8種・本番ビルド・配信HTML実測。
- 2026-08-16: **【ship済・PR #16としてmainへマージ済み】LPヒーロー「スクロールして入る」の中央揃えを直し、動く合図を追加、文言を全11言語で一語に短縮**（マージコミット `36b7b3d`）。原因は `.hero-scroll` の中央寄せ用 `transform:translateX(-50%)` が、共有していた汎用入場演出 `rise`（`transform:none` で終わる）に上書きされ、右に57pxずれたまま静止していたこと。要素専用の `heroScrollIn` キーフレームに差し替えて解消し、下に流れ落ちる粒（スクロールホイールを模した最小限のアイコン）を追加。ユーザーの追加指摘「どこに入るか不明」を受け、動く合図があるので文言側は動作を示す一語に統一（ja「スクロールして入る」→「スクロール」、en "Scroll to enter" → "Scroll" など）。検証: tsc・番人8種・本番ビルド・Playwrightで3幅すべて中央一致とアニメーション動作を確認。
- 2026-08-16: **【ship済・PR #17としてmainへマージ済み】LP廊下6パネルの画像を廃止し、板を壁面タイトル大に拡大、地の板を無くして文字を壁に直接転写する見た目に**（マージコミット `81e6714`）。発端は「LPに画像を入れたいが、ギャラリー風の見た目に図解などがマッチしない」という相談。画像で説明するのをやめ、板（3Dテキストメッシュ）自体を拡大（幅1.7→2.6）して画像の代わりにし、隣の画像枠（管理画面アップロード `panelArt`／`LpPanelsEditor`）と、左下に重複表示していた2Dのスクロール同期見出し（`.journey-hud`）を撤去。板は実在の3Dオブジェクトなのでスクロールに合わせて自然に視差が付く。拡大直後「パネル張りで安っぽい」との追加指摘を受け、地の板（明るい下地カード＋縁取りの箱メッシュ）を撤去し、キャンバスを透明にして文字色を明るい配色へ反転（`--ink`/`--muted` と同色）。壁の色が透けて文字だけが転写されて見える。検証: tsc・番人8種・本番ビルド・Playwrightで6枚の重なり無し・輪郭無し・モバイル可読性を確認。
- 2026-08-16: **【ship済・PR #15としてmainへマージ済み】ユーザー報告のスクショ「ここ直してデザイン／おかしいよね」を受けて、LP4枠統計行の見た目崩れを2件とも特定・修正**（マージコミット `418ea05`）。報告の中身は2つの別問題だった: ①`.stat` の枠線・角丸化 → 既に別コミットで直っていたが未マージだっただけ ②「URL1」「本」の語中改行 → `.stat b` に `white-space` が無く行が入り切らないと単語途中でも折り返せる作りが真因。当初 `.concept-stats` の `max-width` を広げる方向（760px→980px）で直そうとしたが、実測で**本当のボトルネックは大きい数値ではなくその下の説明文（`.stat span`）の1行分の幅**（イタリア語は4枠合計1112px必要でデスクトップ幅を広げても解決しない）と判明。①`.stat b` に `white-space: nowrap` で単語途中の折り返しを禁止 ②`.concept-stats` に `flex-wrap: wrap` を常設して入り切らない言語は2行×2列へ自然に折り返す、という構造的な直しに変更。日本語「URL 1本」・韓国語「URL 1개」には U+2060 WORD JOINER も追加。検証: tsc・番人8種・本番ビルド・Playwrightで全11言語×901〜1920pxの2行化ゼロを確認。
