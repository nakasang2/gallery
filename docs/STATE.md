# STATE.md — 作業状態（セッション横断）

> Claude向け運用ルール: セッション開始時にこのファイルを読んでから作業に入る。作業の節目・中断時・ship後に更新する。終わった項目は「完了ログ」へ移し、完了ログは直近5件だけ残す。

- **最終更新**: 2026-07-28（モバイル6件＋Phase 0（課金・法務）4件＋オンボーディング3件をship。**SQL適用完了で課金は技術的に完成**。残: support@ の受信設定、実カードE2E、ユーザーが自分の個展を1つ公開する実機QA）／別セッションで3Dモデル等のR2配信化をship（本番反映確認済み）

## 進行中
- **【ship済・残は実機QA】モバイル6件の本番QA**（下の完了ログ参照）。Claudeが本番で実測できたところまでは確認済み。**ユーザーの実機でしか確認できないものが3つ残っている** — ①ノッチ付きiPhoneでHUD/ボトムシートの下端がホームバーに食い込まないか（プレビューは`env(safe-area-inset-*)`が常に0で再現不可）②遅い回線でローディングが最後まで閉じずに待つか（ローカル・本番ともキャッシュ済みで再現不可）③サインインして作品をアップロードし、画質が上がっているか＋壊れていないか。
- **【残1件】Phase 0 のユーザー作業**: `support@xibit360.art` の受信設定（Cloudflare Email Routing）。法務3ページすべてに連絡先として記載済みで、届かないとGDPRの権利行使も特商法の開示請求も宙に浮く。**SQL 3本（0019/0028/0031）は2026-07-28に適用完了・検証済み** — `purchases.currency` 存在、`record_capacity_purchase` は6引数版のみ（security definer / owner postgres）、`service_role`=実行可・`anon`=実行不可を `has_function_privilege` で確認。**課金は技術的に完成**（残るは実カードでのE2E）。
- **【ship済・実機QA待ち】オンボーディングの詰まり3件**（コミット `156c275`）。①ダッシュボードから3Dエディタへの導線が**1本も無かった**（`/me` 内の `/demo` は `router.push` のみ）→ サブナビに「Walk the room」追加 ②その存在しない場所を指していた案内2箇所を修正 ③username が隠しゲート（未設定だと公開トグルの行ごと消え、理由がどこにも無かった）→ トグルの位置に理由と入力欄を移動。**`/me` はサインインが要るためローカルで通し検証できていない**（CSSのみ実測）。
- **【ship済・ユーザー作業待ち】Phase 0（課金と法務）4件**（4コミット `74a065e` `88a5a3f` `e934ba3` `a8cc87e`、DECISIONS 2026-07-28参照）。本番反映確認済み。（SQL適用と残作業は上の項目に集約）
- **【要判断・3回ルール到達 その2】「状態に依存する対外文言の直書き」が×3**（LESSONS 2026-07-13 の系列）。今回は課金が有効になってもLPが「Coming soon / billing is not implemented yet」と言い続けていた件。今回の修正で `!!process.env.STRIPE_SECRET_KEY` から導出する形にしたが、**昇格提案は未実施**。/kaizen で ×3到達の「CSSが効いていない」型と合わせて棚卸しする。
- **【要判断】レビューで挙がった未着手の指摘**。優先度順に整理済み（このセッションの報告参照）: **Phase 0 は完了（上記参照）**。(Phase 2) `/admin`に通報一覧も非公開化ボタンも無い（reportsはSELECTポリシー自体が無く件数表示のみ）／芳名帳が匿名・無審査・ON/OFF不可／公開＝Explore自動掲載で限定公開が選べない／本番の`/explore`が0件。(Phase 3) i18n基盤ゼロ・sitemap/robots無し／CJKタイトルで3Dプレート・タイトル壁が溢れる。
- **【解決済・記録のみ】3D展示だけ作品が空の額縁になる不具合**（DECISIONS 2026-07-27「CDN配信のCORS」参照）。**CDNキャッシュのCORS汚染**が真因で、コード変更なし・**Cloudflare設定のみで解消**（①R2バケットCORSの読み取りを`*`に ②Transform Ruleで`Access-Control-Allow-Origin: *`を無条件付与 ③Purge Everything）。ヘッダを実測して両方`*`を確認、実ブラウザで3作品の3D描画とコンソールエラーゼロも確認済み。**同種の症状が出たら、まずブラウザではなくCDNを疑う**（`Origin`あり/なしで`curl`を叩き分ける。手順は R2_SETUP §5.1 末尾）。
- **容量制限の実測化の実機確認**（DECISIONS 2026-07-27「容量制限の実測化」参照）。コードは本番反映済み、migration **0030 も適用済み**（`reserve_storage`の存在をユーザーが`pg_proc`で確認）。**残るは実機での挙動確認** — ①作品をアップロードして`/me`の「Storage: N MB of 300 MB used」が増えるか ②作品を削除して減るか ③BGMを上げてもメーターが増えるか（旧実装では増えなかった）。※SQLはローカルにPostgresが無く未実行で、本番適用でDDLが通ったことが唯一の実行証跡。判定ロジックが実際に動くのを見るのはこれが初めてになる。
- **削除時のCDNキャッシュパージの実効確認**（DECISIONS 2026-07-27参照）。コードは本番反映済み、Cloudflareトークン(`CLOUDFLARE_ZONE_ID`/`CLOUDFLARE_PURGE_TOKEN`)もVercel設定済み。**残るは実際に効いているかのE2E確認だけ** — R2_SETUP §7.5の手順で、捨ててよいテスト作品を1枚上げる→URLをエッジに載せる(`cf-cache-status: HIT`)→削除→同じURLが404になるか。効いていなければVercelログに `cache purge rejected` が出るので、§7.5の切り分け（トークン権限 / プレフィックス指定が拒否されるならURL指定へ切替）に従う。
- **Stripe決済の本番有効化**（DECISIONS 2026-07-24参照、本番モード＋Xibit360専用の新規アカウントで確定）。Claude済: `NEXT_PUBLIC_SITE_URL=https://www.xibit360.art`設定。ユーザー待ち: ⓪同じログイン下にXibit360専用アカウントを新規作成し本番有効化 ①`0019_checkout.sql`をSupabase SQL Editor適用 ②新アカウント(live)の`STRIPE_SECRET_KEY`(sk_live_)をVercelに ③新アカウントでWebhook作成(`https://www.xibit360.art/api/stripe/webhook`・`checkout.session.completed`)→`STRIPE_WEBHOOK_SECRET`(whsec_)をVercelに ④価格USD化に伴い`0028_capacity_clamp.sql`もSQL Editorで適用。完了後Claudeが再デプロイ＋疎通検証(checkout=401/webhook=400)、実カードで**スロット追加($3〜)購入→返金**でE2E確認（USD化で¥580→$3/枚に変更済み）。

## 次にやること（再開ポイント）
- **描画品質ティアの実機QA**: low/mediumティア（スマホ）はローカルでポインタエミュ不可のため未検証。本番反映後、実スマホで①普通のスマホ=影あり(1024)・反射なし ②古い/安いスマホ=影なし＋疑似影のみ、のフレームレートと見た目を確認（DECISIONS 2026-07-23参照）
- **読み上げTTS**: 本番ENV(`OPENAI_API_KEY`/`SUPABASE_SERVICE_ROLE_KEY`)設定済み。`/api/tts`は本番で200・mp3公開再生可・キャッシュ動作を確認済み。ボイスは**shimmer確定**。残: アプリ実画面での再生体験（ツアー▶/作品ガイド）＆日本語キャプションでの発音は本番QAで最終確認。
- **Stripe本番接続の運用作業**（コードは完成・未接続）: `0019_checkout.sql`適用 → `STRIPE_SECRET_KEY`/`STRIPE_WEBHOOK_SECRET`/`SUPABASE_SERVICE_ROLE_KEY`/`NEXT_PUBLIC_SITE_URL` 設定 → Stripe CLIで実カード確認（手順は supabase/README §5）
- **画像/動画ストレージのR2移行**: **ship完了・本番反映済み（2026-07-27）**。残: ユーザーによる本番の実機QA（サインイン→作品表示・新規アップロード・動画アップロード・削除・読み上げ・容量上限）。それが通ったら **Supabase Storageの`artworks`バケットを空にする**（数日様子見してから。ロールバック用にまだ残してある）。
- docs/STRATEGY.md **§7** の残タスク:
  - P1-5 有料テーマ/レイアウト第1弾。前提(FOREVER_FREE固定化)は**解消済**。残るは実際の有料テーマ/レイアウトの制作という事業/制作判断
  - P2-8 ウォークスルー動画は**実装済**。フォローアップ: MP4/GIF変換(X/IG直投稿用。要ffmpeg.wasm/サーバ)・録画に音声を載せる
  - P2-9 企画展(特集)は**実装済**。運用: `/admin`で特集を設定するだけ。フォローアップ候補: 期間の自動切替・作家からの応募フロー
  - P2-10 記事/ガイド機能は**実装済**(migration 0020)。運用: `/admin`の「Guides」で執筆・公開するだけ。「Ktlyst参考に」の指定は同名企業が複数あり特定できず・候補サイトも403のため、Xibit360自身のデザインで実装(参考URLをもらえれば個別に寄せる)。フォローアップ候補: 記事内の作品/ギャラリー埋め込み、タグ/カテゴリ、関連記事
  - P3-12 音声ガイド=**作品ごとガイド実装済**(migration 0021)。空間BGM=**案A実装済**(作家が音源アップ→1トラックloop、migration 0027)。残フォローアップ: 案B(権利処理済みアンビエント音源のライブラリ内蔵)。※市販曲のInstagram型は包括ライセンスが要り個人規模では非現実的(2026-07-21相談で確認)
  - P3-11 グループ展(卒展向け・最高価値)は**設計判断待ち**: 「複数作家でどう共同編集するか」(招待制の共同編集 / 主催者が他人の公開作品をキュレーション)。Stripe本番にも依存
  - P3-13 AR / P3-14 立体(glTF)
- レビューで見送った低優先の既知事項:
  - 署名なし決済フローで未サインイン時に「Checkout isn't live yet」と表示（現状モーダルはサインイン文脈でしか開かず到達不可）
  - 購入完了後の再取得はギャラリー（キャパ）のみ更新。テーマ/レイアウト所有は要手動リロード（バナーで案内済み・現状テーマ購入は到達不可）
- Q2の残り: `/me`のalert/confirmは意図的にネイティブのまま（没入を壊さないため対象外）
- U3（WebGL対応環境でのキーボード/スクリーンリーダー回遊）は未対応（非WebGLの2Dフォールバックのみ）

## 注意（並行セッション・未コミット変更）
- なし

## 完了ログ（直近5件）
- 2026-07-27: **【ship済・本番反映確認】UX/UI/法務レビュー由来のモバイル6件**（5コミット `e28fd19` `a04a3d7` `9703ffd` `fa0c8b0` ＋台帳、DECISIONS 2026-07-27「CSSが書いてあるのに効いていない」参照）。①**電話幅でLPの主要CTA「Start free」が画面外に切れて押せなかった**（390pxで21px・375pxで36pxはみ出し。`overflow-x:hidden`のため横スクロールでも救えない）→ 600px以下でExplore/Guidesを畳む。②**縦画面の視野角**: threeの`fov`は縦方向の角度なので、固定60ではiPhone縦の横視野が29.9°（ノートPCは85.5°）まで潰れ、来場者が部屋の1/3しか見ていなかった。aspect 1.3未満のみ横視野一定の縦角を解き78°でクランプする`AdaptiveFov`を追加（1.3以上は60のままでデスクトップ画角は不変）。実測でiPhone縦41.0°・iPad縦62.6°。③**ローディングが「終わったふり」**: フォント+タイマーだけで約2秒後に必ず閉じ、テクスチャ/GLB/作品画像を待っていなかった（本番実機で「真っ黒な部屋＋HUD」を観測）。dreiの`useProgress`を条件に追加＋400msデバウンス＋12秒上限、バーも実測%表示に。④**作品画像の二重JPEG圧縮**: 全経路が`fileToDataUrl`(q0.85)→`dataUrlToJpegBlob`(**同サイズのまま**q0.85)と2回エンコードしており、2回目は純粋に世代劣化を足すだけだった。原本のFileを持ち回りデコード1回・エンコード1回に統一し品質0.92へ（合成イラストで実測: 原画との平均誤差0.673→0.457、サイズ75KB→84KB）。アバター/ロゴ/LP画像/動画ポスターも同じ二重圧縮だったので同時に解消。⑤**`viewport-fit=cover`が未設定**でiOSが`env(safe-area-inset-*)`を全て0に解決し、`app/gallery.css`のノッチ/ホームバー回避**14箇所が丸ごと無効**だった → cover設定＋非ギャラリーページのbodyベースラインinset＋fixed chrome（`.nav`/`.me-sticky-save`/`.me-toast`）のopt-in。⑥**`.hud-actions`/`.hud-record`はマークアップに存在しないクラス名**（実体は`.hud-cluster`。リネーム時にCSS側が取り残された）で、640px以下のHUD再配置が一度も適用されず、右下ボタン群が中央ページャーと同じ段にいた。検証: tsc/buildクリーン、320/360/375/390/768/1280pxで`getBoundingClientRect()`実測（CTAは16px以上の余白・HUDの矩形交差ゼロ）、本番で`viewport-fit=cover`のmeta配信と`/` `/explore` `/demo` `/me` `/terms` 200、本番375pxでナビ・ギャラリー描画をスクショ確認。**別視点レビューはエージェントが応答停止で失敗したため、リスク4点（fov競合・`artworks.width/height`の用途・ローディングが閉じない経路・`100vw`との干渉）をClaudeが手動で追試**。**残: ユーザー実機QA3点**（ノッチ実機の下端／遅い回線でのローディング／サインイン後のアップロード画質）。
- 2026-07-27: **【ship済・本番反映確認】3Dモデル・床テクスチャ・デモ動画もR2配信へ（egress無料化の残りピース）**（1コミット `bdf8a38`、DECISIONS 2026-07-27「3Dモデル…もR2配信に移す」参照）。※前提となる**R2移行そのもの**（Supabase Storage→R2、DNS移管、`0029`でのURL書き換え、CORS/crossOriginの退行と修正）は5件上限で完了ログから押し出された。記録は DECISIONS 2026-07-27「画像/動画のストレージを…R2へ移行」と docs/R2_SETUP.md にある。「動画作品に10万人来たらコストは増えるか」の試算中に、**作品ファイルはR2に移したのにアプリの部品（3Dモデル等）は`public/`＝Vercel配信のまま**で、そこが訪問者1人あたり最大の転送源だと判明。特に`GhostVisitors`がモジュールスコープで両モデルを`useGLTF.preload()`し`GalleryScene`が無条件描画するため、**ギャラリーを開いた全員が2.6MBを取得**していた。`models`/`textures`/`demo-works` 計7ファイル・4.1MBをR2の`static/v1/`へ移し`assetUrl()`経由に変更（10万人試算でVercelからの転送 約510GB→約100GB）。**`public/draco/`は意図的に据え置き**（デコーダを別オリジンに依存させない過去判断=LESSONS 2026-07-16を尊重）。ファイルは`public/`に残しフォールバック維持。差し替え時は`STATIC_VERSION`を上げて再アップロード（スクリプトが同じ定数をTSから読むのでズレない）。検証: tsc/build通過、CDN側で7件のバイト数一致・Content-Type・CORS実測、本番`/demo`を実ブラウザで開き`performance.getEntriesByType('resource')`で**7件すべてcdn経由・Dracoのみwww（意図どおり）**・コンソールエラー0・スクリーンショットで描画正常を確認。
- 2026-07-27: **オーナーの非公開ギャラリー下見（公開URLで本人だけ歩ける）**（DECISIONS 2026-07-27参照、コミット`4549dd0`）。`/@name`が非公開でも、サインイン中の所有者本人だけ全画面3Dで歩けるプレビューを追加（「Private preview」バナー・訪問カウント除外）。公開ページのサーバー取得は無変更＝匿名/他人/公開時は無影響。`fetchOwnExhibition()`＋`OwnerPreview.tsx`＋`VisitorGallery`の`ownerPreview`。R2並行作業と同ツリーだったため自ファイルのみ先行コミット→R2がFF統合、tsc/buildクリーン、origin同期済み。**残: オーナー実機E2E（本番QA）・`/@name/slug`側は未対応**。
- 2026-07-27: **【ship済・本番反映確認】容量制限をR2の実測値で判定するようにした**（1コミット `de90ccf`、DECISIONS 2026-07-27「容量制限の実測化」参照）。R2移行でクォータ判定はサーバーへ移っていたが、**判定の材料が`artworks.bytes`（クライアントの言い値）のまま**で、認証済みユーザーが300MB上限を無制限に超えられる経路が3本残っていた: ①作品ファイルはDB行を作る前に上げるので行を挿入しなければ記録が残らない（毎回新しいuuidで無限反復）②`bytes: 0`で挿入すれば合計は増えない ③BGM(15MB)はどこにも記録されない。使用量の正を**R2に実在するバイト数**（署名のたびに`{uid}/`をListObjectsV2で実測）に変えて3本を一括封鎖。同時実行対策の予約テーブルが migration **0030**（ユーザー適用済み）で、判定は「実測値＋自分の生きた予約」、予約の寿命(720秒)は署名URL(600秒)より長い（R2はPUT開始時にしか期限を見ないため）。cronは不要で期限切れ行は次の署名要求のついでに消える。クォータ対象は`{uid}/`配下の**全ファイル**に統一（アバター/ロゴ/LP画像も算入。除外はキー形状のパターンマッチが要り壊れやすいため）。`/me`のメーターも同じ数字を読むよう`GET /api/storage/usage`を新設。`artworks.bytes`は情報用として残置。**0030未適用でも503にせず実測のみの判定に縮退**する。検証: tsc/buildクリーン、本番で`/api/storage/usage`が404→**401**（＝デプロイ済み・認証ガード動作・R2環境変数もOK。501でないため）、`/` `/explore` `/me` `/demo` 200。**SQLはローカルにPostgresが無く未実行**で、本番適用でDDLが通ったことが唯一の実行証跡。**残: 実機での挙動確認**（アップロード/削除でメーターが増減するか、BGMでも増えるか）。※並行セッション3本が同じファイルを触っていたため、`main`へ2本を先に取り込んでからrebaseして統合（`RULES`テーブルで衝突: 音声経路の削除＋`quota:`フィールド全廃の両方を採用）。
- 2026-07-27: **【ship済・本番反映確認】作品/アカウント削除時にCloudflareのCDNキャッシュをパージ**（3コミット、DECISIONS 2026-07-27参照）。R2から実体を消してもエッジのコピーが最大4時間配信され続け、URLを知っている人はダウンロードできる状態だった（R2が全オブジェクトに`cache-control: max-age=14400`を付けて返すのが原因。本番の`cdn.xibit360.art`で200/404とも実測して確定）。`app/privacy`の「削除したらファイルも消える」に実態が追いついていなかった。**URL指定でなくプレフィックス指定のパージ**を採用 — 削除したキー一覧が不要で1回の呼び出しで作品フォルダ全体を消せ、クエリ文字列を問わずマッチするので`?v=`付きURL（アバター/ロゴ/BGM）にも届く。調査の結果、以前は上位プラン限定だったプレフィックス指定が**無料プランでも使える**ようになっていた。順序はR2から消した後（先だと隙間のアクセスで再キャッシュされる）、失敗しても削除は成功扱い（ベストエフォート）、実体0件のときは呼ばない（存在しないIDの連打でアカウント共有のパージ上限を消費されるのを防ぐ）。ゾーン権限の新規トークンをユーザーが発行し`CLOUDFLARE_ZONE_ID`/`CLOUDFLARE_PURGE_TOKEN`をVercelに設定済み（Vercel CLIで変数名を確認）。検証: tsc/build通過、本番で`/` `/explore` `/me` `/demo` 200・`/api/storage/delete` 401（501でない）。**残: パージが実際に効いているかのE2E確認**（テスト作品を上げて削除→URLが404になるか）。手順とフォールバックは R2_SETUP §7.5。
