# STATE.md — 作業状態（セッション横断）

> Claude向け運用ルール: セッション開始時にこのファイルを読んでから作業に入る。作業の節目・中断時・ship後に更新する。終わった項目は「完了ログ」へ移し、完了ログは直近5件だけ残す。

- **最終更新**: 2026-07-29（**未pushで取り残されていたコミットを全部着地させた（ユーザー指示）— `d3f972f`（決済不具合の台帳記録）は内容を現在の台帳へ移し、`dd4c59b`（{式}内リテラルの検出＋英語44文言の翻訳）は17ファイルの衝突を解消して `476d54e` として ship。未pushコミットは全ブランチでゼロになった**）。以下は前回の記録: 2026-07-29（**`schema.sql` が migrations から取り残されるのを検出する番人 `npm run check:schema` を追加（LESSONS の「番人化は未実施」を機械化）**）。以下は前回の記録: 2026-07-29（**/kaizen で昇格2件を ship（プレビュー検証のスキル化 ＋ push直前の並行セッション番人 `check:ship-ready`）**）。以下は前回の記録: 2026-07-29（**i18nの複数形フォールバックのバグを修正して ship（count=1 で「1 works」と読める文が出ていた）**）。以下は前回の記録: 2026-07-29（**統合スキーマ `supabase/schema.sql` が 0024 で止まっていた（0025〜0034 の10本が欠落）のを修復して ship。ローカルにPostgres 16を入れて実行検証できる体制になった**）。以下は前回の記録: 2026-07-29（**トップページの3Dヒーローに残っていた英語14文を辞書化し、CJKで壁から溢れる折り返しを直して ship**）。以下は前回の記録: 2026-07-29（**モバイルUIの細部6件と販売構造2件を ship（`563ed60` まで push・本番反映確認済み）。額を売る工事（C）も ship 済（`b500dd8`）＝残るは `0034_frame_purchases.sql` の適用**）。以下は前回の記録: 2026-07-28（**/kaizen で昇格2件（番人の自己テスト `check:gates` ／ `.next` 共有ガードの穴2つ）＋モバイル修正の取り残し `6bc728e` を cherry-pick で出荷（入場ガイド1行化＋レール縦リスト化）＋404ページの新設（`6a5f3c2`、別セッション）＋死んだCSS90件の掃除＝基準線を空配列に（`a15237c`）＋`check:css`をIDセレクタにも広げ空振りの`#stage`を削除（`949f8a3`、本番反映確認済み）**。以下は既存の記録: モバイル6件＋Phase 0（課金・法務）4件＋オンボーディング3件＋通報運用/芳名帳＋CJK組版＋i18n基盤とEN/JA辞書＋B15/B16/B17（タブレット幅・入力欄ズーム・タップ対象）＋**B18（srcset）**＋**UI文言の直書き検出（`npm run check:i18n`）と残っていた英語29箇所の辞書化**をship。辞書は614キーがen/jaで一致。残: support@ の受信設定、実カードE2E、ユーザーが自分の個展を1つ公開する実機QA、**多言語の網羅範囲（どの言語を入れるか）はユーザー判断待ち**、B14は他の改善完了まで待機）

## 進行中
- **【ship済・本番反映確認】複数形フォールバックのバグ修正（i18n）**（コミット `d8f53d7`）。本番で確認したのは、**配信JSに新しい鎖が載っていること**（`for(let o of new Set("one"===e?[…"_one"],t,[…"_other"]]:[…]))` を実物のチャンクから抽出。`_one` という文字列リテラルはこの変更で初めて入るので、そのまま反映の証跡になる）と、**死んだキー `addsSlots` が全チャンクから0件**になったこと、`/en` `/ja` `/ja/explore` `/ja/demo` `/en/articles` `/legal` `/terms` が200のまま。（DECISIONS 2026-07-29「複数形の単数は…」、ユーザー報告）。英語など単複を区別する言語で **count=1 のとき複数形の文が出ていた**（"1 works"）。`translate()` の鎖が `_one → _other → 素のキー` で、単数を素のキーに書いた8キーが**構造上単数に到達できなかった**。**A′**（`one` のときだけ素のキーを読み、`few`/`many` は飛ばして `_other`）に変更。つでに ①`panel.overCapacity` の呼び出しに `count` が無く複数形分岐に入っていなかったのを修正 ②死んだキー `purchase.addsSlots`（11言語×2）を削除 ③**再発防止の番人**を `check:i18n` に追加（`_other` の相方が無いキーを検出、`check:gates` に5ケース＋変異4種で確認）④`check:gates` 自身の `notContains` が一度も照合されていなかったのを修正。
  - 検証: en/de/fr の count=1・2 の実出力を `translate()` 直呼びで確認（`npx tsc lib/i18n/index.ts --outDir <tmp> --module commonjs` でコンパイルすればnodeから呼べる）、CJK+id は常に `other` で1と2が同一＝影響なしを確認。tsc / check:i18n / check:css / check:gates（22件）/ 本番ビルド通過。**ブラウザでの実画面確認はしていない**（該当文言は `/me` のサインイン下と「作品1点だけ」等の特定データでしか出ないため、辞書を直接評価する方が確実）。
  - **出荷前レビューで自分の差分から3件直した**: ①form が `other` のとき鎖の先頭2つが同じキーで、CJK+id（常に other）が毎回同じ木を2度歩いていた → `Set` で重複を落とす ②番人が全キーで行番号を計算していた → 報告するときだけ数える ③その過程で発覚、**複数行コメントを1文字に潰していたため、コメントの後ろにあるキーの行番号がずれて報告が嘘になる** → 長さと行数を保つ伏せ字にし、ケースと変異テストで固定。
  - **【解消済 2026-07-29】並行ブランチ `dd4c59b` との衝突は着地させて解消した**（別セッションが実施。ユーザー指示「未処理のコミットをすべて処理して」）。17ファイルが衝突したが、**`check-i18n.mjs` の「複数形キーの対の点検」と `check-gates.mjs` のケースは両方残っている**（`check:gates` は48件＝両側のケースが揃っている）。解消の判断はコミットメッセージに全部書いてある。要点は3つ — ①`purchase` グループは main 側（`buyAria: 'Buy {name}'` / `fewerAria` / `moreAria`）を残し、あちらの `buyAria: 'Buy {item}'` / `fewer` / `more` は重複なので落とした ②`AdminDashboard` は「main の売り物の単一の出どころ（`paidThemeIds()` 等）＋あちらの `t()` ラベル」に合成し、不足していた `adminUi.frameItem` を11言語に足した（あちら側だけを採ると額の対応が丸ごと抜ける）③参照ゼロになった `common.upload` / `common.replace` / `adminUi.slotN` を11辞書から削除。
  - **見つけたが直していない小さな穴**: 他言語混入の番人（`SCRIPT_GUARD`）は `//` 行コメントだけを除外していて**ブロックコメントの中身を見ている**ため、en.ts などに日本語の `/* */` コメントを書くと誤検知で push が止まる（en.ts は日本語の `//` コメントを多用しているので踏みうる）。今回は衝突面を増やさないため触っていない。
  - **残（別工事）**: 購入モーダルの `{unit}` 差し込みが**5言語**で壊れている — 独 `slot: 'Plätze'`・伊 `posti` が複数形の語なので単数の文が「1 Plätze」、西/葡は `{unit}s` で `lugars`（正しくは `lugares`）、韓は助詞が `걸이을`（正しくは `걸이를`）。今 `unitLabel` を渡す呼び出しは1か所だけなので、`{unit}` をやめて各言語の文に語を書くのが筋。**詳細は下の【調査済・着手待ち】の項目**（着手は `dd4c59b` の着地待ち）。

- **【調査済・着手できる状態（ユーザー判断待ち）】購入モーダルの `{unit}` 差し込みが5言語で壊れている**（上の複数形修正の作業中に見つけた別の穴。修正は分けてある。**当初「4言語」としていたが、`translate()` の実出力で ko の助詞不一致が加わって5言語になった**）。**着手条件は満たされた** — ユーザー判断（2026-07-29「2本が main に入るまで待つ」）の2本は両方 `origin/main` に着地した（`73a185f` → `d8f53d7`、`dd4c59b` → `476d54e`）。**残るはユーザーの着手指示だけ**。命名は `fewerAria` / `moreAria` / `buyAria: 'Buy {name}'` が残った（下の「衝突の中身」参照）。
  - **症状**（`lib/i18n/*.ts` の `purchase` グループ、単位の語は `slot`）:
    - `de.slot = 'Plätze'`（複数形の語）／`it.slot = 'posti'`（同）→ 単数の文が「1 Plätze mehr」「Aggiunge 1 posti」になる。de/it は `optionDesc` と `optionDesc_other` が**同一文**なので、`d8f53d7` の前後どちらでも壊れている。
    - `es` / `pt-br` は `slot = 'lugar'` で、`optionDesc_other` が `{unit}s` と**語尾にsを足す**ため **"2 lugars"**（正しくは `lugares`）。`howMany`（`¿Cuántos {unit}s?` / `Quantos {unit}s?`）と **`fewerAria` / `moreAria`（`Menos lugars` / `Mais lugars`）も同じく壊れている**（当初の指摘に無かった追加分）。※`d8f53d7` で単数側は素のキーに到達するようになったので count=1 は直ったが、**count=2 は今も "lugars"**。
    - 語尾sで正しく複数になるのは **en / fr だけ**（`slot`/`slots`、`emplacement`/`emplacements`）。
    - **`ko` は単複ではなく助詞が壊れている**（`translate()` の実出力で判明、当初の指摘に無かった5つ目の言語）: `이 전시장에 걸이을 1개…` / `걸이을 몇 개 추가할까요?` — `걸이` は母音で終わるので目的格助詞は **`를`**（`걸이를`）が正しく、`을` は子音終わりの語に付く形。**`{unit}` に名詞を差し込む作りは韓国語の助詞の一致を壊すので、単数・複数を書き分けても直らない**（文に語を直接書く形にして初めて直る）。同種の一致は `은/는`・`이/가`・`와/과` にもある。
    - `ja` `zh-hans` `zh-hant` は同じ語を両方で使うので問題なし。`id` も単複同形で問題ないが、`fewerAria` / `moreAria` が `{unit}` 始まりのため **`tempat lebih sedikit` と小文字で始まる**（読み上げラベルとしては軽微。文に直接書くとき大文字で始める）。
  - **やること（筋）**: `{unit}` の差し込みをやめ、**各言語の文に単位の語を直接書く**（`optionDesc` / `optionDesc_other` を言語ごとに自然な単数・複数で書く）。`{unit}` は単位が複数種類ある想定の作りだが、**`unitLabel` を渡している呼び出しは `app/me/page.tsx:1739` の `t('purchase.slot')` の1か所だけ**＝単位は今1種類しかない。将来ほんとうに2種類目が来たら単位ごとにキーを分ける。関係するキー: `purchase.optionDesc` / `optionDesc_other` / `howMany` / `fewerAria` / `moreAria` / `slot`。
  - **各言語の正しい語形（調査済）**: en `slot`/`slots`・fr `emplacement`/`emplacements`・de `Platz`/`Plätze`・it `posto`/`posti`・es `lugar`/`lugares`・pt-BR `lugar`/`lugares`・id `tempat`（単複同形）・ja `枠`・ko `걸이`（**助詞は `를`／`는`／`가` を選ぶ側＝母音終わり**）・zh-Hans/zh-Hant `作品位`。
  - **実出力の証跡**: 11言語 × count=1・2 ＋ `howMany` / `fewerAria` / `moreAria` を `translate()` 直呼びで出力して上記を確定した（`npx tsc lib/i18n/index.ts --outDir <tmp> …` でコンパイルし、`getDictionary(loc)` と `translate()` を node から呼ぶ。`DICTS` という名前のexportは無いので `getDictionary` を使う）。
  - **【解消済】衝突の中身と、残った命名**: `dd4c59b` は `purchase` グループの同じ場所を書き換えていた（`buyAria` を `'Buy {item}'` で追加＝main の `'Buy {name}'` と**同じキーで差し込み名が違う**／`purchase.fewer` / `more` が main の `fewerAria` / `moreAria` と役割重複／`PurchaseModal.tsx` の該当行も差し替え済み）。着地時に **main 側を残した**ので、**触るキーは `optionDesc` / `optionDesc_other` / `howMany` / `fewerAria` / `moreAria` / `slot` で確定**（`fewer` / `more` は存在しない）。`buyAria` は `{name}` を受ける。
  - 検証手順（着手時）: `npx tsc --noEmit` / `check:i18n` / `check:css` / `check:gates` / `npm run build` ＋ `translate()` を直接呼ぶスクリプトで count=1 と 2 の実出力を11言語ぶん出す（`npx tsc lib/i18n/index.ts --outDir <tmp> --module commonjs --target es2022 --moduleResolution node --skipLibCheck` でコンパイルすれば node から呼べる）。
- **【ship済】統合スキーマ `supabase/schema.sql` の修復（0025〜0034 の欠落）**（コミット `32739ce`）。**本番は無関係・影響なし**（migrationは個別に適用済みで、壊れていたのは「1ファイル貼り付けで新しい環境を作る」経路だけ。この経路が最後に使われたのは 0022 の頃で、以後1回も試されていなかった）。0025〜0034 を番号順に追記し、README §1 の一覧も 0034 まで揃えた。**後の番号が前を上書きする4箇所**を schema.sql 冒頭に一覧化: `purchases_kind_check` 0019→0034 / `record_capacity_purchase` 0019→0028→0031 / `grant_entitlement` 0022→0034 / **`guestbook_insert_public` 0008→0033**（4つ目は指示に無かったもの。ポリシー名が違っていたら、Postgres は permissive な INSERT ポリシーを OR で評価するので「芳名帳をOFFにしても書き込める」状態になっていた）。
  - **検証（ローカルのPostgres 16.14で実行）**: 空DBへ全文実行→エラーゼロ、2回目・3回目も冪等。**`schema.sql` で作ったDBと `migrations/` を0001から個別適用したDBのスキーマが完全一致**（`pg_dump --schema-only` の差分は pg_dump が毎回変えるトークンのみ）＝「番号順に並べれば最終状態が正しい」を実測で確認。行が入ったDBへの再実行でデータ消失なし。未知の `kind` / `status` が実際に拒否されることも確認。**範囲外**: 実際のJWTでのRLS挙動とStorageの実挙動（Supabase固有の前提は最小限スタブした素のPostgresで検証したため）。
  - **この経路のズレを検出する番人を実装した → `npm run check:schema`**（`scripts/check-schema-sync.mjs`、AGENTS.md 5.8。別セッションで実施）。migration を足したら同じコミットで `schema.sql` と README §1 にも足すこと自体は変わらないが、忘れても push 前に機械が止める。
- **【残1件・実機QA】3Dヒーローの多言語化**（本体は ship 済・完了ログ参照、`d63a798`）。**`/admin` のLPヒーロー枠のラベル（中央/左/右 ＋ アップロード/差し替え/保存）だけローカルでサインインできず実画面未確認**。キーが11言語すべてに存在することと、本番の配信HTMLに載っていることは確認済み。
  - **【要判断】「番人の視界の外にある表示文字列」が ×2**（モジュール直下の定数配列＝このセッション／`lib/pricing.ts` の購入モーダル＝別セッション、同日）。`check:i18n` の検出範囲を広げるかは**訳す対象でない定数の誤検知が読めない**ため未着手。次に同型が出たら3回ルールで昇格を検討する。
- **【ship済・本番反映確認】決済に進めない不具合（ユーザー報告 2026-07-29）**（DECISIONS / LESSONS 2026-07-29。コードは別セッションのコミット `9d02ab5` に同梱される形で本番に出ている＝件名は `docs:` なので `git log` では追えない）。報告は「本番のモバイルで作品スロットを複数まとめて買おうとしたら決済に進めなかった」。
  - **真因は文言の畳み込み**。`startCheckout` が「販売がまだ始まっていない」と「ログインが切れた」を同じ `unavailable` に畳んでいたため、**セッションが切れた人に「この機能はまだ存在しません」と表示**していた。決済リクエストはサーバーに届いてもいない（本番 `/api/checkout` は無認証POSTに401を返す＝環境変数は揃っている）。
  - **「複数まとめて」は原因ではなかった**（数量1→5・金額計算・上限表示・サーバー側クランプすべて実機相当で正常）。ユーザーの推測と違ったことは報告済み。
  - 直したもの: ①`signed-out` を別の戻り値にし、`purchase.signedOut` ＋ `/signin` 導線を出す（`getSession()` の `error` をログに残し、`refreshSession()` を一度試す。サーバー401も同じ扱い）②**同意チェック未チェックで購入ボタンが完全に無反応だった**のを、押せるまま `purchase.needsConsent` で理由を出す形に（`disabled` は処理中だけ）。新規2キー×11言語。
  - 検証: tsc / check:i18n（10言語100%・660キー）/ check:css / check:gates / クリーンbuild。**本番ビルドに対して**ダミー資格情報でセッション無しを再現し、2つの状態を実機相当（375×812）で2回ずつ実測。
  - **本番反映の確認（2026-07-29）**: 配信JSに `ログインの有効期限が切れました。もう一度サインインすると、この購入を続けられます。` が載っていること、`purchase.signedOut` / `purchase.needsConsent` が11辞書すべてにあること、`lib/checkout.ts` が `signed-out` を独立した戻り値として返すことを実測。
  - **残**: ①**実カードでのE2E**（サインインとStripeキーが要るのでローカル不可）②実機QAに1行追加: **購入モーダルを操作中に勝手にトップページへ戻らないか**（検証中にプレビューのタブが3回サイト直下へ飛んだ。クリックイベントは常に正しい要素で `location.*` も `beforeunload` も発火せず、本番ビルドでは再現しなかったため**プレビュー側の挙動が濃厚**だが、アプリ側の原因を特定できていないので残す）。
- **【ship済・本番反映確認】販売構造の作り直し 2件**（DECISIONS 2026-07-29、コミット `d1f4f4e` `f073347`。**別セッションの作業を、ユーザーの指示でモバイルUI修正と一緒に push した**）。本番で価格表示が1セントも変わっていないことを確認済み（トップ ja/en とも「1つ $3 / $8 / $5」、特商法ページも同額）。発端はユーザーの「テーマやフレームの種類を追加できる構造か、価格も別に設定できる構造か確認してほしい」。
  - **①1点ごとの価格（A）**: `lib/pricing.ts` の `ITEM_PRICE_CENTS` に id を書いた分だけその価格。**表は空で出荷したので現行価格は1セントも変わらない**。対外の価格表示（トップの料金カード・特商法）は表から導出し、1点でも違えば「1つ $8〜$12」に自動で変わる。「何が売り物か」は `paidThemeIds()` / `paidLayoutIds()` に集約（checkout・/admin の付与・webhook・価格範囲が同じ答えを見る）。checkout は売っていない id を400で拒否。
  - **②カスタム間取りを未公開に（B）**: `CUSTOM_LAYOUT_RELEASED = false`。有料扱いなのにロックが抜けていて無料で配られていた。既に `layout='custom'` の部屋はチップを残す（他へ切り替えると片道で消えるが `layout_params` は残る）。
  - **③額（フレーム）を売れる構造に（C）＝ship済・本番反映確認（`b500dd8`）**（DECISIONS 2026-07-29）。本番で確認したのは、価格表示が1セントも変わっていないこと（トップ ja「1つ $3 / $8 / $5」・額の行は出ない、特商法の価格欄も同じ3項目で額の1文なし）と、購入モーダルが日本語で出ること（「新しいテーマ / Chic のみ $8 / このテーマだけを使えるようにします。…」）。カスタム間取りのチップが本番の間取り一覧から消えていることも確認。ユーザー判断は「今あるものは全部無料のまま、今後追加する額を有料に」＋基本価格 $3。既存5種は `FOREVER_FREE_FRAME_IDS` に固定（遡ってロックしない）、`FRAMES` に足した額は自動で有料・鍵付き・購入可能。**パラメトリック額（素材×色×太さ）は無料のままだが、`makeFrameKey` が有料idに畳まないようにした**（無料パネルで有料の額を手に入れられる穴）。対外の価格表示（トップ・特商法）は売り物があるときだけ額の行を出す。
    - **⚠ ユーザー作業: `supabase/migrations/0034_frame_purchases.sql` を Supabase SQL Editor で適用**（`purchases.kind` に 'frame' を追加＋`grant_entitlement` の許可リスト更新）。**未適用のままでは額は売れない**（決済は通っても台帳に書けない）。他の機能には影響しない。
    - **有料の額は「素材×色×太さでは出せないもの」でないと意味がない**（見た目だけなら無料パネルで再現できる）。新しい仕上げ・断面はコード側（`Room.tsx` / `Exhibit`）の作業になる。
  - **副産物として修理: 購入モーダルの文言が11言語すべて英語だった**（`lib/pricing.ts` が英文を組み立てており、`check:i18n` は `lib/` を見ないので0件のまま通っていた）。11キー×10言語を辞書に入れ、モーダルは `t()` からだけ組む形に。既に辞書にあった `purchase.oneTimeSlots` を使わず同じ英文をpropsに直書きしていた箇所も直した（LESSONS 2026-07-29）。
- **【ship済・実機QA待ち】モバイルUIの細部6件**（ユーザー指摘 2026-07-29、コミット `656dc0b`）。①ボタンの文字が上に寄る（`.btn-line` を着せた `<a>`/`<label>` はUAの縦中央寄せが効かない）②作品パネルの「›」が閉じるの矩形にほぼ丸ごと内包されタップを奪われていた ③`/me`・`/admin` 上部の行き先を640px以下でハンバーガーに畳んだ（DECISIONS 2026-07-29）④照明行だけ見出しと選択肢が同じ行だったのをダッシュボード内で統一 ⑤音源アップロードの見出し〜ボタンの隙間 29px→10px ⑥（指摘外の発見）フッターが375pxで縦1文字組み・高さ148pxになっていた。**ユーザーの実機で見てほしいのは `/me` と `/admin`**（ローカルはサインインできず、幾何は合成DOMで実測・ハンバーガーの開閉挙動だけ公開ページに一時設置して実操作で確認した）。本番のCSSに4つの新ルールが載っていること・`/me` のHTMLに `me-top-burger` があることは確認済み。
  - **ship時に踏んだこと（LESSONS「検証・ツール」に記録）**: push直前の `tsc` が3件のエラーで落ちたが、原因は**別セッションがこの作業ツリーで額販売を実装中（`lib/entitlements.ts` が未コミット）**だったこと。コミット済みのHEADを一時worktreeに取り出し（`node_modules` は1階層ぶんのシンボリックリンクで共有）、そこで tsc / 番人3種 / 本番ビルドを通してから push した。

- **【ship済】多言語SEOと10言語の翻訳**（DECISIONS 2026-07-28 / 07-29）。11言語のサブディレクトリURL（`/ja/explore`）、middlewareでの書き換えと307/308、hreflang 12本＋canonical、言語別sitemap、`LocaleLink`、URL遷移する言語切替。
  - **カバレッジ: 10言語すべて 100%（631/631）**。分母から外しているのは、訳さないと決めた `legal.*`（特商法は日本語版が正）・`footer.legal`（導線は ja だけ）・`notFound.code`（"404"）で、`scripts/check-i18n.mjs` の `NOT_TRANSLATED` に理由付きで載せてある。
  - **英語のまま残しているのは意図的なもののみ**: `metadata` のページタイトル（URLに言語を入れる形にした今は言語別にできるが、`<title>` を訳すと検索結果の見え方が変わるので別途判断が必要）、テーマ名（`Chic` `White Cube` `Noir`）とテンプレート名、`/terms` `/privacy`（英語版が正）。
  - **`metadata` も言語別にした**（DECISIONS 2026-07-29）。言語別URLを持つ4ページの `title` / `description` / `openGraph` を `seo` グループから出す。単一URLのページは英語のまま。
  - **アラビア語（RTL）はやらない**（ユーザー判断 2026-07-29）。物理CSSプロパティの転換と `textures.ts` の文字方向対応が要る別工事で、必要になったら `LOCALE_META.dir` がフックとして残っている。

- **【待機・他の改善完了後】B14（多言語の残り）**（DECISIONS 2026-07-28）。他の改善が終わるまで着手しない。着手前に「どの言語を網羅するか」を決める（**この判断がユーザー待ち**）。
- **【ship済・残は実機QA】モバイル6件の本番QA**（下の完了ログ参照）。Claudeが本番で実測できたところまでは確認済み。**ユーザーの実機でしか確認できないものが3つ残っている** — ①ノッチ付きiPhoneでHUD/ボトムシートの下端がホームバーに食い込まないか（プレビューは`env(safe-area-inset-*)`が常に0で再現不可）②遅い回線でローディングが最後まで閉じずに待つか（ローカル・本番ともキャッシュ済みで再現不可）③サインインして作品をアップロードし、画質が上がっているか＋壊れていないか。
- **【残1件】Phase 0 のユーザー作業**: `support@xibit360.art` の受信設定（Cloudflare Email Routing）。法務3ページすべてに連絡先として記載済みで、届かないとGDPRの権利行使も特商法の開示請求も宙に浮く。**SQL 3本（0019/0028/0031）は2026-07-28に適用完了・検証済み** — `purchases.currency` 存在、`record_capacity_purchase` は6引数版のみ（security definer / owner postgres）、`service_role`=実行可・`anon`=実行不可を `has_function_privilege` で確認。**課金は技術的に完成**（残るは実カードでのE2E）。
- **【ship済・実機QA待ち】オンボーディングの詰まり3件**（コミット `156c275`）。①ダッシュボードから3Dエディタへの導線が**1本も無かった**（`/me` 内の `/demo` は `router.push` のみ）→ サブナビに「Walk the room」追加 ②その存在しない場所を指していた案内2箇所を修正 ③username が隠しゲート（未設定だと公開トグルの行ごと消え、理由がどこにも無かった）→ トグルの位置に理由と入力欄を移動。**`/me` はサインインが要るためローカルで通し検証できていない**（CSSのみ実測）。
- **【ship済・ユーザー作業待ち】Phase 0（課金と法務）4件**（4コミット `74a065e` `88a5a3f` `e934ba3` `a8cc87e`、DECISIONS 2026-07-28参照）。本番反映確認済み。（SQL適用と残作業は上の項目に集約）
- **【ship済・本番検証済み】Phase 1〜2の先行4件**（`4d62bbe` `d5f05da` `430e6e8` ＋DOM取得方式への修正）。①来場者HUDを静止0.55／歩行中0.16／接触1.0に（実機で画面の33%を占めていた）②長辺800pxの `card.jpg` を追加し、カード=card・小タイル=thumb・3D=display に整理（実測 display 105KB → card 25KB）③WebGLコンテキスト消失からの復帰（本番で消失→オーバーレイ→再構築→描画まで通しで確認）④`robots.txt` / `sitemap.xml` を新設（本番のsitemapに作家ページと展示が入ることを確認）。**migration 0031・0032 は適用済み**。
- **【要判断】レビューで挙がった未着手の指摘**。優先度順に整理済み（このセッションの報告参照）: **Phase 0 は完了（上記参照）**。(Phase 2) `/admin`に通報一覧も非公開化ボタンも無い（reportsはSELECTポリシー自体が無く件数表示のみ）／芳名帳が匿名・無審査・ON/OFF不可／公開＝Explore自動掲載で限定公開が選べない／本番の`/explore`が0件。(Phase 3) i18n基盤ゼロ・sitemap/robots無し／CJKタイトルで3Dプレート・タイトル壁が溢れる。
- **【解決済・記録のみ】3D展示だけ作品が空の額縁になる不具合**（DECISIONS 2026-07-27「CDN配信のCORS」参照）。**CDNキャッシュのCORS汚染**が真因で、コード変更なし・**Cloudflare設定のみで解消**（①R2バケットCORSの読み取りを`*`に ②Transform Ruleで`Access-Control-Allow-Origin: *`を無条件付与 ③Purge Everything）。ヘッダを実測して両方`*`を確認、実ブラウザで3作品の3D描画とコンソールエラーゼロも確認済み。**同種の症状が出たら、まずブラウザではなくCDNを疑う**（`Origin`あり/なしで`curl`を叩き分ける。手順は R2_SETUP §5.1 末尾）。
- **容量制限の実測化の実機確認**（DECISIONS 2026-07-27「容量制限の実測化」参照）。コードは本番反映済み、migration **0030 も適用済み**（`reserve_storage`の存在をユーザーが`pg_proc`で確認）。**残るは実機での挙動確認** — ①作品をアップロードして`/me`の「Storage: N MB of 300 MB used」が増えるか ②作品を削除して減るか ③BGMを上げてもメーターが増えるか（旧実装では増えなかった）。※**0030のDDLは 2026-07-29 にローカルのPostgres 16でも実行確認済み**（`storage_reservations` と `reserve_storage` が作られること・冪等であること）。ただし**判定ロジックが実際に効くところ（同時実行での予約・実測値との合算）はまだ誰も見ていない**ので、上の実機確認が初回になる。
- **削除時のCDNキャッシュパージの実効確認**（DECISIONS 2026-07-27参照）。コードは本番反映済み、Cloudflareトークン(`CLOUDFLARE_ZONE_ID`/`CLOUDFLARE_PURGE_TOKEN`)もVercel設定済み。**残るは実際に効いているかのE2E確認だけ** — R2_SETUP §7.5の手順で、捨ててよいテスト作品を1枚上げる→URLをエッジに載せる(`cf-cache-status: HIT`)→削除→同じURLが404になるか。効いていなければVercelログに `cache purge rejected` が出るので、§7.5の切り分け（トークン権限 / プレフィックス指定が拒否されるならURL指定へ切替）に従う。
- **Stripe決済の本番有効化**（DECISIONS 2026-07-24参照、本番モード＋Xibit360専用の新規アカウントで確定）。Claude済: `NEXT_PUBLIC_SITE_URL=https://www.xibit360.art`設定。ユーザー待ち: ⓪同じログイン下にXibit360専用アカウントを新規作成し本番有効化 ①`0019_checkout.sql`をSupabase SQL Editor適用 ②新アカウント(live)の`STRIPE_SECRET_KEY`(sk_live_)をVercelに ③新アカウントでWebhook作成(`https://www.xibit360.art/api/stripe/webhook`・`checkout.session.completed`)→`STRIPE_WEBHOOK_SECRET`(whsec_)をVercelに ④価格USD化に伴い`0028_capacity_clamp.sql`もSQL Editorで適用。完了後Claudeが再デプロイ＋疎通検証(checkout=401/webhook=400)、実カードで**スロット追加($3〜)購入→返金**でE2E確認（USD化で¥580→$3/枚に変更済み）。

## 次にやること（再開ポイント）
- **描画品質ティアの実機QA**: low/mediumティア（スマホ）はローカルでポインタエミュ不可のため未検証。本番反映後、実スマホで①普通のスマホ=影あり(1024)・反射なし ②古い/安いスマホ=影なし＋疑似影のみ、のフレームレートと見た目を確認（DECISIONS 2026-07-23参照）
- **読み上げTTS**: 本番ENV(`OPENAI_API_KEY`/`SUPABASE_SERVICE_ROLE_KEY`)設定済み。`/api/tts`は本番で200・mp3公開再生可・キャッシュ動作を確認済み。ボイスは**shimmer確定**。残: アプリ実画面での再生体験（ツアー▶/作品ガイド）＆日本語キャプションでの発音は本番QAで最終確認。
- **Stripe本番接続の運用作業**（コードは完成・未接続）: `0019_checkout.sql`適用 → `STRIPE_SECRET_KEY`/`STRIPE_WEBHOOK_SECRET`/`SUPABASE_SERVICE_ROLE_KEY`/`NEXT_PUBLIC_SITE_URL` 設定 → Stripe CLIで実カード確認（手順は supabase/README §5）
- **画像/動画ストレージのR2移行**: **ship完了・本番反映済み（2026-07-27）**。残: ユーザーによる本番の実機QA（サインイン→作品表示・新規アップロード・動画アップロード・削除・読み上げ・容量上限）。それが通ったら **Supabase Storageの`artworks`バケットを空にする**（数日様子見してから。ロールバック用にまだ残してある）。
- docs/STRATEGY.md **§7** の残タスク:
  - P1-5 有料テーマ/レイアウト第1弾。前提(FOREVER_FREE固定化)は**解消済**。残るは実際の有料テーマ/レイアウトの制作という事業/制作判断
  - P2-8 ウォークスルー動画は**実装済**。フォローアップ: MP4/GIF変換(X/IG直投稿用。要ffmpeg.wasm/サーバ)・録画に音声を載せる
  - P2-9 企画展(特集)は**実装済**。運用: `/admin`で特集を設定するだけ。フォローアップ候補: 期間の自動切替・作家からの応募フロー
  - P2-10 記事/ガイド機能は**実装済**(migration 0020)。運用: `/admin`の「Guides」で執筆・公開するだけ。「Ktlyst参考に」の指定は同名企業が複数あり特定できず・候補サイトも403のため、Xibit360自身のデザインで実装(参考URLをもらえれば個別に寄せる)。フォローアップ候補: 記事内の作品/ギャラリー埋め込み、タグ/カテゴリ、関連記事
  - P3-12 音声ガイド=**作品ごとガイド実装済**(migration 0021)。空間BGM=**案A実装済**(作家が音源アップ→1トラックloop、migration 0027)。残フォローアップ: 案B(権利処理済みアンビエント音源のライブラリ内蔵)。※市販曲のInstagram型は包括ライセンスが要り個人規模では非現実的(2026-07-21相談で確認)
  - P3-11 グループ展(卒展向け・最高価値)は**設計判断待ち**: 「複数作家でどう共同編集するか」(招待制の共同編集 / 主催者が他人の公開作品をキュレーション)。Stripe本番にも依存
  - P3-13 AR / P3-14 立体(glTF)
- レビューで見送った低優先の既知事項:
  - 署名なし決済フローで未サインイン時に「Checkout isn't live yet」と表示（現状モーダルはサインイン文脈でしか開かず到達不可）
  - 購入完了後の再取得はギャラリー（キャパ）のみ更新。テーマ/レイアウト所有は要手動リロード（バナーで案内済み・現状テーマ購入は到達不可）
- Q2の残り: `/me`のalert/confirmは意図的にネイティブのまま（没入を壊さないため対象外）
- U3（WebGL対応環境でのキーボード/スクリーンリーダー回遊）は未対応（非WebGLの2Dフォールバックのみ）

## 注意（並行セッション・未コミット変更）
- **SQLはローカルで実行検証できる**（2026-07-29に `brew install postgresql@16` 済み。`/opt/homebrew/opt/postgresql@16/bin`）。migration や RPC を書いたら本番に貼る前にローカルで通すこと。起動の作法とハマりどころ（ソケットパス103バイト上限・`LC_ALL`必須・ロールはクラスタ単位）は LESSONS「検証・ツール」2026-07-29 に記録。**Supabase固有の前提は `auth.users` / `auth.uid()` / `storage.buckets` / `storage.objects` / `storage.foldername()` ＋ anon・authenticated・service_role の3ロールをスタブすれば足りる**。
- **2026-07-29 時点で作業ツリーが6つ動いている**（`git worktree list` で確認）。`origin/main` は1日に何度も進むので、**セッション開始時とpush直前の両方で `git fetch` する**（この日は1回の ship の最中に2回動き、rebase 3回になった。LESSONS「検証・ツール」×2）。
- **【2026-07-29 時点】未pushのコミットは全ブランチでゼロ**（ユーザー指示で棚卸し・全部着地させた）。`d3f972f`（台帳）は内容を現在の台帳へ移して `main` を `origin/main` へ早送り、`dd4c59b`（コード）は rebase して `476d54e` として着地。**棚卸しは `npm run check:ship-ready` が push 直前に自動でやってくれる**（他の作業ツリーの未pushコミットと未コミット変更を報告する）。手で全ブランチを見たいときは `for b in $(git for-each-ref --format='%(refname:short)' refs/heads/); do git rev-list --count origin/main..$b; done`。
- **【解消済 2026-07-29】メインチェックアウトの未pushコミット `d3f972f`（決済不具合の台帳記録）は取り込んだ**。ユーザー指示（「未処理のコミットをすべて処理して」）で、台帳3ファイルの追加分だけを現在の台帳に手で移した（`origin/main` 側が6コミットぶん進んでいて cherry-pick が3ファイルとも衝突したため）。`main` ブランチは `origin/main` へ早送りしてある。**履歴の書き換え（`9d02ab5` からコードを分離し直す案）は実施していない** — 既に本番に出ているコミットなので、書き換えると他の作業ツリーの土台が壊れる。件名と中身の食い違いは下の「コミットの件名を信用しない」に残す。
- **コミットの件名を信用しない**: `9d02ab5` は件名が `docs:` なのに `components/PurchaseModal.tsx` +45行・`lib/checkout.ts` +35行・`app/gallery.css` +10行を含んでいた。**rebase したら `git show --stat <相手のコミット>` で実際の変更ファイルを見てから検証範囲を決める**。

## 完了ログ（直近5件）
- 2026-07-29: **`schema.sql` の取り残しを検出する番人 `npm run check:schema` を追加**（AGENTS.md 5.8 / LESSONS「検証・ツール」2026-07-29）。直前のセッションが `schema.sql` の欠落10本を修復し「番人化は未実施」と残した宿題を機械化した。`scripts/check-schema-sync.mjs` が migration 1本ごとに ①`-- # NNNN_*.sql` の節があるか ②**コメントを除いたコード行が自分の節より後ろに順序どおり全部あるか**（部分列一致）③README §1 の**箇条書き**に載っているか を静的照合する（SQLは実行しないのでPostgres不要）。おまけに schema.sql 前文の統合範囲と、migration が消えた取り残しヘッダも見る。`check:gates` に13ケース追加（**計39件**）。**変異テスト13種すべてで落ちることを確認**し、その過程で穴を1つ実際に見つけて塞いだ — 「本文は自分のヘッダより後ろ」という下限を外しても緑のままだった（migration どうしは `);` や grant 行を共有するので、下限が無いと別の節の行で新しい節が満たされる）。また**番人を入れた自分の作業で番人を空振りさせた** — README §1 の地の文に「下の 0001〜0034 が」と書くとそれが全34本をカバーし照合が無効化された（→ 箇条書き限定に修正）。**この番人を、別セッションが修復済みの `schema.sql`（Postgres 16.14 で実行検証済み）に当てて 0件＝緑**なので、番人と実測の答えが一致している。検証: check:schema / check:gates 39件 / check:i18n / check:css / check:ship-ready / tsc / 本番ビルド。**ship 中に base が8コミット進んでいた**（`check:ship-ready` が検出）ため、重複していた自分の schema.sql 同期コミットは捨てて番人だけを乗せ直した。
- 2026-07-29: **【/kaizen 昇格2件・ship済】プレビュー検証をスキル化し、push直前の並行セッション番人を機械化した**（`8866595`、DECISIONS 2026-07-29「/kaizen 昇格2件」）。未昇格のまま ×6 に達したパターン2つをユーザー判断で昇格。①**`.claude/skills/preview-verify`**（＋AGENTS.md 5.6、`~/.claude/skills/` にも同一内容で複製）— 「検証ツールに嘘をつかれて実装のバグだと誤診する」が6回、1回は誤診に基づいて別のバグを作った。原則は「**測った値が理屈と食い違ったら、まず測り方を疑う**」。信用度は スクショ ＞ `getBoundingClientRect` ＞ `getComputedStyle`。**真っ黒スクショの真因も判明** — 撮影側ではなく、ペインが前面に無いと `document.hidden` が真になり3Dが意図的に描画を止めている（`canvas.width` が 300x150 のままなのが証跡）。②**`npm run check:ship-ready`**（＋AGENTS.md 5.7、`scripts/check-ship-ready.mjs`）— base が HEAD の祖先でないときと作業ツリーが汚れているときだけ止め、他の作業ツリーの未pushは警告に留める（他人のものだが、知らせないと本番に出ないまま消える）。`check:gates` に4ケース追加（**相手の22件と合わせて26件**）・変異テスト済み。**初日から効いた** — 導入時に4セッション分の取り残し（他セッションの実バグ修正を含む）を検出し、この ship の直前に2回止めた。**この作業中に `origin/main` が2回動き rebase 2回**。**衝突解消で相手の作業項目2件を一度消した**（正規表現が複数の衝突ブロックを巻き込んだ）— `git show :2:` / `:3:` の集合比較で気づいて復元、LESSONS に ×4 として記録。
- 2026-07-29: **【ship済・本番反映確認】トップの3Dヒーローを多言語化し、CJKで壁から溢れる折り返しを直した**（`d63a798`、DECISIONS 2026-07-29）。**「10言語100%・直書き0件」で通していたのに、最も人目に触れるトップページに英語が14文残っていた** — 壁パネル6枚の見出しと本文、廊下のHUD 3文とラベルが**モジュール直下の定数配列**にあり、`check:i18n` はJSXのテキストしか見ないので構文として視界の外だった（同日、別セッションが `lib/pricing.ts` の購入モーダルで同じ穴を独立に踏んでいる＝×2）。壁は Features セクションと同じ `lp.f1〜f6` を共有させ（訳を2組持つと壁と一覧がずれる）、新設はHUDの4キーと管理画面の6キーだけ。あわせて **canvasの折り返しを文字体系ごとに変えた** — ラテンとハングルは空白、かな・漢字・全角記号は1文字ずつ、行頭禁則つき、入り切らなければ38→26pxへ段階的に縮小。`lib/siteConfig.ts` の枠ラベルは辞書のキーに置き換えた（`lib/` は直書き検出の対象外なので、置いた文字列は静かに残る）。**検証は焼いた canvas を画面外のDOMに逃がして実測**（`javascript_tool` はページのグローバルを見られない）— ja/zh-Hans/ko/de/en の5言語×6枚で全行が幅770px以内・インクが板の内側・行頭に句読点なし、英語は折り返し位置が従来と同一。本番の配信HTMLで日本語のHUDキーと英語のem dashを確認。**別視点レビューが自分では気づけない確定バグを1件出した** — 「1語が幅を超えたら文字で割る」保険が行の途中では一度も通らず、ドイツ語の複合語なら1581px（板は900px）になった。現在の訳文では発動しないので**私の実測は全部グリーンだった**（LESSONS「保険として書いたコード」）。**この ship 中に `origin/main` が2回動き、rebase 3回・検証3回やり直した**。件名が `docs:` のコミットが決済まわりのコード90行を含んでいたので、rebase 後は毎回 `git show --stat` で中身を見た。**残: `/admin` の枠ラベルはローカルでサインインできず実画面未確認。**
- 2026-07-28: **【/kaizen 昇格2件】番人の自己テスト（`npm run check:gates`）と `.next` 共有ガードの穴2つを機械で塞いだ**（AGENTS.md 5.4 / 5.5、DECISIONS「現在の前提・絶対ルール」）。①**「検出器が自分の検査範囲を取りこぼして0件が嘘になる」が×4**に達したので機械化した（pathspec `**` で `app/page.tsx` が漏れる／`accept="image/*"` をコメント開始と誤認して270行抜ける／未追跡ファイルを見ない／`check:css` がIDセレクタとカンマ列挙の非最終行を見ない）。`scripts/check-gates.mjs` が一時gitリポジトリに合成入力を書いて**本番の番人をそのまま**走らせ、落ちるべき入力が落ちること・生きているものを誤検知しないことを10ケースで検査する。未追跡ファイルは番人自身が落ちるようにした（`scripts/untracked-guard.mjs`）。**空振りでない証明として変異テスト済み** — IDループを消すと2件、カンマ持ち越しを消すと2件落ちる。②`.next` 共有ガードの穴2つを封鎖: `prebuild` を **cwd判定**にして別worktreeのdevでは止まらないようにし（毎回ポートを差し替える手作業が不要に）、`predev`（`scripts/check-stale-next.mjs`）で **build直後にdevを起動したときの古いCSS配信**を防いだ（`.next/BUILD_ID` があれば消してから起動）。両方向を実測で確認。**ship済（`ea2743a` まで）・本番ビルド成功を確認**。さらに /code-review で**自己テスト自身の確定バグ5件**が出て修正した — 最重要は「10件すべて通過と言いながら、番人の検出ルールを消しても緑のまま」（ケースを過去の穴だけに書き、ルール本体を守っていなかった）。ルールごとに5ケース足して15件にし、**変異7種すべてで落ちること**を確認した。ほか、`prebuild` の cwd 前方一致が逆向きの誤爆（worktree はメインの内側にあるため）／パスに空白があると壊れる／Ctrl-Cで一時リポジトリが残る／1ポートに2回 lsof を叩く、を修正。
- 2026-07-28: **【ship済・本番反映確認】入場ガイドを1行に・ダッシュボードのレールを縦リストと空き枠に**（1コミット、`6bc728e` を `origin/main` の上に cherry-pick して `077a155` として出荷）。**このコミットは別セッションがローカルの `main` にコミットしたまま push し忘れていたもの**で、その間に `origin/main` が別系統（CSS掃除→404→IDセレクタ対応）で3回進み、履歴が分岐していた。ユーザーの指示で本セッションが乗せ替えた。内容: ①触る端末の入場ガイドを3行→1行（375x667で画面下1/3を占めていた）②モバイルのレールを横スクロールのタブ列から縦リスト＋既定たたみへ（作品が増えると「+追加」が画面外へ出ていた）③残り枠を「枠 N — 空き」として1行ずつ表示（予算8行、作品は必ず全表示）。**乗せ替えで一番危なかったのは、掃除（`a15237c`）が消したCSSをこのコミットのマークアップが使っていないか**（`check:css` は「CSSがあってマークアップが無い」方向しか見ないので、逆向きは検出できない）→ 掃除前の基準線48クラスを全部リポジトリのマークアップと突き合わせ、ヒットは404の6クラスだけ（別セッションが同じコミットでCSSごと戻した分）と確認。追加された `.me-subnav*` 7クラスにもCSSが揃っていることと、コミットが `app/me.css` に足した48行が掃除後のファイルに全部残っていることを確認。検証: tsc / check:css / check:i18n / クリーンbuild。375x667 の実測で入場ガイドが 84px→37px・1行・ミニマップ(16,536)とHUD(308,441)のどちらとも矩形交差ゼロ・画面外へのはみ出しなし、スクリーンショットでピル表示を確認。レールは `/me` がローカルで開けないため（`.env.local` なし）合成DOM＋`pointer:coarse` ルールの再注入で実測 — たたむと44pxのトグル1行のみ、開くと44px×5行が left=0 で縦に積まれ横スクロールなし。**残: 実機QA**（サインインしてレールを開閉し、空き枠の番号と自動たたみが期待どおりか）。**踏んだ落とし穴**: build直後に同じworktreeでdevを起動すると古いCSSが配信され、矩形測定とスクリーンショットが矛盾した（LESSONS「ビルド・デプロイ（dev環境）」に逆順パターンとして追記）。
