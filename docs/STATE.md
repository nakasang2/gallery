# STATE.md — 作業状態（セッション横断）

> Claude向け運用ルール: セッション開始時にこのファイルを読んでから作業に入る。作業の節目・中断時・ship後に更新する。
> **「最終更新」は直近1回分だけを書く**（過去の記録を後ろに継ぎ足さない）。終わった項目は「完了ログ」へ移し、完了ログは直近5件だけ残す。
> 押し出された過去の記録は [STATE-ARCHIVE.md](STATE-ARCHIVE.md) へ移す。
> ※この形にしたのは 2026-08-13 の /kaizen。それまで「最終更新」の1行が26,171文字・連鎖51回・`## 進行中` が250行まで育ち、
> **Read が途中で打ち切られてこのファイル自体が読めなくなっていた**（＝自己改善ループの入口が壊れていた）。

- **最終更新**: 2026-08-17（**LPのパフォーマンス改善・第1弾（A＝能力ゲート）を [PR #24](https://github.com/nakasang2/gallery/pull/24) として提出。マージはユーザー作業**（このセッションでは `gh pr merge` が権限で拒否された）。ユーザー「LPのパフォーマンス改善です。現状ですと page speed insight に載せてもエラーが出ます」。方針はユーザー選択の**B案＝A＋3D本体の軽量化**で、**まずAだけを単体で出してPSIが通るかを確かめる段**。詳細は DECISIONS 2026-08-17 の1件目。）
  - **原因（調査で確定）**: PSI のエラー `Target closed` は「**測定中にタブごと死んだ**」の意味。PSI と Googlebot のマシンには **GPU が無く**、WebGL は SwiftShader（CPUで1ピクセルずつ描く）に落ちる。そこへLPは **照明30個**（うちスポット23個）・**テクスチャ23枚≒150MB**・**PC幅では毎フレーム、シーンをもう一度描く反射床**・**止まらない描画ループ**を渡していた。**開発機のGPUでは60fpsで動くので、手元の動作は何の反証にもならない。**
  - **入れたもの（2点）**: ①`lib/hero3d.ts` の `canRunHero3d()` ── 省モーション／WebGL非対応に加え、**レンダラー名がソフトウェア描画なら3Dを出さない**。UAは見ない（それは cloaking）。判定は1か所に置き、`HeroCanvas` と `LandingEffects` の両方が同じ答えを見る。②**3Dが出ているときはCSS額装の生成アートを1枚も焼かない** ── `.has-hero3d .hero-floats { display: none }` で消える要素のために、660pxの手続き型の絵を毎回 canvas に描いていた（完全な無駄）。
  - **検証**: tsc／番人9種／本番ビルド／別視点レビュー（指摘0件）／**両方の経路をブラウザで実測** ── ハードウェアGPU側は変更前と同じ絵（1280px・375pxとも）で `hero-floats` の子が **1→0**、ソフトウェア側は**判定を一時的に倒したビルドを焼いて確認**（`.hero-canvas` 自体が生成されない・`has-hero3d` が付かない・CSS額装ヒーローが出る・6つの「できること」が本物の見出しとして見える・ページ高が 8342px に収まる・横はみ出し0）。
  - **【要確認・この出荷の目的】ユーザーに PSI を再実行してもらう**。ここが**唯一の答え合わせ**で、通れば第2弾（3D本体の軽量化）へ進む。**通らなかった場合の原因はほぼ1つに絞れる** ── `WEBGL_debug_renderer_info`（レンダラー名を読む拡張）が PSI の Chrome で読めない場合、判定が空振りする。そのときは「最初の数十フレームを測って遅すぎたらCSSに切り替える」方式を足す（実機の遅い端末にも効く）。
  - **【ユーザー判断待ち】3Dなしのヒーローは、スマホ幅で文字と絵が重なる**（`.hero-eyebrow` が作品の上に乗り、H1が額の下端に接する）。**これは今回入れた変更ではなく既存のCSS**だが、Googlebot と PSI が見るのはこの画面になったので、直すかどうかを決めたい。
  - **【運用】会場費の数字を変えるときは `docs/EVIDENCE-COMPARE.md` も必ず直す**（根拠が無いと不当表示になる）。
  - **【ユーザー作業・繰越】`supabase/migrations/0066_drop_design_colours.sql` を本番DBへ適用する**（前回分）。未適用でも見た目は正しい（アプリ側が色の上書きを読まなくなっている）。消すのは残った値そのもの。再実行安全。
  - **【ユーザー確認・繰越】3Dの重さ**: ダッシュボードの大プレビューは**作品タブと公開タブにはまだある**。テーマ切替のカクつきは実機確認が必要。

## 進行中
- **【中断・明日ここから 2026-08-17】LPパフォーマンス改善の第2弾（3D本体を軽くする）**。ブランチ `claude/lp-perf-software-gate` に**コミット済み・push済み。まだPRにしていない＝`main` には入っていない**。ユーザーの「続きは明日でいい？」で中断した。
  - **第1弾（PR #24）はマージ済み・PSIのエラーは解消した**。ただし**ユーザー指摘のとおり、その点数はCSS版（クローラーが受け取るページ）の点数**であって、来場者が見ている3Dのページの速さではない。第2弾はそこを直す本題。
  - **入れたもの（2点・実装済み）**: ①**スポットライトの共有プール**（`SPOT_POOL = 8`）── 照らす場所は17か所あるが、three.js は前方レンダリングで**シーンにある照明を全ピクセルで毎フレーム計算する**ので、画面いっぱいの廊下の壁に対して17個ぶんのループが回っていた。実照明は8個だけ置き、毎フレーム「カメラの**前**にあって近い8か所」へ割り当て直す。**数を固定するのが要点**（`visible` の切替や出し入れで数が変わると three.js は全材質のシェーダーを作り直し、スクロール中に数十msの停止が入る）。②**遠くの区画（通路の作品7枚＋大部屋）を初回描画では作らない** ── 霧が `far=38` なので入口からは1枚も見えないのに、14枚の生成アート（720px）を初回描画と同じ時間に焼いていた。暇になってから2段で足し、スクロールが先に進んだら待たない。
  - **効果（コードから確定する数）**: 初回に焼くテクスチャ **23枚 → 9枚**／シーンに常駐する照明 **30個 → 廊下で12個・最大15個**。
  - **【重要な発見・要ユーザー判断】板6枚のスポットライトは、6枚とも壁の裏に立っていて1枚も照らしていなかった。** `Panel` の `<Spot>` が `group`（`position=[-WALL_X+0.08, ITEM_Y, p.z]`・`rotation-y=π/2`）の**中**にあり、世界座標のつもりで書かれた数値がローカル座標として解釈されていた。three.js で解くと光源の世界座標は `(p.z - 4.62, 5.8, p.z + 2.1)` ＝ **板ごとに5ずつ左へずれ、左壁（x=-4.7）の裏側**。板までの距離は 01=7.6 / 02=11.9 / 03=16.6 / 04=21.4 / 05=26.3 / **06=31.3**（`distance` は13）。**PR #19〜#21 の「まだ暗い」×3 と、`emissiveMap` で文字を自ら光らせた回避策（`0788372` で撤回済み）は、すべてこれが原因。** 今回は**撤去だけ**した（見た目は1ピクセルも変わらないことをスクショで確認済み）。**正しい位置に置き直して板を明るくするかどうかは見た目の判断なのでユーザーに聞く。**
  - **済んだ検証**: tsc／番人9種すべてOK／本番ビルド／**`git stash` で変更前をビルドし直して同じ位置のスクショと突き合わせ**（①入口＝一致 ②板01の位置＝一致 ③大部屋＝遠壁3枚の光だまりまで一致）。
  - **明日やる残り**: ①通路（廊下の先の暗い区間）と375px幅の見え方の確認 ②区画が足される瞬間に画面が飛ばないかの確認 ③/code-review ④`check:ship-ready` → PR → マージ ⑤板の照明を直すかをユーザーに確認。
  - **測り方の注意（明日の自分へ）**: このページのスクショは**真っ黒になることがある**（LESSONS 2026-08-17）。`window.scrollTo` の前に `document.documentElement.style.scrollBehavior = 'auto'` を入れないと位置がずれる。**大部屋は 375px 幅なら安定して撮れた**。ローカルはGPUがあるので、フレーム時間の前後比較では差が出ない ── **実機の体感と、ユーザーのChromeのDevTools（Lighthouseタブ）が本当の答え合わせ**。
- **【完了 2026-08-16】C-7: WebGL環境でのキーボード/スクリーンリーダー回遊（U3）を小さく前進させた**。着手前にコードを追ったところ、**2026-08-12 の監査で来場者側3D HUDは既に高水準**（フォーカス可能44要素・全てaria-label・前/次ボタンで巡回可・`aria-expanded` 正しい）だと分かり、WASD/矢印キーの徒歩移動・`.`/`,` での作品間ジャンプ・HUDメニュー常設の部屋切替ボタンも実装済みであることを確認した。**REQUIREMENTS.md の「2Dフォールバックはスクリーンリーダー向け」は文字どおりには実装不能**（JSからスクリーンリーダーの有無を検出する信頼できる方法は無い＝機能検出（WebGL）だけでは彼らも3D側に振り分けられる）ため、ユーザー承認のもと3点だけ追加した。
  - **①いつでも押せる「リストで見る」への切替**（スキップリンク。ページ先頭・視覚的には隠しフォーカスで現れる）。`GalleryApp.tsx` にローカル `forceFlat` を追加し、押すと`FlatGallery`（既存の等価な代替＝`FlatWorkList`）へ切り替わる。3D側に`onBackTo3d`を渡し、`FlatGallery`側にも戻るリンクを表示（WebGL非対応でここに来た場合は出さない＝戻り先が無い）。
  - **②`<canvas>` に `aria-hidden="true"`**（`onCreated` で `gl.domElement` に設定）。
  - **③作品パネルの切替を `aria-live="polite"` の常設領域で読み上げる**（`ArtworkPanel.tsx`）。**フォーカスはボタンに残したまま**短く読み上げる方式を選んだ（決定4045と同じ理由：フォーカスを飛ばすと連打時に体験が重くなる）。**常時マウントされた領域の中身だけを変える**設計にした（`aria-hidden`/`inert` が切り替わる要素の内側に置くと、ATによっては変化に気づけない実装がある）。
  - **検証**: tsc 0件／番人8種／本番ビルド／**実CSSでPlaywright実測**（未フォーカス時はスキップリンクが画面外・最初のTabでフォーカスと同時に出現・`.sr-only` は視覚サイズ1×1pxだが `display` は保たれテキストも読める）。**3Dの実描画そのもの（canvasの中身）はこの環境で検証できない**（既知の制約）ため、DOM/CSS/フォーカス管理という**canvasの外側**で検証可能な部分だけを実測している。
  - **範囲外として記録**: 2026-08-12以降に増えたHUD要素（部屋切替モード切替・ExpoManagerの参加者タブ等）の再監査はスコープに含めていない。実作業したのはユーザー承認の3点のみ。
- **【2026-08-15・対応済み】`0057_notify_unsubmit.sql` は本番に当たらない。`0065_notify_unsubmit_retry.sql` を使う**。0057 の一覧（`unsubmit` まで）は先に適用済みの 0062 の一覧（`unsubmit` ＋ `room_ready`）の**部分集合**なので、あとから当てると許可済みの `room_ready` を取り上げる後退になり、実在した `room_ready` の行に弾かれる（`ERROR: 23514`）。0065 は制約を**10種すべて**（`lib/notifications.ts` の `NotificationKind` と一致）で `begin; … commit;` の中に建て直し、0057 の関数とトリガも入れる。**本番でユーザーが適用済み・実測で確認**（トリガ1本／制約に `unsubmit`・`room_ready` の両方あり）。
  - **Claude の誤報を1件記録**: 「0057 の失敗で本番の制約が消えているはず」と報告したが**誤りだった**。ローカルの `psql -f` は文ごとに自動コミットするので drop だけが残るが、**Supabase の SQL Editor はスクリプト全体を1トランザクションで実行する**ため本番では巻き戻っており、制約は無傷だった（ユーザーの実測で判明）。**再現環境と本番環境でトランザクションの単位が違うことを確かめずに断定した**（LESSONS 2026-08-15）。
  - **`0057` はもう本番に当ててはいけない**（ヘッダに警告済み）。番号順に流す経路＝新しい環境・`schema.sql` ではこのままで正しい。
- **【2026-08-14・ship済・本番反映済み／DB適用はユーザー作業】合同展示の参加作家が「通常展示に戻す」で展示から消える**。本番で再現・原因確定済み。決定と根拠の全文は DECISIONS 2026-08-14。
  - **入れたもの（6点）**: ①migration `0063_expo_leave_guard.sql` — `switch_room_expo` が**主催者以外からの「展示から外す」を拒否**する（SQLテスト13項目・新規）②`/me` が部屋0室の口座に**無料の部屋を自動で作る**（`createGallery(templateId:'studio')`。ウィザードは自動作成が失敗したときの退路としてだけ残す）③**部屋タブに合同展示の部屋も並べる**（`switchableRooms`。`rooms` の側は玄関・部屋枠・共有プールの勘定に使うので通常展示だけのまま）④`RoomExpoBadge` の「通常展示に戻す」を**主催者限定＋「この部屋を展示から外す」に改名**、参加作家には受信箱の「降りる」を案内⑤`isOrganizer` の初期値を**主催者→参加者に反転**（読み込み中に参加作家へ「参加者」タブが出る／トグルが出ないのを解消。タブの弾き出しは `expo` を読み終えてから判定）⑥**最後に開いていた部屋を覚える**＋**初回は合同展示の部屋を開く**（`ROOM_KEY`）。
  - **あわせて塞いだ再発**: 部屋タブの表示条件を `!row.expo_id` から「**いまの部屋がこの一覧に居るか**」に置き換えた。主催者が参加作家の部屋を開くとその部屋は他人の持ち物なので一覧に無く、2026-08-13 に直した「宙に浮いたタブ」が別経路で再発していた。
  - **検証済み**: tsc／番人8種（gates 71）／SQLテスト全320項目（新規13項目を含む）／本番ビルド／`switch_room_expo` を0062の版に戻すとテスト02・03が赤くなり **03は「部屋が展示から消えた」＝本番の事故そのものを再現**することを実測／`stubs.sql`＋全migration のDBと `schema.sql` 1枚のDBを `pg_dump` で diff（差分20行はすべて 0057 のコメントと 0060 の列順＝既存の差分で、0063 は無関係）／0063 の3回適用（冪等）。
- **【2026-08-14・ship済・本番反映済み／DB適用はユーザー作業】主催者の可視性ほか4件**（migration `0064_expo_ready_room_visible.sql`）。決定の全文は DECISIONS 2026-08-14 の決定5。
  - **入れたもの**: ①述語 `expo_room_ready_for_organizer()` ＋ `artworks`・`placements` の select ポリシー ── **「準備できた」がオンの部屋だけ**主催者が中身を読める（作家がオフに戻せばまた見えなくなる＝決定権は作家）②`listRoomArtworks` を**所有者ではなく部屋で絞る**ように変更（0061 が「合同展示の部屋に名乗れるのは持ち主の作品だけ」を強制しているので集合は同じ。所有者で絞っていたせいで、RLSを開けても主催者には0点に見えていた）③他人の部屋を開いているときは**読み取り専用の断り書き＋保存ボタンを出さない**（DB側に書き込みポリシーが1つも無いので、出せば必ず失敗する）④参加者タブの「0点提出」を**「部屋がありません（× で取り消して招き直してください）」**に（旧・提出モデルの言い方で、提出という仕組みを見ていない作家を誤解させていた）⑤決済の「This exhibition is already running.」を、終わっている場合は**終了＋新規作成の案内**に分岐。
  - **検証済み（第1弾・第2弾あわせて）**: tsc／番人8種（gates 71）／**SQLテスト全339項目**（0063 の13項目・0064 の19項目を含む）／本番ビルド／**両方の番人が本当に効くことを実測**（0063 を外すと「部屋が展示から消えた」が再現、0064 のポリシーを外すと主催者に中身が見えなくなる）／`schema.sql` 1枚のDBと全migrationのDBを `pg_dump` で diff（差分20行はすべて 0057 のコメントと 0060 の列順＝既存の差分）／**ブラウザで実操作確認**（preview-verify の実バンドル手順。部屋タブに両方の部屋＋「合同」バッジ・本物のクリックで切替してどちらも消えない・参加作家に「参加者」タブが出ない・「準備できた」トグルが出る・他人の部屋では断り書きが出て保存ボタンが消え部屋タブも出ない・375pxではみ出し0件）。
  - **止めた過剰報告**: 「準備できた」トグルの当たり判定は実効35×21pxで24pxを割るが、最寄りの操作対象まで228px離れており **WCAG 2.2 SC 2.5.8 の間隔例外**に当たる（かつ公開トグルと同じ既存部品）。
  - **`0063` は本番適用済み（2026-08-15・ユーザー）。`0064` はまだ。**
  - 以下は原因の記録:
  - `RoomExpoBadge` の「通常展示に戻す」は名前に反して**変換**で、参加作家が押すと自分の合同展示の部屋が通常展示の部屋に変わり**展示から消える。招待は accepted のまま残るので復帰する導線が1つも無い**（0062のトリガは再発火しない／バッジからの参加は 2026-08-12 に撤去／受信箱の「部屋を開く」も出ない）。
  - **ユーザー決定は「変換ではなく行き来にする」**: 参加作家は通常展示の部屋（5枠）と合同展示の部屋（15枠）を**両方持ち**、切替はどちらを開くかを選ぶだけ。枠の分離は 0061 で実装済み。
  - あわせて直した: **部屋0室の口座に無料テンプレート `studio`（White Cube）で部屋を自動作成**（DBトリガではなく画面側。理由は DECISIONS 参照）／**承認後の初期表示を合同展示の部屋にする**。
  - **合同展示のフローは一通り確認し終えた（2026-08-14）**。招待→承諾→部屋の自動生成／主催者の参加者タブ／会期の決済／「準備できた」トグルと通知／公開ページ `/expo/{名前}` の見え方／会期終了→猶予→消滅、のすべて。公開ページの時期ごとの見え方はSQLで16項目を実測（会期前は404・会期中は全部見える・猶予中も見えて「終了しました」の帯・猶予明けは掃除の前でも404・消えたら同じ名前を使い回せる・**作品そのものは残る**）。
  - **既に被害に遭っている作家が本番に1人いる**（展示 `combination` の `test`）。主催者が「参加者」タブで `×` → 招待し直し → 承諾で復旧するが、**修正前にやると同じ操作でまた消える**。
- **【解消済 2026-08-15】0062 のSQLテストの穴（招待URL経路）**: `requested` → `approve_expo_request` の経路を項目22〜27として追加した。**この経路は `security definer` の中で走るので `auth.uid()` が主催者のまま作家の部屋を insert する**という、直接UPDATE経路には無い形をしている。**新しい項目が既存では捕まらないことを実測** ── トリガの持ち主を `new.artist_id` から `auth.uid()` に変えると、項目02・21（直接UPDATE）は緑のまま項目25〜27だけが赤くなる。書くときに `reset role` がJWTクレームを消さない罠（LESSONS 2026-08-09）を1度踏んだ（`not your exhibition`）。
- **このセッションの作業は全部出荷済み**（0058・0059 も本番適用済み）。以下は判断・確認を待っているものだけ。
- **【解消済 2026-08-15・台帳が古かった】R2の孤児ファイル**: `uploadArtwork` / `uploadVideoArtwork` とも insert 失敗時に `deleteArtworkFiles(id)` を呼んでおり、**`756410e` で既に直っていた**（件名が `docs:` なのでコード変更が埋もれていた）。`/api/storage/delete` は `{uid}/{artworkId}/` を prefix で消すので**行が無くても消せる**ことも確認済み。**この確認の途中で本物のバグを1件見つけて直した** ── DBの拒否をクライアントの入れ直しが迂回していた件（DECISIONS 2026-08-15・番人 `check:insert-retry`）。
- **【解消済 2026-08-16・ユーザー確認】C-5: 元の不具合の後始末**。旧コードが `placements` から削った行（`galleries.arrangement` には残っていた分）は、各部屋で一度ダッシュボードの配置を動かして再保存する必要があった。**ユーザーが完了を確認済み。**
- **【完了 2026-08-16】SQLテストの空振りを全部書き直した（61 → 0 件）**。`scripts/sql-vacuous-baseline.json` は**空配列＝目標値0**になり、`check:css` の基準線と同じく「増えたら落ちる」だけの意味になった。12ファイル61項目すべてが `__tNN_state(p_sql)` で SQLSTATE を突き合わせる positive なアサーションになっている。
  - **手順（残っている同種の作業に再利用できる）**: ①test に `__tNN_state` を1つ置く ②**savepoint と席の設定には触らず、検査したい1文だけ**を包む（`rollback to` は `set local role` も巻き戻すので、savepoint を外すと後続の項目の席が変わる）③**echo は包んだ文の直前へ動かす**（あいだに `savepoint` や準備の文が残ると psql がコマンドタグを出してラベルの後ろに混ざり、判定器が値を読めない）④**まず素の SQLSTATE を観測してから**断定に変える ⑤ラベルの末尾を `→ ` から `: ` にする（判定器は行末の `: t` / `: f` で読む）⑥基準線から外す ⑦守りを外して赤くなることを実測する。
  - **検査対象の選び方が要**: 「echo の直後の文」ではなく「**echo から次の echo までの窓の中の savepoint…rollback に囲まれた文**」。前者で書いたら 0045 の項目16が**準備の `update` を包んで `no-error`** を返し、危うく「本番の守りが壊れている」と報告するところだった（囲みがあればそちらが検査対象）。
  - **赤くなることの実測（4ファイル）**: 0044＝`starts_at` の番人を外すと 04・06 が `f`／0048＝自己承認の番人を外すと16だけ `f`／0047＝`may_place_artwork` を常に true にすると 22・23 が `f`／0056＝**DMCA §512(c)(3) の4要件チェックを外すと 04・05・06 が `f`**（別の制約を見ている08は緑のまま）。**この3項目は以前なら空振りで、法定要件の守りを外しても「すべて期待どおり」と出ていた。**
- **【別セッション進行中】** `components/gallery/*`（3Dの扉・廊下）／`lib/i18n/*` 11言語／`app/me/*`／migration 0060（プロフィールCV）。**このセッションでは触っていない**。

## 次にやること（再開ポイント）
- **【完了 2026-08-15・PR #14】訴求内容の作り直し（提案5）**: 「できること」6枚が初期リリース時のままで、出荷済みの合同展示（$15〜$40）・ビデオパスがLPに1文字も無かった。ユーザー確定の文言で6枚とヒーローを全面差し替え済み（詳細は上の「最終更新」）。**単価の高いものほど不可視という形は、料金表（2026-08-12）に続いて「できること」側でも塞いだ。**
- **【LPレビュー 2026-08-15 の残り・ユーザー作業】提案8（専門家確認2件）**: ①特商法11条の電話番号を「請求があれば遅滞なく開示」で伏せている件（消費者庁の運用では氏名・住所・電話番号は省略できないとされる）②**Stripe Managed Payments が merchant of record** であることと、`/legal` の「販売業者=Nakamae Yusuke」表記の整合（特商法の表示主体・消費税の納税義務者・返金の一次窓口がここに乗る）。**Claudeでは決められない。**
- **【再提案の待ち・DECISIONS 2026-08-15 決定1】実作例セクション**: 公開展示が集まったら再提案する。土台は既にある（`site_config` の枠＝`lp_panels` の作法、`/explore` の Spotlight）。**LPの絵が全部生成アートで「自分の作品がこの部屋でどう見えるか」が分からない状態は続いている。**
- **【完了 2026-08-14】期限切れの合同展示を消す掃除**: 本番の `pg_cron` が無効でジョブ未登録だったのを、ユーザーが拡張を有効化して `cron.schedule('purge-expired-expos', '17 3 * * *', …)` で登録済み（jobid 1・毎日3:17）。走ったかは `select start_time, status, return_message from cron.job_run_details where jobid = 1 order by start_time desc limit 5;` で見られる。
- **【ユーザー作業・最優先】`supabase/migrations/0064_expo_ready_room_visible.sql` を本番DBへ適用する**（`0063` は 2026-08-15 に適用済み）。未適用のあいだは**主催者が「準備できた」参加作家の部屋の中身を見られない**（＝$15〜$40 を払う前に中身を確認できない）。再実行安全。
- **【ユーザー確認・優先】本番で `combination` の `test` さんを復旧する**。参加者タブで `×` して招待し直し、承諾してもらえば新しい部屋ができる（0063適用後なら再発しない）。
- **【完了 2026-08-14】`0061_expo_room_scope.sql` と `0062_expo_participant_rooms.sql` は本番適用済み**（トリガと列の存在を本番DBで確認）。①1展示・1作家1部屋②作品プール分離③招待承諾での部屋自動生成④「準備できた」トグル、はいずれもDB側では効いている。**ただし③で作られた部屋を作家が自分で消せてしまう不具合がある**（上の「進行中」を読むこと）。
- **【一部完了 2026-08-14】** 0062のフロント側は、2026-08-14 に **preview-verify の実バンドル手順でブラウザ実操作を確認済み**（部屋タブ・「参加者」タブの主催者限定化・参加作家向けの「準備できた」トグル・読み取り専用の表示）。**`InviteInbox` の「部屋を開く」と `ExpoManager` の「参加している合同展示」節は、認証と実データが要るため未確認**（本番でのユーザー確認に委ねる）。
- **【ユーザー作業】`supabase/migrations/0057_notify_unsubmit.sql` を本番に適用する**（0056 は適用済み）。未適用でも通報・提出は動くが、**会期中に参加作家が作品を引っ込めたときの主催者への通知だけが出ない**。再実行安全。
- **【解消済 2026-08-16・ユーザー確認】C-6: 実機QA**（プレビューの制約でClaude側では測れなかった分）: ①3Dの実描画全般（歩行・作品の掛かり方・ローディング→入場の切替・扉の位置）②額縁の太さスライダー（高さ16px・つまみの当たり判定）③ノッチ端末の安全域（`env(safe-area-inset-*)`）④スマホの配置トレイ（1行4点）とPCのキーボード配置（配置タブでTab→Enterで持ち上げ→Enterで置く）。**ユーザーが実機で確認済み。**
- **【解消済 2026-08-15】ミニマップとステッパーの重なり**: 実CSSのハーネスで再現（320px で **28.2×0.4px**）してから直した ── **3Dが描画されなくても、HUDはDOM要素なので実測できる**（前回「目で確認できない」として保留したのは、測り方の選択を誤っていた）。原因は同じ `4.4rem` が `.minimap` / `.hud-cluster` / `.roomswitch` の**3か所に写されていた**こと（【絶対ルール】2026-08-12 の形）で、しかもページャーの実高さ 70.8px に **0.4px 足りない**値だった。`--hud-row-bottom: 5rem` を `:root`（≤640px）に1つ置いて3か所から引くようにし、9.2px の余白を確保。320/375/414/640px で重なり0を実測（ミニマップとHUD束が下端から80px、部屋切替がその上の181.6px）。**custom property をセレクタの外に書いて一度無効なCSSにした**（`@media` 直下の裸の宣言は落ちる）── 実測していなければ、電話でミニマップがページャーの上に落ちたまま気づかなかった。
- **【ユーザー確認済み 2026-08-09】描画品質ティアの実機QA**（ユーザー「1-4まで確認しました」）。判定は `lib/controller.detectQuality()` で、**コア数が主**（`pointer:coarse` でなければ high、coarse かつ `deviceMemory<=4` または `hardwareConcurrency<=4` なら low、それ以外 medium）。
- **読み上げTTS**: 本番ENV(`OPENAI_API_KEY`/`SUPABASE_SERVICE_ROLE_KEY`)設定済み。`/api/tts`は本番で200・mp3公開再生可・キャッシュ動作を確認済み。ボイスは**shimmer確定**。残: アプリ実画面での再生体験（ツアー▶/作品ガイド）＆日本語キャプションでの発音は本番QAで最終確認。
- **【ユーザー確認済み 2026-08-09】Stripe の実カードE2E**（ユーザー「1-4まで確認しました」）。以下は経緯 — **Stripe: 残るのは実カードE2Eだけ**（2026-08-09 に台帳を突き合わせて訂正）。「本番接続の運用作業」として長く残っていたが、**接続はすでに済んでいる**: SQL 0019/0028/0031 は2026-07-28適用・検証済み（同ファイル「Phase 0 のユーザー作業」参照）、2026-07-29には**本番の決済が Managed Payments の制約で400を返すのを修理**している＝live鍵とWebhookが動いていなければ起こらない事象。したがって残タスクは **実カードで少額（スロット追加$3）を購入→返金するE2E**の1点。※`www.xibit360.art` はこの実行環境のネットワークポリシーで遮断されているため、Claude側から疎通確認（checkout=401/webhook=400）はできない。
- **画像/動画ストレージのR2移行**: **ship完了・本番反映済み（2026-07-27）／実機QAはユーザー確認済み（2026-08-09）**。**コード側は Supabase Storage を一切読まない**ことを 2026-08-09 に確認済み（`supabase.storage` の呼び出しはアプリ全体でゼロ。画像URLは `storage_path` から読み出し時に組み立てるので、DBに古いURLが残る余地もない）。残るのは **Supabase Storageの`artworks`バケットを空にする**判断だけ。**空にする前に 0029 が書き換えた6箇所（profiles.avatar_url / artworks.audio_url / galleries.bgm_url / articles.cover_url / galleries.design_overrides / site_config.value）が全部0行であることをSQLで確認する** — 削除は取り返しがつかないので、ここは目視より機械で。
- docs/STRATEGY.md **§7** の残タスク:
  - P1-5 有料テーマ/レイアウト第1弾。前提(FOREVER_FREE固定化)は**解消済**。残るは実際の有料テーマ/レイアウトの制作という事業/制作判断
  - P2-8 ウォークスルー動画は**実装済**。フォローアップ: MP4/GIF変換(X/IG直投稿用。要ffmpeg.wasm/サーバ)・録画に音声を載せる
  - P2-9 企画展(特集)は**実装済**。運用: `/admin`で特集を設定するだけ。フォローアップ候補: 期間の自動切替・作家からの応募フロー
  - P2-10 記事/ガイド機能は**実装済**(migration 0020)。運用: `/admin`の「Guides」で執筆・公開するだけ。「Ktlyst参考に」の指定は同名企業が複数あり特定できず・候補サイトも403のため、Xibit360自身のデザインで実装(参考URLをもらえれば個別に寄せる)。フォローアップ候補: 記事内の作品/ギャラリー埋め込み、タグ/カテゴリ、関連記事
  - P3-12 音声ガイド=**作品ごとガイド実装済**(migration 0021)。空間BGM=**案A実装済**(作家が音源アップ→1トラックloop、migration 0027)。残フォローアップ: 案B(権利処理済みアンビエント音源のライブラリ内蔵)。※市販曲のInstagram型は包括ライセンスが要り個人規模では非現実的(2026-07-21相談で確認)
  - P3-11 グループ展(卒展向け・最高価値)は**設計判断待ち**: 「複数作家でどう共同編集するか」(招待制の共同編集 / 主催者が他人の公開作品をキュレーション)。Stripe本番にも依存
  - P3-13 AR / P3-14 立体(glTF)
- レビューで見送った低優先の既知事項:
  - 署名なし決済フローで未サインイン時に「Checkout isn't live yet」と表示（現状モーダルはサインイン文脈でしか開かず到達不可）
  - 購入完了後の再取得はギャラリー（キャパ）のみ更新。テーマ/レイアウト所有は要手動リロード（バナーで案内済み・現状テーマ購入は到達不可）
- Q2の残り: `/me`のalert/confirmは意図的にネイティブのまま（没入を壊さないため対象外）
- U3（WebGL対応環境でのキーボード/スクリーンリーダー回遊）は未対応（非WebGLの2Dフォールバックのみ）

## 注意（並行セッション・未コミット変更）
- **SQLはローカルで実行検証できる**（2026-07-29に `brew install postgresql@16` 済み。`/opt/homebrew/opt/postgresql@16/bin`）。migration や RPC を書いたら本番に貼る前にローカルで通すこと。起動の作法とハマりどころ（ソケットパス103バイト上限・`LC_ALL`必須・ロールはクラスタ単位）は LESSONS「検証・ツール」2026-07-29 に記録。**Supabase固有の前提は `auth.users` / `auth.uid()` / `storage.buckets` / `storage.objects` / `storage.foldername()` ＋ anon・authenticated・service_role の3ロールをスタブすれば足りる**。
- **2026-07-29 時点で作業ツリーが6つ動いている**（`git worktree list` で確認）。`origin/main` は1日に何度も進むので、**セッション開始時とpush直前の両方で `git fetch` する**（この日は1回の ship の最中に2回動き、rebase 3回になった。LESSONS「検証・ツール」×2）。
- **【2026-07-29 時点】未pushのコミットは全ブランチでゼロ**（ユーザー指示で棚卸し・全部着地させた）。`d3f972f`（台帳）は内容を現在の台帳へ移して `main` を `origin/main` へ早送り、`dd4c59b`（コード）は rebase して `476d54e` として着地。**`claude/infallible-neumann-086455` は着地前の `dd4c59b` を指したままの古いポインタ**（中身は `476d54e` として main にあるので未処理ではない。消してよいが、別セッションの枝なので残してある。`git branch -d` ではなく `-D` が必要＝rebase で別コミットになっているため）。**棚卸しは `npm run check:ship-ready` が push 直前に自動でやってくれる**（他の作業ツリーの未pushコミットと未コミット変更を報告する）。手で全ブランチを見たいときは `for b in $(git for-each-ref --format='%(refname:short)' refs/heads/); do git rev-list --count origin/main..$b; done`。
- **【解消済 2026-07-29】メインチェックアウトの未pushコミット `d3f972f`（決済不具合の台帳記録）は取り込んだ**。ユーザー指示（「未処理のコミットをすべて処理して」）で、台帳3ファイルの追加分だけを現在の台帳に手で移した（`origin/main` 側が6コミットぶん進んでいて cherry-pick が3ファイルとも衝突したため）。`main` ブランチは `origin/main` へ早送りしてある。**履歴の書き換え（`9d02ab5` からコードを分離し直す案）は実施していない** — 既に本番に出ているコミットなので、書き換えると他の作業ツリーの土台が壊れる。件名と中身の食い違いは下の「コミットの件名を信用しない」に残す。
- **コミットの件名を信用しない**: `9d02ab5` は件名が `docs:` なのに `components/PurchaseModal.tsx` +45行・`lib/checkout.ts` +35行・`app/gallery.css` +10行を含んでいた。**rebase したら `git show --stat <相手のコミット>` で実際の変更ファイルを見てから検証範囲を決める**。

## 完了ログ（直近5件）
- 2026-08-17: **【ship済】この日はまず7本出荷した**（①明朝を Bodoni Moda に ②テーマプレビューを実物の部品で建て直し ③テーマ選択を焼いた写真のカードに ④部屋タブから Design Tools と大プレビューを撤去 ⑤LP廊下の画像枠は写真だけに ⑥LP「くらべる」節＋廊下の光 ⑦くらべる表に会場費）。**全文は [STATE-ARCHIVE.md](STATE-ARCHIVE.md) の「移した『最終更新』」の先頭**。決定は DECISIONS 2026-08-17 の4件。
- 2026-08-16: **【ship済・PR #22としてmainへマージ済み】LP廊下パネル、テクスチャ解像度を上げてスマホはカメラを寄せた**（マージコミット `ad07c76`。PR #21の直後）。ユーザー「3D側のテキストはスマホだとジャギって読みにくい」。原因はスマホが3D全体の描画解像度を負荷対策で落としている（`dpr`上限1.25）ため、板のテクスチャ(900x1180)がその粗い解像度で描かれてから引き伸ばされていたこと。①板のテクスチャ解像度を2倍に（`ctx.scale`で描画コードは論理サイズのまま。焼き込みは1回だけなので常時コストの`dpr`には触れない）②縦画面（主にスマホ）は板を見るカメラを壁へ寄せた（距離約6.5→3.6units）。`PORTRAIT`モジュール変数は別コンポーネントが非同期更新するため初回ロードでずれる恐れがあり、`buildAnchors`の中で`window.innerHeight`を直接読んで`corEnd`等と同じタイミングで判定するようにした。検証: tsc・番人8種・本番ビルド・Playwrightで3倍DPR接写の文字の縁と縦画面の拡大表示、横画面構図の不変を確認。**未解決**: 「アートと違い光の輪郭が無い」はユーザー判断待ち。
- 2026-08-16: **【ship済・PR #21としてmainへマージ済み】LP廊下パネル、3度目の「まだ見えない」で根本原因を特定して修正**（マージコミット `bf91cfe`。PR #20の直後）。板は元々「明るい下地の板（マット紙）」に光を当てる設計で、旧基準値130もそのマットを照らして初めて成立する数字だった。PR #17で地の板を撤去し暗い壁に直接光を当てる構成に変えていたため、**光を受け止める明るい面自体が無くなっていた**（PR #19・#20は光の量だけを動かしていて前提の崩れに未着目）。修正は光の量ではなく仕組み — `meshStandardMaterial` に `emissiveMap={tex}` を足し、文字自体が周囲の照明に依存せず光るようにした（透明部分はRGBが黒なので発光の影響を受けない）。フォーカス連動のスポットは雰囲気づけとして残すが可読性はもう照明強度に依存しない。**×3としてLESSONS.mdに記録**（「数値を大きくしても直らないときは前提を疑う」）。検証: tsc・番人8種・本番ビルド・Playwrightでユーザーのスクショと同じ構図の明るさ改善を確認。
- 2026-08-16: **【ship済・PR #20としてmainへマージ済み】LP廊下パネルの照明、「まだ暗い」との再報告を受けて再修正**（マージコミット `583464c`。PR #19の直後）。PR #19で「素通り時100は旧来の常時点灯(130)より明るい」とコミットに書いたが**誤りだった**（100<130で実際には旧水準より暗いまま）。数字を並べて比べずに断定していたのが原因で、ユーザーの再指摘で発覚。素通り時150／フォーカス時230に修正し、旧定数(130)を明確に上回る値を下限にした。LESSONS.mdに「基準値と比べる主張はその場で数字を並べて確認してから書く」を追加。検証: tsc・番人8種・本番ビルド・Playwrightで前回と同じ最悪ケースの明るさ改善を確認。
- 2026-08-16: **【ship済・PR #19としてmainへマージ済み】LP廊下パネル、実機で暗すぎた照明を修正し、フォールバック文言をSEOで読める形に**（マージコミット `ed1d6de`。PR #18の続き）。①実機（スマホ）で「暗すぎて見えない」との指摘を受け、PR #18の素通り時18／フォーカス時170を100／190に修正（指スクロールは狙った1pxで止まらず、暗い方を見ている時間が長かった）。②SEO相談「壁の文字をcanvasに焼いたが大丈夫か」を受け、3D非対応フォールバック（`.corridor-fallback`、本物の`<h3>`/`<p>`）が `.has-hero3d` 時に `display:none` で丸ごと隠れ、WebGL対応のGooglebotにもほぼ見えていなかった可能性を特定。`display:none` をやめ既存の `.sr-only`（U3のa11y対応）にセレクタを足し、視覚的には隠しつつクローラー・スクリーンリーダーには読める形に（同じ変更で3D有効時のa11yの隙間も解消）。検証: tsc・番人8種・本番ビルド・Playwrightでモバイル可読性と`.corridor-fallback`のDOM残存を確認。**未解決**: 「アートと違い光の輪郭が無い」はユーザー判断待ち。
