# LESSONS.md — 失敗・成功パターン台帳

> Claude向け運用ルール: ビルド/デプロイの失敗、ユーザーからの修正指摘、同じ質問の繰り返しが起きたら**対応した直後に**1エントリ追記する。同種の作業を始める前に該当カテゴリを読む。同種パターンの発生回数（×N）が3回に達したら、DECISIONS.md の絶対ルール化またはスキル化を /kaizen で提案する。

フォーマット: `日付 | 事象 → 原因 → 回避策`（1〜3行。再発したら新規追加せず既存エントリの ×N を加算）

## 失敗パターン

### セキュリティ・上限の強制
- 2026-07-27 | R2移行でクォータ判定を「ブラウザ→サーバー」へ移したのに、**判定の材料はクライアントの申告値のまま**だった（`artworks.bytes`の合計）。監査で、認証済みユーザーが300MB上限を無制限に超えられる経路が3本見つかった: ①作品ファイルはDB行を作る**前**に上げるので、行を挿入しなければ何MB上げても記録が残らない（毎回新しいuuidで無限に反復可能）②行を作る場合も`bytes: 0`で挿入すれば合計は増えない ③音声ガイド/BGMは署名時に数えるだけでどこにも保存されない → **チェックを実行する場所（サーバー）を移しただけでは強制にならない。「その数字は誰が決めたか」まで遡ること。上限の入力は、クライアントが書ける値ではなく、こちらが実測できる値にする**（採用: 署名のたびにR2の`{uid}/`配下をListObjectsV2で実測。DECISIONS 2026-07-27「容量制限の実測化」）
- 2026-07-27 | 同じ監査で、上限判定に**「先に書き込む場所を予約する」処理が無い**ことも判明（`used`を読む→署名、の間に何も押さえないので同時リクエストが全て同じ`used`を見る） → **「残量を読む→許可を出す」形のゲートは、読みと許可の間に予約を挟まないと同時実行で素通りする。予約はユーザー単位の`pg_advisory_xact_lock`で直列化し、予約の寿命は許可（署名URL）の寿命より長くする** — R2はPUT**開始時**にしか期限を見ないため、同時に切らすと「予約は切れた・LISTは未完了のPUTを見られない」無防備な数秒ができる（`supabase/migrations/0030_storage_reservations.sql`）

### ビルド・デプロイ
- 2026-07-21 | 別セッション成果をpullしたら `git-lfs: command not found` で途中中断し、作業ツリーが中途半端に更新（HEADは旧のまま／tracked=modified・新規=untrackedが大量に発生） → このリポジトリはLFS管理（`visitor`系glb等）なのに実行環境へgit-lfs未導入 → **セッション開始時にpullが「Filtering content」やlfs関連で失敗したら、まず `brew install git-lfs`+`git lfs install`。中途破損した作業ツリーは、開始時クリーンなら `git reset --hard origin/main` で一括復旧できる（HEADは未移動＝コミット履歴は無傷）。破壊的コマンドがauto-mode classifierにブロックされたらユーザー承認を取ってから実行する**

### ビルド・デプロイ（dev環境）
- 2026-07-23 | git pushしてもVercelの自動デプロイが8分以上起動しないことがあった（同日多数デプロイ後） → **ship時は`npx vercel ls`で新しい行が出ない場合、`npx vercel --prod`で手動デプロイして先に進む（コミット済みクリーンな作業ツリーであることを確認してから）。エイリアス割当は`npx vercel inspect <url>`で確認**
- 2026-07-23 | devサーバーのモジュール破損が繰り返発生（`main-app.js` 404 / `__webpack_modules__[moduleId] is not a function`）×2 → **devサーバー起動中に `next build` を実行したのが真因**（devとbuildが同じ `.next` を共有していて上書き破壊される） → **push前ビルド検証は必ずdevサーバー（preview）を停止してから実行する。壊れたら `rm -rf .next` → dev再起動で復旧**
  - **×3（2026-07-28 再発）**: i18n作業中、previewを起動したまま `npm run build` を実行し `Cannot find module './331.js'` で全ページが白画面に。記録済みの手順（dev停止 → `rm -rf .next` → 再起動）で復旧。**記録があっても、検証を急いでいると踏む**。→ **【昇格済 2026-07-28 /kaizen】`prebuild` フックで機械化**（`scripts/check-dev-server.mjs`）。`.claude/launch.json` のポートに接続できたら build を止め、停止手順を表示する。CI/Vercel では飛ばす。手順書に書くだけでは ship を通さない build を守れないため機械側を選んだ。プレビュー起動中に `npm run build` して実際に止まることを確認済み。
  - **副作用（2026-07-28）**: このガードは **worktree を区別しない**。`.claude/launch.json` のポートに誰かが listen していれば止めるので、**別セッションがメインチェックアウト（`/Users/ynakamae/gallery`）で dev を動かしているだけで、worktree 側の build が打てなくなる**（`.next` は別ディレクトリなので衝突は起きていない）。`SKIP_DEV_SERVER_CHECK=1` はガードを丸ごと無効化するうえ auto-mode classifier に止められる → **worktree で作業するときは、その worktree の `.claude/launch.json` のポートを空きポートへ変えてから build/preview する（例 5173/5174 → 5273/5274）。ガードは有効なまま意味のある判定になり、プレビューも他セッションと衝突しない。コミット前に launch.json を元に戻す**。listen しているプロセスが自分のものかは `lsof -a -p <pid> -d cwd` で cwd を見て判断する（他セッションのサーバーは勝手に落とさない）。 → **【昇格済 2026-07-28 /kaizen】`prebuild` を cwd 判定にした** — listenしているプロセスの cwd がこの作業ツリーの中かを見て、外なら「別の作業ツリーの dev です」と表示して続行する。実測で両方向を確認（メインチェックアウトのlistener→続行 / 同じworktreeのlistener→exit 1）。
  - **逆順も壊れる（2026-07-28）**: ガードが守るのは「dev → build」の順だけ。**「build → dev」の順**、つまり `npm run build` の直後に同じ worktree で preview を起動すると、**devサーバーが build の `.next` を引き継いで古いCSSを配信する**（`prebuild` はこの向きを検査しない）。今回は `app/gallery.css` に足したはずの `.hint.hint-touch` が配信CSSに存在せず、`getBoundingClientRect` は新レイアウトを返すのにスクリーンショットは旧レイアウトのまま、という**測定結果が自己矛盾する状態**になって10分ほど原因を探した → **push前ビルドの後にプレビューで実測するなら、`rm -rf .next` してから dev を起動する。配信されているCSSに当該セレクタが本当に入っているかは `curl -s <preview>/_next/static/css/... | grep <selector>` で1回確かめるのが最速**（`document.styleSheets` の走査はプレビューのペインでは空を返すことがあり信用できない）。 → **【昇格済 2026-07-28 /kaizen】`predev` フックで機械化**（`scripts/check-stale-next.mjs`）。`.next/BUILD_ID`（本番ビルドしか作らない）があれば `.next` を消してから dev を起動する。残したい場合は `KEEP_STALE_NEXT=1`。

### 品質・レビュー
- 2026-07-13 | デモ→戻るで環境音が鳴り止まない（ユーザー報告）→ `galleryAudio`がモジュールsingletonで`AudioContext`を停止する手段が無く、`GalleryApp`アンマウント時に何も止めていなかった → **没入体験が使うsingletonの外部リソース（AudioContext等）は体験コンポーネントのアンマウントで明示的にsuspend/停止する。`ctx.suspend()`は非同期で反映に約1秒かかるため、master gainを即0にして即時ミュートを併用する**（`lib/audio.ts`/`lib/videohub.ts`/`GalleryApp`。Playwright+同梱Chromiumで離脱後にsuspendされることを実挙動検証）
- 2026-07-13 | 「サインイン中はデモ作品を混ぜない」を`rowSpace`で`showDemo:false`にしたら、その値が`localStorage`へ永続化され、サインアウト後のゲスト体験でデモが空になる副作用 → 永続設定(`Settings.showDemo`)にサインイン依存の値を書き込むと状態が漏れる → **サインイン状態に依存する表示分岐は永続設定に書かず派生状態（フック`useIsOwnerEditing`）で計算する**
- 2026-07-13 | 対外文言（ランディング料金表）が実装・要件のピボット後もそのまま残り、¥980/月サブスク×無制限という古いモデルを提示していた → LPを実装/要件と別管理で更新し忘れ → **料金・数値の文言はプラン変数(`lib/limits.ts`)・価格定数(`lib/pricing.ts`)から参照し、直書きしない**（kaizen 2026-07-21: 07-21のブランド名直書き150箇所と合わせ「外向き文言の直書き」×2。自然文の言及は中央化不可でルール化の実益が限定的なため、絶対ルール化は**見送り**。次に固有名詞/料金の変更が来て×3になったら再検討）
- 2026-07-27 | **↑の×3に到達**。本番でStripeキーを設定して決済が起動できるようになった後も、LPは「Coming soon / billing is not implemented yet」、購入モーダルは「これはプレビューです」と表示し続け、さらに料金欄は「only Video Pass renews yearly」と**売っていないサブスクの存在を宣言**していた（サブスクなしが最大の差別化なのに自分で否定）。前2件は「値の直書き」だったが、今回は**状態（課金が有効か）の直書き**。値と違って定数化では防げず、誰かが思い出すまで嘘をつき続ける → **状態に依存する対外文言は、その状態を実際に決めている変数から導出する。LPはサーバーコンポーネントなので `!!process.env.STRIPE_SECRET_KEY` を直接読める**（`app/page.tsx`）。実装/設定が進んだら文言も自動で追従する形にしておくこと。**【昇格済 2026-07-28 /kaizen】絶対ルール化＋定型句の機械検出**。DECISIONS「現在の前提・絶対ルール」に載せ、`npm run check:i18n` が Coming soon / not implemented / 準備中 / 未実装 等を検出する（i18n化で文言が辞書に移るため `lib/i18n/*.ts` も走査対象）。静的な事実なら `state-ok` と導出元を書く。導入時に検出された2件は `lp.comingSoon` で、`billingLive`（`!!process.env.STRIPE_SECRET_KEY`）から分岐する**正しい実装**だったため注記で通した。
- 2026-07-21 | LPフッターに固定3D背景（HeroCanvas）が透けて雑然と見える（ユーザー報告）→ 3Dヒーローがページ全体の背後に常駐する構成なのに `.footer` に background 指定がなく透明で、フィックスされた3Dシーン（額縁/床）がフッターを透過していた（`::before` の継ぎ目グラデは `var(--bg)` へ向かうのに本体が透明） → **固定3D/canvasが背後に常駐するページでは、前面の各セクション・フッターに不透明背景（`var(--bg)`）を必ず与える。透明だと3Dが透ける。追加後は `getComputedStyle(el).backgroundColor` で不透明を確認できる**（`app/landing.css` の `.footer`）
- 2026-07-21 | 埋め込み機能: ダッシュボードのEmbedボタンが生成する `/@name?embed=1` が埋め込みモードにならず、iframe内でもフルHUD（Start free CTA等）が出ていた → 同じ機能に2つのルート形（`/@name` ハンドル直下 と `/@name/[slug]` 個別展示）があり、embed対応が slug 側にしか入っておらず、生成URLが指す**ハンドル直下側が `searchParams` を未処理** → **自分でURL/ディープリンクを生成する機能では、受け側ルートがそのクエリ/パラメータを実際に処理するか必ず確認する。同一機能が複数ルート形を持つときは全ルートで同じparam対応を揃える。UI確認はプレビューでHUDの差（省略/バックリンク化）まで見る**（`app/[handle]/page.tsx` と `app/[handle]/[slug]/page.tsx`）
- 2026-07-21 | ⓘツールチップ実装（§11.29）で入力欄がアクセシブルネームを失う回帰（マージ前レビューで検出） → `<label>`内で入力欄より前に `<button>`（=labelable要素）を置くと、labelが「最初のlabelableな子孫」＝ボタンに関連付き、入力欄からラベルが外れる（ラベルクリックもボタンにフォーカス） → **`<label>`でラップするフォームでは、ラベル装飾に置く操作要素（ⓘ/ヘルプ等）に labelable 要素（button/input/select/textarea/meter/output/progress）を使わない。tooltipトリガーは `<span tabIndex={0}>`（labelableでないのでlabelは入力欄に付く。CSS `:focus`/`:focus-within` でキーボード表示も維持）。疑わしければ `label.control` が入力欄を指すか実DOMで確認する**（`app/me/page.tsx` の `FieldLabel`）
- 2026-07-17 | ダッシュボード設定画面のUX改善で、密度/スクロールの表層問題だけ見て「Room/This-workスコープ切替」を実装 → ユーザー「50点・微妙」。真因は**情報設計の粒度**（「Roomの中に作品がある」のに作品カルーセルが親Roomより上にあり入れ子が破綻。トグルは別の抽象を足しただけ）→ **UIを直す前に「概念の包含関係(親⊃子: Room⊃作品)」を先に確定し、DOM/視覚の並び順をそれに一致させる。表層の症状(スクロールが長い/密度が高い)に対症療法(トグルで隠す等)を足すと粒度がずれて逆に悪化する。採用形は we-right を The room→Design Tools→Works in this room(カルーセル)→選択作品 の親⊃子一本フロー**（`app/me/page.tsx`）

- 2026-07-22 | ガイドツアーが作品の音声読み上げを最後まで再生せず次の作品へ進む（ユーザー報告）→ `useTour`が音声の長さと無関係に固定6.2秒で自動送りしていた（TTS化で音声が6.2秒を超え顕在化） → **「自動再生＋自動送り」を組み合わせる体験では、送り間隔を固定にせず『最低尺 + 再生完了待ち（暴走防止の上限つき）』にする。完了は再生singletonの状態購読で検知（`audioGuide.subscribe`で`playing`→falseを待つ）。ただし録画ツアーはcanvas映像のみで音声がファイルに入らないため待つ意味がなく固定尺のまま＝ライブと録画で送りロジックを分ける（`store.tourRecording`フラグ）**（`components/gallery/GalleryApp.tsx` の `useTour`。3D＋実音声要のため実挙動は本番QA）

- 2026-07-22 | 既存の`<audio>`直再生をWebAudio（Convolverリバーブ）へ通す拡張。**落とし穴**: `AudioContext.createMediaElementSource()`はcrossOrigin未設定/CORS無しのソースだと無音（tainted）になる → **クロスオリジン音源をWebAudioで加工するなら`el.crossOrigin='anonymous'`をsrc設定前に立て、配信側のCORS（`access-control-allow-origin`）を`curl -H "Origin:..."`で事前確認する（Supabase Storage公開は`*`でOK）。ctx未構築（ユーザー未操作）時はグラフに繋がず素で再生にフォールバックする**（`lib/audio.ts` `connectGuide` / `lib/guide.ts`）

- 2026-07-23 | 額装作品の影が「フレームの輪郭だけ」で中抜けになる（ユーザー報告）→ Exhibitで`castShadow`が付いていたのは中抜き形状の**フレーム枠メッシュだけ**で、開口部を覆うマット面・アート面(`<mesh>`平面)は`castShadow`なし→枠の縁しか影を落とさなかった → **額装/合成オブジェクトの影は「一番外側の枠」ではなく開口部を埋める面（マット/アート/バッキング）に`castShadow`を付けて“塗りつぶしの影”にする。中空フレーム＋非castShadowの内側面はhollowシルエットになる。baked影（`shadowMap.autoUpdate=false`）でもcastShadow付与は次回bake（GallerySceneのneedsUpdate）で反映される**（`components/gallery/Exhibit.tsx` 額装分岐のmat/art plane）
  - **×2（2026-07-23 再発・真の完治）**: castShadowを付けても中抜けが再発（影が実際に効き始めた後にユーザー報告で発覚）→ threeの影パスは**カリングを自動反転**（FrontSide→BackSide）するため、**片面プレーンは光の方向へ影をそもそも落とせない**（castShadowだけでは不十分だった）→ **開口部を埋める片面プレーンには`shadowSide={THREE.DoubleSide}`を明示する**（受影しない面なのでacneの副作用なし）

- 2026-07-23 | 額装作品の壁への落ち影が「物理的には正しいのに弱くて見えない」問題を、実シャドウマップの調整（castShadow付与→radius→解像度）で3回追っても安定せず、ユーザーは参考写真並みの明確な影を要求 → 近接マウント（フレームが壁から数cm）＋スポット角度＋壁の明るさで、実影はどうしても淡くなる（PCFSoft・baked・低コントラスト） → **「参考写真のような見た目」を狙うUIでは、物理シャドウの微調整に固執せず“アートディレクションされた偽ソフト影”（壁面に置くブラー済みの暗い角丸プレーン。作品の背後・少し下・やや大きめ・opacity可変）を併用する。ライトモードに依存せず確実に出て、スマホ（AOオフ）でも効く。実影は接地/床用に残す**（`components/gallery/textures.ts` `getSoftShadowTexture` ＋ `Exhibit` のshadow plane。opacity/offset/sizeで調整可）

- 2026-07-23 | drei `PerformanceMonitor`のDPR自動降格が、シーン安定後でなく初期化直後に発動し高解像度が一度も出ない（プレビュー実測: 起動数秒でdpr2→降格） → 初期ロード（シャドウbake・テクスチャロード）のFPS低下や、タブ/ペイン非表示中のrAFスロットリングを「性能不足」と誤判定する → **FPSベースの自動降格は (1)ロード完了後にマウント、(2)`document.visibilityState==='visible'`のときだけ降格、(3)降格は段階ラダー（2→1.5→1.25）にする。復帰昇格は画質の行き来がチラつくので付けない**（`components/gallery/GalleryApp.tsx`）

- 2026-07-23 | 「読み込み直後だけ影が出てすぐ消える」（ユーザー報告）→ R3Fは`<Canvas>`のprops変更（`dpr`自動降格等）のたびに`configure()`を再実行し、**`shadows` prop未指定だと`gl.shadowMap.enabled=false`で上書きリセット**する。onCreatedで手動`enabled=true`していたのは初回のみで、数秒後のdpr降格で全実影が消えていた → **shadowMapの設定はonCreatedでなく`<Canvas shadows=...>`で宣言する（R3Fが管理するプロパティはpropで渡す）。なおthree r185は`PCFSoftShadowMap`廃止・PCFへ強制降格するので`'percentage'`を指定し、柔らかさはlightの`shadow-radius`で出す**（`components/gallery/GalleryApp.tsx`）
- 2026-07-23 | 白い写り込みの**最後の残存**はユーザーの観察「入室して左の壁だけ・アートの有無は無関係」＝左右非対称で確定 → シーンで唯一方向性を持つ**`RoomEnvironment`環境マップの疑似窓**（片側に高輝度の矩形光源があり、その方向を向いた壁・床だけがenvMap反射で白いシート状に光る）→ **envMapに`RoomEnvironment`を使うと方向性の白シミが出る。屋内ギャラリーはY軸回転対称の自作グラデーション（上=暖色光/下=暗）をequirect→PMREMで使う。金属（金縁等）はenv光で光るため輝度を落としすぎない（要視認調整）**（`textures.ts` `getNeutralEnvTexture` / `GalleryScene` / `Preview3D`）。「片側だけ・特定方向だけ」の症状はまずenvの非対称を疑う
- 2026-07-23 | 「床の白い写り込み」が数回の修正後も残存。決め手はユーザーの観察**「真正面から壁を見た時だけ出る・真横からは出ない」**＝視線依存 → 正体は**スポットライト光源の鏡面ハイライト**（半ツヤ床のGGXスペキュラ。目・光源・床の角度が揃う正面視だけで発生、強度54相当×複数灯で白飛び）→ **「特定視点でだけ出る白飛び」はまず光源スペキュラを疑う（光源トグルではなく「roughness/metalnessを一時的に1/0へ」で切り分けるのが速い）。展示床はroughness≥0.9・metalness 0が安全圏**（`Room.tsx` 反射床/clearcoat床）。なお切り分け中に`MeshReflectorMaterial`のdepthフェード（16bit深度・GPU依存）も除去済み
- 2026-07-23 | EffectComposerのMSAA有効化（チラつき対策）直後、光錐の縁に「点線状に光る楕円」が出現 → MSAAはシルエット端でvaryingを外挿するため`uv.y`が僅かに負になり、シェーダーの`pow(vAxis,1.7)`が**NaN**→加算ブレンドで白点化 → **カスタムシェーダーで`pow(x, 非整数)`する変数は必ず`clamp(x,0.,1.)`してから。MSAA導入時は端の外挿でシェーダー入力が範囲外に出る前提で見直す**（`components/gallery/LightCone.tsx`）

- 2026-07-24 | ピクチャーライト(overhead)で「作品上端が白飛び・光の幅が器具幅と不一致・影も見えない」（ユーザー報告。前日の減光×0.45でも未解決）→ 点光源スポットを実器具の位置（額上22cm・decay²）に置いたのが構造的原因: 距離の逆二乗で上端は中央の約15倍照度、円錐光は中央の丸い塊（横長チューブと不一致）、壁下方はpool急減で焼き込み影α(=遮蔽×pool比)がほぼゼロ化 → **近接の線光源(ピクチャーライト等)を1灯スポットで表すときは、発光点を実器具から切り離して後方へ引き（投射~1.5m）、コーン角を「額幅/距離」から算出して光幅を器具幅に一致させる。露出係数の微調整で直らない光量問題は、まず光源距離と減衰の構造を疑う**（`Exhibit.tsx` `exhibitLightRig`。光錐非表示のモード限定で発光点の分離が成立）
- 2026-07-24 | コンタクトハードニング導入で「くり抜かれ」が再発（ユーザー報告）→ RT読み出しで判明: 「遠い遮蔽物の影は環境光で薄まる」補正(×0.62)を**大きな遮蔽物の本影の内部にまで適用**していたため、輪郭(近接判定)より内側が薄くなる逆転が発生 → **本影(umbra)の内部は最も暗い場所。距離による「薄まり」表現は半影の拡幅だけに担わせ、濃度補正はほぼ無効(×0.92)にする。影の見え方の不具合は毎回RTのチャンネル読み出しで項別に数値切り分けする（今回も遮蔽は正常でdensity項が犯人だった）**（`WallShadowBaker.tsx`）
- 2026-07-23 | 焼き込み影が「まだら/くり抜かれ」状態（ユーザー報告×2。RT読み出しで遮蔽値が16〜76の斑点と判明）→ **バイアス(0.0025)が近接キャスターの深度差とほぼ同値**だった。影パスは裏面カリング反転のため額なしキャンバスは壁から3.8cmの「背面」しか深度に入らず、深度差0.0025 ≒ バイアス → 比較が五分五分のノイズに → **①受光面（壁）が深度マップに入らない構成では自己影アクネが起きないのでバイアスは小さく(0.0008)②近接キャスターの箱/プレーンには`shadowSide=DoubleSide`で光に近い前面を深度に採用させる。「影がまだら・実行毎に変わる」はバイアス≒深度差を疑い、RT読み出しで遮蔽チャンネルを直接数値確認する**（`WallShadowBaker.tsx`/`Exhibit.tsx`。デバッグ: `__bakedRTs`＋rgbに遮蔽/照度比を格納済み）
- 2026-07-23 | 焼き込み影が「額の周囲に全高の黒帯」として見える（ユーザー報告「焼き込みはされてるが正しくない」）→ 遮蔽(occlusion)だけを一様な黒αで重ねていたのが原因。**本物の影は「その光源の寄与分」しか暗くできない＝光だまりの外では影は見えない** → **ベイク時にスポットの相対照度（コーン減衰×入射角×距離²、ターゲットで正規化）を掛け、α = 遮蔽 × pool/(pool+ambient) にする。「影のデカール」を作るときは必ず光源寄与でマスクする**（`WallShadowBaker.tsx`）
- 2026-07-23 | シャドウマップの自前サンプリング実装が「ceilingモードでは偶然それらしく見える」状態で通りかけた（レビューで検出）→ three **r185は影の深度をRGBAパックでなく`shadow.map.depthTexture`（ネイティブ深度・compareFunction=LessEqual）に格納**しており、カラー添付は1-zのグレースケール。`unpackRGBAToDepth`は無意味な値を返す → **r185+で影を自前サンプルするなら GLSL3 + `sampler2DShadow` で depthTexture をハードウェア比較する。「片方のモードでだけ動いて見える」実装はパッキング仕様の取り違えを疑う。バージョン依存の内部仕様(WebGLShadowMap)はnode_modulesの実ソースで確認する**（`WallShadowBaker.tsx`）
- 2026-07-27 | 作品ごと音声ガイドのアップロードUIをサーバー側TTSへ置き換えた際（5f7aade）、UIだけ消して裏の書き込み経路（`uploadArtworkAudio`・`AUDIO_GUIDE_MAX_BYTES`・`updateArtworkDetails`の`audioUrl`フィールド）が10日以上残置。呼び出し元ゼロなのでtsc/buildは通り気付けなかった。さらに**R2移行(dcc38cf)がその死んだ関数を丁寧に移植**し、`/api/upload-url`の`'artwork-audio'`ルール・`lib/limits.ts`の定数まで新設して死角が増殖した → 機能撤去のとき「画面から消す」で完了扱いにし、後続の大規模移行も既存コードを疑わず機械的に追随した → **UI/機能を撤去したら、その機能専用のlibヘルパー・定数・サーバー側ルール・API引数まで同じコミットで消す。撤去前に `grep -rn '<関数名>' --include='*.ts*' . | grep -v node_modules` で呼び出し元を数え、0なら消す。大規模移行では移植前に各関数の呼び出し元を数え、0のものは移植せず削除する。読み取り側（既存データを再生/表示する経路）は書き込み側と分けて判断する（今回は`audio_url`カラムと`lib/guide.ts`の再生経路は残置が正解。R2の削除はプレフィックス単位なので既存guideオブジェクトの掃除も従来通り効く）**（`lib/cloud.ts`/`app/api/upload-url/route.ts`/`lib/limits.ts`。孤児残置は 2026-07-21 の旧glb残置と合わせ×2）

### 決済（Stripe）
- 2026-07-27 | 本番でcheckoutが502（実カード投入前・セッション作成で失敗）→ Stripeログにエラー: 「the product tax code is missing. **Managed Payments**, which is enabled by default on your account, requires an eligible tax code」→ 新しめのStripeアカウントは**Managed Payments（Stripeが販売者=MoRとして世界の税を代行）が既定でオン**で、`price_data.product_data`に`tax_code`が無いと400 → **`product_data.tax_code`を付与**（デジタルサービスは`txcd_10000000`）。回避策は`managed_payments[enabled]=false`でも可。**502の切り分けはStripeダッシュボードのDevelopers→Logsのレスポンス本文が最短**（うちのcatchは詳細を握り潰し502に丸めるため）。

### 品質・レビュー（課金）
- 2026-07-24 | スロット従量課金の導入で、checkoutは「セッション作成時のwork_cap」で残枠をクランプするが、加算RPC(`record_capacity_purchase`)が上限を持たず加算のみ → **同一部屋への並行チェックアウトが両方通ると work_cap が最大(15)を超え得る**（別視点レビューで検出）。超過分は「飾れないのに課金」＋作品がどこにも表示されない不整合 → **原子的な上限クランプは"最終の書き込み地点"(RPC/SQL)に置く。アプリ側の読み取り時クランプは並行実行で破れる**。`update ... set work_cap = least(work_cap + n, MAX)`（migration 0028）。金額はサーバー計算・数量はクライアント値を`min(want, remaining)`で必ず絞る。

### 品質・レビュー（3D/R3F）
- 2026-07-24 | 3Dプレビュー実装をship前レビューで検出3件 → ①`new THREE.TextureLoader().load()`で作った動画ポスターtextureに破棄処理が無くリーク（開閉ごとにGPU texture残留）②動画作品でposter無しだと`getArtTexture`が`.webm`を`TextureLoader`で読んで空テクスチャ③`useMemo`のdepsに`applyMat`が毎回返す新規`frameDef`オブジェクトを入れており、オーバーレイ表示中の親再描画のたびExtrudeGeometryを再生成 → **①自前でnewしたthreeリソースは必ずunmountで`.dispose()`（共有キャッシュ由来=`getArtTexture`は破棄しない、を`useRef`で区別）②画像を要求するAPIには画像URLだけ渡す（動画srcを渡さない・poster無しはmap無し＋暗色fallback）③useMemoのdepsは毎回新規生成されるオブジェクトでなくプリミティブ値（`frameDef.bar`/`.gap`）にする**（`components/gallery/ArtworkPreview3D.tsx`）

### 品質・レビュー（モバイル/縦画面）
- 2026-07-27 | UX/UI/法務レビューで、電話幅のLPナビから主要CTA「Start free」が画面外に21〜36pxはみ出して押せないのを実測検出。CSSには「Sign in と CTA は残す(導線を消さない)」という**意図のコメントが書いてあるのに結果が逆**だった（900pxのブロックでアンカーだけ隠し、ロゴ+Explore/Guides+ボタン2つが横に収まるかは未検証） → **「消さない」と書いた導線こそ、一番狭い実幅（320/360/375/390）で `getBoundingClientRect().right > innerWidth` を実測する。`overflow-x:hidden` があると document.scrollWidth は正常値を返すので、ページ幅では検出できない**
- 2026-07-27 | 同レビューで、3Dギャラリーの `fov: 60` が固定のため縦持ちの横視野が iPhone 29.9° / iPad 46.8°（ノートPCは85.5°）まで潰れ、来場者の大半が部屋の1/3しか見ていなかった。**LPヒーローだけは `PORTRAIT ? 66 : 48` と対応済み**で、本体に反映されていなかった → **three の `fov` は縦方向の角度。縦画面のことは「横視野 = 2·atan(tan(fov/2)·aspect)」で必ず数値化して確認する。既にプロダクト内に縦対応の前例があるなら、同じ考え方が本体に入っているか横展開を確認する**（`components/gallery/GalleryApp.tsx` の `AdaptiveFov`）
- 2026-07-27 | 同レビューで、ローディング画面が `document.fonts.ready`＋タイマーだけで約2秒後に必ず閉じており、テクスチャ/GLB/作品画像を待っていなかった（本番の実機で「真っ黒な部屋＋HUD」を観測。SNS流入の初見が見る画面） → **「読み込み中」の表示を閉じる条件に、実際に読み込んでいる対象を入れる。タイマーは最低尺と上限（スタック対策）に使うもので、完了判定に使わない**（drei `useProgress` は three の default loading manager を見るので `TextureLoader`/`useGLTF` 経由のアセットを拾える）

- 2026-07-29 | ユーザーが実機で細かい崩れを5件指摘（①ボタンの文字が箱の中心より上 ②作品パネルで「›」と閉じるが重なる ③管理者の上部3ボタンが窮屈 ④照明行だけ見出しと選択肢が同じ行 ⑤音源アップロードの見出し〜ボタンが空きすぎ）。調べると原因は**3つの型**に収まり、どれも「宣言は正しいが、隣に置いたものとの関係を実測していない」だった → ①**44px化を箱にだけ効かせた**: `min-height` は増やしたが中身の縦位置はブラウザ任せで、UAが中身を縦中央に置くのは `<button>` だけ。同じ `.btn-line` を着せた `<a>`/`<label>` は文字が上に寄り（実測3.4px）、`line-height` も本文の1.8を継いで4px背が高かった（38px対34px） ②**絶対配置の上に、後から行が生えた**: `.panel-close` は行の外にいるつもりの絶対配置だが、あとから `.panel-nav` が同じ座標に入り、「›」(34px)が閉じる(44px)の矩形にほぼ丸ごと内包されて**次の作品へ進むタップが閉じるに奪われていた**（実測 34x31px の重なり） ③**flex nowrap の行に後から幅のある部品を足した**: フッターに言語切替(166px)が入った結果、375pxで残りのリンクが min-content まで潰され「ガ/イ/ド」と縦1文字組み・高さ148pxになっていた（ユーザー指摘には無く、この調査で発見） → **回避策: (a) タップ対象を広げるときは「箱」と「中身」をセットで直す（`display:inline-flex; align-items:center; justify-content:center` と `line-height: normal`）。`min-height` だけ足した差分は要素の種類（button / a / label）ごとに文字の位置を測る (b) 絶対配置の要素を足す・動かすときは、同じ領域に来る他要素と矩形交差を実測する（`getBoundingClientRect()` で `overlap = min(right)-max(left) > 0 && min(bottom)-max(top) > 0`） (c) flexの行に部品を足したら、最も狭い実幅で各項目の `width` と行数を測る。`overflow` が出ないことは「潰れていない」ことの証明にならない — 潰れる側は縦に伸びるだけで横にはみ出さない**
- 2026-07-29 | ↑の①は1箇所直しても再発しやすいので、**ページ内の全ボタンを機械で洗う小スクリプトを使った**（`button, a, label, [role=button]` を走査し、テキストノードの矩形の中心と箱の中心のズレが2.5pxを超えるものを報告。絶対配置の子＝値札バッジ等は箱の外に出るので除外し、`pointer: coarse` のメディアクエリ本体は CSSOM から拾って素のルールとして再注入する）。最初は絶対配置の子を除外しておらず、健全なチップを7件も誤検知した → **「同じ型の崩れが他にもあるか」は目視で探さず、その型を数式にして全要素に当てる。ただし誤検知が出たら閾値ではなく計測方法（何を含めて測っているか）を疑う**

### 品質・レビュー（CSSが「書いてあるのに効いていない」） ×5
> ×5 の内訳: ×4 は機械化（`npm run check:css`）の引き金になった人的ミス。**×5 は機械化した後にすり抜けた1件**（`#stage`。番人がIDセレクタを検査していなかった）。機械を入れても「機械が何を見ていないか」を書き残さなければ同じ穴が空く。
- 2026-07-27 | ①LPナビ: 「Sign in と CTA は残す(導線を消さない)」とコメントされたブロックで、実際は主要CTAが画面外にはみ出していた ②`viewport-fit=cover` 未設定のため `app/gallery.css` のノッチ/ホームバー回避**14箇所が全て無効**（iOSは cover が無いと `env(safe-area-inset-*)` を0に解決する）③`.hud-actions` / `.hud-record` は**マークアップに存在しないクラス名**で、640px以下のHUD再配置が一度も適用されていなかった（実体は `.hud-cluster`。リネーム時にCSS側が取り残された） → 3件とも「宣言は存在し、構文も正しく、しかし効果はゼロ」。CSSは効かなくてもエラーを出さないので、tsc もビルドも素通りする → **CSSの意図は、宣言があることではなく"結果"で確認する。①レイアウト意図は `getBoundingClientRect()` で実測（`overflow-x:hidden` があると `document.scrollWidth` は正常値を返すので、ページ幅では検出できない）②`env()` を使ったら、それを有効化する前提（`viewport-fit=cover`）が揃っているか確認する ③クラス名をリネームしたら `grep -rn '<旧クラス名>' app components` でCSS側の取り残しを確認する**
- 2026-07-28 | タップ対象を44pxに広げるCSSを書いた際、`.place-map-wrap` / `.works-cell` / `.hud-icon` の**3クラスがどれもマークアップに存在しなかった**（実体は `.place-editor` / `.my-works button` / `.hud-action`）。さらに `.panel-close` は既に `@media (max-width: 640px)` で44pxになっており、私の「40pxを修正」は**電話では重複、効くのはタブレットだけ**だった → **適用前に必ず①追加ブロックのクラス名を `grep -rn "<class>" --include=*.tsx app components` で全数照合する ②同じセレクタに既存のメディアクエリが無いかをCSSOMで確認する（`r.style.width` と `matchMedia(条件).matches` を並べて見る）。「基底値がいくらか」を見ずに差分を書くと、直っていないものを直したと報告してしまう**
- **【昇格済 2026-07-28 /kaizen】機械化を採用**（`npm run check:css`）。マークアップに存在しないクラスへのCSSを検出する。チェックリスト（ship 2.6）は×3〜×4を止められなかったが、同日 `check:i18n` が目視で漏れた29箇所を出した実績から「機械 > 手順書」と判断。既知の死んだCSS90件は `scripts/css-dead-baseline.json` にラチェットとして固定（**減らす方向にしか変えない**）。動的にクラス名を組む箇所を巻き込む危険があるため一括削除はせず、掃除は別タスク。DECISIONS「現在の前提・絶対ルール」にも昇格。
- **【掃除完了 2026-07-28】基準線の90件を個別確認して削除し、`css-dead-baseline.json` は空配列（＝ラチェットの目標値0）になった**。危険視していた「動的にクラス名を組む箇所」は、リポジトリ全体で **1箇所だけ** だった（`AdminDashboard.tsx` の `` `report-status is-${r.status}` ``）。判定方法: `className={\`...\`}` の全用例を洗い出し、**「静的プレフィックス + 空白 + 状態語」以外の組み立て**（`-\$\{`／`\}[a-zA-Z-]`／`+` 連結）を機械で探す。前者は grep が語として拾えるので誤検知にならず、後者だけが危険 → **「テンプレート文字列でクラス名を組んでいるかも」という不安は、パターンで数えれば有限個に落ちる。90件を怖がって温存するより、1つの実在パターンを見つけて `css-ok` を1箇所書く方が安い**。削除の安全性は、実ブラウザで削除した47セレクタすべてを `querySelectorAll(sel).length === 0` で確認して裏付けた（`/` `/demo` `/articles` `/terms`）。
- 2026-07-28 | ↑の掃除でも取り切れず、**`check:css` が見ていない領域（IDセレクタ）に死んだCSSが残っていた**。`app/gallery.css` の `#stage` / `#stage.dragging` は `id="stage"` の要素がリポジトリに一度も存在せず（実体は `.stage-root`）、`/demo` 実機でも `document.querySelectorAll('#stage').length === 0`。掃除した側もこれに気づいて STATE に「検査対象外」と書き残していたが、**番人が見ていない領域は、記録に残しても次の人が踏む** → 番人を書くときに「何を見ていないか」を宣言していなかったのが原因。**回避策: IDセレクタも照合対象に加えた。ただし判定はクラスと同じ「TS/TSX内のどこかに語として現れるか」では機能しない**（`#stage` は無関係な `.stage-root`・英文の "stage"・JSの状態フィールド `s.stage` に当たって生き残る）。IDは `className` と違って `id=` 属性・aria の参照（`htmlFor`/`aria-labelledby` 等）・`#foo` フラグメント・`getElementById` という限られた形でしか付かないので、**その文脈だけを見る**（空白区切りの列挙とテンプレート文字列の前方一致は許す）。新しくゲートを足すときは、対象外にした構文を必ずコメントに書き残す。
- 2026-07-28 | 掃除で90件のCSSを消した `main` の上に、別セッションの未pushコミット（レールのマークアップを書き換えるもの）をcherry-pick する場面で気づいたこと: **`check:css` は「CSSがあるのにマークアップが無い」片方向しか見ていない**。逆向き（**マークアップがあるのにCSSが無い**）は検出できないので、掃除が消したCSSを後から入るマークアップが使っていた場合、番人は何も言わずに見た目だけ壊れる。しかも掃除は「そのマークアップが存在しない木」の上で正しく行われているので、両方のコミットそれぞれでは全部グリーンになる → **削除系のコミットの上に、別の枝で書かれた変更を乗せるときは、「消したCSSのクラス名」を全部、乗せた後のマークアップに対して grep で突き合わせる**（`git show <掃除>^:scripts/css-dead-baseline.json` から名前を出して総当たり。今回48クラスで実行し、ヒットは404の6クラス＝別セッションがCSSごと同じコミットで戻した分だけだった）。**逆向きの検出（使われているクラスにCSSがあるか）は番人に入れていない — 命名の自由度が高すぎて誤検知が多くなるため、意図的に手作業にしている。**
- 2026-07-28 | 上のID対応を /code-review にかけたら、**番人の照合そのものに取りこぼしが3つ**見つかった（自分では気づけなかった）。①`if (!line.includes('{')) return` のため、**カンマ区切りで複数行に分かれたセレクタは最終行以外が一度も照合されない**（`.a,` 改行 `.b {` の `.a` が素通り。このリポジトリのCSSはカンマ列挙を多用しており、`#stage` が2行で書かれていたら今回も捕まえられなかった）→ `{` が来るまでセレクタ行を溜める形に変更。②`id={PANEL_ID}` や `id={open ? 'a' : 'b'}` のように**式で渡すIDを「死んでいる」と誤検知**し、正しい変更のpushを止める→ 「そのID名と完全一致するクォート済み文字列がTS内にあるか」を最後の砦に追加（`'stage'` は全リポジトリで0件なので `#stage` の検出は維持される）。③`color: #ff0000; } .live {` のように1行に前ルールの `}` が残ると**16進カラーをIDと誤読** → `}` より後ろだけをセレクタとして見る。 → **番人（lint/ゲート）を書いたら、番人自身を別視点レビューにかける。「検出できた1件」で満足すると、検出漏れの構文が残っていることに気づけない。検証は合成CSSを流し込んで「検出されるべきものが検出され、生きているものが素通りする」の両方を実行で確かめる**。

### 品質・レビュー（課金・法務）
- 2026-07-27 | 有料販売をしているのに **purchases に通貨列が無く**、webhook が `session.currency` を捨てていた。Stripe の Managed Payments / Adaptive Pricing は購入者に現地通貨で提示でき、その場合 `amount_total` は「その通貨の最小単位」で返る（¥500 も $5.00 も整数 500）。同じ列に混ざると後から分離できず、売上集計が静かに無意味になる → **金額を保存するときは通貨を必ず隣に置く。「今は1通貨だから」は理由にならない — 決済代行の設定ひとつで多通貨になり、しかも壊れたことに気づけない。購入0件のうちなら列追加だけで済むので、課金を開ける前が唯一の安全な修正タイミング**（migration 0031）
- 2026-07-27 | 同レビューで、規約・プライバシーに**事業者の特定・返金方針・年齢・準拠法・処理者の列挙が無い**まま有料販売が可能な状態だったことが判明。特に **Stripe と OpenAI という2つの処理者がプライバシーポリシーに一切書かれていなかった**（読み上げ機能がキャプション文をOpenAIへ送っている） → **機能を足したら、その機能が「誰にデータを渡すか」をプライバシーポリシーに反映する。外部APIの導入はコードだけでなく法務文書の変更でもある**

### 品質・レビュー（画像パイプライン）
- 2026-07-27 | 同レビューで、全アップロードが `fileToDataUrl`(JPEG q0.85) → `dataUrlToJpegBlob`(**同じ1600pxのまま** q0.85) と二重にJPEG化していたのを発見。2回目は縮小もしないので世代劣化を足すだけ。作品/アバター/ロゴ/LP画像/動画ポスターの全経路が同じ形だった。原因は「localStorage用にデータURL化する関数」を、クラウドアップロードの前処理としても流用したこと → **中間表現に非可逆形式（データURL=JPEG）を挟むと、後段のエンコードが必ず二重になる。アップロード経路では原本の File/Blob を最後まで持ち回り、デコード1回・エンコード1回にする。`fileToDataUrl` のような "保存先の都合で作った変換" を別用途に流用しない**（`lib/cloud.ts` の `decodeSource`/`encodeJpeg`。合成イラストで実測: 原画との平均誤差 0.673→0.457、サイズ 75KB→84KB）

### 品質・レビュー（UI文言）
- 2026-07-29 | 番人の穴 ×2。①`looksEnglish` が「英単語2語以上」を条件にしていたため、**ボタンの1語ラベルが全部素通り**していた（9言語を42%まで訳した後、実画面でパネルを開いて "Add" が英語のまま残っているのに気づいた。該当5件）。②翻訳カバレッジの計測が**行頭インデントでキーを数えていた**ため、入れ子グループを1行で書いた辞書（`layout: { hall: '…', … },`）の中身が数から漏れ、逆にグループ見出し（`layout: {`）を1件と数えていた。ja が 98% と出て翻訳漏れを疑ったが、計測の穴だった → **①検出条件に「1語ラベル」を足し、ブランド名・略語の誤検知が無いことまで自己テストに入れる（`check:gates`）②数を数えるときは「行の形」ではなく「値の形」で数える（`key: '…'` にマッチさせる）。指標そのものが壊れていると、直すべきものを直したかも分からなくなる**
- 2026-07-29 | 額の色と「額」という語を `{color} {frameWord}` と並べて組み立てたら、ドイツ語で `Gold Rahmen`・`Weiß Rahmen` という非文になった（形容詞の格変化）。フランス語では `Or cadre` と語順まで逆 → **単語を並べて文を作ってよいのは英語だけ。語を挟む文は、プレースホルダを持つ1つのキーにして語順を言語側に委ねる**（`presets.frameOf: '{name} frame'` → 独 `Rahmen: {name}` / 日 `{name}の額`）
- 2026-07-28 | i18n対応を「571キー・未定義0」と検証して完了報告した直後、機械的な検出器を作ったら**英語の直書きが29箇所残っていた**（LPのジャンル帯、購入モーダルの「Adds N more …」、空間を編集パネルの注記5件、ユーザー名ラベル、管理画面のサブエディタ12件ほか）。前回の網羅確認は「辞書に載っているキーが両言語にあるか」であって「画面の文言が辞書を通っているか」ではなかった → **i18nの網羅は「辞書側の整合」ではなく「呼び出し側に直書きが残っていないか」で測る。両者は別の指標で、前者が満点でも後者は0点になり得る。`npm run check:i18n` を必ず通す（AGENTS.md 5.1）**
- 2026-07-24 | 3Dプレビュー追加時、ボタン「空間で見る」とヒント「ドラッグで回転…」を日本語で書きユーザーが「急に日本語UI」と指摘 → このアプリのUIは**全面英語**（Read aloud / Available for purchase / Tour 等）なのに、対ユーザー応答が日本語というルールに引きずられUI文言まで日本語にした → **プロダクトのUI文言は既存の言語慣習に合わせる（このリポジトリは英語UI）。応答＝日本語 と UI文言＝英語 は別物。新規の可視テキストを足したら、周囲の既存文言と言語が揃っているか必ず確認する（`grep -rn '[ぁ-んァ-ヶ一-龠]' 変更ファイル` で混入検出）**（"View in 3D" / "Drag to rotate · scroll or pinch to zoom" に修正）

### 検証・ツール
- 2026-07-29 | push直前のローカル検証で `tsc` が3件のエラーで落ちた（`OwnedIds` に `frameIds` が無い）。自分の変更にもコミット済みの履歴にも心当たりが無く、原因は**別セッションが同じ作業ツリーで次の機能（額の販売）を実装中で、`lib/entitlements.ts` が未コミットのまま型が合っていなかった**こと。**作業ツリーは共有、コミットは別**という並行作業では、`npm run build` は「これから出すもの」ではなく「いま机の上にあるもの」を検証してしまう → **並行セッションがいる環境では、出荷前の検証は必ずコミット済みのHEADを別の場所に取り出して行う**（`git worktree add --detach <tmp> HEAD`。`node_modules` は**中身1階層ぶんのシンボリックリンクを張った実ディレクトリ**として作る — `node_modules` 自体をシンボリックリンクにすると `.gitignore` の `node_modules/` が末尾スラッシュのためディレクトリ扱いされず未追跡として残り、番人の untracked-guard が落ちる）。他人の未コミット変更を `git stash` で退避してはいけない。
- 2026-07-28 | 番人の自己テスト（`npm run check:gates`）を入れた直後に /code-review をかけたら、**自己テストが「10件すべて通過」と言いながら、番人の検出ルールを消しても緑のままだった**（4ルールで実証）。原因は、ケースを**過去に空いた4つの穴**に対してだけ書き、その穴が住んでいる**ルール本体**（属性の直書き検出／タグをまたぐJSXテキスト／CSSの複数行コメント処理／同一行の `}` 処理）を1つも守っていなかったこと → **自己テストは「直したバグ」ではなく「守りたい振る舞い」に対して書く。書いたら必ず変異テストで確かめる — 該当ルールをわざと壊して、自己テストが実際に落ちることを見る。落ちなければそのケースは飾り。** 今回は7種の変異すべてで落ちることを確認した。さらに、**合成入力が問題を再現していないケースが1つあった** — `.live { color: #ff0000; } #live-id { …` を1行で書いたが、`split('{')[0]` は最初の `{` より前を返すので16進カラーがそもそもセレクタ部分に入らず、変異させても落ちなかった（宣言が行頭から始まる2行の形にして再現）。**「落ちるはずのケースが落ちない」ときは、番人ではなく自分の入力を疑う。**
- 2026-07-28 | `prebuild` ガードを「listenしているプロセスの cwd がこの作業ツリーの中か」で判定するようにしたら、**逆向きの誤爆を作っていた**（レビューで実測）。Claude Code の worktree は `<main>/.claude/worktrees/<name>` と**メインチェックアウトの内側**に置かれるので、`cwd.startsWith(process.cwd() + '/')` だと「worktree で dev が動いているだけでメイン側の build が止まる」ことになる → **「同じ作業ツリーか」はパスの前方一致で判定してはいけない。`git -C <cwd> rev-parse --show-toplevel` を両方で取って一致を見る**（worktree はメインの内側にあってもルートが別なので正しく分かれる）。判定不能（`lsof` が無い等）のときは止める側に倒す。両方向を実測で確認した。
- 2026-07-28 | 1回の ship の最中に **`origin/main` が3回動き**（並行セッションが 掃除 → 台帳整理 → 404ページ を次々にpush）、そのたびに rebase → 衝突解消 → 検証やり直しが必要になった。`docs/STATE.md` は毎回衝突する（全セッションが同じ節を書くため）。さらに、あるセッションが**ローカルの `main` にコミットしたまま push し忘れており**、その1件（モバイル修正）は `origin/main` が別系統で3コミット進む間ずっと本番に出ていなかった（`git branch -r --contains <sha>` が空で判明）→ **①`git push` の直前に必ず `git fetch` し、`git merge-base --is-ancestor origin/main HEAD` で早送りできるかを確かめる（動いていたら rebase して**検証を頭からやり直す** — 別セッションの変更と自分の変更が組み合わさった状態は誰も検証していない）②台帳の衝突は「相手の構造を土台にして自分の項目を足す」形で解消し、相手が書いた文言は事実が変わった箇所以外いじらない③セッションを終える前に `git branch -r --contains HEAD` で自分のコミットがリモートに載ったかを確認する。ローカル `main` へのコミットは push しない限り他の誰にも見えず、`git worktree list` にも「進んでいる」ようにしか映らない。**
  - **付記（2026-07-28）**: worktree は**refストアを共有する**ので、`refs/remotes/origin/main` は**自分が fetch していなくても他セッションの fetch で書き換わる**。「`git fetch` して 90f68fb を確認 → `git rebase origin/main`」の間に別セッションが fetch し、実際には1つ先（b2e18bd）に乗った。害は無かったが、rebase 先は思っているものと違いうる → **rebase の直後に `git log --oneline -3` で実際の親を確認する。検証はその「実際に乗った base」の上でやり直す（今回は結果的にそうなっていた）。**
- 2026-07-28 | worktreeで404ページの作業を始めた際、`docs/STATE.md` と `app/landing.css` と基準線を**ローカルの内容だけ**で読み、タスク指示の前提（「`.notfound*` は削除済み」「基準線は空配列」）を**「リポジトリと違う。前提が2点間違っている」とユーザーに断定報告した**。実際は指示が正しく、こちらが間違っていた。真因は **`git fetch` を一度もしていなかった**こと — ローカル `main` は remote `main` より**2コミット古く**、掃除（`a15237c`）は既にmerge済み・本番反映済みだった。そのうえ remote の STATE.md には「**`a15237c` を取り込んでから作業すること**」と明記されていた（＝この作業自体がそのセッションから起票されたもの）。さらに悪いことに、古いbaseで実装・検証・コミットまで通してしまい、`check:css` も `check:i18n` も build も**全部グリーンだった**（古い世界の中では整合していたため、番人は誰も気づけない） → **セッション開始時、STATE.md を読む前に `git fetch origin` し、`git log --oneline HEAD..origin/main` が空かを確認する。worktree は自分のブランチしか見ないので、他セッションが進めた main はfetchするまで見えず、ローカル `main` は「最後に誰かがpullした時点」で止まっている。`git worktree list` で並行セッションを、`git diff --name-only main...<他ブランチ>` で触っているファイルを必ず見る。そして最重要: **与えられた前提がリポジトリと食い違って見えたら、前提を疑う前に自分のbaseが古いことを疑う**（`git ls-remote --heads origin main` と `git rev-parse main` の比較で1往復で分かる）。番人が全部通ることは「baseが正しい」ことの証明にはならない。**
- 2026-07-28 | LPの下部セクションを確認しようと `window.scrollTo()` してスクリーンショットを撮ったら**真っ黒な画面**が返り、CSS削除の回帰かと疑った。実際は `elementFromPoint()` が見出しの `<em>` を返し、`opacity:1`・`getBoundingClientRect().top:0` で**DOM上は正常**だった。`git stash` で変更前のCSSに戻して同じ座標を撮っても**同じ真っ黒**＝プレビューペインの撮影側の問題（固定WebGLキャンバスがあるページで、JSスクロール後の合成結果が撮影に反映されない。`computer` の scroll/click も "pane is hidden" でタイムアウトする） → **プレビューのスクリーンショットが期待と違ったら、まず「変更前でも同じか」を `git stash` で1回確かめる。撮影は初期表示（scrollY=0）だけ信用し、下部の検証は DOM側の事実（`querySelectorAll().length` / `getBoundingClientRect()` / `getComputedStyle()`）で行う**。「消したCSSが効いていないこと」の証明は、削除した全セレクタを実ブラウザで `querySelectorAll(sel).length === 0` と数える方がピクセル比較より強い。
- 2026-07-28 | 404ページ（新規ファイル）を足して `npm run check:css` / `check:i18n` を回したら、その新規ファイルの内容が**まるごと検査されていなかった**。真因は両スクリプトが照合対象を **`git ls-files` で集めている**こと — 新規ファイルは `git add` するまで**未追跡＝検査対象に存在しない**。`git add` した瞬間に検査ファイル数が 67→68 に増え、正しい結果が出た → **新規ファイルを含む変更では、番人を回す前に `git add` する（`git status --short` に `??` が無いことを確認）。この2つの番人は「gitに登録されたもの」しか見ないので、未追跡ファイル内の英語直書きも効いていないCSSも検出されない — 新規ファイルほど検出をすり抜けるという逆転が起きる。検査ファイル数が前回より増えているかを毎回見るのが一番安い検算。** ※検出器の対象ファイル集合や検査範囲が想定とズレて嘘の結果を出したのは**×4**（①pathspec `**` で `app/page.tsx` が漏れた ②`accept="image/*"` をコメント開始と誤認し270行抜けた ③未追跡ファイルを見ていない ④`check:css` がIDセレクタとカンマ列挙の非最終行を見ていない）。→ **【昇格済 2026-07-28 /kaizen】`npm run check:gates` として機械化**（`scripts/check-gates.mjs`）。一時gitリポジトリに合成入力を書いて本番の番人をそのまま走らせ、**落ちるべき入力が落ちること**と**生きているものを誤検知しないこと**の両方を10ケースで検査する。上の4穴すべてをケース化した。あわせて未追跡ファイルは番人自身が落ちるようにした（`scripts/untracked-guard.mjs`）。**空振りでないことは変異テストで確認済み** — `check-css.mjs` からIDセレクタのループを消すと2件、カンマ列挙の持ち越しを消すと2件、それぞれ自己テストが落ちる。なお自己テストを書いた最初の版は「出力全文に生きているクラス名が含まれるか」で誤検知を判定していて、`←` の後ろにエコーされる元セレクタに反応して**自分の判定式の甘さで落ちた**（報告行の名前だけを見るよう直した）。番人の番人にも同じ罠がある。
- 2026-07-28 | /kaizen で kaizen スキル自体のローカル（`~/.claude/skills/kaizen`）とリポジトリ（`.claude/skills/kaizen`）に差分を発見（リポジトリ側が新しく、昇格の選択肢C「Obsidian第二の脳へのwiki化」を持っている）。**ユーザー判断で今回は同期を見送った** → 同じ検討を繰り返さないため記録に残す。ローカル実行の /kaizen ではwiki化の提案が出ない状態が続く。
- 2026-07-28 | 直書き検出スクリプトを作って「0件」になったので通したが、**その0件は検出器の穴によるものだった**。穴は2つ: ①`git ls-files "app/**/*.tsx"` は pathspec の `**` が1階層以上を要求するため **`app/page.tsx`（LP本体）にマッチせず**、トップレベル20ファイルがまるごと検査対象外（対象47→67ファイル） ②`accept="image/*,video/mp4"` の `/*` をブロックコメントの開始と誤認し、`*/` が現れるまで**約270行が検査から抜けていた** → **「守るための仕組み」を作ったら、その仕組み自体が本当に見ているかを別の手段で確かめる。今回は"実画面のテキストを日本語表示で走査する"という独立した方法で穴に気づいた。検査対象のファイル数を必ず出力させ、想定と合うか見る。コメントや文字列の境界を扱うときは、文字列リテラルを先に伏せ字にしてから境界を探す**
- 2026-07-28 | `@media (pointer: coarse)` のタップ対象を検証しようとしたが、**プレビューのブラウザペインは coarse pointer をエミュレートしない**（`resize_window` を mobile にしても `matchMedia("(pointer: coarse)").matches === false`）。そのため書いたルールが一行も適用されず、実測できなかった → **読み込み済みの `document.styleSheets` から該当 `CSSMediaRule` の `cssRules` を取り出し、メディア指定を外して `<style>` に再注入してから測る。手打ちでコピーせず"出荷される宣言そのもの"を使うのが要点で、`style.disabled` の切替でbefore/afterを1回の実行で取れる。認証が要る画面（`/me`）がローカルで開けない場合は、同じクラス構成の合成DOMを `document.body` に足して測れば、カスケードの検証としては成立する**
- 2026-07-28 | 地図SVGを広げるつもりで `margin-inline: -1.2rem` だけを書いたら、**幅は変わらず左に19.2pxずれただけ**で右に隙間ができた（`width: 100%` の基準は親の内容幅で、負のマージンでは変わらない）→ **要素を親のpaddingぶん広げるときは `margin-inline: -X` と `width: calc(100% + 2X)` を必ず対で書く。左右のはみ出し量を両方測って対称かを確認する**
- 2026-07-28 | HUDの不透明度変更を検証中、`getComputedStyle(el).opacity` が**インラインで `opacity:1` を入れても古い値を返し続けた** ×2。マッチするCSSルールを列挙しても矛盾する結果になり、原因究明に多くの往復を費やした。真因は**プレビューのブラウザペインがスタイル再計算を止めていた**こと（ペインが前面に無いとレイアウト/スタイルの更新が走らない） → **プレビューでの見た目の検証は `getComputedStyle` を信用しない。`computer{action:'screenshot'}` は描画を強制するので、視覚的な状態は必ずスクリーンショットで判定する。`getBoundingClientRect()` は比較的信頼できたが、値が理屈と食い違ったら真っ先にこれを疑う**
  - **×2（2026-07-28 再発）**: 入場ガイドの検証で `getComputedStyle` が `flex-wrap: wrap` / `border-radius: 16px` を返し続けたが、**同時に測った矩形は「3行が全部同じ top」＝nowrap が効いている**ことを示しており、スクリーンショットでもピル型1行だった。→ **矛盾したときの優先順位は スクリーンショット ＞ `getBoundingClientRect` ＞ `getComputedStyle`。「宣言が当たっているか」を computed style で確かめようとせず、レイアウトの結果（行が折り返しているか＝各行の top が同じか）で判定する**。
- 2026-07-28 | WebGLコンテキスト消失のリスナーが、**登録場所を3回変えても一度も発火しなかった**（親コンポーネントの effect → `onCreated` → Canvas内の `useThree`）。切り分けで判明したこと: ①親の effect は R3F がキャンバスを生成する前に走るので `canvasRef.current` が null ②**`onCreated` はこの構成では実行されていない**（設定するはずの `gl.domElement.style.touchAction` が実キャンバスで未設定だった。CSS側にも同じ指定があるため誰も気づいていなかった）③Canvas内の `useThree` コンポーネントにもイベントが届かなかった。一方 `document.querySelector('canvas')` に直接付けた素のリスナーは**確実に発火する** → **R3Fのライフサイクルに乗せたDOMイベント登録が効かないときは、登録できたかを"副作用の実測"で確かめる（今回は touchAction の有無）。動かない抽象を積み重ねるより、確実に発火する経路（DOM直取り＋短いポーリング）に降りる方が速い。なぜその経路にしたかはコメントに残し、次の人が同じ3経路を試さないようにする**（`components/gallery/GalleryApp.tsx`）
- 2026-07-28 | `git push` してもVercelの自動デプロイが起動せず、本番が古いままだった（`/robots.txt` が `[handle]` ルートに拾われる形で発覚）→ 既知パターン（LESSONS 2026-07-23）の再発 ×2 → **本番反映の確認は「pushしたから出たはず」で済ませず、必ず変更点の実物を叩いて確かめる。出ていなければ `npx vercel --prod` で手動デプロイ**
- 2026-07-27 | `git commit -m "$(cat <<'EOF' … EOF)"` でコミットしたら、本文中の `` `.hud-actions` `` 等のバッククォートがコマンド置換として実行され（`.hud-actions: command not found`）、**クラス名が全部消え末尾に `EOF )` が残ったメッセージがコミットされた**。ヒアドキュメントの区切りを `'EOF'` とクォートしても、`$( )` の中では旧式バッククォート置換が先に解釈される → **コミットメッセージにバッククォート（コード引用）を含めるときは `$(cat <<EOF)` を使わず、ファイルに書いて `git commit -F <file>` する。やってしまったら `git commit --amend -F <file>` で直せる（差分は無傷）**
- 2026-07-24 | JSXを複数ステップで編集中、preview(devサーバー)のconsoleに「Unexpected eof / 未閉じJSX」等の構文エラーが大量に残り、最終保存版は正常なのに壊れて見えた ×2 → dev serverは保存のたび途中状態を再コンパイルし、`read_console_messages`は**累積バッファ**を返すため、編集途中の一過性エラーが後から読んでも出てくる（存在しない行番号を指すのが目印） → **HMR中の編集後の健全性は「console error文字列」でなく①`tsc --noEmit`が通る②実画面が実際にレンダリングされる、で判断する。console errorが今のファイルに無い行番号を指していたら過去の保存の残骸と見なす**

### ストレージ・インフラ移行
- 2026-07-27 | Supabase Storage→R2移行で、レビューが「これを出したら作品が1枚も表示されない」級の抜けを3件検出（実害はレビューで止めたので本番未到達） → ①**CORS**: 3D展示は画像を`crossOrigin="anonymous"`で読む（three.jsの既定）ため`Access-Control-Allow-Origin`が必須。Supabase Storageは黙って返していたがR2はバケットCORSを入れるまで返さない ②**署名URLに空ボディのCRC32が混入**: `@aws-sdk/client-s3`の既定`requestChecksumCalculation:'WHEN_SUPPORTED'`が、ボディ未確定の署名時に`x-amz-checksum-crc32=AAAAAA==`（0バイトのCRC）を署名済みクエリに埋め込む。クライアントは除去できないので実データPUTが全部落ちる ③**移行SQLがトリガを起こす**: `galleries`の`updated_at`自動更新トリガが発火し、URL書き換えだけで全ギャラリーが/exploreの「最近更新」先頭に浮上 → **ストレージ提供元を替えるときは「今の提供元が暗黙に返していたヘッダ」を洗い出す（CORS/Content-Type/Cache-Control）。presignerは`requestChecksumCalculation:'WHEN_REQUIRED'`にし、生成した署名URLのクエリを実際に出力して目視する。一括UPDATEの前に対象テーブルのトリガを`\d+`相当で確認し、必要なら`disable trigger`で囲む**
- 2026-07-27 | **R2移行後、3D展示の作品が空の額縁になった（ユーザー報告）。2Dのサムネイルは正常に出るのに3Dテクスチャだけ出ない、という切り分けが決定的だった** → 原因は**ブラウザキャッシュのCORS汚染**。R2は`Origin`ヘッダが付いたリクエストにしか`Access-Control-Allow-Origin`を返さない（Supabase Storageは無条件に返していた）。かつ**Originなしの応答には`Vary: Origin`も付かない**ため、`crossOrigin`なしの`<img>`が先に取得したCORSヘッダ無しの応答が、後続のWebGLテクスチャ用リクエスト（必ずCORS）にも再利用されて失敗する。実ブラウザで「①CORS→plain=両方成功／②plain→CORS=**CORSブロック**」を再現して確定 → **同じファイルを2Dの`<img>`と3Dテクスチャの両方で使う構成では、`<img>`側にも必ず`crossOrigin="anonymous"`を付ける**（全16箇所に適用。ルールは`lib/publicUrl.ts`冒頭に明記）。**教訓の本体は「提供元を替えたら、旧提供元が"無条件に"返していたヘッダを洗い出す」**（CORSヘッダの有無だけでなく、その`Vary`まで）。既に汚染されたキャッシュはスーパーリロードで解消する
  - **×2（2026-07-27・同日再発。ブラウザではなくCDNが汚染されていた）**: `<img>`全16箇所に`crossOrigin`を付けた後も、ユーザー環境で3枚とも空の額縁のまま（**シークレットウィンドウでも再発**＝ブラウザキャッシュではない）。しかも**私の環境では再現しなかった** → 汚染されていたのは**CloudflareのCDNキャッシュ**。CDNは1つのURLに実質1つの応答しか持たないため、(a)`Origin`なしの取得（クローラー/OGP/`curl`）が先に来ると**許可ヘッダ無しの応答**が入って全員が失敗する (b)オリジンごとに違う許可を返す設定（`www…`宛/apex宛）だと、**wwwで入ったキャッシュがapexの人に配られて**失敗する (c)**キャッシュは配信拠点ごとに別**なので**特定地域の人だけ壊れる**＝開発者環境では再現しない → **配信元の応答を「相手によって変えない」形にするのが本質的な対策**: R2バケットCORSの読み取りを`*`にし（応答が1種類になる）、さらにCloudflareのTransform Rule（Modify **Response** Header / Set static `Access-Control-Allow-Origin: *` / `http.host eq "cdn.xibit360.art"`）で**キャッシュの中身に関係なく出口でヘッダを付ける**。Transform RuleはHIT時にも適用されるので汚染済みキャッシュごと救える。**切り分けは`Origin`の有無で叩き分ける**（`curl -sI` と `curl -sI -H "Origin: …"`で両方に`*`が出れば正常）。注意: **素の`curl`は`Origin`なし＝汚染側のキャッシュを自分で作ってしまう**（調査中に実際にやった）。※Transform Ruleは**Request**側と**Response**側が別物。Requestに作っても何も起きない（一度間違えた）
  - **一般化**: 「自分の環境では再現しない」「シークレットウィンドウでも直らない」の2つが揃ったら、ブラウザではなく**CDN/中間キャッシュ**を疑う。そして**リクエストの内容（ヘッダ）によって応答が変わるリソースは、キャッシュと本質的に相性が悪い**。`Vary`は当てにしない（Cloudflareは`Accept-Encoding`以外を確実には尊重しない）
- 2026-07-27 | **RLSで「見える」ことを「所有している」と誤って扱った**（レビュー検出）。R2移行でアップロード先の`id`が本人のものか検証する`ownsRow`を追加したが、`select ... where id = ?`だけで判定しており、`galleries_select_public`(`using (is_public)`)と`artworks_select_in_public_gallery`(0001)により**ログイン中の誰でも他人の公開ギャラリーIDを「自分のもの」と判定できた**（/exploreからID列挙可能） → **サーバー側の認可判定では`owner_id`を明示的に突き合わせる。RLSの可視性は認可ではない**（公開読み取りポリシーがあるテーブルでは特に）。`.eq('owner_id', uid)`の1行が抜けただけで検証の目的が達成できていなかった
- 2026-07-27 | **Supabase SQL Editorに貼った移行SQLが「エラーも出ずに1行も適用されない」**。原因: SQL Editorは貼り付けたスクリプト全体を1トランザクションで実行するため、**途中1文の失敗で全体がロールバックする**。今回は`alter table ... disable trigger`（テーブル所有者権限が必要）が落ちて、DML部分もろとも巻き戻った。単文のUPDATEに切り出したら通ったのが決定的な切り分け → **SQL Editor向けの移行SQLはDMLのみで書く。`ALTER TABLE`等の権限を要する文を混ぜない（必要なら別途1文だけ実行して権限を確かめる）。適用後は必ず検証クエリで0件を確認し、「実行した」ではなく「結果が変わった」で判断する**
- 2026-07-27 | **R2移行で「削除＝実体を消す」だけを実装し、CDNキャッシュを消し忘れた**（移行の検証中に、削除済みオブジェクトが片方のcoloから200/`cf-cache-status: HIT`で返るのを実観測して発覚）。DB行もR2オブジェクトも即座に消えるのに、URLを知っている人は最大4時間ダウンロードできる状態が残っていた。原因はR2が全オブジェクトに`cache-control: max-age=14400`を付けて返すこと（アップロード時に`CacheControl`未指定のため既定値） → **オリジンの前にCDNを挟んだ時点で、削除は「実体の削除」＋「キャッシュの破棄」の2手順になる。片方だけでは`app/privacy`の「削除したら消えます」という約束が実態と食い違う** → 削除直後にCloudflareのパージAPIを呼ぶ実装を追加（`lib/cachePurge.ts`）。実装上の勘所は ①**URL指定でなくプレフィックス指定**（キー一覧が不要＋`?v=`付きURLにクエリを問わず届く。無料プランでも使える） ②**パージはオリジンから消した後**（先だと隙間のアクセスで再キャッシュされる） ③**ベストエフォート**（ファイルは既に消えているので、失敗＝従来の窓に戻るだけ。ユーザーの削除を失敗させない）。なお`max-age`は`s-maxage`と違い**閲覧者のブラウザにも効く**ので、パージで消えるのはCDN側だけという限界も併せて理解しておく
- 2026-07-27 | 同移行で、`NEXT_PUBLIC_`系の設定漏れが「壊れた相対URLをDBに永続化する」経路になっていた（レビュー検出） → `NEXT_PUBLIC_*`はビルド時埋め込みなので「Vercelに変数を足したが再デプロイしていない」だけで空になる。空ベースURLでも処理が続行し、`avatar_url`等に`/uid/avatar.jpg`が保存され、後で変数を直しても復旧しない → **外部URLを組んでDBに保存する経路は、ベースURL未設定を「設定不備」として書き込み自体を拒否する（501）。読み取り専用の劣化なら許容できても、永続化する値の生成は必ずfail-closedにする**

### 3Dアセット
- 2026-07-16 | 「使えるモデルを追加した」と渡された`walk.glb`/`idle.glb`が各約200MB(Web要件に3桁オーバー) → Blenderエクスポート時にKitBash3D「NeoCity」街並みキット(99%)が誤同梱され、実キャラは約2MBだった → **取得した3Dアセットは使う前に必ず中身を検分する(`gltf-transform inspect`/glbのJSONチャンク解析でメッシュ名・skin有無・テクスチャ解像度・シーン構成を見る)。巨大化の一次対処は「圧縮」ではなく「不要コンテンツの除去」。skin付き(=キャラ)とstatic(=環境)を分けて実サイズを測ると原因が即分かる。仕上げに `gltf-transform` で街シーン破棄→テクスチャWebP縮小→Draco圧縮で200MB→1.6MB**
- 2026-07-16 | glTFキャラを多数インスタンス化したら全員T字ポーズで固まりアニメが効かない → skinメッシュを`scene.clone()`するとスケルトン(ボーン)参照が切れ、mixerが駆動できずバインドポーズのまま → **skinメッシュの複製は必ず`three/examples/jsm/utils/SkeletonUtils.js`の`clone()`を使う。`useGLTF`はurl単位でキャッシュされるので、共有シーンを各インスタンスで`SkeletonUtils.clone`し、`useAnimations(clips, instanceRef)`で個別mixerを張る**
- 2026-07-16 | Draco圧縮glbを`useGLTF`で読むとデコーダをCDN(gstatic)に取りに行く → 自己完結アプリ/オフライン/CSPで壊れる恐れ → **`three/examples/jsm/libs/draco/gltf/`のデコーダを`public/draco/`へvendorし、`useGLTF.setDecoderPath('/draco/')`+`useGLTF(url,'/draco/')`でローカル解決する**
- 2026-07-23 | ゴーストが「Tポーズのまま移動」（ユーザー報告）→ dreiの`useAnimations`はmixerとactionを**最初のクローンのボーン**にキャッシュするため、`skeletonClone`をやり直す（tintのuseMemo依存にbaseColorが入っていた／headcount変化でmodelUrlが差し替わる）と新クローンがバインドポーズで固まる → **ライブなmixer配下でskinnedクローンを作り直さない。色などの見た目はマテリアルをin-placeで更新（クローンのdepはscene/モデルのみ）、モデル自体を差し替えるときはReactの`key`にモデル識別子を入れてコンポーネントごと再マウントする**（`components/gallery/GhostVisitors.tsx`）
- 2026-07-23 | スポットの疑似ボリューム光錐（DoubleSide＋加算）が、横から見ると白い板に見え、反射床（`MeshReflectorMaterial`）に映り込んで床一面が白飛びする（ユーザー報告「白い写り込み」）→ 加算ブレンドは視線が錐面を貫く角度で不自然に累積し、平面反射のRTにもそのまま乗る → **疑似ボリュームは「ビーム軸と視線の平行度」でフェードさせ（横視点≈0.2倍）、専用レイヤーに載せて主カメラだけ`layers.enable`する（dreiの反射仮想カメラはlayer 0固定なので反射から自動除外できる）**（`LightCone.tsx`/`GalleryScene.tsx`）
- 2026-07-21 | KitBash混入で巨大化した旧 `idle/walk/idle2/walk2.glb`（計528MB）が、軽量 `visitor.glb`/`visitor2.glb` へ一本化した後もmainに数日残置 → 差し替え時に旧アセットの `git rm` を忘れ、LFS帯域とclone/pull時間を浪費（今回のpull中断の遠因） → **アセットを差し替えたら旧ファイルは同じPRで `git rm` する。掃除前に `grep -rn '/models/' lib components app` で参照を突き合わせ、孤児glb/LFSがないか確認する**

## 成功パターン（スキル化候補の芽）
- 2026-07-13 | 3D没入を壊すネイティブ`alert()`を、疎結合の極小toast(`lib/toast.ts` = モジュールレベルのlistener集合 + `useToast`フック)へ置換。store等の非Reactコードからも`showToast()`で呼べる。破壊的`confirm()`だけは意図的にネイティブ据え置き
- 2026-07-21 | スクロール同期3DのLPは下部(フッター等)まで実スクロールで到達しようとすると重くてプレビュー操作がタイムアウトする。検証時は `javascript_tool` で `.corridor` の height を潰し・上位セクションを `display:none` にして対象を最上部へ引き上げ、`scrollIntoView`+スクショ／`getComputedStyle(el)` で確認すると速い（本番URLでも同じ手が使える。ページ本体は変更しないデバッグ操作）
- 2026-07-21 | 認証必須の画面(`/me`ダッシュボード等)はローカルにSupabase接続が無いとデータを引けず実描画できない。CSS/レイアウトの検証は、実CSSは layout で全ページに効くので **一時ルート `app/<name>/page.tsx` を作り、対象カード構造をプレースホルダ(`.wall-preview3d`等の空div)で再現→スクショ/computed style で確認→確認後に削除** すると認証なしで幾何が見られる。注意: `_`始まりフォルダはNextの非公開扱いでルーティングされない(404)ので `app/harness` 等の名前にする。ページ本体・機能ロジックの最終QAはデプロイ後にユーザー(ログイン済み)へ委ねる
- 2026-07-21 | 大規模リネーム(HAKONIWA→Xibit360 約150箇所)は二段構えが安全: ①決定的で誤爆しない置換(ドメイン・保存キー・識別子・package名)を先に `perl`/スクリプトで一括、②文脈判断が要る分(ロゴ/文中の大小・普通名詞化・日本語の語)は詳細ルールを書いてサブエージェントに委譲。最後に「旧語の残存0・誤TLD 0・二重置換0」をgrep検証＋tsc/build＋実画面。macOSのbashは3.2で `mapfile` 不可・`grep -Z|xargs -0` がパスの`[handle]`等で崩れることがあるので `while IFS= read -r` が堅い
