<!-- BEGIN:self-improvement-loop -->
# 自己改善ループ（このリポジトリの運用ルール）

このリポジトリは Claude を「実行のたびに賢くなる」自己改善型で運用する。ローカルの `~/.claude/` にも同じルールがあるが、リモート実行（claude.ai の Web セッション等）でも効くよう本文をここに置く（ハイブリッド構成）。

1. **セッション開始時**: docs/STATE.md → docs/DECISIONS.md の順に読み、現状を把握してから作業に入る。
2. **STATE.md の更新**: 作業の節目・中断時・出荷後に docs/STATE.md を更新する（進行中 / 再開ポイント / 完了ログ。完了ログは直近5件まで）。**「最終更新」は直近1回分だけを書き、過去の記録を後ろに継ぎ足さない。押し出された分は `docs/STATE-ARCHIVE.md` へ移す**（/kaizen 昇格 2026-08-13）。継ぎ足す運用にしていたら「最終更新」の1行が**26,171文字**・「以下が前回の記録」の連鎖が**51回**・`## 進行中` が**250行**まで育ち、**Read が途中で打ち切られてこのルール1（セッション開始時に読む）が実行できなくなっていた**＝自己改善ループの入口が壊れていた。**STATE.md が一度に読めるサイズであることは、この運用の前提条件**なので、書き足すときは必ず古い分を出す。
3. **失敗の記録**: ビルド・デプロイの失敗やユーザーからの修正指摘に対応した直後、docs/LESSONS.md へ「事象 → 原因 → 回避策」を1エントリ追記する（再発は新規追加せず ×N を加算）。同種の作業を始める前に該当カテゴリを読み、同じ失敗を繰り返さない。
4. **3回ルール（昇格）**: 同種パターンが ×3 に達したら、DECISIONS.md の絶対ルール化・スキル化・Obsidian第二の脳へのwiki化のいずれかをユーザーに提案する。勝手に恒久ルール化しない（Human-in-the-Loop）。
5. **検証ループ**: push 前にこのリポジトリの型チェック・ビルド・`npm run check:i18n`・`npm run check:css`・`npm run check:schema`・`npm run check:sns`・`npm run check:insert-retry`・`npm run check:expo`・`npm run check:price-disclosure`・`npm run check:theme-images`・`npm run check:gates`・`npm run check:ship-ready` を実行し、/code-review による別視点レビューを行う。ローカルではさらに ship スキルの手順に従う。
5.1 **多言語対応は必須（DECISIONS 2026-07-28）**: 来場者や作家の目に入る文言を追加・変更したら、必ず `lib/i18n/en.ts` と `ja.ts` の両方に入れる。`npm run check:i18n` が0件でないとpushしない。訳す対象でないもの（ブランド名・URLの見本など）は、その行か直前の行に `i18n-ok` とコメントを書いて理由を残す。ファイル単位の例外は `scripts/check-i18n.mjs` の `ALLOW` に理由付きで足す（現在は英語版が正の法務2ページとOG画像のみ）。
5.2 **効いていないCSSを書かない（DECISIONS 2026-07-28）**: `npm run check:css` が、マークアップに存在しないクラス／IDへのCSSを検出する。基準線 `scripts/css-dead-baseline.json` は**空配列＝目標値0に到達済み**で、**減らす方向にしか変えない**。新規に増えたら落ちる。意図的な場合は該当行か直前のコメントに `css-ok` と理由を書く。※この番人は「CSSがあってマークアップが無い」片方向しか見ない。**削除系のコミットの上に別の枝の変更を乗せるときは、消したクラス名を乗せた後のマークアップに grep で突き合わせる**（逆向きは機械化していない）。
5.3 **状態を語る対外文言を直書きしない（DECISIONS 2026-07-28）**: 「Coming soon」「未実装」等は、その状態を決めている変数から導出する。`npm run check:i18n` が定型句を検出する。静的な事実なら `state-ok` と導出元を書く。
5.4 **dev と build で `.next` を共有しない（DECISIONS 2026-07-28）**: 両方向を機械で塞いである。`prebuild`（`scripts/check-dev-server.mjs`）は `.claude/launch.json` のポートを検査し、**この作業ツリーで動いている dev だけ**を理由に build を止める（listenしているプロセスの cwd を見るので、別 worktree の dev では止まらない）。`predev`（`scripts/check-stale-next.mjs`）は `.next/BUILD_ID` があれば `.next` を消してから dev を起動する（build 直後に dev を起動すると**古いCSSが配信され、実測とスクリーンショットが食い違う**）。
5.5 **番人を触ったら自己テストを通す（DECISIONS 2026-07-28）**: `npm run check:gates` が、合成入力を流して check:css / check:i18n / check:ship-ready が本当に検出しているかを確かめる（過去に×4、検出器が自分の検査範囲を取りこぼして「0件」が嘘になった）。**番人を追加・変更したら、その変更で守れるようになった入力をケースとして `scripts/check-gates.mjs` に足す。** 番人は git に登録されたものしか見ないので、未追跡ファイルがあると番人自身が落ちる（`git add` してから回す）。
5.6 **プレビューで測る前に `.claude/skills/preview-verify` を読む（/kaizen 昇格 2026-07-29、×6 → 2026-08-12 に ×10 まで増えたので中身を拡張）**: 「検証ツールに嘘をつかれて実装のバグだと誤診する」が10回起きた（2回は誤診に基づいて別のバグを作った）。原則は2つ ──「**測った値が理屈と食い違ったら、まず測り方を疑う**」と「**閾値を割っていても、それが何を意味するかまで確かめてから報告する**」。最低限、測る直前に `location.pathname`（ペインは勝手に別URLへ戻る）と `document.hidden`（`true` ならペインが前面に無く、3Dは本当に描画を止めている＝スクショは真っ黒・`canvas.width` は 300x150 のまま）を見る。信用度は **スクリーンショット ＞ `getBoundingClientRect` ＞ `getComputedStyle`**。※アクセシビリティ監査で閾値を割った要素を見つけたときは、スキルの「過剰報告を防ぐ」5項目（スクロール親／当たり判定の実装側補正／WCAG 2.2の間隔例外／要素の箱と文字インク／`pointer-events: none`）を必ず当てる。※認証が要る画面の**操作性**まで見るなら、同スキルの実バンドル手順（esbuild ＋ 実CSS ＋ 本物のポインタ/キーボード操作）を使う。
5.7 **push 直前に `npm run check:ship-ready`（/kaizen 昇格 2026-07-29、×6）**: 並行セッションが同時に4つ動き `origin/main` が1日に何度も進むため、**base が動いていないか**と**作業ツリーがクリーンか**を機械で確かめる。base が動いていたら rebase して**検証を頭からやり直す**（別セッションの変更と自分の変更が組み合わさった状態は誰も検証していない）。rebase 後は `git show --stat <相手のコミット>` で中身を見る — **件名が `docs:` でもコードを含むことがある**（実際にあった）。未コミットの変更に心当たりが無ければ別セッションの作業なので、勝手にコミットせずユーザーに確認する。
5.8 **migration を足したら `supabase/schema.sql` にも足す（LESSONS 2026-07-29 の「番人化は未実施」を機械化）**: `npm run check:schema`（`scripts/check-schema-sync.mjs`）が、`supabase/migrations/NNNN_*.sql` の各本について ①schema.sql に `-- # NNNN_*.sql` の節があるか ②**コメントを除いたコード行が自分の節より後ろに順序どおり全部あるか**（部分列一致）③`supabase/README.md` §1 の**箇条書き**に載っているか（`0011`〜`0015` のような範囲行でも可）を静的に照合する（SQLは実行しないのでPostgresは不要）。schema.sql は「これ1枚で新しい環境を作る」経路の唯一の入口で、**本番DBには個別に適用済みなので壊れても動作確認では絶対に出ない**（実際に 0025〜0034 の10本が入っていない状態で放置された 2026-07-29）。番人は `git ls-files` で対象を集めるので、新しい migration は `git add` してから回す。※照合するのは箇条書きだけ — 節の地の文に「0001〜0034 が適用されます」と書くと、それが全本をカバーしたことになり照合が空振りする。
5.9 **売り物を足したら公開の価格表示にも足す（/kaizen 昇格 2026-08-12、×3）**: `npm run check:price-disclosure`（`scripts/check-price-disclosure.mjs`）が、`app/api/checkout/route.ts` の `ONE_TIME_SKUS`（＝実際に決済できるSKUの許可リスト）の各SKUについて ①`scripts/check-price-disclosure.mjs` の `DISCLOSURE_TOKENS` に対応が書かれているか ②その識別子が **`/legal`（特商法の価格表示）と LP の料金セクションの両方**の、**コメントと import を除いたコード**に現れているか ③`PRICE_USD_CENTS` と食い違っていないか（値段が無い／0円のまま売れる）を静的に照合する。**同じ漏れが3回起きた**（ビデオパス$20・部屋$25・合同展示$15〜$40。うち1件は本番で**開示のないまま売れていた**）。金額は `lib/pricing` から導出しているので「載っているものが正しい」は既に守られているが、**「載るべきものが載っている」は別問題**で、どの番人の視界にも入っていなかった。※**知らないSKUは素通りさせず落とす** — 新しいSKUがまさにこの穴から漏れるため、`DISCLOSURE_TOKENS` に1行足す作業を人間に必ず1度通させる（`expo_*` を3つ別々に書いてあるのも同じ理由）。※**コメントの中の言及と import だけは開示と数えない**（開示面には実際に注意書きがあり、`noUnusedLocals` が無いので使わない import が残っても tsc は黙る — どちらも実測で穴だと確認して塞いだ）。
5.10 **文言を撤去したら辞書のキーも消す（/kaizen 昇格 2026-08-14）**: `npm run check:i18n` が、**英語の辞書にあるのにコードのどこからも呼ばれていないキー**を検出する。基準線 `scripts/i18n-orphan-baseline.json` は**34件から始め、減らす方向にしか変えない**（`check:css` と同じラチェット）。昇格の根拠は **1セッションで i18n キー12個とエクスポート3つが呼び手ゼロになり、その全部を人（別視点レビュー）が拾った**こと ── 機械は1つも止めていなかった。残っても画面は壊れないので気づく機会が無く、**辞書は11言語ぶんあるので次に訳す人が「もう誰も呼ばない文」を訳す**。誤検出の手当ては実測して必要だと分かった3つだけ: ①複数形（`_one`/`_other`）はコードが基底キーを呼ぶ ②**祖先の接頭辞で組み立てる**キー（``t(`presets.layout.${x}`)`` の下の葉は一度もソースに現れない。これを入れる前は presets 配下の33件を誤検出＝67件→34件）③辞書自身は探索対象から外す。※`check:gates` では**合成辞書のキーを既定で基準線に載せる** — 大半のケースは別の観点を見るために辞書を置いているだけで、載せないと6ケースを巻き添えにする（実際に落ちた）。**この検出を試す2ケースだけ自前で空の基準線を置く。**
5.11 **テーマを触ったらカードの写真を焼き直す（2026-08-17）**: `npm run check:theme-images`（`scripts/check-theme-images.mjs`）が、ダッシュボードのテーマ選択に出る写真（`public/themes/{key}.jpg`）が `lib/presets` の `THEMES` と食い違っていないかを見る。①テーマごとに写真があるか（新しいテーマの取りこぼし）②その写真が空でないか（**描き終わる前に焼くと数KBの真っ黒が入る。実際に踏んだ**）③`THEMES` の定義のハッシュが基準線と一致するか。写真は「見えているが古い」形で腐るので画面を見ても気づけない ── だから機械で見張る。焼き直しは `/theme-capture`（開発時のみ・本番は404）を開いて保存ボタン、そのあと `node scripts/check-theme-images.mjs --update`。※**見張るのはテーマ定義だけ** — 部屋の材質（`Room.tsx`）を触ったときは機械では気づけないので手で焼き直す（材質のファイルまでハッシュに入れると、コメントを直しただけで落ちて番人が「とりあえず基準線を更新するもの」に成り下がる）。

6. **棚卸し**: 「一区切り」のタイミングで /kaizen（.claude/skills/kaizen）を実行し、台帳の整理と昇格提案を行う。
<!-- END:self-improvement-loop -->
