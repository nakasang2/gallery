# HAKONIWA — 3Dバーチャルギャラリー 要件定義(ライト版)

> あなたの作品が、空間になる。

## 1. コンセプト

InstagramやXでアートを発表しているクリエイターが、**自分専用の3Dギャラリー空間**に作品を展示し、URLひとつで世界に公開できるプラットフォーム。
タイムラインに流れて消える「投稿」ではなく、来場者が歩いて鑑賞する「展覧会」として作品を見せる。

- 名称(仮): **HAKONIWA(箱庭)** — 自分だけの小さな世界を作り込む、という意味を込めて

## 2. ターゲットユーザー

| ユーザー | 課題 | HAKONIWAが提供する価値 |
|---|---|---|
| イラストレーター / 画家 | SNSでは作品が流れて埋もれる | 常設の個展空間・ポートフォリオ |
| 写真家 | 一覧グリッドでは世界観が伝わらない | 順路・照明・空間演出で文脈ごと見せる |
| 美大生 / 卒展主催者 | 物理会場のコスト・期間の制約 | 無料で無期限のオンライン展示 |
| アートファン(来場者) | 気軽にギャラリーに行けない | ブラウザだけで入場、没入体験 |

## 3. 機能要件

### MVP(フェーズ1)— まずこれだけで成立させる

- **アカウント**: 登録 / ログイン(メール + SNS認証)
- **作品アップロード**: 画像(JPG/PNG/WebP)+ タイトル・説明・制作年・タグ
- **ギャラリー作成**: テンプレート空間を選び、壁面に作品をドラッグ配置
- **3D鑑賞体験(来場者)**:
  - ブラウザのみで動作(アプリ・プラグイン不要)
  - WASD / ドラッグ / タップで移動・視点操作
  - 作品クリックで拡大 + キャプション表示
- **公開・共有**: 固有URL発行(`hakoniwa.app/@username/exhibition`)、OGP対応
- **レスポンシブ**: PC / スマホ両対応

### フェーズ2(公開後に拡張)

- いいね・コメント・芳名帳(来場記録)
- 空間カスタマイズ(壁色・床材・照明・BGM)
- 同時接続マルチプレイ(他の来場者のアバターが見える)
  — **優先度: 低(意図的に後回し)**。重量級メタバース路線は Mozilla Hubs(2024終了)・NTT DOOR(2025終了)が
  示すとおり生存戦略にならない。軽量・ブラウザのみ・アート展示特化を維持する(docs/STRATEGY.md §5-7)
- 作品販売 / デジタル原画のEC連携
- アクセス解析(来場者数・滞在時間・注目作品)
- ギャラリー横断の「街」— 特集・キュレーション導線

## 4. 非機能要件

- **パフォーマンス**: 初回ロード3秒以内(空間は遅延ロード)、モバイル30fps以上
- **対応環境**: 最新のChrome / Safari / Edge / Firefox(WebGL 2.0)
- **画像処理**: アップロード時に自動リサイズ・テクスチャ最適化
- **セキュリティ**: 画像の無断DL抑止(右クリック対策は気休め程度と明記)、通報機能
- **アクセシビリティ**: 2D一覧フォールバック(WebGL非対応環境・スクリーンリーダー向け)

## 5. 技術スタック(想定)

| レイヤー | 技術 | 状況 |
|---|---|---|
| 3D描画 | Three.js + React Three Fiber | v0.3で移行済み |
| フロントエンド | Next.js + TypeScript(SPA風運用) | v0.3で移行済み |
| バックエンド | Supabase(認証・DB・ストレージを一括) | 移行ステップ2(未着手) |
| 画像配信 | 当面クライアントリサイズ + Supabase Storage、増加後にCDN変換 | 未着手 |
| ホスティング | Vercel | 未着手 |

※ 決定の経緯と詳細は `docs/ARCHITECTURE.md` を参照。

## 6. データモデル(概略)

```
User      : id, username, displayName, bio, avatarUrl, sns links
Artwork   : id, userId, imageUrl, title, description, year, tags[]
Gallery   : id, userId, title, statement, templateId, theme, isPublic, slug
Placement : id, galleryId, artworkId, wallId, position(x,y), scale
Visit     : id, galleryId, visitorId?, timestamp   (フェーズ2: 芳名帳・解析用)
```

## 7. 画面構成

1. **ランディングページ** — サービス紹介、デモギャラリーへの導線
2. **3Dギャラリー(鑑賞)** — 来場者向けのメイン体験
3. マイページ(作品管理)※MVP後半
4. ギャラリーエディタ(配置ツール)※MVP後半

## 8. 追加要件(v0.2 〜)

プロトタイプ初版へのフィードバックから決定した拡張要件。

1. **回遊性**: モバイル / PC それぞれの操作系で直感的にギャラリーを回遊できる
   - → v0.2で実装: タッチ端末にバーチャルジョイスティック(+従来の床タップ移動)、全端末に作品を順に巡る「順路ツアー」。任意の操作でツアーは中断
2. **出展フロー**: Instagram等に投稿済みのアートから選んでギャラリーに出展できる。作品のアスペクト比に合わせて額装・配置が自動調整される
   - 注: Instagram Basic Display APIは2024年12月に廃止済み。公式連携はGraph API(ビジネス/クリエイターアカウント)が必要なため、プロトタイプではURL貼り付け・アップロードで代替し、連携UIはモックとする
   - → v0.2で実装: 画像アップロード(長辺1280pxへ自動縮小しlocalStorageへ保存)/ 画像URL貼り付け / IG連携モック。縦横比から額装サイズを自動算出(極端なパノラマは幅2.6mで頭打ち)
3. **空間カスタマイズ**: ギャラリーのデザイン(壁・床・照明のテーマ)とレイアウト(部屋の形・順路)を任意に変更できる
   - → v0.2で実装: テーマ3種(シック / ホワイトキューブ / ノワール)× レイアウト3種(ワンルーム / 回廊 / 中央壁)。「空間を編集」パネルから即時切替
4. **額装カスタマイズ**: アートのフレームデザイン(色・材質・マット紙)を任意に変更できる
   - → v0.2で実装: 額装5種(墨 / 金 / 白 / 木 / なし=キャンバス張り)。全体一括と作品ごとの個別指定(鑑賞パネルから)の両対応
5. **没入感**: ローポリ感を脱し、実在のギャラリーに近い質感・光・空気感を実現する
   - → v0.2で実装: PBRテクスチャ、環境マップ、静的ベイク影、面取り額縁、AO + ブルーム + SMAA(モバイルは自動で軽量化)
6. **展示テンプレートと空間軸の拡張**(v0.35で実装):
   - テンプレート4種(テーマ/レイアウト/額装/吊し方/キャプションを一括適用する出発点)
   - 吊し方3種(レールワイヤー / 直付け / 棚置き)・キャプション3種(横 / 下 / なし)を新軸として追加
   - テーマは推奨値(額装・吊し方・キャプション)を持ち、テーマ選択時に適用。後から個別変更可(ハード結合ではない)
   - 額装・マット(有無+色: 額縁既定/なし/白/アイボリー/グレー/黒)・吊し方・キャプションは
     **作品単位でも上書き可**(鑑賞パネル / ダッシュボードのプレビュー横の構造化パネル。
     ギャラリー既定と同じ値を選ぶと上書き解除 → 以後テーマに追従)(mat: migration 0012)
   - 額縁は**パラメトリック指定**(有無スイッチ × 素材3種 × 色8色サークル × 太さスライダー3〜15cm)。
     プリセット5種は curated な組み合わせとして共存。カスタム値は `c:素材:色:太さmm` キーとして
     既存のテキストカラム(frame_default / frame_override)をそのまま流れる — migration不要
   - **作品のタイトル・キャプション文はダッシュボードで編集**でき、ネームプレートに描画される
     (キャプションがあればタグの代わりに最大2行・省略記号付き。artworks.description 既存カラム利用)
   - **ダッシュボードのGalleryタブは単一ワークベンチ**: 作品ライブラリ(左)+実3Dプレビュー(右)に
     プレート文言 / 作品別デザイン / 部屋全体のテーマ・レイアウトを集約。3D確認は「Preview in 3D」から
   - 縦長レイアウト「Portrait Hall」追加
7. **操作系の改善**(v0.35で実装):
   - ステア型移動(オートヘディング): 左右入力=旋回、前後=歩行で、常に進行方向を向く。床タップも移動方向へ自動で向く
   - ジョイスティックを全端末に表示(PCはマウスで操作)
   - 自分のペースで巡る「‹ 前へ / 次へ ›」ステッパー(移動+正対+解説表示を1タップで)
   - スマホの作品詳細をボトムシート化し、作品を画面上部にフレーミング(詳細が作品を隠さない)

## 9. 今回のプロトタイプの範囲

このリポジトリで作るのは **1と2**:

- ランディングページ(コンセプト提示・世界観)
- 歩ける3Dギャラリーデモ(サンプル作品を展示、移動・鑑賞・作品詳細表示)

認証・アップロード等のバックエンドはプロトタイプでは対象外(モック)。

## 10. 追加要件(v0.4)— アカウントと複数箱庭

> ステータス: **確定**(2026-07-09)。10.8参照。

### 10.1 認証(メール + パスワード)

- **新規登録**: メールアドレス + パスワード(8文字以上)。確認メールのリンクで有効化
- **サインイン**: メールアドレス + パスワード
- 既存の**マジックリンク / Google OAuth は併存**(TBD-1)
- **パスワードリセット**: 「パスワードを忘れた」→ メールのリンク → 再設定画面
- ログイン中の操作: パスワード変更 / メールアドレス変更(要確認メール)/ 表示名・bio編集(既存)
- ログアウト / **アカウント削除**(箱庭・作品・Storageファイルも削除)
- 画面: `/signup` `/signin` `/reset` の専用ページ。LPと `/demo` から導線
- **ゲスト移行**: ゲスト時にlocalStorageへ出展した作品を、登録直後に「アカウントへ取り込みますか?」と提案

### 10.2 箱庭(ギャラリー)の複数化

- **設計は複数箱庭対応、リリース時の上限は1人1個**。上限は将来プランごとに変えられるよう
  プラン変数(10.10)として持つ(DBスキーマは既に複数対応済み。制限はアプリ層で実施)
- 箱庭ごとに保持するもの:
  - タイトル / slug(公開URL用・ユーザー内一意・変更可)/ ステートメント
  - 空間設定: テーマ / レイアウト / 額装 / 吊し方 / キャプション(テンプレート一括適用も可)
  - 配置: どのスロットにどの作品(+作品ごとの額装・吊し方・キャプション上書き。
    未設定 = ギャラリー既定を継承。テーマ変更時は確認の上リセット)(migration 0011)
- **作成フロー**: ダッシュボード「新規作成」→ テンプレート選択(または個別設定)→ 名前入力 → 3Dエディタが開く
- **編集**: 3D空間(現行 `/demo` 体験)をその箱庭のデータで開き、作品の追加・入替・空間設定変更 → **保存**
- **保存と公開**: 保存 = DBへ書き込み。公開トグルON = `/@username/slug` で公開、OFF = 非公開(404)
  - 公開中の箱庭は保存すると即反映(スナップショット方式は廃止)(TBD-3)
- 削除(確認ダイアログ必須)。複製はTBD-4

### 10.3 作品ライブラリ

- 作品は**箱庭から独立したユーザー資産**(現行スキーマ通り)。複数の箱庭で同じ作品を使い回せる
- ダッシュボードに「作品」タブ: 一覧 / アップロード / 削除(どこかの箱庭で使用中なら警告)
- 上限: ストレージ容量 **300MB/ユーザー**(プラン変数)。動画は既存の40MB/本を踏襲

### 10.4 ダッシュボード(`/me`)

- **箱庭一覧**: サムネイル(代表作)/ タイトル / 公開状態バッジ / 更新日時
- 各箱庭の操作: 開く(3Dエディタ)/ 公開ページを見る / URLコピー / 名称変更 / 公開・非公開 / 削除
- 「新規作成」ボタン(上限到達時は理由を表示)
- **プロフィール編集**: username(公開URLの一部)/ 表示名 / bio / アバター(TBD-6)
- 「作品」タブ(10.3)

### 10.5 公開ページ

- `/@username`: プロフィール + **公開中の箱庭一覧**(URL設計に既存・v0.4で実装)
- `/@username/[slug]`: 既存の3D鑑賞ページ(吊し方・キャプション対応済み)
- OGP: 既存方式(代表作画像)を踏襲。代表作の扱いはTBD-7

### 10.6 非機能・安全

- RLS: 既存方針を複数箱庭へ拡張(ownerのみ書き込み / 公開ギャラリーとその配置・作品のみ誰でも読める)
- アップロードのレート・サイズ検証(クライアント + Storageポリシー)
- 公開ページに「問題を報告」導線(まずは mailto、DB化はフェーズ2)
- 利用規約 / プライバシーポリシーページ(一般公開前に必須)
- メール到達性: 当面Supabase標準SMTP(TBD-8)

### 10.7 画面遷移(v0.4後)

```
LP(/) ─→ 体験(/demo, ゲスト・localStorage)
      └→ 登録(/signup)→ ダッシュボード(/me)─→ 3Dエディタ(箱庭ごと)─ 保存
                                         └→ 公開ページ(/@username/[slug])
```

### 10.8 決定事項(2026-07-09 確定)

| # | 論点 | 決定 |
|---|---|---|
| 1 | マジックリンク / Google を残すか | **残す**(パスワード認証と併存) |
| 2 | 箱庭の上限 | **リリース時は1人1個**。設計は複数対応、プラン変数で拡張 |
| 3 | 公開中の編集の反映 | **保存で即時反映**(草稿/公開の二重管理はフェーズ2) |
| 4 | 箱庭の複製 | **MVPに含めない** |
| 5 | ストレージ上限 | **300MB/ユーザー**(プラン変数) |
| 6 | アバター画像 | **フェーズ2**(まずはイニシャル表示) |
| 7 | OGP代表作 | **slot 0 の作品を自動使用**(手動指定はフェーズ2) |
| 8 | メール送信基盤 | **当面Supabase標準**。一般公開時にResend等を検討 |
| 9 | 箱庭あたりの配置数 | **プラン変数**とする。初期値は決めうちで**10点**(10.10) |

### 10.9 既存実装からの移行メモ

- 現行: 空間設定はブラウザ(localStorage)にグローバル1つ。公開は `slug='main'` 固定で「現在の空間のスナップショット」を書き込む方式
- v0.4: **設定・配置は箱庭ごとにDBが正**となる。`/demo` はゲスト専用体験として現行方式を維持
- 既存ユーザーの `main` ギャラリーは「最初の箱庭」としてそのまま引き継ぐ(データ変換不要、コード側の参照をgallery_id中心に変更)

### 10.10 プラン変数(将来の課金プランに備えた設計)

上限値はコードに散らさず、**1か所のプラン定義**(`lib/limits.ts`)から参照する。
リリース時は全ユーザー同一(フリー相当)。将来プランをDB化する際もこの参照点だけ差し替えれば済む形にする。

| 変数 | 初期値(リリース時) | 備考 |
|---|---|---|
| `galleries` — 箱庭の数 | 1 | DBは複数対応済み。超過時はダッシュボードで案内 |
| `worksPerGallery` — 箱庭あたりの配置数(新規箱庭のデフォルト) | 5 | §11.5のフリー枠に合わせて改定(旧10)。実効スロット数 = min(レイアウトのスロット数, その箱庭の`work_cap`) |
| `storageBytes` — 保存容量 | 300MB | 画像+動画+ポスターの合計 |
| `videoBytes` — 動画1本の上限 | 40MB | 既存踏襲 |

- 配置数の変数はレイアウトと独立(スロットが12個あるレイアウトでも上限まで)。
  プラン拡張時に「同じレイアウトでより多く飾れる」体験差を作れる
- キャパは**部屋(galleries行)ごとに固定**(`work_cap`列、購入時点のプラン値を継承・§11.7)。
  `worksPerGallery` はその値の"現在の初期値"であり、既存の部屋を遡って変えない

### 10.11 実装状況(v0.41)

- 10.1: メールアドレス変更・アカウント削除(migration 0007)を `/me` の Account カードに実装。
  パスワード変更は `/reset`(サインイン中は新パスワード設定モード)
- 10.2: slug変更をダッシュボードに実装(公開中の変更は共有済みリンクが切れる旨を警告)
- 10.3: 作品ライブラリを `/me` の Works タブに実装(画像アップロード / 削除。
  公開壁に掛かっている作品は削除時に警告。動画アップロードはエディタから)
- 10.5: OGPを専用カード生成(`next/og`: 代表作 + 展覧会名 + 作家名)に更新
- 非機能要件: WebGL非対応環境向けの2D一覧フォールバックを実装(スクリーンリーダー対応を兼ねる)
- 未着手(運用): 独自SMTP、本番ドメイン設定。未着手(P2): 規約の日本語版検討 ほか

### 10.12 実装状況(v0.42 — フェーズ2の一部)

- **来場者エンゲージメント**(migration 0008): 訪問記録(タブセッションごと1回)、
  作品ごとの匿名いいね(鑑賞パネルの♥)、芳名帳(来場者は閲覧+記帳、オーナーはダッシュボードで閲覧・削除)。
  ダッシュボードの箱庭カードに 訪問数・♥・記帳数 を表示
- **カスタムレイアウト**(migration 0009): レイアウトに「Custom」を追加。部屋の幅(16–36m)・
  奥行き(8–20m)・中央壁の有無をスライダー/トグルで調整(以前の相談の「パラメトリック(B)」)
- **OGP代表作の手動指定**(同0009): Works の★でカバー作品を指定。未指定は slot 0(決定10.8-7の拡張)
- **アバター**: プロフィールにアップロード(512px)。作家ページに表示
- **通報のDB化**(migration 0010): `/report` フォーム → reports テーブル。鑑賞HUD・作家ページから導線。
  mailto方式(NEXT_PUBLIC_REPORT_EMAIL)は廃止

### 10.13 実装状況(v0.44 — 発見導線とSNS)

- **Exploreフィード**(`/explore`): 公開中の全ユーザーの箱庭を更新日時順に一覧表示し、他の人のギャラリーへ回遊できるように。
  マイグレーション不要(`galleries_select_public`/`profiles_select_all` の既存RLSがそのまま使える)。
  ランディングナビ・ダッシュボード・作家ページ・訪問中の3D HUD(アーティストリンクの隣)にリンクを設置
- **プロフィールのSNSリンク**(既存の`profiles.sns` jsonb列を配線): ダッシュボードのProfileタブでX/Instagramハンドル・
  ウェブサイトURLを設定可能に。作家ページのヘッダーと、来場中の3D HUD(作家名の隣)にアイコンで表示し、
  訪問者が作家の他チャネルをフォローできるようにする(=作家側もSNSでリンクを拡散する動機になり、流入を促す)

## 11. ビジネスモデル・原価・配信アーキテクチャ

> ステータス: **方針(2026-07-11)**。実装(Stripe/プランDB化)は未着手。金額は要検証のたたき台。

### 11.1 基本方針 —— 「できるだけ買い切り」

- **無料で"作って公開する"を完結**させ、口コミの起点にする。作品を人質にしない(公開は無料でも取り上げない)
- 課金は **「もっと・より重く」に対してだけ**。原価の性質でモデルを分ける:
  - **限界コストがほぼゼロのもの**(創作機能・画像スロット)→ **買い切り**(サブスク疲れの創作者に刺さる)
  - **配信コストが変動するもの**(動画)→ **有料+上限管理**(買い切りの無限責任を避ける唯一の例外)
- 原則: **買い切りが成立するのは"在庫ゼロの機能"に対してだけ**。帯域は在庫があるので、配信最適化(11.3)で在庫化コストを潰してから買い切りに載せる

### 11.2 原価構造 —— 主犯は「保管」ではなく「配信(egress)」

| 対象 | 実コストの正体 | 大きさ |
|---|---|---|
| 画像の保管 | ストレージ | **極小**(1作品 ≈ 0.34MB。300MBに約800枚) |
| 画像の配信 | egress(来場ごとの転送) | 配信先次第。R2なら**≒ ゼロ**(11.3) |
| 動画の配信 | egress(再生ごとの転送) | **外れ値**。人気に比例して青天井になり得る |
| 固定基盤 | 認証・DB・計算 | 全ユーザーで按分。無料ユーザー1人あたりは僅少 |

**画像は保管もR2配信もほぼ無料**なので、画像に関しては買い切りが原価的に完全に成立する。**唯一の変動費は動画の配信**。

### 11.3 配信・原価アーキテクチャ(egress対策)

egressは設計でほぼ潰せる。効く順:

1. **ゼロegressストレージへ載せ替え**: 画像・サムネを **Cloudflare R2**(egress $0、保管のみ課金)へ。画像配信コストを実質消滅させる。代替: Bunny.net(帯域激安+画像最適化内蔵)
2. **CDNキャッシュ**: メディアを**コンテンツハッシュのファイル名 + `Cache-Control: immutable, max-age=1年`**。2回目以降の来場はエッジ配信=オリジンegressゼロ(人気ギャラリーほど効く)
3. **見た分だけ配信**: ストリップ/遠景はサムネ(0.04MB)のみ、**フォーカスした作品だけ**高解像度を取得。画像は **WebP/AVIF**(同画質−30〜50%)。3Dテクスチャは **KTX2/Basis** 圧縮
4. **動画は自前配信しない**: **Cloudflare Stream / Bunny Stream / Mux** に載せ、アダプティブビットレート+エッジ配信。既存の近接再生に加え **ポスター先行→接近/操作で初めてストリーム開始** で素通り再生を排除

効果目安: R2移設で画像配信 ≒ ゼロ / CDNで再訪 −90%超 / 遅延+AVIFで初回 −50〜70% / 動画をStream系へで桁減。

### 11.4 買い切り成立の裏付け(原価計算)

- **画像1作品** ≈ 表示用0.3MB + サムネ0.04MB ≈ **0.34MB**
- **Proユーザー(画像20点)** = 6.8MB。R2保管 $0.015/GB月 × 60か月 ≈ **$0.006(1円未満/5年)** + egress $0
  → 一度きりの支払いが**数世紀分の原価をカバー**。画像スロットの買い切りは余裕で持続可能
- **動画**(Cloudflare Stream ≒ 配信1,000分あたり$1): 1分の動画が1,000回再生で約$1。**再生数=人気に比例する変動費**なので、純粋な無制限買い切りは不可 → 上限管理が必須

### 11.5 料金設計(確定たたき台)—— 3本柱

課金は**3つの軸**に集約する。軸をまたぐ単品売り(スロット1点・テーマ1カテゴリだけ等)はしない —— 意思決定を増やすだけで、原価がほぼゼロなので分割する経済的メリットもないため。

| 軸 | モデル | 中身 |
|---|---|---|
| **① ビデオパス** | サブスク(年額) | 動画展示を有効化。フェアユースの配信分数上限つき。配信コストが人気に比例する変動費のため、**唯一の継続課金** |
| **② 展示室の広さ**(キャパ) | 買い切り・繰り返し購入可 | 2通りの増やし方: (a)「展示室を追加」= 新しい部屋(新URL・新テーマ)+ その部屋のキャパ上限までの作品枠が最初から込み。(b)「**今の部屋**にスロット追加」= 既存の部屋のキャパだけを+5点ずつ引き上げる買い切りアドオン(新URLは増えない、単純な容量拡張)。物理的な部屋の寸法(カスタムレイアウトのスライダー)自体は無料のまま(11.8) |
| **③ 展示室のデザイン** | 買い切り | 2層構造(11.8): 道具(Design Tools)は1回買い切りで永久、テーマ/レイアウトはコンテンツとして単品売り(新作=新収益) |

**無料(¥0・永久)**: 1展示室・画像5点(その部屋の物理キャパ)・プリセットのテーマ3種/レイアウト5種/額縁一式・芳名帳/基本分析。
5点は「試して公開」には十分、「作品集として見せる」には少し足りない絶妙なライン(アップグレード動機を残す。10点だと満足して課金理由が消えがち)。

価格目安(日本の個人クリエイター向け相場感「¥980 / ¥1,480 / ¥1,980」ラインを基準):

| SKU | 属する軸 | 目安 |
|---|---|---|
| 展示室を追加 | ② | ¥1,480 / 室 |
| 今の部屋にスロット+5 | ② | ¥580 / 回(繰り返し購入可) |
| Video Pass | ① | ¥980 / 年 |
| Design Tools(道具・買い切り1回) | ③ | ¥1,480 |
| テーマ / レイアウト単品(コンテンツ) | ③ | ¥300〜¥500 / 点 |
| Theme Collection Vol.1(まとめ買い・購入時点スコープ) | ③ | ¥2,480 |

上限に達したら停止/ポスター静止に**フォールバック**(課金トラブルで展示が壊れない設計)。

### 11.6 買い切りの"無限責任"を塞ぐ安全弁(規約)

- **休眠アーカイブ**: 非アクティブ(例: 2年ログインなし)の公開ギャラリーは非公開化 or 静的スナップショット化する場合がある旨を明記
- **保有と公開の切り分け**(代替案): 「作品の**保有・編集**は永久、3D**公開ホスティング**は年ごとの維持」とすることも可能
- **画像帯域フェアユース**: R2+CDNで実質不要になるが、異常トラフィックへの但し書きは残す

### 11.7 実装マッピング(将来)

- 料金プランは `lib/limits.ts` の **プラン変数(10.10)** に写像する。無料/アドオンをプラン定義+アドオンフラグ(`videoEnabled`、所有テーマIDの配列 等)へ展開
- **「展示室を追加」(②)= `PLAN.galleries` を+1し、新規 `createGallery` を1回実行**。その部屋の `worksPerGallery` は購入時点のプラン値を継承する(1レコード=1室のキャパ一体管理。将来キャパ改定時、既存室を購入時の値で固定するか全室追従させるかは別途ビジネス判断)
- **Design Tools(③・道具)** は、ユーザーのプランレコードに単一フラグとして持たせ、**全展示室が参照**する(部屋ごとに再購入させない)
- **テーマ/レイアウト単品・Collection Vol.N(③・コンテンツ)** は、ユーザーが所有するテーマ/レイアウトIDの配列として管理する(購入時点のカタログをスナップショットするのではなく、IDベースの所有リストにすることで「Vol.1所有者が個別に追加のテーマだけ後から買う」といった混在にも対応できる)
- 決済は Stripe。**買い切り = one-time Checkout(展示室追加・テーマ単品は複数回購入可能な同一/類似SKU)、Video Passのみ年額サブスク**。購入結果をユーザーのプランレコード(DB)に反映し、`limits.ts` の参照点を差し替える
- 配信最適化(11.3)は課金より先行して入れてよい(無料ユーザーの原価を下げるほど無料枠を広く保てる)

### 11.8 「展示室の広さ」と「展示室のデザイン」の内訳

**広さ(カスタムレイアウトの寸法)は課金ポイントにしない。** 理由は実装上すでに保険がかかっているため:

```
effectiveSlotCount = min(レイアウトのスロット数, その部屋の work_cap)
```

部屋を物理的にどれだけ広げても、実際に飾れる作品数は必ずその部屋の `work_cap`(= ②「展示室を追加」で買うキャパ。新規作成時に`PLAN.worksPerGallery`を継承して固定)で頭打ちになる。**寸法スライダー自体は無料のまま解放**してよく、キャパの上限だけが課金軸。ここを二重に制限すると複雑さの割に得るものがない。つまり「展示室の広さ」を買うとは、正確には**部屋の見た目の広さではなく"何点飾れるか"を買うこと**であり、その実装が「新しい展示室を1つ追加する」という形を取る。

**デザインは「道具」と「コンテンツ」で性質が違うので、売り方を分ける。**

| 層 | モデル | 中身 | 理由 |
|---|---|---|---|
| **Design Tools**(道具) | 買い切り・1回・永久 | 壁/床の自由なカラーピッカー・照明ムード(色温度・強さ)のカスタム・壁面ロゴ/ブランディング配置 | 一度手に入れれば自分で無限に組み合わせられる**能力**。単品に分解できない(「色を1つずつ買う」は成立しない) |
| **テーマ/レイアウト**(コンテンツ) | 買い切り・単品売り | 現行3テーマ・5レイアウトを超える新作(大理石ホール、和紙の間、屋外パビリオン、上位レイアウト等) | 審美的な選択(見て選ぶ買い物)。新作を出すたびに**新しい購入機会**になる。Notionテンプレート/アイコンパック/フォントと同じ、単品売りが機能するカテゴリ |

> 既に無料で出荷済みの機能(現行テーマ3種・レイアウト5種・額縁のパラメトリック指定〔素材×色×太さ〕)を、後から有料化してはならない。既存ユーザーへの裏切りになり信頼を失う。Design Tools・テーマ/レイアウトの単品売りは、**これから追加する新要素にのみ**適用する前方拡張型。

**まとめ買い「Theme Collection Vol.1」は購入時点までのラインナップに限定し、将来分は含まない。** 含めてしまうと、テーマを単品売りにした本来の目的(新作=新収益)と直接矛盾する。将来リリース分は新規購入(単品、またはVol.2として新規パッケージ化)。Vol.1所有者にはVol.2への**アップグレード割引**を用意すると、コレクター心理を満たしつつ新作の収益機会も守れる。

**アップセル導線**: 無料ユーザーの画面でも、有料テーマ/レイアウトのチップは🔒付きで見せる(既存の `chip-visual` パターンに鍵アイコンを足すだけ)。タップすると単品購入かVol.1購入かを選べる。「あると知っているのに使えない」状態がアップセルの力になる。

### 11.9 実装状況(v0.43 — スキーマ下地のみ、決済なし)

この設計に合わせて、**決済は未実装のまま**土台だけ先行させた:

- `lib/limits.ts`: 無料枠 `worksPerGallery` を10→5に改定。`effectiveSlotCount` は部屋ごとの`cap`を受け取れるよう拡張(省略時は現行プラン値にフォールバック)
- migration 0013: `galleries.work_cap`(既定10 = 既存室の据え置き値)を追加。新規 `createGallery` は作成時点の `PLAN.worksPerGallery`(5)を明示的に書き込み — 既存室を遡って縮小しない
- `Settings`/`PublicExhibition` に `workCap` を追加し、オーナー編集・訪問者表示の両方で「その部屋自身のキャパ」を`effectiveSlotCount`/`rebuildPlacements`/ダッシュボードの残枠表示に反映
- `lib/entitlements.ts`: Video Pass・Design Tools・所有テーマ/レイアウトIDのための型を新設。現状は全員フルアクセス(`FULL_ACCESS_ENTITLEMENTS`)を返すのみ
- **🔒ロックUI(v0.44)**: `isThemeUnlocked`/`isLayoutUnlocked`(既存3テーマ・4レイアウト+customは`FOREVER_FREE_*_IDS`として永久無料判定、それ以外は`ownedThemeIds`/`ownedLayoutIds`を参照)を追加し、`chip-visual`パターンに🔒表示とタップ時のヒント(`components/LockToast.tsx`、「まだ購入できません」+自動消滅)を実装(`SettingsPanel.tsx`のテーマ/レイアウトチップ、`app/me/page.tsx`の同チップ)。ロック中のチップはタップしても適用されない。現行の全テーマ/レイアウトは`FOREVER_FREE_*_IDS`に含まれるため、**今のところどのチップにも🔒は表示されない**(将来テーマ/レイアウトを追加した時に自動でロック対象になる下地)
- 未着手: Stripe連携、購入モーダル(単品/Collection選択)、Design Tools自体のカラーピッカー/照明ムード実装

### 11.10 実装状況(v0.45 — 購入モーダルとDesign Tools本体)

前節の続き。決済は依然未接続だが、UIと3D側の実体を実装:

- **購入モーダル**(`components/PurchaseModal.tsx` + `lib/pricing.ts`): ロックされたテーマ/レイアウトのチップをタップすると、鍵アイコンのトースト(`LockToast`)の代わりにこのモーダルが開く。単品(¥400)/ テーマのみ「Theme Collection Vol.1」(¥2,480、レイアウトには非該当 — §11.8の記述どおりレイアウト単独のCollectionは設けない)の2択(テーマ)/1択(レイアウト)を提示し、価格は`REQUIREMENTS.md`のたたき台をそのまま転記。決済ボタンは押すと「決済はまだ準備中です」という誠実な文言を表示するのみ(偽の購入を成立させない)。Design Toolsのロック(単一機能・カタログではない)は引き続き`LockToast`のみで、購入モーダルは開かない
- **Design Tools本体**(migration 0014・`galleries.design_overrides` jsonb): 壁色・床色・光の色・光の強さ(0.5〜1.5倍)・ロゴ(タイトル壁の角に合成)の5項目を、選択中テーマの上に上書きできるように。`lib/presets.ts`の`resolveTheme(themeKey, overrides)`が全ての3D描画(`GalleryScene.tsx`)の唯一の注入点で、タイトル壁(アクセント壁)自体の色や`titleInk`など他のテーマパラメータには一切触れない(意図的なスコープ限定)。ロゴは`TitleWall`が既にアバターをタイトル壁のキャンバステクスチャに合成しているのと全く同じ経路(`makeTitleTexture`)を使い、固定の右上コーナーに小さく重ねる
- 訪問者側にも反映: `PublicExhibition.designOverrides`を追加し、オーナー編集時だけでなく実際に公開ページを訪れた来場者にも同じ壁色/照明/ロゴが見える
- `entitlements.designToolsEnabled`(現状全員true)でDesign Tools全体をゲート — 無効化されれば自動的に🔒ロック表示に切り替わる下地は用意済み、UIの実体はまだ有効時の1パターンのみ検証
- 未着手: Stripe連携、購入モーダルの「Continue to checkout」を実際の決済に接続、Design Toolsの追加軸(例: 天井/什器の質感、複数ロゴ配置)

### 11.11 実装状況(v0.46 — 「今の部屋にスロット追加」アドオン)

②軸に、既存の「展示室を追加」(新しい部屋を1室丸ごと買う)とは別の増枚手段を追加。ユーザーからの「レイアウトごとに枚数上限を設定するのか、5→10→15と段階的にアンロックするのか」という設計相談を受け、**部屋(`work_cap`)は変えず、既存の部屋のキャパだけを+5点ずつ引き上げる買い切りアドオン**として実装:

- `lib/galleries.ts`の`addWorkSlots(id, currentCap, amount)`: 既存の`galleries.work_cap`を`currentCap + amount`に更新するだけの関数。新しいカラムやマイグレーションは不要(`work_cap`は0013で既にある)
- **UIのみ先行、実際の加算はまだ呼ばれない**: ダッシュボードの作品ストリップで`cloudArtworks.length >= row.work_cap`(部屋が満杯)になると、通常の追加タイルの代わりに「🔒 Room is full — get 5 more slots」というアップセルが表示され、タップすると購入モーダル(単品のみ、¥580/回、繰り返し購入可)が開く。「Continue to checkout」は他の購入モーダルと同じく「決済はまだ準備中です」を表示するのみで、`addWorkSlots`はまだどこからも呼ばれていない(Stripeの決済確認Webhookが呼ぶための下地)
- `components/PurchaseModal.tsx`に`previewNote`propを追加し、フッターの文言をテーマ/レイアウト以外の購入(今回のキャパ追加)向けにも差し替えられるように一般化
- レイアウト自体の物理スロット数(Single Hall/Corridor/Center Wall=10、Portrait Hall=9、Custom=4〜16+)は現状すべてwork_cap(無料枠5)を上回るため、**実運用では`work_cap`が常に先に効く** — レイアウト選択がボトルネックになる場面は今のところない
- 未着手: Stripe連携(`addWorkSlots`を実際に呼ぶWebhook)、複数回購入時の合計表示・購入履歴

### 11.12 購入導線のビジュアルブラッシュアップ(v0.47)

ユーザーから「展示スロット・展示室デザインは購入するものとして魅力的に見えるか、購買意欲を掻き立てるか」という問いを受け、`/redesign-existing-projects`スキルでダッシュボード→購入の動線を監査・改善:

- **ロックされたチップ**(`chip.locked`): 従来は`opacity:0.6`+🔒だけで「壊れている/使えない」ように見えていた。プレビュー(色/間取り)はほぼそのまま見せつつ(=見せることが動機付けになる)、右上に金色の価格タグ(🔒 ¥400)を追加。ホバーで浮き上がる動きも追加し、「無効」ではなく「タップできる」ことを伝える
- **購入モーダル**(`components/PurchaseModal.tsx`): プレビュー画像の背後にアンビエントな金色グロー、価格のコンテキストを示すeyebrow(New theme / New layout / Room capacity / Design Tools)、Collectionオプションへの正直な「Best value」バッジ、CTAボタンを`.btn-line`(サインアウトボタンと同じ見た目)から金グラデーションの塗りボタンへ格上げ、モーダルのフェード+スケールインアニメーション(prefers-reduced-motion対応)を追加。「Checkout isn't live yet」の文言も「we'll email you」という実際には集めていないメール通知の空約束をやめ、正直な言い回しに修正
- **「Room is full」アップセル**(`.works-upsell`): 単なる細い枠線ボタンから、購入CTAと同じ金グラデーション+ホバーで浮き上がる処理に格上げ。満杯という一番刺さる瞬間のボタンなので、他の副次的操作(Resetボタンなど)と見た目が同じままなのは機会損失だった
- **Design Toolsのロック状態**: 一枚岩のテキストボタンから、実際のテーマ色から作った3色のスウォッチストリップ+コピー+金ボタンの「商品カード」に再設計。単品¥1,480という最大チケットの購入導線が一番貧弱だったのを是正
- **統合**: Design Toolsのロックはこれまで自動消滅するトースト(`LockToast`)だったが、¥1,480の検討にトーストは軽すぎる(3.6秒で消える)ため廃止し、他の3種(テーマ/レイアウト/キャパ)と同じ`PurchaseModal`に統一。`LockToast`はどこからも呼ばれなくなったため`components/LockToast.tsx`ごと削除(死んだコードを残さない)
- 検証: 一時的な検証用ルートで、ロックされたチップの価格タグ・4種類すべての購入モーダル(テーマ/レイアウト/キャパ/Design Tools)・「Room is full」アップセル・チェックアウト後の正直な文言表示を確認(削除済み)。`tsc`・`next build`ともにクリーン
- 変わっていないもの: 決済は依然未接続。無料機能(テーマ/レイアウト切り替え、Design Toolsの操作自体)の動作は一切変更していない

### 11.13 テーマ購入プレビューを「本物の壁」に(v0.48)

ユーザーから「テーマ購入時、雰囲気がわからないと買わないのでは」という指摘。確かに購入モーダルのプレビューは小さな`ThemeSwatch`(チップのアイコンと同じ、2em程度の豆粒サイズ)のままで、実際の部屋の空気感は伝わっていなかった。ダッシュボードには既に「作品が実際にどう見えるか」を見せる`WallPreview`(壁色+床色+スポットライト+額装込みの実寸イメージ、`components/SpacePreviews.tsx`)が存在していたので、これを購入モーダルに転用:

- テーマ購入: `ThemeSwatch`(豆粒)→ `WallPreview`(壁一面、そのテーマの推奨フレーム・吊し方・キャプションで、**購入者が今選んでいる実際の作品**を掛けて表示)。作品が1枚もない場合は既存のプレースホルダー(ニュートラルなグラデーション)に自動フォールバックするので壊れない
  - ダッシュボード(`app/me/page.tsx`)ではフィルムストリップで選択中の作品(`selected`)を使用
  - 3D内の簡易エディタ(`SettingsPanel.tsx`)には「選択中」の概念がないため、手持ちの最初の作品(`ownArtworks[0]`)を使用
- レイアウト購入: プレビューのサイズをチップと同じ小ささ(`chip-plan`)から、モーダル用に一回り大きい`purchase-plan-preview`へ。テーマ側が大きくなった分、見劣りしないよう底上げ
- 検証: 一時的な検証用ルートで、作品ありのテーマプレビュー・作品なしのフォールバック・拡大レイアウト図の3パターンを確認(削除済み)。`tsc`・`next build`ともにクリーン

### 11.14 訪問者向けHUDの登録導線 + 作品ごとの購入リンク(v0.49)

ユーザーから2点: (a)「作家のギャラリーにユーザー登録動線を作りたい。左上がギャラリー名とユーザー名、右上が登録動線になるように」、(b)「ユーザーのキャプションに商品の売買リンクなどを設定できるように」。

**(a) 訪問者HUD(`components/gallery/Hud.tsx`の`HudTop`)**: `visitor`ステート(本物の`/@username`ページを見ている時だけ非null。デモ/オーナー自身のプレビューでは常にnullなので、この判定一つで安全に分岐できる)で分岐:
- 訪問者モードでは、左上の「← HAKONIWA」ブランドリンクをやめ、小さな「HAKONIWA」ホームリンク→ギャラリー名(タイトルが未設定のプレースホルダーなら代わりに作家名)→サブ行に作家名(タイトルと重複しない時だけ)・@ハンドル・SNSアイコン・Explore・Reportの導線を配置
- 右上には金色の「Start free」ボタン(`/signup`へ)を新設。訪問者は全員「将来の作家候補」でもあるため、閲覧の対極cornerを登録導線に充てた
- デモ(`visitor === null`)側の見た目・挙動は一切変更なし
- 新規CSS: `.hud-identity` / `.hud-identity-home` / `.hud-identity-main` / `.hud-identity-sub` / `.hud-signup-cta`(`app/gallery.css`、モバイル2箇所のブレークポイントにも追記)。使われなくなった`.hud-artist-link`は削除

**(b) 作品ごとの購入リンク**: `supabase/migrations/0015_artwork_purchase_link.sql`で`artworks.purchase_url text`を追加。`ArtworkData`(`lib/artworks.ts`)に`purchaseUrl?: string`、`lib/cloud.ts`の`ArtworkRow`/`rowToArtwork`/`updateArtworkDetails`を対応拡張(0015未適用環境でもtitle/captionの保存は壊れないよう、既存の列欠落パターンに倣ってグレースフルデグレード)。ダッシュボード(`app/me/page.tsx`)の「Title & caption」編集グループに「Purchase link」入力欄を追加(タイトル/キャプションと同じ automatic-save に相乗り)。`ArtworkPanel.tsx`にはリンクが設定されている作品にだけ金色の「Available for purchase ↗」リンクをタグの直後に表示(訪問者・オーナープレビュー両方で表示、プロトコル省略時は`https://`を自動補完)

- 検証: 一時的な検証用ルート(`app/qa-hud/page.tsx`、削除済み)で、実ストアに偽の`PublicExhibition`/`ArtworkData`を注入し、本物の`HudTop`/`ArtworkPanel`をレンダリング。タイトルあり/プレースホルダーの両方でのHUD表示、デモモードが無変化であること、モバイルでの折り返し、購入リンクの表示とhref正規化を確認。`tsc`・`next build`ともにクリーン
- 変わっていないもの: 「Start free」はまだ本物の登録フォームへのリンクに過ぎず(サインアップ自体は既存の`/signup`をそのまま使用)、購入リンクは外部サイトへの単純なリンクであり決済機能はHAKONIWA側には一切ない

### 11.15 テーマ/レイアウトの所有権を実データ台帳に(v0.50)

ユーザーから「テーマ/レイアウトを後から追加していける仕組みは合ってる」旨の確認を受け、「決済連携が無くても今のうちにできる開発をしておいて」という指示。`lib/entitlements.ts`の`getEntitlements()`は今まで`_userId`を完全に無視し常に`FULL_ACCESS_ENTITLEMENTS`(全員フルアクセス)を返すだけの仮実装で、`ownedThemeIds`/`ownedLayoutIds`が実データに繋がっていなかった。決済がまだ無い以上「Design Tools/Video Passを今すぐ実際にロックする」のは開発ではなく事業判断(既存ユーザーの無料体験を後から有料化することになる)なのでそこは触らず、実データが繋がっても挙動が一切変わらない部分——テーマ/レイアウトの所有権——だけを配線した(現状フォーエバーフリー対象外のテーマ/レイアウトが1つも存在しないため、繋いでも今日時点でのUIは無変化):

- `supabase/migrations/0016_purchases.sql`: `purchases`台帳テーブル(`user_id, kind('theme'|'layout'|'design_tools'|'video_pass'), item_key, created_at`)。RLSは**selectのみ**許可し、insertポリシーは意図的に用意していない — 本物の購入は将来Stripeのwebhookなどサーバー側(service roleキーはRLSを無視できる)でしか記録できないようにし、クライアントから自分に無料で権利を付与できる穴を作らないため
- `lib/purchases.ts`(新規): `usePurchasedIds(userId)`フック。`purchases`をkind別に振り分けて`{ themeIds, layoutIds }`を返す。0016未適用/オフライン/未サインインの場合は空配列にフォールバックし、今日までと完全に同じ「全部無料」の見え方を保つ
- `lib/entitlements.ts`: `getEntitlements(userId, owned?)`が第2引数(`usePurchasedIds`の戻り値)を受け取り、`ownedThemeIds`/`ownedLayoutIds`に実データをマージするように変更。`videoEnabled`/`designToolsEnabled`は引き続き固定`true`(事業判断待ちのため意図的に未接続のまま)
- 呼び出し側(`app/me/page.tsx`、`components/gallery/SettingsPanel.tsx`)で`usePurchasedIds`を呼び、`getEntitlements`に渡すよう更新
- 検証: `tsc`・`next build`ともにクリーン。この変更単体はロジックの配線のみで見た目の差分が無いため、スクリーンショットでの確認は行っていない(既存のテーマ/レイアウト購入モーダルのUI検証は11.12/11.13で実施済み)
- 変わっていないもの: 実際に有料テーマ/レイアウトが1つも存在しないため`owned`は常に空、UI上のロック挙動は今日時点で一切変化なし。Design Tools/Video Passのゲーティングはまだ未接続(事業判断が必要)。`purchases`への書き込み経路(決済Webhook)自体もまだ存在しない

### 11.16 Exploreフィードのページネーション(v0.51)

`/explore`は今まで`fetchPublicFeed(limit = 48)`で最大48件を1ページで返すだけで、それ以上のギャラリーは表示されなかった(バックログ記載の既知の未対応)。「Exploreフィード進めよう」の指示で対応:

- `lib/publish.ts`: `fetchPublicFeed(offset, limit)`に変更し、`{ items, hasMore }`(`FeedPage`)を返すように。`limit`件のつもりで実際は`limit + 1`件を`.range()`で取得し、余分な1件が来たかどうかで次ページの有無を判定(往復1回で済ませる)。`updated_at`が同値のギャラリーがページを跨いで重複/欠落しないよう、`id`を安定タイブレークとして二次ソートに追加。ページサイズは`EXPLORE_PAGE_SIZE`(24)としてエクスポートし、呼び出し側と共有
- `components/ExploreFeed.tsx`(新規、クライアントコンポーネント): カード一覧+「Load more」ボタン。`supabase`クライアントはどこでも匿名キーで動く(ダッシュボードの他の画面と同じ)ため、専用のAPIルートを用意せずブラウザから直接`fetchPublicFeed`を呼べる。クリックで次ページを取得し配列に追記、`hasMore=false`になったらボタンごと消える
- `app/explore/page.tsx`: 初回ページは引き続きサーバーコンポーネントのままSSR(SEO・初期表示速度を維持)し、`ExploreFeed`に`initialItems`/`initialHasMore`を渡す形に縮小
- 検証: 一時的な検証用ルート(`app/qa-explore/page.tsx`、削除済み)で、偽データ6件+`hasMore=true`での一覧/ボタン表示、0件時の空状態、ボタン押下後にクラッシュせず状態遷移することを確認(このサンドボックスにはSupabaseの実データが無いため、実際に次ページが読み込まれる様子そのものは確認できていない — 正直に申告)。`tsc`・`next build`ともにクリーン
- 変わっていないもの: 検索/フィルタ/ソート順の変更は無し(newest-edited firstのまま)。実データでの次ページ取得の動作確認は本番のSupabase接続でのみ可能

### 11.17 ダッシュボードの設定順序を「部屋→作品」に(v0.52)

ユーザーからのスクリーンショット付き指摘: 「Room — whole gallery」(テーマ/レイアウト)は展示室全体にまつわる上位の設定なので、各作品に対する設定(キャプション等)より上に来るべき。実際`app/me/page.tsx`の`we-right`列は今まで「選択中の作品」設定(Title & caption → per-work frame/mat/hanging)が先、「Room — whole gallery」(テーマ/レイアウト)とDesign Toolsが後、という逆順になっていた。room-levelの設定を先頭に、per-work設定をその下に来るよう並べ替え:

- 新しい順序: 「Room — whole gallery」(テーマ/レイアウト) → Design Tools(同じくroom-level) → 両者の関係を説明する注記 → 「選択中の作品」ステータス行 → Title & caption(作品ごと) → per-work frame/mat/hanging(`WorkDesign`)。上位のスコープ→下位のスコープの順に統一
- 注記の文言も並び順の変化に合わせて修正: 「the per-work controls **above** it」→「the per-work controls **below**」
- 副作用として見つけた表示崩れ: 「"{選択作品名}" — this work」の行が元々`marginTop: 0`だったのは、それが`we-right`列の一番最初の要素だったため。並べ替え後は直前にroom注記が来るので、`marginTop: 0`のままだと隙間がほぼ無く詰まって見える。検証用ルートでスクリーンショットを見て発見し、`marginTop: 0`を除去(デフォルトの`0.8rem`に戻す)して修正
- 検証: 一時的な検証用ルート(`app/qa-order/page.tsx`、削除済み)で、実際のクラス名・コンポーネント(`ThemeSwatch`/`LayoutPlan`/`WorkDesign`)を使い新しい並び順をスクリーンショットで確認。上記の隙間詰まりバグもこの過程で発見・修正。`tsc`・`next build`ともにクリーン
- 変わっていないもの: 各セクション内部のUI・挙動(テーマ/レイアウトの選択、Design Toolsの操作、作品ごとの編集内容)は一切変更していない。純粋な並べ替えのみ

### 11.18 手動スロット配置(v0.53 — 狙った壁に飾る/空き枠を残す)

ユーザーからの相談:「5枠に3点しか無いとき、3Dギャラリーで端から詰めるのではなく、間隔を空けて展示したい。あるいはスロットとレイアウトを連動させ、狙った場所に対象の作品を設定できるか」。②の**手動配置**を実装。②軸(枚数=`work_cap`)や③軸(レイアウトの形)とは独立した「どの作品をどの壁枠に掛けるか」という新しい軸:

- **データモデル**(`lib/arrangement.ts`・migration `0023`): 部屋ごとに `galleries.arrangement`(jsonb配列)を追加。`arrangement[slotIndex] = artworkId | null`(nullや配列末尾より先=意図的な空き枠)。**未設定=従来通り0番から詰める**ので既存の部屋は一切影響なし。`placeWorks()`は純粋関数(store非依存)で、明示配置を尊重しつつ、どこにも割り当てられていない作品(新規アップロード等)だけを空き枠に前詰めで自動配置する — 「意図的な空き」と「まだ置いていない新作」を区別できるのが肝
- **描画/ナビ層**(`components/gallery/{GalleryScene,WalkControls,MiniMap}.tsx`・`lib/exhibition.ts`): 従来は「作品リストの並び順=スロット番号」という暗黙の前提だった。`usePlacement()`が返す `{ list, slots }`(作品と、それぞれが掛かる物理スロット番号の並行配列)を導入し、`layout.slots[i]`→`layout.slots[slots[i]]`に変更。空き枠は素の壁のまま(§11で確認した「来場者には空フレームを出さない」方針を維持)
- **公開ページへの伝播**(`lib/publish.ts`): 公開時は`rebuildPlacements`が`arrangement`に沿って`placements.slot_index`へ疎に書き込む(空き枠は行を作らない・使われなくなった枠はトリム)。公開ページの読み込みでは`slot_index`から`arrangement`を復元するので、来場者もオーナーが配置したのと同じ壁・同じ空き枠を見る
- **編集UI**(`components/PlacementEditor.tsx`): ダッシュボード「Room — whole gallery」内に俯瞰のルームマップを追加。壁枠をタップ→その場に飾る作品を選ぶ(または空にする)。1作品は1枠だけ(別枠へ移すと元の枠は空く)。テーマ/レイアウト変更と同じ`rowToSettings`→`saveGallerySpace`(+`rebuildPlacements`)経路でデバウンス保存 — 配置編集とレイアウト変更が1つのgalleries行を奪い合うレースを避けるため、storeではなくrow基準で永続化
- 検証: `placeWorks`/`toPlacement`の純ロジックをNodeで単体検証(空配列→従来動作、意図的な空き枠の維持、新作の空き枠自動補充、重複/存在しないid無視、超過時の非表示とoverflow計上、疎なslot_index)。`/demo`をheadless Chromiumで撮影し従来動作(0番から詰める)が無傷なこと、`PlacementEditor`のSSRで5枠中3枠に作品・2枠空き・初期ヒントが崩れず出ることを確認。`tsc`・`next build`ともにクリーン
- 未着手: レイアウト変更に伴う空きスロットの3Dゴースト表示は今回もオーナー含め非表示のまま(ユーザー指定「3D上に空きスロットは見えなくて良い」)。/demo内(3D歩行中)からの直接ドラッグ配置は将来課題(現状はダッシュボードのマップから)
- 追補(v0.53.1): ユーザー指摘「作品ストリップが 2/5 なのにセルが3つ(2作品+追加タイル)しか無い」に対応。ダッシュボードの作品フィルムストリップを**キャパ分のスロットを常に見せる**よう変更(`app/me/page.tsx`)。作品 → アップロードタイル → 残りキャパぶんの空きスロット(破線プレースホルダ `.works-cell--empty`)を並べ、2/5 なら5セル表示に。①「あらかじめ上限分のスロットを用意」の2D版(3Dには出さないというユーザー指定は維持)

### 11.19 過去の来場者の“気配”(v0.54 — 非同期プレゼンス)

ユーザー提案「3D空間なので、他の人の気配を作りたい。過去の訪問者数に応じて非同期で見えると良い」。リアルタイム同時接続(=以前フェーズ2に後回しにしたマルチプレイ)ではなく、**過去の累計訪問数に応じた半透明シルエットが会場を徘徊する**非同期プレゼンスとして実装。人気の箱庭ほど賑わって見え、そのまま社会的証明になる:

- **人数=訪問数の対数写像・上限キャップ**(`lib/ghosts.ts` `ghostCountForVisits`): `<3→0`(新設は正直に静か)、増えるほど増え、`MAX_GHOSTS=4`で頭打ち(多すぎて鬱陶しくならないよう)。純関数でNode単体検証(12ケース)
- **訪問数の公開読み取り**(migration `0024`): `visits`のSELECTはオーナー限定(0008)なので、**集計だけを返すSECURITY DEFINER RPC `public_visit_count`**を追加(公開箱庭のみ・個票/時刻は返さない・非公開は0)。`fetchPublicExhibition`が読み`PublicExhibition.visitCount`へ
- **描画**(`components/gallery/GhostVisitors.tsx`): 壁の明度で色を反転(白壁→濃い影/黒壁→淡い影)、距離で不透明度フェード(=カメラ間近で威圧しない)。作品スロット前に寄って滞在→移動、を手続き的に。**公開ページ(来場者ビュー)専用**でオーナー編集/`/demo`には出さない、**作品フォーカス中は非表示**、`LOW_POWER`(モバイル)では自動オフ
- **環境ざわめき**(`lib/audio.ts` `setCrowdLevel`): 声域のバンドパスノイズをリバーブ経由でごく小音量。**ゴースト数に比例**(無人=無音)。既存のmute/離脱ゲート(master gain)にそのまま乗る
- 検証: `ghostCountForVisits`をNodeで12ケース単体、headless Chromium(swiftshader)で会場に人影が並ぶことを確認。`tsc`・`next build`クリーン。QAルートは削除済み
- 未着手(将来の“上乗せ”): いいねの多い作品前にゴーストが集まる(注目ヒートマップ化)。実際の歩行経路は記録していないので動きは手続き的(密度だけ実データ)。リアルタイム同時接続(B案)はイベント機能を作るときに上乗せ

#### 11.19.1 “気配”を実体化(v0.55 — ローポリ人体＋歩行モーション)

ユーザー指摘「シルエットをもう少しリアルに。歩いたり作品の前でポーズしたり」。初版のビルボード平面スプライトを廃止し、**プリミティブで組んだローポリ人体＋手続き的な歩行サイクル**へ差し替え(アセット追加なし=zero-dep維持):
- 頭(球)/胴(箱)/腕・脚(円柱)を`THREE.Group`のピボットで構成。**脚は互い違い・腕は脚と逆位相にsinで振り**、進行方向を向いて歩き、作品前で止まると**その壁(=絵)の方を向く**。立ち止まると四肢はレストへ
- ユーザー選択「もう少し実体化」に合わせ半透明を強め(`opacity≈0.85`・カメラ至近のみフェード)、**足元に薄い接地シャドウ**(共有Basicマテリアルの円盤)を足して浮かないように。1体1マテリアル(微妙な色ゆらぎでクローン感を回避)
- 検証: 専用QAルートで実体を至近撮影し、頭/胴/腕/脚・接地シャドウ・歩行で脚が開閉すること・時間差で人影が視界内へ歩いてくること(=移動)を確認。`tsc`・`next build`クリーン。QAルート・QA用exportは削除済み
- 将来フォトリアルにしたい場合は、この手続き人体を**リグ付きglTF＋アニメ(drei `useGLTF`/`useAnimations`)に差し替え可能**な構造(不気味の谷/資産管理の観点で今回は手続きローポリを採用)
- 追補: ユーザー指摘「絵の前で見てるわけじゃない」→ **到着した瞬間に“次の目的地”の向きを向いてしまうバグを修正**(滞在中は「今いる作品」を向き、次の行き先は滞在終了時に選ぶ)。加えて、ほぼ前後対称な体でも「見ている」と伝わるよう、**滞在中は上半身を作品側へ少し傾ける前傾ポーズ**(股関節ピボットの`upper`グループ)＋頭をわずかに前方オフセット。壁+作品を置いたQAで、作品の前に立ち正対して前傾するのを確認

#### 11.19.2 “気配”をリグ付きglTFキャラに(v0.57 — 実キャラ＋walk/idleアニメ)

ユーザーが別経路でmainへ人物モデル(`walk.glb`/`idle.glb`)を追加し「このモデルで残りの実装を」と指示。§11.19.1で予告した「手続きローポリ→リグ付きglTF差し替え」を実施。ただし取得したモデルには**重大な問題**があった:
- **取得ファイルは各約200MB(計約400MB)**で、Web配信要件(初回3秒/モバイル30fps)に3桁オーバー。中身を解析すると**99%が誤同梱のKitBash3D「NeoCity」街並みキット**(ビル49メッシュ・214万三角形)で、肝心のskin付き人物は約2MBのみだった。walk/idleは同一メッシュ+街を丸ごと二重に持ち、実は歩行(1.07s)+待機(14.37s)の2アニメが**片方のファイルに両方入っていた**
- 対処: `gltf-transform`で**街のシーンを丸ごと破棄→人物だけ残す**+テクスチャ7枚を1024px WebP化+Draco圧縮+1ファイル集約 → **200MB→1.6MB**(`public/models/visitor.glb`、リグ65ジョイント・2アニメ・テクスチャ健在)。1.6MBは小さいのでLFS対象外の通常blobとしてコミット(`.gitattributes`で上書き)。Dracoデコーダはローカル同梱(`public/draco/`、CDN非依存)
- `GhostVisitors.tsx`: プリミティブ人体を`visitor.glb`に差し替え。**移動AI(徘徊/滞在/向き)は流用**し、手続き歩行→**walk/idleクリップのクロスフェード**(移動度合いをスムージングした重みで補間)に。多数インスタンスは`SkeletonUtils.clone`で複製(skinメッシュを`scene.clone()`するとスケルトン参照が切れT字ポーズで固まるため必須)。**A案(気配)を踏襲**し、テクスチャは載せず**壁コントラストに合わせて着色した半透明(opacity 0.46)の単色シルエット**として描画(特定人物に見えず、同一モデルでもクローン感が出ない)
- 検証: 専用QAルートで実シーンにゴースト×4を出し、**明壁(whitecube)=暗色ゴースト/暗壁(noir)=淡色ゴースト**の反転・歩行アニメ・人間大スケール・半透明で壁が透ける“気配”を至近撮影で確認。`tsc`・`next build`クリーン。QAルート・一時スクリプト・最適化用devパッケージは削除済み
- 未着手/将来: 元の200MB `walk.glb`/`idle.glb`は`visitor.glb`が代替したため未参照(削除は原本保全のため保留)。人物モデルのポリゴン削減(現状約5.5万三角形×最大4体)や複数モデルでのバリエーションは将来の最適化候補
- 追補: ユーザー指摘「歩くのが遅い/半透明にしたら服の重なり(シャツ・ネクタイ等)が視線方向に累積して継ぎ目だけ濃くなる。完全なシルエットにしたい」→ 2点修正:
  - **歩行速度**を0.5〜0.8→0.9〜1.4 m/s(実際の散歩ペース)に上げ、足滑り低減のため walk クリップの`timeScale`を1.35に
  - **一律のフラットシルエット化**: 半透明ソリッドメッシュは体の各パーツ(前後・重ね着)が視線上で重なるたびに二重ブレンドし継ぎ目が濃くなる。**深度プリパス**を導入——各skinメッシュに`colorWrite:false`の不透明な双子(同geometry/skeletonを`bind`で共有)を追加して先に深度だけ書き、可視側は無光源`MeshBasicMaterial`(`depthWrite:false`/`depthTest:true`)で**最前面の1フラグメントのみ**描画。結果、各ピクセルは正確に1回だけブレンド=どこも均一な不透明度の真のシルエットに。無光源化で陰影ムラも解消(壁コントラスト着色・距離フェード・他ゴースト/部屋を遮蔽しない性質は維持)。QAで継ぎ目の濃淡が消えたことを実シーン撮影で確認。`tsc`・`next build`クリーン
- 追補2: ユーザー指摘の2点をさらに修正:
  - **モーションと移動速度の不一致(足の空回り)**: 前回`timeScale`を1.35に上げたが移動速度(0.9〜1.4)がそれに追いつかず、足が接地せず滑っていた。歩行クリップの自然な地上速度を`walk.glb`の足ボーンから実測(接地足が後退する速度 ≒ **V0=1.44 m/s**@timeScale1)し、**移動速度をアニメから導出**(`speed = V0 × timeScale`)。これで**足が常に接地に一致**(=ムーンウォーク解消)。ユーザー了承どおり再生速度(≈1.35)は据え置き、ゴーストごとに`pace`(0.92〜1.08)で微差をつけつつ`speed`と`timeScale`を連動させ、どの個体も足がロックされたまま自然なばらつきを出す
  - **作品の無い壁の前で止まって眺めてしまう**: `pickTarget`が全レイアウトスロットから滞在先を選んでいたため、作品2点だけの部屋でも空きスロットや内壁面の前で立ち止まっていた。`usePlacement().slots`(=実際に作品が載っているスロット番号)から**作品のあるスロットだけ**を`artSlots`として渡し、滞在(正対して眺める)はそこに限定。作品ゼロなら滞在せず床を回遊するだけ。QAで実行時エラーなし・描画確認、`tsc`・`next build`クリーン
- 追補7: 群衆の質感を4点調整。(a) **歩行速度を男女別に**: `site_config`の`ghost`値を`{walkSpeedMale, walkSpeedFemale}`へ(旧`walkSpeed`は両方にフォールバックする後方互換あり)。admin`GhostSpeedEditor`は男女2スライダーに。既定を1.94→**1.30 m/s**(やや早い指摘を受け落ち着いた散歩ペースへ)。各ゴーストは自分のモデルに応じた速度を使用(足ロックは維持)。(b) **障害物回避の離隔を約1m→約0.5mに**: `GHOST_BODY`0.4→0.25・`AVOID_LOOK`1.2→0.4。最高速2.6 m/sでも貫通ゼロ・壁面最接近≈0.6mをシミュレーションで確認。(c) **男女比を保証**: `GhostVisitors`の割り当てをランダム独立から**均等配分+シャッフル**へ。2体以上なら必ず男女両方を含み、全体≈50:50(単体テストで確認: count2/3/4いずれも100%両方・平均50%男性、単体は50%でランダム)。(d) **同じ絵を複数人で鑑賞できるレンジ拡大**: `pickTarget`の滞在位置を「正面1.5〜2.2m固定」から**奥行き1.6〜3.6m×左右±1.3m**のレンジへ広げ、オフセット位置からでも作品中心を向くよう`face`を`atan2(slot-位置)`で算出。遠く・やや左右からでも同じ絵を見られる。QAで男女2スライダーUI・4体シルエット描画(エラーなし)を確認。`tsc`・`next build`クリーン
- 追補6: 女性アバターを追加してバリエーション化(ユーザーが本番へ`walk2.glb`/`idle2.glb`をアップ)。`walk2.glb`(51.5MB)を解析すると街の混入は無く、バルクはテクスチャ(PNG 6枚=49MB)。ゴーストはテクスチャを外した単色シルエットなので、同じ`gltf-transform`処理(テクスチャWebP化+Draco)で**51.5MB→1.2MB**の`visitor2.glb`を生成(LFS外の通常blob)。**クリップ特定は要注意だった**: `walk2.glb`は4クリップ入りで、名前や尺では判別できず(`mixamo.com`/`mixamo.com.001`は**両方Tポーズ**の不要クリップ)。足ボーンの移動量と手の横開き量を実測し、**walk=`mixamo.com.002`**(足動く・腕下)、**idle=`Armature|mixamo.com|Layer0`**(足静止・腕下=自然な立ち待機)を特定して2クリップだけ残した。女性のwalk地上速度も実測(V0≈1.42≒男性1.44なので共通定数を流用)。`GhostVisitors`は2モデル(`visitor.glb`/`visitor2.glb`)を`MODELS`配列で持ち、各ゴーストが`useMemo`で安定ランダムに1体選択(`useGLTF`はURL単位キャッシュ)。**クリップ選択をindexから尺ベース(最短=walk/最長=idle)に変更**——2モデルでクリップの名前/順序が違うため。QAで女性のwalk/idle(Tポーズでない自然な待機)と、実シーンでの2モデル混在シルエット描画・エラーなしを撮影確認。`tsc`・`next build`クリーン。原本`walk2.glb`/`idle2.glb`は`visitor2.glb`が代替のため未参照(保全のため残置)
- 追補5: 音まわりの調整。(a) **環境ざわめきを無効化**(ユーザー判断「ざわめき不要」): `GhostVisitors`の`setCrowdLevel`呼び出しを削除しゴーストが群衆音を鳴らさないように(`lib/audio`の`setCrowdLevel`基盤は残置=可逆)。(b) **プレイヤー足音を落ち着いた歩調に**: 距離ベースのトリガー閾値を0.72m→**1.15m**に広げ(歩幅を長く=歩調ゆっくり)。(c) **アート/ツアーのグライド中は足音を消音**: `WalkControls`でフォーカスグライド(トゥイーン中かつ`targetIndex>=0`)を検出し足音をスキップ(高速移動で走っているように聞こえる問題を解消)。床タップ移動(`targetIndex=-1`)と手動歩行は足音を維持。※音の変更のためスクショ検証は不可、ロジックのみ確認。`tsc`・`next build`クリーン
- 追補4: ユーザー指摘「中央壁レイアウトなどで、目的地への直線が壁を横切ると壁をすり抜けて歩く」→ 障害物回避を実装(選択肢B=壁を検知して回り込むステアリング)。`GhostVisitors`の移動を、目的地への直線から**壁沿いステアリング**(`steerDir`)に変更: 最寄りの`Solid`(ベンチ/中央壁、軸並行ボックス)が`AVOID_LOOK`(1.2m)以内にあると、目的地方向を捨てて主に**接線方向**(目的地側の面を回る向き)へ進み、近いほど強い外向き押し出しを足して壁面に触れず回り込む。加えて**ウォッチドッグ**(`bestDist`/`stuckT`): 中央壁の真裏など反応型では回り込めない目的地で無限旋回しないよう、5秒間 目的地に近づけなければ目的地を選び直す。外周壁はレイアウトが凸なので目的地内クランプで足り、`solids`にも含めない(=クランプのみ)。Nodeシミュレーションで、正面/斜め/対称の各ケース・**adminで設定可能な最高速2.6 m/sでも貫通ゼロ**を確認(壁面から約1mの自然な離隔)。実シーン(island=中央壁+作品6点)でも中央ブロックを誰も貫通せず配置されることを俯瞰スクショで確認。`tsc`・`next build`クリーン
- 追補3: 歩行速度をadminから調整できるダイヤルを追加。`site_config`(migration 0018・admin書込/公開読取)に`ghost.walkSpeed`(m/s)キーを追加(**migration不要**の拡張key/value)。`lib/siteConfig`に`fetchGhostConfig`/`saveGhostConfig`(0.6〜2.6にクランプ・デフォルト1.94)、admin編集UI`components/GhostSpeedEditor`(スライダー+ライブ数値/ラベル+Save/Reset、`app/admin`に配置)。`GhostVisitors`は公開ページで`fetchGhostConfig`を読み、各ゴーストの`speed`基準に使用(取得までゴースト生成を保留し、各個体が正しいペースで湧く)。**足ロック維持**: `speed = walkSpeed × pace`、`timeScale = speed / V0` なので、どの速度に変えても足は接地に一致(空回りしない)。QAでスライダー操作・ラベル・Save有効化・デフォルト表示を確認。`tsc`・`next build`クリーン

### 11.20 Customレイアウトの調整UIをダッシュボードへ(v0.56)

ユーザー指摘「Customを選んでも編集できない/どう編集するの?」。Customの調整つまみ(幅/奥行き/中央壁)は**3D編集画面の`SettingsPanel`にしか無く**、ダッシュボードからは選ぶだけで触れなかった(「Preview in 3D」経由でしか到達できず、名前も"Preview"で編集だと気づけない)。**ダッシュボードのRoom行に同じ調整UIを追加**:
- `app/me/page.tsx`: `row.layout === 'custom'` のとき「Custom size」ブロック(Width 16–36m / Depth 8–20m スライダー + 「Centre wall」チェック)を表示。既存グローバルCSS(`.slider-row`/`.toggle`)を流用しCSS追加なし
- 保存はローカルstate + デバウンス(500ms)で`saveGallerySpace`(+公開時`rebuildPlacements`。サイズ変更=スロット数変化のため)。テーマ/レイアウト・配置編集と同じrow基準の書き戻し
- **課金扱いはユーザー判断で「無料のまま」**。ただし別途確認した**Customチップにロック判定が無い件(無料枠は`corridor`のみ、custom未ゲート)は既知**として保留(今回は無料方針なので実害なし)
